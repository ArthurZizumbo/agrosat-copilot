# US-050 — Gemma 4 26B LoRA — documentar como FUTURE (ADR + correcciones)

> Estado: done. Rama: `feature/E7-US-050-gemma-future-adr`. EPIC 7. Estimacion 2 SP (documental).

## Criterios de aceptacion (verificacion)

| AC | Criterio | Estado |
|----|----------|--------|
| AC-1 | ADR corrige el skill: id real `gemma-4-26B-A4B-it`; experts 3D fused -> QLoRA bloqueado; `target_modules` no matchea (~0.91%); via real `target_parameters` (PEFT >= 0.17). | OK (ADR-011) |
| AC-2 | ADR documenta AgroMind eval-only -> fine-tune = leakage; AgroMind-IT/ES tambien eval. | OK |
| AC-3 | ADR ratifica: reasoner = Gemini 2.5-pro; on-prem = Qwen vLLM; Gemma LoRA = FUTURE. | OK |
| AC-4 | No se entrena Gemma 4 en este horizonte. | OK (decision) |
| T-1 | Redactar el ADR. | OK -> ADR-011 (ADR-009 ya existia para H100/FarSLIP) |
| T-2 | Actualizar el skill `agrosat-llm-finetuning`. | OK |
| T-3 | Marcar Gemma 4 LoRA FUTURE en CLAUDE.md / AGENTS.md. | OK (espejos sincronizados) |

## Nota de numeracion

El plan decia "ADR-009-*" pero ADR-009 ya existe (reactivacion H100 + FarSLIP).
Se creo **ADR-011** para no sobrescribir esa decision; la intencion (documentar
Gemma FUTURE) se cumple. ADR-010 ya existia (geo-context E-c).

## Hechos verificados (jun-2026)

- HF: `google/gemma-4-26B-A4B-it` existe (MoE 8/128 +1 shared, 3.8B/25.2B); `gemma-4-26b-it` no existe.
- PEFT docs: `target_parameters` es la via para LoRA sobre `nn.Parameter` 3D de MoE (`mlp.experts.gate_up_proj`/`down_proj`); caveats (overhead por-experto, no multi-adapter, 2D/3D).
- `bitsandbytes` no cuantiza el layout 3D fused -> QLoRA bloqueado.

## Archivos

- `docs/decisions/ADR-011-gemma4-lora-future.md` (nuevo).
- `.claude/skills/agrosat-llm-finetuning/SKILL.md` (corregido).
- `CLAUDE.md`, `AGENTS.md` (Decisiones Irrevocables + Stack header).

## Checklist de cierre

- [x] ADR-011 redactado con hechos verificados.
- [x] Skill corregido (id, QLoRA, target_parameters, AgroMind eval-only, serving).
- [x] CLAUDE.md/AGENTS.md FUTURE + espejos sincronizados.
- [x] Handoff actualizado.
