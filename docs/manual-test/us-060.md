# Manual Test — US-060: Drift detection con Evidently (asset Dagster semanal)

Verificacion del pipeline de drift Evidently 0.7.x + asset `drift_check` + resource `drift_notifier` +
schedule semanal. Lo automatizable esta cubierto por `tests/ml/monitoring/test_drift.py` +
`tests/dagster/test_drift_asset.py` (11 passed). El drift se computa sobre subpoblaciones PASTIS
REALES del parquet FarSLIP, nunca sintetico. La subida GCS y el envio SMTP no se ejercitan en dev
(blockers B11/B12).

> US-060 cubre el criterio Monitoreo / Mantenimiento (Avance 7).

---

## MT-1 — Suite del pipeline de drift

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run pytest tests/ml/monitoring/test_drift.py tests/dagster/test_drift_asset.py -q` | tests pasan (no-drift, bandas desplazadas, conteo 64-dim embedding, Chi-cuadrado clases, trigger umbral, HTML no vacio, materializacion, skip graceful) | [x] verificado (11 passed, warnings benignos scipy) |
| KS en bandas / indices | `DataDriftPreset(num_method="ks")` | [x] verificado (`ml/monitoring/drift.py`) |
| MMD nativo en embeddings (no proxy) | `EmbeddingsDrift`; umbral 0.55 (baseline ~0.48, shift ~0.79) | [x] verificado (B13; `drift.py`) |
| Chi-cuadrado en clases | `cat_method="chisquare"`, `class_id` a str | [x] verificado (`drift.py`) |

---

## MT-2 — Asset Dagster `drift_check` + schedule

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `python -c "import dagster_project.definitions"` | importa limpio | [x] verificado (`schedules=['drift_check_weekly_schedule']`, `jobs=['farslip_full_pipeline_job','drift_check_job']`) |
| Asset `drift_check` registrado con deps | deps `farslip_embeddings_consolidated` + `parcel_features_fused` | [x] verificado (`test_drift_check_registered_in_definitions`) |
| Schedule semanal | `drift_check_weekly_schedule` cron `0 6 * * 1` (lunes 06:00 UTC), default STOPPED | [x] verificado (`dagster_project/schedules/__init__.py`; `definitions.py:53`) |
| Reporte HTML local | `data/monitoring/drift/report_{week}.html` (clave ISO `YYYY-Www`) | [x] verificado (materializacion del test) |
| Plan B (contraste real) | reference = sin clase mayoritaria, current = clase mayoritaria; `MAX_ROWS_PER_SIDE=5000`, embeddings recortados a 64 dims | [x] verificado (`dagster_project/assets/drift.py:63,182`; B10) |
| Skip graceful sin upstream | `status="skipped_no_upstream"`, `rows=0` si falta parquet | [x] verificado (test) |

---

## MT-3 — Alerta por umbral

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `drift_score > 0.3` | dispara `drift_notifier` | [x] verificado (`DRIFT_SCORE_THRESHOLD=0.3`, `ml/monitoring/drift.py:42`; test `test_drift_check_alert_triggers_notifier` con mock) |
| `drift_notifier` en dev | `enabled=False` -> loggea `drift_alert` via structlog, retorna False | [x] verificado (`dagster_project/resources/notification.py`; B12) |
| Lineage | `MaterializeResult` con `data_version`/`code_version`; run MLflow best-effort | [x] verificado (asset) |

---

## MT-4 — Caveats honestos (blockers documentados)

| Item | Nota | Estado |
|------|------|--------|
| Current set temporal | sin particion por fecha -> Plan B (subpoblaciones PASTIS reales), parametrizado para apuntar a un trimestre fechado | [x] documentado (B10) |
| Subida GCS | requiere ADC + bucket `gs://agrosat-reports/drift/`; sin ellos escribe local sin fallar | [x] documentado (B11) |
| Envio SMTP | solo en prod (`enabled=True`); dev loggea via structlog | [x] documentado (B12) |
| MMD metodo | nativo de Evidently 0.7.21 (no proxy PSI/Wasserstein) | [x] documentado (B13) |
| `dagster definitions validate` CLI | requiere Postgres (pre-existente); validacion en proceso suficiente | [x] documentado (B14) |
| Schedule default STOPPED | opt-in; no dispara en dev hasta encenderse | [x] verificado |
