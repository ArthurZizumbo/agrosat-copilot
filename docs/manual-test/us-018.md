# Manual Test — US-018 · Seleccion, extraccion y normalizacion de features

**Epic**: E3 · **Avance**: A2 (CRISP-ML(Q) Data Preparation) · **Sprint**: S4 · **Estado**: ready-to-close

> US-018 es 100 % ML + notebook (sin UI, sin chat, sin LLM, sin endpoints HTTP).
> La mayoria de los AC se validan automaticamente con `pytest` (49/49 pass,
> cobertura 90 % `ml.features.selection`). Esta guia cubre los flujos que
> requieren juicio humano: regenerar el subset PASTIS, ejecutar el notebook
> end-to-end con outputs visibles, inspeccionar los reportes y validar la
> seccion de conclusiones CRISP-ML(Q) del Avance 2.

---

## Pre-requisitos

- Repo clonado y `poetry install` ejecutado con grupos `dev,test,ml`.
- Python 3.12.x + Poetry >= 1.7.
- (Opcional) Dataset PASTIS-R descargado en `data/PASTIS-R/` (>15 GB).
  Si **no** esta presente, el subset puede regenerarse en modo sintetico
  o reutilizar el parquet ya commiteado `data/test_fixtures/feature_selection_subset.parquet`.
- Subset PASTIS pre-generado y commiteado: `data/test_fixtures/feature_selection_subset.parquet`
  (139 KB, 77 muestras × 189 cols, 17 clases PASTIS).
- Para el flujo 4 (Avance 2 visual): visor markdown que renderice tablas
  y un visor PNG.

---

## Flujo 1 — Regenerar subset PASTIS desde patches reales (15 min, requiere PASTIS-R)

**Objetivo**: validar `scripts/generate_feature_selection_subset.py` Typer CLI
y la composicion de `temporal_features` (US-015) + `spectral_indices` (US-014)
sobre patches PASTIS reales. Skippable si PASTIS-R no esta descargado.

| Paso | Comando o accion | Resultado esperado |
|------|------------------|---------------------|
| 1.1 | `make feature-selection-subset` | Logs structlog `subset_generation_started`, `patch_processed` (uno por patch valido), `subset_generated` con `n_rows`, `n_cols` y `per_class` |
| 1.2 | Si `data/PASTIS-R/` NO existe | El script imprime warning `pastis_root_missing` y sale con exit 0 (degradado, NO falla CI) |
| 1.3 | `ls -lh data/test_fixtures/feature_selection_subset.parquet` | Archivo presente, < 1 MB (no requiere DVC) |
| 1.4 | `poetry run python -c "import polars as pl; df = pl.read_parquet('data/test_fixtures/feature_selection_subset.parquet'); print(df.shape, df.columns[:5])"` | shape `(N, 189)` donde N >= 50; primeras cols `['parcel_id','year','NDVI_mean','NDVI_std',...]` |
| 1.5 | `poetry run python -c "import polars as pl; df = pl.read_parquet('data/test_fixtures/feature_selection_subset.parquet'); print(df['class_id'].value_counts().sort('class_id'))"` | >=10 clases representadas, cada una con >=1 muestra (stratified) |
| 1.6 | Re-ejecutar `make feature-selection-subset` con misma `--seed 42` | Conteo por clase identico (determinismo del muestreo) |

---

## Flujo 2 — Notebook 03b_fe_spectral_temporal_pastis.ipynb end-to-end con outputs poblados (5 min)

**Objetivo**: validar AC-11 (notebook ejecutado end-to-end con papermill y
conclusiones CRISP-ML(Q)) + AC-13 (reportes persistidos).

| Paso | Comando o accion | Resultado esperado |
|------|------------------|---------------------|
| 2.1 | `make feature-selection-notebook` | Papermill ejecuta `notebooks/feature_engineering/03b_fe_spectral_temporal_pastis.ipynb` en su sitio con `MPLBACKEND=Agg`. Tiempo total ~3-5 min. Exit 0 |
| 2.2 | Abrir el `.ipynb` en JupyterLab o VSCode | Las 22 celdas tienen outputs poblados (HTML tables + figuras PNG inline). Header `# Feature Engineering — US-018` visible |
| 2.3 | Buscar `# Conclusiones CRISP-ML(Q) Data Preparation` en el notebook | Encontrada como markdown cell H1 con 5 secciones: (a) features finales, (b) strategy de normalizacion, (c) 3 hallazgos no triviales, (d) limitaciones, (e) mapeo CA -> seccion |
| 2.4 | Revisar §3.2 §3.3 §4.1 §4.2 §4.3 visualmente | Tablas con scores chi2/ANOVA visibles, scree plot PCA, scatter UMAP coloreado por clase, heatmap correlacion inline |
| 2.5 | `ls -la reports/feature_selection/` | 12 archivos generados: `correlation_matrix.png`, `umap_2d.png`, `pca_scree.png`, `variance_threshold.json`, `chi2_scores.csv`, `anova_f_scores.csv`, `fa_loadings.csv`, `feature_importance_rf.csv`, `feature_importance_xgb.csv`, `before_after.csv`, `before_after.md`, `normalization_decisions.csv` |
| 2.6 | `ls -la reports/feature_selection/correlation_matrix.png reports/feature_selection/umap_2d.png` | 2 PNG presentes (~30-100 KB cada uno), commiteables (no `.gitignored`) |
| 2.7 | `git check-ignore reports/feature_selection/correlation_matrix.png` | exit 1 (no esta ignorado) |
| 2.8 | `git check-ignore reports/feature_selection/anova_f_scores.csv` | exit 0 (esta ignorado por `.gitignore` US-018) |

---

## Flujo 3 — Inspeccion visual de reportes Avance 2 (10 min, juicio humano)

**Objetivo**: validar visualmente que los entregables del Avance 2 son
interpretables y agronomicamente coherentes.

| Paso | Comando o accion | Resultado esperado |
|------|------------------|---------------------|
| 3.1 | Abrir `reports/feature_selection/correlation_matrix.png` | Heatmap simetrico con diagonal en 1.0; identificable visualmente un cluster rojo intenso entre derivados de `NDVI/NDRE/NDWI/SAVI` (hipotesis EDA US-013 confirmada o documentada como negativa) |
| 3.2 | Abrir `reports/feature_selection/umap_2d.png` | Scatter 2D coloreado por clase PASTIS; al menos 5 clases distinguibles visualmente como clusters (no perfecta separacion — esperable con 17/20 clases y subset pequeno) |
| 3.3 | Abrir `reports/feature_selection/pca_scree.png` | Curva monotona decreciente del ratio de varianza explicada con linea punteada en 0.95 y `n_components` etiquetado |
| 3.4 | `cat reports/feature_selection/before_after.md` | Tabla markdown con >=3 filas (raw / variance+correlation / pca_0.95). Columnas `f1_macro_mean ± std` + `miou_mean ± std`. Diferencia raw vs filtered <= +/-0.10 |
| 3.5 | `cat reports/feature_selection/normalization_decisions.csv` | CSV con columnas `feature,scaler,skew,justification`. Al menos 2 scalers distintos aparecen (e.g. `standard`, `yeo-johnson`, opcionalmente `log1p` si LAI esta en el subset) |
| 3.6 | `head -5 reports/feature_selection/anova_f_scores.csv` y `head -5 reports/feature_selection/chi2_scores.csv` | Top features tipo `*_mean`, `*_p95`, `*_std` ordenadas descendente. `GCVI`, `LAI`, `NDVI`, `EVI`, `MSAVI2`, `NDWI` deberian aparecer entre las primeras |
| 3.7 | `head -5 reports/feature_selection/feature_importance_rf.csv` y `feature_importance_xgb.csv` | DataFrame con `feature,importance,rank`. Top features ordenadas desc. Solapamiento >= 3 features entre top 10 RF y XGB |

---

## Flujo 4 — Smoke test API publica desde Python REPL (3 min)

**Objetivo**: validar que las 11 funciones publicas estan reexportadas
correctamente desde `ml.features` y que un caller downstream (US-019 baseline
RF/XGB) puede importarlas sin errores.

| Paso | Comando o accion | Resultado esperado |
|------|------------------|---------------------|
| 4.1 | `poetry run python -c "from ml.features import apply_variance_threshold, drop_correlated_features, chi2_select, anova_f_select, fit_pca, fit_factor_analysis, fit_umap_2d, compute_feature_importance, compare_before_after, select_normalizer, make_preprocessor; print('OK 11/11')"` | Imprime `OK 11/11` sin ImportError |
| 4.2 | `poetry run python -c "from ml.features.selection import select_normalizer; print(select_normalizer('LAI_mean', {'skew': 0.5}))"` | `('log1p', \"feature 'LAI_mean' es positiva y sesgada (LAI/biomasa)\")` |
| 4.3 | `poetry run python -c "from ml.features.selection import select_normalizer; print(select_normalizer('peak_doy', {'skew': 0.3}, strategy='nn'))"` | `('minmax', \"feature 'peak_doy' a [0,1] para red neuronal\")` |
| 4.4 | `poetry run python -c "import polars as pl; import joblib; from ml.features.selection import make_preprocessor; df = pl.read_parquet('data/test_fixtures/feature_selection_subset.parquet').drop(['class_id','fold']); pp = make_preprocessor(df, strategy='linear'); joblib.dump(pp, '/tmp/pp.pkl'); print('joblib OK', len(open('/tmp/pp.pkl','rb').read()), 'bytes')"` | Imprime `joblib OK <N> bytes` (preprocessor serializable para futuro endpoint backend) |

---

## Flujo 5 — Test suite completa con cobertura (5 min)

**Objetivo**: validar AC-12 (cobertura >= 80 % en `selection.py`).

| Paso | Comando o accion | Resultado esperado |
|------|------------------|---------------------|
| 5.1 | `make feature-selection-test` | Si numpy 2.3.5 / scipy 1.17.1 incompat (bug R7 conocido) -> fallback al paso 5.2 |
| 5.2 | `poetry run coverage run --include="ml/features/selection.py" -m pytest tests/ml/features/test_selection.py -q` | `49 passed` en ~40 s (incluye 7 slow + 2 empirical si subset disponible) |
| 5.3 | `poetry run coverage report -m` | `ml/features/selection.py` con cobertura >= 80 % (actual 90 %) |
| 5.4 | `poetry run pytest tests/ml/features/test_selection.py -q -m "not slow and not empirical"` | `40 passed, 9 deselected` en ~5 s (suite rapida CI-friendly) |

---

## Checklist de cierre QA (humano)

- [ ] Flujo 2 (notebook end-to-end con outputs poblados): visualmente OK
- [ ] Flujo 3.1 (heatmap correlacion): cluster NDVI/NDRE/NDWI/SAVI identificable o documentado como hallazgo negativo
- [ ] Flujo 3.2 (UMAP 2D): clusters por clase visibles
- [ ] Seccion `# Conclusiones CRISP-ML(Q) Data Preparation` con 5 sub-secciones
- [ ] 2 PNG commiteables presentes
- [ ] 49/49 tests pass (40 rapidos + 7 slow + 2 empirical)
- [ ] Cobertura >= 80 % sobre `ml.features.selection`
- [ ] `ruff check ml/` limpio
