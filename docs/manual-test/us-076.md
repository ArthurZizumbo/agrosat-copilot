# Manual Test — US-076 (EuroCropsML few-shot transfer, curva k-shot real)

**Fecha**: 2026-06-20 · **Rama**: `feature/E12-transfer-multiregion` · **QA**: ejecucion propia (no reporte de terceros).
**Epic**: E12 · **Veredicto**: ready-to-close con nota de descarga (DVC del subset crudo pendiente).

---

## Entorno

- `cd ml && poetry run ...` (venv `agrosatcopilot-Kq8fUqSH-py3.12`, Python 3.12.6).
- Datos reales presentes: `data/transfer/eurocropsml/preprocess/*.npz`.
- Crosswalk US-074 presente: `data/reference/hcat_crosswalk.parquet` + `eurocrops_hcat3.csv`.

---

## 1. Lint (ruff)

```
cd ml && poetry run ruff check .
-> All checks passed!
```

**Estado**: VERDE.

## 2. Tests del pipeline nuevo

```
poetry run pytest ../tests/ml/transfer/test_eurocropsml_fewshot.py -v
-> 17 passed, 1 warning in 16.98s
```

17/17 verdes. Cubren: ladder k-shot, dimensionalidad fija del feature vector (independiente de T),
alineacion HCAT macro via crosswalk real, lectura de `.npz`, contrato `EuroCropsMLDataMissing`,
entrenamiento XGB k-shot, schema de la curva, ausencia de import GEE en el camino base, y descarga
no-interactiva (mocks de red, sin Zenodo real). Un solo warning de XGBoost (device cuda vs cpu en
predict) — cosmetico, no afecta resultado.

**Estado**: VERDE.

## 3. Datos reales (regla Arthur) — la curva NO es sintetica

- **NPZ reales en disco**: `(Get-ChildItem ...preprocess -Filter *.npz).Count` -> **706,683** parcelas
  (coincide con el total documentado: EE 175,906 + LV 431,143 + PT 99,634). 81+ GB enumerados.
- **Curva exportada** `data/transfer/eurocropsml_fewshot_results.parquet`: 63 filas, 8 columnas
  (`source, target, k, seed, f1_macro, n_classes, use_pretrain, scenario`).
  - 3 escenarios x 7 k x 3 seeds = 63. Seeds = {0,1,2}.
  - **63 de 63 valores f1_macro son UNICOS** -> no hay valores duplicados/hardcodeados ni columna
    rellenada con constantes.
- **Generacion real verificada en el notebook**: la celda 7 llama `fs.run_fewshot_curve(...)` sobre
  `root = data/transfer/eurocropsml` (los .npz reales), entrena XGBoost real (`build_estimator("xgb")`
  importado de `ml/train/baseline.py`) y la celda 13 escribe el parquet desde `curve` calculada en
  vivo (`curve.write_parquet(results_path)` + assert de readback). **No hay np.random ni literales F1.**
- **Curva (F1-macro media sobre 3 seeds, query EE)** — monotona y coherente con el domain gap:

  | escenario          | k=1   | k=5   | k=20  | k=100 | k=500 |
  |--------------------|-------|-------|-------|-------|-------|
  | LV->EE (pre-train) | 0.321 | 0.349 | 0.382 | 0.444 | 0.482 |
  | LV+PT->EE          | 0.315 | 0.321 | 0.350 | 0.380 | 0.420 |
  | sin-pretrain->EE   | 0.015 | 0.198 | 0.273 | 0.380 | 0.472 |

  Lectura: sin pre-train ni datos locales el modelo arranca perdido (0.015 @k=1); el pre-train LV
  da arranque inmediato (0.32) y converge con la referencia ~k=500. Espejo de la FORMA del paper
  (0.66 > 0.57 @500); es domain gap medido, NO claim zero-shot.

**Estado**: VERDE — curva de datos reales.

## 4. Notebook (`notebooks/eda/02g_eurocropsml_fewshot.ipynb`)

- Leido con `encoding='utf-8'`. 16 celdas (8 codigo), **6 celdas de codigo con output, 0 errores**.
- Outputs poblados: **1 PNG inline** (curva F1-vs-k log con bandas +/- std y lineas paper @0.66/0.57)
  + **4 tablas HTML** (conteos por pais, distribucion macro long-tail, summary por escenario, etc.).
- `execution_count` secuencial 1..8; `metadata.papermill.duration = 477.9 s`, `exception = None`
  -> ejecutado end-to-end con papermill, sin excepcion.
- `degraded = False` (camino datos reales). Existe la rama `degraded=True` con placeholder honesto
  (Plan B) por si la descarga falla; aqui NO se uso porque hay datos reales.

**Estado**: VERDE — outputs poblados.

## 5. Alineacion a HCAT (crosswalk US-074)

- `ml/transfer/label_align.py` colapsa `EC_hcat_c` (10 digitos) al group code (6 significativos) y lo
  mapea via `data/reference/hcat_crosswalk.parquet` (`hcat_group_code -> macro_hcat_group`) a las 11
  macro-grupos del label-space `hcat-macro` de US-074. Codigos fuera del crosswalk -> `null-class`
  (partial-label honesto, no clase fabricada).
- Test `test_hcat_codes_resolve_via_real_crosswalk` y `test_align_labels_to_hcat_macro` pasan contra
  el parquet REAL. La curva se reporta en el mismo label-space que el resto de EPIC 12.

**Estado**: VERDE.

## 6. No-tocar (verificacion)

```
git diff HEAD --name-only -- ml/eval/class_remap.py ml/agent/tools/classify.py ml/train/baseline.py
-> (vacio)
```

`class_remap.py`, `classify.py` y `baseline.py` SIN cambios — el label-space `hcat-macro` se consume,
no se redefine; el recipe XGB se importa, no se edita.

**Estado**: VERDE.

## 7. DVC del subset crudo — PENDIENTE (nota honesta)

- `data/transfer/eurocropsml/` esta **gitignored** (`.gitignore:368`), correcto (706k .npz crudos no
  van a Git).
- La curva ligera `eurocropsml_fewshot_results.parquet` esta **whitelisted** a Git
  (`.gitignore:374 !...results.parquet`) — correcto, va versionada en Git.
- **PERO no existe ningun `.dvc`** para el subset crudo:
  - `data/transfer/eurocropsml.dvc` -> `does not exist`.
  - `git ls-files "*.dvc" | grep transfer` -> vacio.
- El handoff afirmaba "`.dvc` commiteado"; la realidad es que el DVC del subset **esta pendiente**.
  Los datos reales SI estan en disco y la curva SI sale de ellos, por lo que la ciencia esta completa;
  lo que falta es `dvc add data/transfer/eurocropsml` + commit del `.dvc` para reproducibilidad/lineage.

**Estado**: PARCIAL — datos reales presentes y usados; versionado DVC del crudo pendiente.

---

## Tabla AC

| Criterio | Estado |
|---|---|
| AC-1: ladder k-shot del protocolo EuroCropsML (1..500) | OK |
| AC-2: recipe XGB reusado de baseline (importado, no editado) + curva F1-vs-k real | OK |
| AC-3: labels alineados a HCAT macro via crosswalk US-074 | OK |
| AC-4: protocolo LV->EE y LV+PT->EE (Francia corregida, no existe en EuroCropsML) | OK |
| AC-5: notebook con outputs poblados (curva PNG + tablas), papermill end-to-end | OK |
| AC-6: pipeline nunca fabrica numeros (EuroCropsMLDataMissing, sin np.random en curva) | OK |
| AC-7: descarga real no-interactiva de EuroCropsML (706,683 .npz, 3 paises) | OK |
| AC-8: no-tocar class_remap / classify / baseline | OK |
| AC-9: ruff + tests verdes | OK |
| AC-10: subset crudo versionado con DVC (`.dvc` commiteado) | **PARCIAL** (datos en disco; `.dvc` pendiente) |

## Veredicto

**ready-to-close con nota de descarga**: ruff verde, 17 tests verdes, curva k-shot REAL derivada de
706,683 parcelas EuroCropsML reales (63 F1 unicos, cero hardcode), notebook ejecutado con outputs
poblados, alineacion HCAT US-074 correcta, no-tocar respetado. Unico pendiente NO bloqueante:
`dvc add data/transfer/eurocropsml` + commit del `.dvc` (el crudo esta gitignored y en disco; falta
solo el puntero DVC para lineage). Cerrar tras `dvc add` o documentar el pendiente en el handoff.
