# Manual Test — US-067: FinOps operativo (finops.md)

Verificacion del entregable documental `docs/operations/finops.md` contra los 4 criterios verbatim del
plan v8. US documental: los pasos verifican presupuesto + gasto, las 4 palancas reproducibles, el caveat
de creditos, el costo por modelo y los enlaces relativos a scripts/Make/TF. Cifras dentro del rango
oficial, sin sinteticos.

> US-067 cubre el criterio FinOps / Mantenimiento del Avance 7.

---

## MT-1 — Presupuesto, gasto y caveat de creditos

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Abrir `docs/operations/finops.md` | guia con corte de fecha (20-jun-2026) y mantenedor | [x] verificado (9.753 B) |
| Presupuesto objetivo | ~$115/mes operativo (Cloud Run `min_instances=0`); training $262 (spot)-$602 (on-demand); GCP ~$0.30-0.49; Gemini centavos | [x] verificado (`finops.md:7-14`) |
| Nota H100 sponsor | H100 NVL 96GB del sponsor 24/7 (no apagar, VM `gjcamacho-gpuh1`) + L4 spot con daemon idle | [x] verificado (`finops.md:26-29`) |
| Caveat de creditos | credito GenAI App Builder ($17,178) NO cubre la SKU de Gemini API | [x] verificado (`finops.md:16-24`) |
| Consistencia con US-061/063 | mismas cifras (~$115/mes, $262-602, ~$0.30-0.49, $17,178) | [x] verificado (cruce entre docs) |

---

## MT-2 — Cuatro palancas reproducibles + scripts

| Palanca | Resultado esperado | Estado |
|---------|--------------------|--------|
| Cloud SQL dev `activation_policy=NEVER` | var TF `db_activation_policy` (evita drift) enlazada a `modules/gcp/variables.tf` | [x] verificado (`finops.md:36-49`) |
| Shrink disco `farslip-data` 250->125 GB | receta snapshot->disco nuevo->rsync->import TF (GCP no encoge in-place) | [x] verificado (`finops.md:50-58`) |
| Daemon auto-shutdown L4 por idle | por ausencia de mensajes Pub/Sub, NO por GPU; pararlo antes de runs largos | [x] verificado (`finops.md:59-67`) |
| Cloud Run scale-to-zero | `make scale-to-zero-check` lista `minScale`; nota push gateway para `/metrics` | [x] verificado (`finops.md:68-76`) |
| Scripts + skill enlazados | `scripts/azure_h100_{start,stop,status}.sh`, `cost_audit.sh`, targets Make, skill `agrosat-finops` | [x] verificado (`finops.md:78-98`) |

---

## MT-3 — Costo por modelo del switch A/B

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Tabla por variante | `gemini` (default), `qwen-onprem` ($0 marginal), `qwen-api`, `gemma` (FUTURE) con precio in/out + costo marginal | [x] verificado (`finops.md:121-127`) |
| Metodo de calculo | `costo_turno = prompt/1e6*precio_in + completion/1e6*precio_out`, agregado por `variant` x latencia | [x] verificado (`finops.md:129-133`) |
| Fuente real de datos | `chat_service.py` logs `chat_model_resolved`/`chat_stream_finished`, `DoneEvent.usage` | [x] verificado (`finops.md:107-116`) |

---

## MT-4 — Caveats honestos (blockers documentados)

| Item | Nota | Estado |
|------|------|--------|
| Tokens de Qwen en streaming | `None` hasta el fix `include_usage`; tabla con estructura + metodo, no valores sinteticos | [x] documentado (B6/B16) |
| Billing fino por SKU | requiere `gcloud billing` con permiso; se usa el rango oficial; `make cost-audit` cuando haya permisos | [x] documentado (B15) |
| Desalineacion nominal "US-059" | los logs los emite US-054, se reportan con US-065; US-059 = paraguas de observabilidad | [x] documentado |
| Enlaces relativos | a Makefile / `.tf` / scripts / skill (artefactos vivos, no transcripcion) | [x] verificado |
