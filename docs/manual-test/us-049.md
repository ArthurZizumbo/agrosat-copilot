# Manual Test — US-049: Evaluacion del copiloto (AgroMind + GeoAnalystBench)

Verificacion del harness de evaluacion **eval-only** (NUNCA fine-tune; AgroMind sin train split =
leakage, ADR-009/011) de las 3 variantes del reasoner: **Gemini 3.5-flash** (cloud, multimodal),
**Qwen3-30B-A3B** (on-prem llama.cpp, solo texto) y **Gemma 4 26B-A4B MoE** (on-prem Ollama,
multimodal, referencia). Lo automatizable esta en `tests/ml/eval/` (46 tests: agent_bench 10,
agent_metrics 36). La **corrida real** (500 AgroMind + 50 GeoAnalystBench, 1 seed) es un flujo
**GPU/manual** con endpoints on-prem vivos + budget de API.

> Commit: `860b01a feat(E7): US-049 harness evaluacion AgroMind + GeoAnalystBench (eval-only)`
> (+ cableado de endpoints en vivo: `scripts/run_us049_eval.py`, `OllamaBackend`, `gemini-3.5-flash`).
> Handoff: [docs/us-handoff/us-049.md](../us-handoff/us-049.md) · Plan v8 §US-049.

---

## MT-1 — Suite de tests + lint + cobertura (automatizado)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python -m pytest tests/ml/eval/test_agent_bench.py test_agent_metrics.py -q` | pasan | [x] verificado (46 tests) |
| `poetry run ruff check ml/eval/agent_bench.py agent_metrics.py agent_report.py scripts/run_us049_eval.py` | "All checks passed!" | [x] verificado |
| Cobertura | >= 70% | [x] verificado (agent_metrics 97%, agent_report 97%, agent_bench 77%) |

---

## MT-2 — Loaders con datos reales (validado)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `load_agromind_subset` sobre `data/agromind/agromind_subset_500.json` | 500 items estratificados (494 multimodal + 6 textual) | [x] verificado |
| Imagenes AgroMind descargadas | `scripts/download_agromind_images.py` (subset bajo demanda, idempotente) | [x] verificado (657 imagenes extraidas) |
| `load_geoanalystbench` sobre `data/geoanalystbench/GeoAnalystBench.csv` | 50 tasks (100% texto) | [x] verificado |
| Datos en .gitignore (no al repo) | DVC/descarga bajo demanda | [x] verificado |

---

## MT-3 — Metricas (automatizado, mocks)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `eval_agromind`: exact_match, f1_squad, bertscore_f1, tool_call_accuracy, hallucination_rate | LLM-as-judge (DeepEval) inyectable -> NaN sin judge | [x] verificado (test_agent_metrics, 36) |
| `eval_geoanalyst`: workflow_semantic_similarity + codebleu, pass por umbral | plan-and-react | [x] verificado (test_agent_bench, 10) |
| Qwen (text-only) salta items multimodales | `n_skipped` reportado honestamente | [x] verificado |
| `run_benchmark`: mean+std multi-seed, MLflow track inyectable, genera HTML | reporte comparativo | [x] verificado |

---

## MT-4 — Corrida REAL de las 3 variantes (GPU/manual, EN CURSO)

> Flujo no automatizable en CI: requiere Gemini 3.5-flash (API, budget acotado), Qwen on-prem
> (`:8002`) y Gemma MoE on-prem (`:11435`) vivos. Lanzado con
> `poetry run python scripts/run_us049_eval.py --seeds 0 --report reports/agent_bench/us049_report.html`.
> Endpoints verificados vivos antes de lanzar (Qwen/Gemma/MLflow = 200).

> **Corrida real CERRADA (2026-06-16, seed 0).** Se uso el subset acotado de **167
> AgroMind** (165 multimodal + 2 textual) + **50 GeoAnalyst** para caber dentro de la
> ventana estable del quick tunnel (la corrida de 500 no completa antes de que el tunel
> se degrade; ver caveats). Numeros guardados en `us049_checkpoint.json` + reporte HTML +
> MLflow run `c7643c8` (`us049_agent_bench`, FINISHED, code_version=`4429e75`).

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Gemini 3.5-flash evalua 167 AgroMind + 50 GeoAnalyst | exact/f1/bertscore + pass GeoAnalyst | [x] verificado (AgroMind exact=**0.539** n=167; GeoAnalyst pass=**0.18**) |
| Qwen3-30B-A3B evalua subset textual AgroMind + 50 GeoAnalyst | n_skipped=165 en AgroMind (text-only) | [x] verificado (AgroMind=**0.0** sobre **n=2** ruido; GeoAnalyst pass=**0.54**) |
| Gemma 4 26B-A4B MoE evalua 167 AgroMind + 50 GeoAnalyst | multimodal | [x] verificado (AgroMind exact=**0.335** n=167; GeoAnalyst pass=**0.36**) |
| Tabla comparativa A/B/base (HTML) | `reports/agent_bench/us049_report.html` | [x] verificado (58 KB, 3 variantes) |
| MLflow `:5010` con `data_version` + `code_version` | experimento `us049_agent_bench` | [x] verificado (run `c7643c8` FINISHED, 19 metricas, code_version=4429e75) |

### Targets de rubrica vs resultado real

| Metrica | Target plan v8 | Resultado real (seed 0, subset) | Estado |
|---------|----------------|----------------|--------|
| AgroMind (Gemini) | >= 0.75 | **0.539** (3.5-flash, n=167) | [ ] NO alcanzado (target era 2.5-pro; flash < pro) |
| AgroMind (Gemma-base, ref) | referencia | **0.335** (n=167) | [~] referencia (Gemini > Gemma en multimodal) |
| AgroMind (Qwen) | >= 0.70 (subset textual) | **0.0** (n=2) | [ ] N/A: subset textual insuficiente (B-7) -> ruido, no comparable |
| GeoAnalystBench pass rate | >= 0.65 | Gemini **0.18** / Gemma **0.36** / **Qwen 0.54** | [ ] NO alcanzado; hallazgo: on-prem (Qwen) > flash en code-gen |

---

## Criterios de aceptacion (plan v8 §US-049)

| Criterio | Estado |
|----------|--------|
| AgroMind con cada variante: exact/f1/bertscore/hallucination | [x] cumplido (corrida real subset 167, seed 0) |
| GeoAnalystBench en plan-and-react | [x] cumplido (50 tasks x 3 variantes) |
| 3 modelos como benchmark, NUNCA fine-tune | [x] cumplido (eval-only) |
| Tabla comparativa + analisis latencia/costo | [x] tabla (HTML + arriba); costo Gemini ~$0.3-0.6, on-prem $0 |
| MLflow con data_version + code_version | [x] cumplido (run `c7643c8` FINISHED, code_version=4429e75) |

---

## Caveats honestos

| Item | Nota | Estado |
|------|------|--------|
| Subset AgroMind casi todo multimodal | 494/500 multimodal -> Qwen (text-only) solo evalua 6; para eval real de Qwen conviene ampliar `make_subset` con filtro `is_multimodal=False` | [x] documentado |
| LLM-as-judge = costo extra | la hallucination_rate usa un juez (Gemini/vLLM) -> dobla las llamadas LLM por item | [x] documentado |
| codebleu aproximado | BLEU n-gram + keyword overlap (sin AST data-flow); documentado | [x] documentado |
| bertscore_f1 = proxy | sentence-transformers, documentado como proxy semantico | [x] documentado |
| Gemini 3.5-flash sustituye a 2.5-pro | decision del usuario (menor costo); el harness es agnostico al id | [x] documentado |
