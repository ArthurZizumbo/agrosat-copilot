# Manual Test — US-065: Observabilidad de chat por turno (latencia SLO + tool-calls + tokens)

Verificacion de la instrumentacion structlog `chat_turn_metrics` + propagacion de `usage` + export
Prometheus gateado + doc operativa. Lo automatizable esta cubierto por
`backend/tests/unit/test_chat_metrics.py` (13 passed). Tokens REALES del proveedor o `None`, nunca
inventados. La US queda WARN: ver MT-5 (deuda honesta).

> US-065 cubre la observabilidad de chat por turno (Avance 7) e insumo del FinOps por modelo (US-067).

---

## MT-1 — Suite del helper de chat metrics

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `cd backend && poetry run pytest tests/unit/test_chat_metrics.py -q` | tests pasan (clasificacion, frontera SLO, inmutabilidad, agregacion tool-calls, `usage` ausente -> `None`, emisor una linea sin `print`, export Prometheus gateado) | [x] verificado (13 passed) |
| `cd backend && poetry run pytest tests/ -q -k metrics` | middleware + chat_metrics juntos | [x] verificado (20 passed) |

---

## MT-2 — `chat_turn_metrics` por turno + SLO + integracion

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Una linea structlog por turno | clave `chat_turn_metrics`, nunca `print()` | [x] verificado (`emit_chat_turn_metrics`; test "una sola linea") |
| Clasificacion SLO | `simple` (0 tool-calls) -> 3000ms; `multi_step` (>=1) -> 15000ms; `slo_met` bool | [x] verificado (`_classify`; test frontera exacta) |
| Conteo de tool-calls | acumulador inmutable `frozen`, `observe_tool_call()` devuelve copia | [x] verificado (test agregacion) |
| `variant` + `model` como clave FinOps | adjuntos al turno | [x] verificado (`ChatTurnMetrics`) |
| Integracion en `ChatService` (SoC) | en `_stream_reasoner`, reusa el `time.perf_counter()` existente; router solo adaptador | [x] verificado (`chat_service.py`) |

---

## MT-3 — Tokens reales del proveedor (o `None`, honesto)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Propagacion del `usage` | `BackendChunk.usage` -> `DoneEvent.usage` -> `tokens_*` | [x] verificado (`ml/agent/{backends,events,agent}.py`) |
| Gemini | tokens REALES via `usage_metadata` (`prompt`/`candidates`/`total`) normalizados | [x] verificado (`GeminiBackend._usage_from_response`) |
| Qwen/vLLM en streaming | `tokens_* = None` sin `stream_options={"include_usage": True}` (no se inventa) | [x] verificado (B6; test `usage` ausente -> `None`) |
| `None` posterior no borra captura real | `observe_usage(None)` no pisa un valor ya capturado | [x] verificado (test) |

---

## MT-4 — Export Prometheus gateado + doc operativa

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Flag por defecto | `chat_metrics_prometheus_enabled = False` (modo solo-structlog) | [x] verificado (`backend/app/core/config.py`) |
| Degradacion honesta (R3) | import perezoso de `prometheus_client`; cualquier fallo se traga con `logger.debug` | [x] verificado (test export gateado) |
| `docs/observability/chat-metrics.md` | catalogo de claves + tabla SLO + receta `jq` p95 + PromQL del panel + gotcha MLflow | [x] verificado (9.297 B) |

---

## MT-5 — Por que WARN (deuda honesta, blockers documentados)

| Item | Nota | Estado |
|------|------|--------|
| Tokens de Qwen en streaming | `None` hasta aplicar `stream_options={"include_usage": True}` (deuda funcional real del switch A/B on-prem) | [x] documentado (B6) — WARN |
| Verificacion MLflow `:5010` en vivo | server Docker no corria en la sesion; tags garantizados por `track_experiment`, auditoria en vivo pendiente | [x] documentado (B7) — WARN |
| Panel Grafana de chat | requiere Prometheus scrapeando `/metrics` con el flag on; no se fabrica trafico sintetico | [x] documentado (B8) |
| Tests de integracion de chat rotos | `Duplicated timeseries` por el middleware HTTP de US-059, NO por US-065 (verificado en arbol limpio); los 13 unitarios pasan | [x] documentado (B9) — fix pertenece a US-059 |
