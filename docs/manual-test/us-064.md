# Manual Test — US-064: Seguridad OWASP + 4 Model Cards + glosario IT/ES/EN

Verificacion de los tres entregables documentales contra los AC. US documental: los pasos verifican
presencia de la tabla OWASP, las 4 Model Cards con metricas trazadas a artefactos reales (verificados en
disco) y el glosario trilingue. El pen-test no se ejecuta contra endpoint live (B17).

> US-064 cubre el cierre documental del MVP (seguridad + trazabilidad de modelos) del Avance 7.

---

## MT-1 — Seguridad OWASP Top 10

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Abrir `docs/security.md` | tabla OWASP A01..A10 con estado + evidencia (ruta o US) | [x] verificado (8.895 B) |
| A03 XSS | saneado del markdown del LLM con `isomorphic-dompurify` (US-057) | [x] verificado |
| A05 CORS | `allow_headers` whitelist + `Settings.extra="forbid"` (`backend/app/main.py`, `core/config.py`) | [x] verificado |
| A10 SSRF | `validate_cog_url()` allowlist antes del fetch GDAL (`cog_tiler.py`, US-055) | [x] verificado |
| Gestion de secretos | `make secrets-scan` (gitleaks) + CI; NO `.pre-commit-config.yaml` ni `detect-secrets` | [x] verificado (§3) |
| Pen-test manual | nmap/nikto + check de aislamiento por sesion, `STAGING_HOST` placeholder | [x] documentado (§5; no ejecutado contra live, B17) |

---

## MT-2 — Cuatro Model Cards (metricas trazadas a artefactos reales)

| Card | Metrica clave | Artefacto fuente | Estado |
|------|---------------|------------------|--------|
| `ensemble-final-e6.md` | E3 Stacking 0.7470 f1_macro; Stacking-5 +FarSLIP 0.6477 | `comparison_us040.csv`, `us043_farslip_stacking_blending.csv` | [x] verificado (artefactos en disco) |
| `tsvit-pheno.md` | mejor segmentador mIoU 0.6139 / F1 0.7401 fold-5 | `model_comparison_fold5.csv` | [x] verificado |
| `farslip-pheno.md` | transfer FR->Catalonia Delta mIoU +0.2468 few-shot | `sen4agrinet_transfer_result.json` | [x] verificado |
| `qwen35-vllm-serving.md` | serving on-prem Qwen GPTQ-Int4 (reemplaza Gemma 4); tokens `None` sin `include_usage` | `ml/agent/backends.py` (honestidad de tokens) | [x] verificado |
| `README.md` (indice) | indice de las 4 cards + plantilla + gotcha MLflow | — | [x] presente |
| Sin card de Gemma 4 fine-tuned | decision (ADR-009/ADR-011), no omision | — | [x] verificado |

---

## MT-3 — Glosario trilingue

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Abrir `docs/glosario.md` | terminos IT/ES/EN agrupados por dominio (EO/datasets, etiquetas, arquitecturas, MLOps, agente/LLM, agronomia) | [x] verificado (7.442 B) |
| AlphaEarth | V1/ANNUAL v1.1 CC-BY-4.0 (nunca "v2.1") | [x] verificado |
| Gotcha MLflow dos almacenes | documentado | [x] verificado |

---

## MT-4 — Caveats honestos (blockers documentados)

| Item | Nota | Estado |
|------|------|--------|
| Pen-test no ejecutado contra live | sin staging activo; procedimiento listo como check manual pre-deploy | [x] documentado (B17) |
| RLS por tenant / `docs/STATUS.md` | A01 = Deuda anotada; estado real RLS-aplicado citado desde la migracion (STATUS.md no existe) | [x] documentado (B18) |
| Cifras de cierre E6 parciales | toda cifra trazada a CSV/JSON; lo ausente queda "pendiente de cierre E6" | [x] documentado (B19) |
| Full post-presentacion | Clerk/JWT/HTTPS LB documentados como deuda, no reclamados | [x] verificado |
