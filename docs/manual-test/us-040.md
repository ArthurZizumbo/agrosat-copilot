# Manual Test — US-040: Cuatro ensambles base de rubrica (Voting/Bagging/Stacking/Blending)

Verificacion de los 4 ensambles base sobre PASTIS-R real, reusando los softmax/OOF de US-031 (fold-5 held-out)
y reportando SIEMPRE en fold-5 (anti-fuga). Lo automatizable esta cubierto por `tests/ml/ensemble/` +
`tests/ml/eval/test_ensemble_figures.py` (160 tests). El run real de los 4 ensambles se ejecuto localmente
(CPU + RTX 4070 para XGBoost) sobre los OOF reales (2026-06-08).

> US-040 cubre el criterio Ensambles (60) + Seleccion (20) + Graficos (20) del Avance 5.

---

## MT-1 — Suite de tests (4 ensambles + anti-fuga + figuras)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python -m pytest tests/ml/ensemble/ tests/ml/eval/test_ensemble_figures.py -q` | tests pasan (base, voting, bagging, stacking, blending, anti-fuga consolidado, figuras) | [x] verificado (160 passed) |
| `poetry run ruff check ml/ensemble/ scripts/run_us040_ensembles.py ml/eval/ensemble_figures.py` | "All checks passed!" | [x] verificado |
| Cobertura ensambles | >=70% | [x] verificado (base 96%, voting 97%, bagging 100%, stacking 94%, blending 98%, figuras 95%) |

---

## MT-2 — Run real de los 4 ensambles (fold-5, datos PASTIS reales)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Ejecutar `scripts/run_us040_ensembles.py --oof-dir ml/eval/oof --pastis-root data/PASTIS-R --features data/features/features_fused_pastis.parquet --fold 5` | exit 0, 4 ensambles evaluados, tabla + figuras + 4 runs MLflow | [x] verificado (4 ensambles corrieron sobre OOF reales fold-5) |
| GT por parcela construido | majority-vote del TARGET semantic18 dentro de ParcelIDs | [x] verificado (28532 parcelas, 496 patches; clases semantic18 0..17) |
| xgb-alphaearth parcel OOF materializado sin leakage | train folds 1-4, predict fold-5 | [x] verificado (n_train=69311, predict fold-5=16640; el modelo nunca ve fold-5) |
| **Tabla comparativa (mejor individual vs 4 ensambles)** | Stacking elegido > individual | [x] verificado (reports/ensemble/metrics/comparison_us040.csv) |

### Tabla comparativa REAL (fold-5 held-out)

| Modelo | f1_macro | accuracy | t_infer (s) | chosen |
|--------|----------|----------|-------------|--------|
| **E3 Stacking (parcela)** | **0.7470** | 0.8490 | 0.042 | **true** |
| E4 Blending (parcela) | 0.7414 | 0.8618 | 0.006 | false |
| TSViT-pheno (individual ref) | 0.6253 | — | — | false |
| E1 Voting (pixel) | 0.6225 | 0.8090 | 42.5 | false |
| E2 Bagging (parcela) | 0.5864 | 0.7816 | 5.50 | false |

**Stacking (0.747) y Blending (0.741) superan al mejor individual (TSViT-pheno 0.6253)** — como premia la rubrica.

---

## MT-3 — Anti-fuga (R-LEAK, NON-NEGOTIABLE)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Reportar en fold-5 (NO fold-4) | evaluate(fold!=5) -> ValueError | [x] verificado (HELD_OUT_FOLD=5; test anti-fuga) |
| Promediar PROBABILIDADES post-softmax (NO logits) | validate_probs rechaza logits | [x] verificado (test_anti_leakage; voting renormaliza float16) |
| Meta-learner (stacking) SOLO sobre OOF | assert_oof_only; CV espacial sub-folds fold-5 | [x] verificado (build_spatial_kfold buffer 1km, NO random; test verifica solape train/eval -> error) |
| Blending sobre holdout espacialmente disjunto | build_spatial_kfold, NO random | [x] verificado (holdout disjunto; spies confirman no-random) |

---

## MT-4 — >=4 graficas interpretadas (criterio Graficos)

| Figura | Archivo | Estado |
|--------|---------|--------|
| Matriz confusion normalizada+absoluta (Stacking) | reports/ensemble/figures/confusion_stacking.png (204KB) | [x] generada |
| ROC one-vs-rest AUC por clase + macro (Stacking) | roc_ovr_stacking.png (211KB), macro_auc=0.976 | [x] generada |
| PR por clase (Stacking) | pr_stacking.png (349KB), macro_ap=0.795 | [x] generada |
| Residuos espaciales sobre geometria real (Blending) | spatial_residuals_blending.png (161KB) | [x] generada |

---

## MT-5 — Lineage MLflow

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| 1 run MLflow `:5010` por ensamble | e1-voting, e2-bagging, e3-stacking, e4-blending | [x] verificado (4 runs experiment 'ensemble' con data_version+code_version) |
| Modelo elegido marcado | chosen=Stacking | [x] verificado (tabla CSV chosen=true; caveat: re-tag MLflow -chosen envuelto en try/except por el artifact-root del MLflow nativo VM, no fatal) |
| OOF de US-031 | fold-5 held_out, NO regenerados | [x] verificado (12 parquet US-031 + xgb materializado) |

---

## MT-6 — Caveats honestos

| Item | Nota | Estado |
|------|------|--------|
| xgb-alphaearth parcel OOF | US-031 NO lo dumpeo -> materializado por el orquestador (folds 1-4 train, fold-5 predict, sin leakage) | [x] documentado |
| Voting 3er miembro | U-Net (TSViT base no dumpeado en US-031); sustituible | [x] documentado (R-VOTE) |
| CV espacial del stacking | sub-folds geograficos del fold-5 (US-031 solo dumpeo fold-5) | [x] documentado (R-OOF-DEPTH) |
| Bug ID/clases/CRS | KeyError '10003_10003_1' por instance_id vs ParcelIDs + clases 0..19 vs semantic18 + CRS EPSG:2154->4326. Resuelto (fix 494232c) | [x] verificado (4 ensambles corren sin crash) |
