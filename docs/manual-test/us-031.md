# Manual Test — US-031: Regenerar softmax/OOF de segmentacion desde best.pt

Verificacion del dump de softmax por-pixel POST-softmax + OOF fold-5 held-out de los 6 modelos.
Lo automatizable esta cubierto por `tests/ml/eval/oof/` + `tests/ml/utils/test_parcel_reconcile.py`
(56 tests). El cierre real (dump 1.47 GB) se ejecuto en la RTX 4070 (2026-06-07).

> US-031 produce los insumos de los ensambles EPIC 6 (US-040/041/042).

---

## MT-1 — Suite de tests OOF + reconciliacion

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python -m pytest tests/ml/eval/oof/ tests/ml/utils/test_parcel_reconcile.py -q` | 56 tests pasan | [x] verificado (56 passed in 13.94s) |
| `poetry run ruff check` sobre los archivos de produccion del diff | "All checks passed!" | [x] verificado |
| Sin regresion en US-030 | suite combinada 97 tests verdes | [x] verificado |

---

## MT-2 — Artefactos OOF (reales, versionados DVC)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `ls ml/eval/oof/*.dvc \| wc -l` | 12 (.dvc: 6 por-pixel + 6 por-parcela) | [x] verificado (12) |
| Leer `ml/eval/oof/manifest.json` | 6 modelos (unet, deeplabv3plus, segformer, utae, tsvit-pheno, anysat), status=ok los 6 | [x] verificado (STATUS=OK COUNT=6) |
| `git check-ignore ml/eval/oof/oof_unet_fold5.parquet` | exit 0 (gitignored, va a DVC no a Git) | [x] verificado (0 .parquet en Git; 12 .dvc + manifest.json en Git) |
| Tamano total | ~1.47 GB (250 MB/modelo por-pixel + 2 MB/modelo por-parcela) | [x] verificado |

---

## MT-3 — Anti-fuga (held_out + post-softmax + norm train)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Verificar held_out solo fold-5 | manifest top-level fold=5 held_out=true; ALL held_out=True; `held_out=fold==5` (dump_oof.py); folds 1-4 NO dumpeados | [x] verificado |
| Verificar POST-softmax (no logits) | `torch.softmax(logits,dim=1)` (segmentation_inference.py:504,566); shape (18,128,128) float16 | [x] verificado |
| `remap_probs_20_to_18` suma 1 | drop Background(0)+Void(19) + renorm, sum-to-1 maxdev 2.98e-7 | [x] verificado (ejecucion directa) |
| `resample_probs_128_bilinear` suma 1 | (18,128,128), sum-to-1 maxdev 3.58e-7 | [x] verificado |
| `pixel_to_parcel_probs` para miembros tabulares | 6 oof_parcel_*.dvc presentes; test_parcel_reconcile verde | [x] verificado |

---

## MT-4 — Pasos NO automatizables (requieren GPU/dvc pull)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Re-ejecutar `scripts/run_us031_dump_oof_fold5.py` | Regenera los 12 parquet (requiere dvc pull checkpoints + PASTIS-R + GPU) | [ ] cubierto por el cierre real del owner (RTX 4070, 2026-06-07); no re-ejecutado en QA-CLI |
