# Manual Test — US-035: Ablacion de bandas FarSLIP (rgb / nir_rgb / 4band-pheno)

Verificacion de la seleccion/reorden de bandas y de los 3 runs de ablacion en H100.
Lo automatizable esta cubierto por `tests/ml/farslip/test_band_selection.py` (24 tests).
Los 3 runs reales se ejecutaron en paralelo en la H100 NVL 96GB (2026-06-08).

> US-035 entrega el argumento de seleccion de bandas que la rubrica premia.

---

## MT-1 — Suite de tests de seleccion de bandas

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python -m pytest tests/ml/farslip/test_band_selection.py tests/ml/farslip/test_pheno_prototypes.py -q` | 38 tests pasan (24 nuevos + 14 US-034) | [x] verificado (38 passed) |
| `poetry run ruff check bands.py train.py distill.py` | "All checks passed!" | [x] verificado |
| Indexado rgb -> (B04,B03,B02) | select_and_reorder_bands(mode=rgb) reordena (2,1,0) | [x] verificado (test vs tensor sintetico) |
| Indexado nir_rgb -> (B08,B04,B03) | (3,2,1) | [x] verificado |
| Indexado 4band -> (B02,B03,B04,B08) | (0,1,2,3) | [x] verificado |
| assert C>=4 (mitiga mismatch 4 vs 10 bandas) | falla rapido si raster no tiene 4 canales | [x] verificado |
| Reusa adapt_patch_embed_to_n_channels (no duplica) | 3->4 init mean-RGB | [x] verificado |

---

## MT-2 — 3 runs de ablacion en H100 (paralelos)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Lanzar 3 runs en paralelo (schtasks farslip_rgb/nir/4band) | 37GB/96GB, 100% util, 3 tasks Running | [x] verificado |
| baseline-rgb: n_in_channels=3, 4 epochs | log `band_selection=rgb n_in_channels=3`, training done | [x] verificado |
| baseline-nir: n_in_channels=3, 4 epochs | log `band_selection=nir_rgb n_in_channels=3`, training done | [x] verificado |
| 4band-pheno: n_in_channels=4 + patch_embed 3->4 | log `band_selection=4band n_in_channels=4`, patch_embed adapted | [x] verificado |
| Checkpoints de las 3 variantes | `F:\checkpoints\farslip\{run}\student_epoch_{0..3}.safetensors` (~343-344MB) | [x] verificado |

---

## MT-3 — Tabla comparativa de ablacion (resultado real)

| Variante | loss_cls | loss_total | Estado |
|----------|----------|------------|--------|
| baseline-rgb | 4.524 | 2.289 | [x] verificado |
| baseline-nir | 4.517 | 2.286 | [x] verificado |
| 4band-pheno | 4.551 | 2.355 | [x] verificado |

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Aporte NIR (loss_cls) | nir 4.517 vs rgb 4.524 = -0.007 (marginal) | [x] verificado |
| Lectura: loss crudo no decide calidad | protos random triviales de memorizar; calidad real = downstream US-037 | [x] verificado (documentado en handoff) |

---

## MT-4 — Pendientes (NO automatizables)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Eval downstream de calidad de embeddings | comparar las 3 variantes vs AlphaEarth | [ ] pendiente US-037 |
| Registrar 3 runs en MLflow :5010 | 3 runs con band_selection + curva loss | [ ] pendiente (server :5010 no levantado en VM; metricas en logs) |
| DVC add de los 3 best | versionado en remote GCS | [ ] pendiente (ADC 401 en VM) |
