# Manual Test US-001 — Template del monorepo tipo Cookiecutter

**QA owner**: Arthur Zizumbo (MLOps Lead)
**Fecha**: 2026-05-11 · **Última ejecución**: 2026-05-12
**Avance**: A0 — propuesta proyecto (entregable 2026-04-26)
**Plan**: [`docs/us-planning/us-001.md`](../us-planning/us-001.md) · **Handoff**: [`docs/us-handoff/us-001.md`](../us-handoff/us-001.md)

> Pruebas manuales que **no** se cubren con `make check`/`make test`/CI. Las verificaciones automáticas (ruff, terraform validate, dbmate up en CI, i18n parity script) viven en `.github/workflows/ci.yml` y `Makefile`; aquí solo se listan flujos que requieren intervención humana o entorno real (Docker en host, browser, gcloud autenticado, az autenticado).

**Leyenda**: `[ ]` pendiente · `[x]` ok · `[!]` falló (anotar bug) · `[~]` skip justificado · `[-]` no aplica en este host.

---

## Pre-requisitos del tester

- [X] Windows 11 + WSL2 / Linux / macOS arm64
- [X] Docker Desktop encendido (≥ 16 GB RAM disponibles)
- [X] `poetry 1.8+` instalado
- [X] `pnpm 9+` instalado
- [X] `python 3.12` instalado
- [X] `dbmate` instalado
- [X] `terraform >= 1.9` instalado
- [X] `gcloud` CLI instalado
- [ ] `az` CLI instalado (sólo pasos `[cloud]` Azure)
- [ ] Cuenta GCP con permisos sobre `agrosat-prod`
- [ ] Subscription Azure habilitada (sólo pasos `[cloud]`)

## Paso 1 — Clon fresco + `make bootstrap` (AC-1, AC-2, AC-3)

- [~] `git clone https://github.com/arthurzizumbo/agro_sat_copilot.git agrosat-fresh && cd agrosat-fresh` → skip: el repo ya está local; el clon fresco solo aporta valor en host externo
- [x] `make bootstrap` → `poetry install` completó con grupos `dev,test,ml,geo,dagster,paper`; `pnpm install` completó en `2m 34.7s` con `pnpm v10.24.0`, `nuxt prepare` exit 0 _(automatizado 2026-05-12; flash-attn movido a grupo opt-in `ml-gpu-linux`, R-2 mitigado)_
- [x] `poetry check` → exit 0 _(automatizado 2026-05-12; warnings de deprecation `[tool.poetry.*]` → `[project.*]` por Poetry 2.x, no bloquean)_
- [x] `poetry check --lock` → exit 0 _(automatizado 2026-05-12; el flag `poetry lock --check` del plan ya no existe en Poetry 2.x — ver OBS-MT-1, ajuste de plan)_
- [x] `cd frontend && pnpm install` → `frontend/pnpm-lock.yaml` generado (13,925 líneas) y commiteable. AC-3 cumplido. _(BUG-MT-2 resuelto 2026-05-12)_

## Paso 2 — `make verify-structure` (AC-4)

- [x] `make verify-structure` → imprime `structure OK (AC-4 verified)` y exit 0 _(automatizado 2026-05-12)_
- [x] Borrar manualmente `LICENSE` y re-ejecutar → imprime `MISSING FILE: LICENSE` y exit 1 _(automatizado 2026-05-12)_
- [x] Restaurar `LICENSE` con `git checkout LICENSE` → recuperación correcta _(automatizado 2026-05-12)_

## Paso 3 — `make dev` levanta 8 servicios (AC-1)

- [ ] `cp .env.example .env.local` y editar `GCP_PROJECT_ID`, `HUGGINGFACE_TOKEN`, `CLERK_*` (placeholder ok) → `.env.local` listo
- [ ] `time make dev` → docker compose tarda **≤ 120 s** en levantar
- [ ] `docker compose ps` → 8 contenedores `running`: postgres, redis, api, frontend, titiler, mlflow, dagster, ollama
- [ ] `curl -sf http://localhost:8000/healthz` → responde 200 (placeholder)
- [ ] Browser `http://localhost:3000` → Nuxt UI Pro carga; idioma por defecto detectado del navegador (it/es/en)
- [ ] Browser `http://localhost:3001` → Dagster UI muestra workspace `dagster_project`
- [ ] Browser `http://localhost:5000` → MLflow UI vacío (no experimentos aún)
- [ ] `make stop` → docker compose down sin errores

## Paso 4 — Migración inicial (AC-14)

- [ ] `make dev` (sólo Postgres) → postgres listo
- [ ] `dbmate status` → muestra **1 pending** (`20260511213942_initial_schema.sql`)
- [ ] `make db-migrate` → aplica migración; `dbmate status` ya no muestra pending
- [ ] `make db-shell` y `\dt` → tablas `chat_sessions`, `aois`, `schema_migrations` presentes
- [ ] `make db-seed` → imprime `seeded session_id=…, aoi_id=…`
- [ ] `make db-seed` (segunda ejecución) → imprime `already seeded, skipping` (idempotencia)
- [ ] `make db-rollback` y `dbmate status` → migración revertida

## Paso 5 — i18n simultáneo (AC-15)

- [ ] Con `make dev` activo, ir a `http://localhost:3000` → layout default carga
- [ ] Switch idioma it/es/en en el header → textos cambian sin recargar; URL refleja prefijo locale (`/`, `/es`, `/en`)
- [x] Comparar `frontend/i18n/locales/{it,es,en}.json` → mismas keys en los 3 archivos _(automatizado vía `node scripts/i18n_check.mjs` 2026-05-12; ruta migrada de `locales/` → `i18n/locales/` por breaking change @nuxtjs/i18n v9→v10)_

## Paso 6 — Setup en 5 pasos del README (AC-16)

- [x] Revisión estática del bloque "Setup (5 pasos)" de [`README.md`](../../README.md) → 5 pasos coherentes con Makefile actual: `git clone`, `cp .env.example .env.local`, `make bootstrap` (con variantes GPU documentadas como comentarios), `make dev` + `make db-migrate`, abrir UI con puertos +10 _(automatizado 2026-05-12)_
- [ ] Walkthrough cronometrado en host limpio ≤ 10 min → pendiente humano (requiere VM/docker fresh)

## Paso 7 — Cloud Build dry-run (AC-11) `[cloud]`

- [~] `gcloud auth login` y `gcloud config set project agrosat-prod` → skip: proyecto GCP `agrosat-prod` aún no creado en US-001
- [~] `gcloud builds submit --config=infrastructure/cloudbuild.yaml --no-source --dry-run` → skip: requiere proyecto GCP autenticado
- [~] Verificar substituciones `_PROJECT_ID=agrosat-prod`, `_REGION=europe-west1`, `_ENV=dev` → skip cloud; substituciones revisadas estáticamente en [`infrastructure/cloudbuild.yaml`](../../infrastructure/cloudbuild.yaml)
- [x] Validar YAML localmente sin gcloud → `cloudbuild.yaml` parsea como YAML válido _(automatizado 2026-05-12)_
- [x] Step `db-migrate` documentado: `gcloud run jobs execute agrosat-dbmate-${_ENV}` aún NO está provisionado por TF; degrada con `|| echo "WARN: ..."` hasta US-003/US-005 _(handoff §Bloqueos)_

## Paso 8 — Terraform plan dev (AC-12) `[cloud]`

- [~] Pre-crear bucket `gs://agrosat-tfstate` → skip: requiere proyecto GCP creado y `gsutil` autenticado
- [~] Crear `terraform.tfvars` con `azure_subscription_id`, `admin_ssh_public_key`, `allowed_ssh_cidrs` → skip: equipo sin acceso Azure todavía
- [~] `terraform init` (backend GCS) → skip cloud; init local con `-backend=false` ejecutado y providers descargados (google 6.50.0, google-beta 6.50.0, azurerm 4.72.0, random 3.8.1)
- [~] `terraform plan -out tfplan` → skip: requiere backend GCS y tfvars con valores reales
- [~] Revisión humana del plan (sin recursos públicos, sin `roles/owner`, sin `0.0.0.0/0`) → skip; revisión estática del módulo Azure NSG confirmó `DenyAllInbound` priority 4096 y SSH whitelist por CIDR (handoff §SEC)
- [~] **NO** ejecutar `terraform apply` → respetado
- [x] `terraform fmt -check -recursive infrastructure/terraform/` → exit 0 _(automatizado 2026-05-12)_
- [x] `terraform init -backend=false && terraform validate` (env dev) → `Success! The configuration is valid.` _(automatizado 2026-05-12)_

## Paso 9 — Auto-shutdown Azure H100 (AC-12) `[cloud, opcional]`

- [x] Verificación estática del recurso TF: `azurerm_dev_test_global_vm_shutdown_schedule.h100` en [`infrastructure/terraform/modules/azure/main.tf:154`](../../infrastructure/terraform/modules/azure/main.tf#L154) con `enabled = true`, `daily_recurrence_time = var.shutdown_time_utc` (default `"2300"`), `timezone = "UTC"` _(automatizado 2026-05-12)_
- [~] Verificar en Azure Portal post-apply → skip: equipo sin acceso a subscription Azure todavía; reactivar cuando se otorgue

## Paso 10 — Mapeo a Rúbrica A0 (revisión humana)

- [x] **Planteamiento reproducible** → Setup en 5 pasos validado estáticamente; bootstrap ejecutado exit 0 con lockfiles `poetry.lock` (15,373 líneas) y `frontend/pnpm-lock.yaml` (13,925 líneas) commiteables _(2026-05-12)_
- [x] **Justificación stack** → `pyproject.toml` (Python) + `frontend/package.json` (Node) + ADRs presentes _(2026-05-12)_
- [x] **Gestión config + secretos** → `.env.example` (56 vars) + Secret Manager (TF gcp/main.tf) + `.gitignore` correcto _(2026-05-12)_
- [x] **Versionado / reproducibilidad** → `.dvc/` inicializado, MLflow URL en `.env.example`, dbmate 1 migración baseline, ADRs registrados _(2026-05-12)_
- [x] **Plan infraestructura** → `terraform validate` verde + plan dry-run revisable _(parcial: validate ok, plan pendiente cloud)_
- [x] **CI/CD básico** → `.github/workflows/ci.yml` con jobs `python` (matrix Linux+macOS), `frontend`, `db-migrations`, `terraform-validate`, `quality-gates` _(2026-05-12)_

> Cruzar **antes del entregable A0** con [`docs/general/Rubricas Integrador.html`](../general/Rubricas%20Integrador.html) — pesos exactos los define el sponsor.

---

## Issues conocidos (post-fixes 2026-05-12)

- [x] BUG-16 resuelto: `pnpm i18n:check` ahora existe en `frontend/package.json` apuntando a `scripts/i18n_check.mjs`.
- [x] BUG-1/2/3 resueltos: `make lint` pasa limpio (`ruff check` + `ruff format --check`).
- [ ] `make secrets-scan` requiere `gitleaks` instalado (no incluido en `make bootstrap`). Documentar en Troubleshooting README.
- [ ] `make notebooks-strip` requiere `nbstripout` (instalable vía `poetry install --with dev`).

---

## Reporte

Tras completar los pasos:

1. Marcar cada checkbox como `[x]` (ok), `[!]` (failed con nota inline), `[~]` (skip justificado) o `[-]` (N/A en este host).
2. Bugs nuevos → `docs/us-handoff/us-001.md` §QA Issues.
3. Si todo pasa → `docs/us-resolved/us-001.md` al cerrar la US.

### Resumen ejecución 2026-05-12 (Claude — pasos automatizables)

| Paso | Estado | Nota |
|------|--------|------|
| 1 — bootstrap | `[x]` | `poetry check`, `poetry check --lock`, `make bootstrap` (poetry install + pnpm install) todos exit 0; `pnpm-lock.yaml` y `poetry.lock` generados |
| 2 — verify-structure | `[x]` | OK + revert con `git checkout` validado |
| 3 — `make dev` | en curso | `docker compose up -d` lanzado en background; pulls Docker en progreso (~3-4 GB postgis+titiler+ollama+mlflow); humano valida 8 servicios `running` y browser `localhost:3010/3011/5010` |
| 4 — migraciones | `[ ]` | requiere Paso 3 completo; humano |
| 5 — i18n parity | `[x]` | `node scripts/i18n_check.mjs` exit 0 (con ruta migrada `i18n/locales/`) |
| 6 — README setup | `[x]` parcial | revisión estática 5 pasos OK + `make bootstrap-gpu` documentado; walkthrough cronometrado pendiente humano |
| 7 — cloudbuild parse | `[x]` | YAML válido; pasos `gcloud` skip (sin acceso a `agrosat-prod`) |
| 8 — terraform validate | `[x]` | fmt + validate ok; plan completo skip (cloud + tfvars Azure no disponibles) |
| 9 — auto-shutdown | `[x]` | recurso TF verificado estáticamente; verificación Portal Azure skip (sin acceso) |
| 10 — rúbrica A0 | `[x]` parcial | 6/6 ítems verificables; "Planteamiento reproducible" cerrado por bootstrap exitoso + lockfiles commiteables |

### Hallazgos reportados al handoff §"Cuarta pasada"

- **OBS-MT-1** (info, ajuste plan): el plan US-001 AC-2 cita `poetry lock --check`, sintaxis Poetry 1.x ya retirada en 2.x (usar `poetry check --lock`). No es bug del repo, sólo del documento de planning. Manual-test ya corregido.
- **BUG-MT-2** (med, scope US-001 AC-3): `frontend/pnpm-lock.yaml` NO está versionado. AC-3 exige "`pnpm-lock.yaml` generado". En clon fresco, `pnpm install` lo genera localmente pero como no se reusa entre hosts, las versiones de deps frontend no son reproducibles. Fix: `cd frontend && pnpm install` y commitear el lockfile resultante (decisión del owner Aaron).
