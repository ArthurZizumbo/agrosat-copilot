# Manual Test — US-034: Fix torch.randn -> prototipos fenologicos reales en FarSLIP

Verificacion del fix del bug contrastivo de FarSLIP (prototipos reales en vez de torch.randn) y del
training real en H100. Lo automatizable esta cubierto por `tests/ml/farslip/test_pheno_prototypes.py`
(22 tests). El training real `farslip-pheno-fix-v1` se ejecuto en la H100 NVL 96GB (2026-06-08).

> US-034 es el fix mas critico del flujo (el bug que hacia perder a FarSLIP 0.163 vs AlphaEarth 0.233).

---

## MT-1 — Suite de tests del fix

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python -m pytest tests/ml/farslip/test_pheno_prototypes.py -q` | 22 tests pasan | [x] verificado (22 passed) |
| `poetry run python -m pytest tests/ml/farslip/ -q` (no romper) | suite FarSLIP verde (65 passed + 2 skip) | [x] verificado |
| `poetry run ruff check` sobre train.py/distill.py/cap_pastis_mapping.py | "All checks passed!" | [x] verificado |
| Reproyeccion 384->768 ortogonal + assert | `_proto_to_clip_proj` produce D=768; 512 lanza ValueError | [x] verificado (test) |
| Mapeo CAP-32 -> PASTIS-18 | expand_to_cap (18->32) + tile = (96,384) region-mayor | [x] verificado (test) |
| torch.randn ya NO es el path por defecto | `--proto-source pastis` default; randn solo fallback | [x] verificado (grep train.py) |

---

## MT-2 — Training real en H100 (farslip-pheno-fix-v1)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Lanzar `--band-selection 4band --proto-source pastis` en H100 (schtasks farslip_4band) | Arranca sin OOM, GPU activa | [x] verificado (VRAM 12GB, util 95-100%) |
| Prototipos reales inyectados (no randn) | log: `proto_source=pastis_prototypes`, `proto_dim_in=384 proto_dim_out=768`, n_protos=96 | [x] verificado |
| patch_embed adaptado 3->4 | log: `patch_embed adapted from_channels=3 to_channels=4 init=mean_rgb_on_extra` | [x] verificado |
| 4 epochs completos | log: `training done`, 4x `epoch done` | [x] verificado |
| Loss contrastiva no diverge | loss_cls por epoch: 4.584 -> 4.569 -> 4.594 -> 4.551 | [x] verificado |
| Checkpoints por epoch | `F:\checkpoints\farslip\4band-pheno\student_epoch_{0..3}.safetensors` (~344MB) | [x] verificado |

---

## MT-3 — Lectura del resultado (NO automatizable, requiere US-037)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Comparar loss_cls vs baselines random | 4band-pheno 4.551 > baseline-nir 4.517 (loss crudo NO comparable entre protos distintos) | [x] verificado (documentado en handoff) |
| Calidad real del embedding | medir downstream vs AlphaEarth (0.163/0.233) | [ ] pendiente US-037 (eval FarSLIP-pheno) |
| Registrar run en MLflow :5010 | run con curva loss + tags | [ ] pendiente (server :5010 no levantado en VM; metricas en log) |
| DVC add del best.safetensors | versionado en remote GCS | [ ] pendiente (ADC 401 en VM; checkpoint en F: del repo) |
