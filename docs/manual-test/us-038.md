# Manual Test — US-038: TSViT full-config (Full-M) retrain H100

Verificacion del re-entrenamiento de TSViT con configuracion full (Tarasiou 2023) sobre PASTIS-R real
en la H100 NVL 96GB. Lo automatizable esta cubierto por `tests/ml/models/test_tsvit_fullm.py` +
`tests/ml/eval/test_registry_tsvit.py` (+ no-regresion rescore). El run real (40 epochs) + re-score fold-5
se ejecutaron en la H100 (2026-06-08).

> US-038 sube el mejor segmentador individual; su mejor checkpoint alimenta US-039 (comparativa) y el ensamble.

---

## MT-1 — Suite de tests (config Full-M + harness)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python -m pytest tests/ml/models/test_tsvit_fullm.py tests/ml/eval/test_registry_tsvit.py tests/ml/eval/test_rescore.py -q` | tests pasan (build_tsvit Full-M, registry tsvit, build_model_for_kind sin shape mismatch, no-regresion rescore) | [x] verificado (36 passed) |
| `poetry run ruff check ml/models/tsvit_wrapper.py ml/eval/dense_metrics.py` | "All checks passed!" | [x] verificado |
| Defaults retrocompat (L4) intactos | build_tsvit sin kwargs = config L4 (no rompe notebooks 5a/5b) | [x] verificado (test) |

---

## MT-2 — Config Full-M (capacidad)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `TSVIT_FULLM_CONFIG` | dim=192, depth_temporal=6, depth_spatial=6, heads=6, dim_head=64, n_timesteps=37, img_size=128, patch_size=8 | [x] verificado (import en VM) |
| n_timesteps=37 (NO 64) | T_MIN real de PASTIS (T_MIN=37, T_MAX=61, T_MEDIAN=46); 64 dejaba patches cortos a su T nativo -> batch no apilable | [x] verificado (fix; check_tmin.py sobre 2468 patches: T_MIN=37) |
| build_model_for_kind reconstruye Full-M | 0 missing/unexpected keys al cargar best.pt | [x] verificado (re-score log: n_missing_keys=0 n_unexpected_keys=0) |

---

## MT-3 — Run real H100 (40 epochs, warmup+cosine)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Lanzar build_and_train("tsvit", Full-M, 40ep, batch 20, folds 1,2,3 / val 4) | exit 0, 40 epochs, warmup+cosine | [x] verificado (40/40 epochs completados, run alt-tsvit-fullm-v1) |
| Curva val_miou ascendente | sube epoch tras epoch (warmup+cosine) | [x] verificado (0.031 ep1 -> 0.455 ep9 -> 0.676 ep~33 -> best_miou 0.699) |
| **Target mIoU fold-4 >= 0.65** | objetivo 0.68-0.72 | [x] verificado (val_miou fold-4 = 0.699; val_f1_macro 0.8036 en ep34) |
| +pp sobre TSViT recortado L4 (0.6215) | +5.5 a +9.5pp | [x] verificado (0.699 vs 0.6215 = +7.7pp val fold-4) |
| Honestidad | NO prometer 0.75+ (PASTIS satura ~70%, Tarasiou 65.1%) | [x] cumplido (0.699 dentro de la banda honesta, sin sobre-afirmar) |
| Checkpointing + auto-resume | best.pt/last.pt por epoch; resume tras corte | [x] verificado (resume desde checkpoint tras 2 cortes de SSH; best_miou preservado) |

---

## MT-4 — Re-score fold-5 held-out (harness US-030)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `rescore_all_checkpoints(fold=5)` sobre best.pt Full-M | re-score apples-to-apples 18-clase contiguo | [x] verificado (model=tsvit status=ok) |
| **mIoU fold-5** | coherente con val fold-4 | [x] verificado (miou 0.6789, f1_macro 0.7942, pixel_acc 0.8878, n_patches 496) |
| Bug del harness corregido | el dataset temporal usa n_timesteps del spec (37), no el default 10 | [x] verificado (sin el fix daba miou 0.17 espurio; con fix 0.6789; ver us-resolved) |
| per_class_iou len=18 | 18 entradas | [x] verificado |

---

## MT-5 — Lineage y DVC

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Run MLflow `:5010` alt-tsvit-fullm-v1 | data_version + code_version + metricas por epoch | [x] verificado (run 180df92e experiment agrosat-segmentation; params+metricas; caveat: artifact upload falla por artifact-root del MLflow nativo VM) |
| best.pt a DVC | versionado en gs://agrosat-dvc-remote | [x] verificado (dvc add checkpoints/segmentation + push: "2 files pushed"; segmentation.dvc committeado; *.pt gitignored) |
| Datos PASTIS real | sin sintetico | [x] verificado (data/PASTIS-R, temporal mode collapse_time=None, NORM por fold, spatial CV folds oficiales) |
