# Manual Test — US-022-b: Deuda tecnica FarSLIP, infra GCP L4 y reencuadre fenologico

Flujos que requieren inspeccion visual, demo local o validacion humana. Lo
automatizable esta cubierto por `tests/ml/models/test_temporal.py` (16),
`tests/ml/train/test_phenology_models.py` (12),
`tests/ml/eval/test_feature_ablation.py` (13),
`tests/ml/features/test_phenology_description.py` (12) y
`tests/dagster/test_farslip_pipeline.py` (10) — **117 tests passing en QA**.

> US-022-b esta en estado **paused** (Fase 3 completa, 4 backlog items
> documentados en `docs/product-backlog/us-022b-*.md`). La rama no se mergea
> hasta cerrar `us-022b.1` (full CUDA run con los 4 fixes ML aplicados) y
> regenerar el notebook 05 con outputs poblados.

---

## MT-1 — Smoke del builder del notebook 05

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python scripts/build_reencuadre_notebook.py --help` | CLI muestra parametros papermill (`n_parcels`, `n_epochs`, `device`, etc.) | [ ] revision visual |
| `make reencuadre-notebook-check` | Smoke ~3 min: 800-1200 parcelas, 2-25 epochs, papermill exit 0; PNG=7, errors=0 | [x] verificado 2026-05-23 (24/24 cells, 7 PNG, 0 errores, 800 parcelas / 2 epochs / CPU) |
| Inspeccionar `notebooks/feature_engineering/05_reencuadre_fenologico.ipynb` tras la ejecucion | 7 PNG embebidos, logs con timestamp `HH:MM:SS +Ns [tag] mensaje`, glosario, conclusiones sin US/EPIC/AC | [x] verificado |
| `make reencuadre-notebook-full` (GPU local CUDA) | Full dataset 85951 parcelas, 200 epochs + early stopping; reemplaza outputs del smoke con los reales | [ ] pendiente — trackeado en `us-022-c` P3 |

---

## MT-2 — Modulos ML nuevos (cobertura unit)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run pytest tests/ml/models/test_temporal.py -q` | 16 tests pasan; cobertura `ml/models/temporal.py` >= 95% | [x] verificado 2026-05-23 (16/16 verde, 99%) |
| `poetry run pytest tests/ml/train/test_phenology_models.py -q` | 20 tests; CPU smoke 2 batches verde; MLflow mockeado (sin red); `_resolve_data_version` + `_resolve_code_version` + `_NullMlflowRun` cubiertos | [x] verificado 2026-05-23 (20/20 verde, cobertura **85%** tras QA-3) |
| `poetry run pytest tests/ml/eval/test_feature_ablation.py -q` | 13 tests; al menos 5 conjuntos (`full`, `no_geom`, `no_geom_no_era5_srtm`, `alphaearth_only`, `phenology_only`) | [x] verificado 2026-05-23 (13/13 verde, 91%) |
| `poetry run pytest tests/ml/eval/test_reencuadre_plots.py -q` | 20 tests; los 6 plot helpers + ramas error / fallback | [x] verificado 2026-05-23 (20/20 verde, cobertura **98%** tras QA-4) |
| `poetry run pytest tests/ml/features/test_phenology_description.py -q` | 12 tests; Gemini mockeado (cero llamadas de red en CI); cache por `parcel_id`; `temperature=0` obligatorio | [x] verificado 2026-05-23 (12/12 verde, 76%) |
| `poetry run pytest tests/ml/features/test_fusion.py -q` | Bloque `pheno_text_*` via LEFT JOIN; `EXPECTED_COL_COUNT_WITH_PHENO_TEXT` valida | [x] verificado 2026-05-23 (26/26 verde, 79%) |
| `poetry run pytest tests/dagster/test_farslip_pipeline.py -q` | 10 tests; `farslip_embeddings_consolidated` skips cuando no hay upstream | [x] verificado 2026-05-23 (10/10 verde) |

> **Cobertura global del diff US-022-b**: 87% (subio de 66% tras QA-3/QA-4). Todos los modulos por encima del target 75%.

---

## MT-3 — Smoke Gemini 3.5 Flash real (sub-US 022b-D)

> Requiere API key valida y APIs habilitadas
> (`generativelanguage.googleapis.com` + ADC quota project `agrosat-copilot`).
> Costo esperado del flujo de prueba: ~$0.001 USD.

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `export AGROSAT_LLM_PROVIDER=google-genai` (o vacio = auto) | Selector elige el cliente nativo `google-genai` 2.6+ | [ ] revision visual |
| Ejecutar smoke `generate_phenology_description` con curva NDVI sintetica de parcela maiz | `gemini-3.5-flash` responde en < 10 s con descripcion agronomica coherente (3-4 frases, sin saltos de linea) | [x] verificado 2026-05-23 (5.07 s, parcela maiz ciclo medio en llanura Padana) |
| Fallback `export AGROSAT_LLM_PROVIDER=litellm` | El generador rutea por LiteLLM sin errores; respuesta normalizada a formato OpenAI | [ ] revision visual |
| Cache hit | Llamar 2 veces con la misma curva: segunda invocacion lee del cache (`PhenologyDescription.from_cache`) — no API call | [ ] revision visual |
| Cuota | Subset estratificado < 5.000 parcelas; gasto total < $5 USD; log de uso en `docs/l4_log.md` | [ ] revision visual cuando se ejecute `us-022b.3` |

---

## MT-4 — Dagster assets de modelos temporales

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run dagster definitions validate -m dagster_project.definitions` | Output verde; 3 assets `phenology_models_*` + 1 `farslip_embeddings_consolidated` registrados | [x] verificado 2026-05-23 — import directo del modulo confirma `phenology_model_tempcnn`, `phenology_model_inceptiontime`, `temporal_models_comparison` definidos. El CLI `dagster definitions validate` requiere `DAGSTER_PG_URL` activo (instancia Dagster); el grafo en si esta valido |
| Modulo `dagster_project.assets.phenology_models` exporta los 3 assets esperados | `python -c "from dagster_project.assets import phenology_models; print(dir(phenology_models))"` lista `phenology_model_tempcnn`, `phenology_model_inceptiontime`, `temporal_models_comparison` | [x] verificado 2026-05-23 |
| UI Dagster local (`make dagster-ui`, puerto 3001) | Los assets aparecen en grupo `phenology_models`; metadata `owner=isaac.avila`, `us=US-022b-C`, `model_arch` visible | [ ] revision visual |
| Materializar `phenology_model_tempcnn` desde la UI | Run completa; emite `MaterializeResult` con `f1_macro`, `mlflow_run_id`, `data_version=us018-phenology-subset` | [ ] revision visual (requiere fixture en disco) |

---

## MT-5 — Infra L4 (sub-US 022b-A)

> Requiere autenticacion `gcloud auth login` + proyecto `agrosat-copilot`.
> El smoke A-5 esta pendiente de billing — ver `docs/l4_log.md`.

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `docker build -f infrastructure/docker/ml-train.Dockerfile -t ml-train:dev .` | Build OK; size < 11 GB compressed | [ ] revision visual |
| `docker run --rm ml-train:dev` (CMD por defecto) | Healthcheck OK: imprime `torch`, `mlflow`, `breizhcrops` versions | [ ] revision visual |
| `gcloud builds submit --config infrastructure/cloudbuild.yaml` | Step `build-ml-train` produce `ml-train:${SHORT_SHA}` en Artifact Registry | [ ] revision visual (pendiente billing) |
| `terraform -chdir=infrastructure/terraform/environments/dev plan` | Plan limpio incluye los 2 SAs nuevos (`mlflow`, `ml_train_run`), bucket `agrosat-artifacts-dev` versioned + lifecycle, Cloud Run `agrosat-mlflow-dev` scale-to-zero | [ ] revision visual |
| `make train-l4 epic=E5 us=US-022b` (smoke A-5) | Job Vertex AI estado `JOB_STATE_SUCCEEDED` con un script trivial (1 epoch sintetica) | [ ] **BLOQUEADO por billing** — ver `docs/l4_log.md` |
| `make cost-audit` | No VM L4 RUNNING/PENDING tras job; gate A-6 verde | [ ] revision visual |

---

## MT-6 — Resultados ML (entregable Avance 4/5)

> Esta es la **interpretacion humana** de los outputs del notebook 05 + las
> tablas en `reports/baseline/reencuadre_fenologico/`. Re-ejecutar tras
> `us-022b.1` (full CUDA con los 4 fixes ML aplicados).

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Tabla `ablation_table.parquet` | >= 5 conjuntos `{full, no_geom, no_geom_no_era5_srtm, alphaearth_only, phenology_only}`; mismo spatial CV; `delta_vs_full` calculado | [x] artefacto en disco (no commiteado) |
| Hipotesis C-2 (Dr. Camacho) | `delta(no_geom vs full) ~= 0.0` confirma que el modelo aprende fenologia, no geografia | [x] confirmado en la corrida del 22-may (delta = 0.0) |
| Hipotesis C-4 (supera baseline 0.32) | XGBoost `full`: F1-macro >= 0.32; objetivo deseable >= 0.45 | [x] 0.4094 (+0.089 vs baseline), gate minimo pasado |
| Comparativa modelos (`model_comparison.parquet`) | TempCNN + InceptionTime + XGBoost, mismo spatial CV | [x] corrida 22-may; pero TempCNN/Inception NO superan baseline (esperaba mejora tras us-022b.1 fixes ML) |
| Plot UMAP clusters | Clusters sin lat/lon; interpretacion documentada en notebook | [ ] revision visual tras us-022b.1 |
| Curvas NDVI por cluster | Plot inline; agronomico (no random) | [ ] revision visual tras us-022b.1 |
| Confusion matrix (best temporal) | Matriz normalizada; clases dominantes vs minoritarias | [ ] revision visual tras us-022b.1 |

---

## MT-7 — Atribuciones de licencia (compliance)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `git diff main -- docs/licenses/DATA_LICENSE.md` | Atribuciones nuevas: Gemini 3.5 Flash (Vertex AI Terms), sentence-transformers (Apache-2.0), breizhcrops (MIT) + TempCNN + InceptionTime + papers Pelletier 2019, Fawaz 2020, Wen 2025 | [x] verificado 2026-05-23 — `grep` encuentra "Gemini 3.5 Flash", "sentence-transformers", "breizhcrops", "TempCNN", "InceptionTime" en el archivo |
| Atribuciones FarSLIP (B-6) | CLIP MIT, RemoteCLIP Apache-2.0, GSAA Italia ODbL **PENDIENTES** | [ ] pendiente — `us-022-c` P1 (FarSLIP materializacion) |

---

## MT-8 — Compliance proyecto (rule 12 CLAUDE.md)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `notebooks/feature_engineering/05_reencuadre_fenologico.ipynb` | Commiteado **con outputs poblados** (HTML tables + PNG inline + logs) | [x] verificado 2026-05-23 (smoke: 24/24 cells, 7 PNG, 0 errores). Full CUDA en `us-022-c` P3 |
| `make notebooks-check` / `make reencuadre-notebook-check` | Papermill end-to-end exit 0 sobre el notebook 05 | [x] verificado en smoke |
| `make check` (Python stack) | ruff + ruff format + mypy backend limpio | [x] verificado tras fix QA-1. Frontend ESLint config issue preexistente (no del diff) |

---

## Bugs y issues encontrados en QA (resumen)

### Resueltos en esta sesion (2026-05-23)

1. **[DONE QA-1] `make check` RUF046** — fix aplicado en `ml/train/phenology_models.py:685` (`n_val = round(len(cls_idx) * val_fraction)`). `ruff check ml/` limpio + format aplicado + 12/12 tests del modulo verdes.
2. **[DONE QA-2] Notebook 05 sin outputs** — `make reencuadre-notebook-check` ejecutado: 24/24 cells executed, 7 PNG embebidos, 0 errores. Cumple rule 12 CLAUDE.md. Full CUDA con dataset completo queda en `us-022-c` P3.
3. **[DONE QA-3] Cobertura `ml/train/phenology_models.py` 71% → 85%** — agregados 8 tests para `_NullMlflowRun`, `_try_mlflow_run`, `_MlflowRun` (lifecycle completo con mlflow mockeado, log_state_dict con torch real), `_resolve_data_version` (DVC md5 + fallback `unknown`), `_resolve_code_version`. 20/20 tests del modulo verde.
4. **[DONE QA-4] Cobertura `ml/eval/reencuadre_plots.py` 0% → 98%** — nuevo `tests/ml/eval/test_reencuadre_plots.py` con 20 tests cubriendo los 6 plot helpers + ramas error (empty, shape mismatch) + fallbacks (sin FFT cols, multi-model vs single-model, NaN filter). 20/20 verde.

### Pendientes consolidados en [`us-022-c`](../product-backlog/us-022-c-cierre-fenologico-farslip-dvc.md)

- FarSLIP Fase 4 (B-1..B-6) — `us-022-c` P1
- Bug XGBoost lookup NaN en `model_comparison.parquet` — `us-022-c` P2
- Full CUDA run con los 4 fixes ML (revalida C-4/C-7) — `us-022-c` P3
- DVC versionado pendiente — `us-022-c` P4
- Gemini 3.5 Flash real sobre subset estratificado (D-4) — `us-022-c` P5

### Infra (no bloqueantes)

5. **[ISSUE-5] gitleaks no instalado** — `make secrets-scan` falla con "El sistema no puede encontrar el archivo". No bloquea US (scan manual sin hallazgos) pero documentar en handoff o usar contenedor.
6. **[INFO] `scripts/cost_audit.sh:29`** — ruff confunde el archivo con Python por el shebang; falsos positivos invalid-syntax. No es Python (.sh), excluir del scope ruff o renombrar.

---

## Comandos de regresion para QA futura

```bash
poetry run pytest \
  tests/ml/models/ tests/ml/train/test_phenology_models.py \
  tests/ml/eval/test_feature_ablation.py \
  tests/ml/features/test_phenology_description.py \
  tests/ml/features/test_fusion.py \
  tests/ml/eval/test_comparison.py \
  tests/dagster/test_farslip_pipeline.py -q

make reencuadre-notebook-check        # smoke ~3 min
make reencuadre-notebook-full         # full CUDA (~30-60 min en GPU local)
poetry run dagster definitions validate -m dagster_project.definitions
poetry run ruff check ml/ dagster_project/ tests/
```
