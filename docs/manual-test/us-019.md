# Manual Test — US-019: Baseline RF/XGB sobre features combinados

Flujos que requieren validacion humana, Docker o inspeccion visual. Los
flujos automatizables ya estan cubiertos por `tests/ml/` (30 tests) y por
la ejecucion del notebook con papermill.

> Estado de los pasos auto-ejecutables: marcados `[x]` cuando Claude los
> verifico durante el cierre de la US. Los `[ ]` requieren un humano.

---

## MT-1 — Servidor MLflow local en Docker

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `make mlflow-up` | El contenedor `mlflow` arranca; `docker compose ps mlflow` muestra `healthy` | [x] verificado |
| Abrir `http://localhost:5010` en el navegador | La UI de MLflow carga, sin error de schema | [x] verificado 2026-05-22 (curl `/health` HTTP 200 + `/` HTTP 200; revision visual sigue recomendada) |
| `make mlflow-down` | El contenedor se detiene limpio | [x] verificado |

**Nota**: el servidor Docker usa la imagen custom
`infrastructure/docker/mlflow.Dockerfile` (MLflow 3.1.0 + `psycopg2-binary`)
con backend store en la DB `mlflow` y artifact root en el bind-mount
`./mlflow-artifacts`. La imagen oficial no trae el driver Postgres.

---

## MT-2 — Entrenamiento del baseline y runs MLflow

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `make train-baseline` (o el CLI con `--mlflow-uri file:./mlruns`) | Entrena RF + XGB; 2 runs `baseline-{rf,xgb}-alphaearth-v1` bajo el experimento `agrosat-baseline`; 2 joblib en `artifacts/` | [x] verificado |
| Inspeccionar los runs en la UI MLflow | Cada run tiene params `hp_*`, metricas `oof_*` y `cv_*`, tags `data_version` + `code_version` | [ ] requiere navegador |
| Verificar el Model Registry | `agrosat-baseline` con 2 versiones registradas | [x] verificado (file store) |

**Resultado obtenido** (dataset completo 85.951 parcelas): RF F1-macro OOF
0.3049, XGBoost F1-macro OOF 0.4055. Ambos por debajo del umbral 0.60 del
Avance 3 — declarado y justificado en el notebook 6.1 (AC-5).

---

## MT-3 — Notebook 04_baseline.ipynb

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `make baseline-notebook` | El notebook se reconstruye y se ejecuta end-to-end con papermill sin excepcion | [x] verificado (3000 muestras) |
| Abrir el notebook y revisar la seccion 2 | Justificacion del algoritmo con 4 argumentos (a-d) y citas | [ ] revision visual |
| Revisar la seccion 6 | Tabla de metricas RF vs XGB + veredicto vs umbral 0.60 + seccion 6.1 con causas y decisiones EPIC 5 | [ ] revision visual |

---

## MT-4 — DVC y buckets GCS

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `dvc status` | El dataset `feature_selection_parcels_subset.parquet` aparece `up to date` | [x] verificado |
| `dvc push` | El dataset se sube a `gs://agrosat-dvc-remote` | [x] verificado |
| `gsutil ls -p agrosat-copilot` | Lista los 4 buckets: `agrosat-tfstate`, `agrosat-data`, `agrosat-artifacts`, `agrosat-dvc-remote` (region `us-central1`) | [x] verificado 2026-05-22 (gsutil lista los 4 buckets en el proyecto `agrosat-copilot`) |
| `dvc pull` en un clone limpio | El dataset se recupera desde el bucket | [ ] requiere clone limpio |

**Adicional 2026-05-22**: `dvc push` del dataset `data/breizhcrops` (1.66 GB,
7 files, md5 `9b065e56…`) ejecutado y verificado contra
`gs://agrosat-dvc-remote` — `dvc status -c` reporta `Cache and remote in sync`.
El caché local se habia perdido y el push reporto `Missing cache files`; tras
`dvc add data/breizhcrops` se regenero el caché (mismo md5, contenido
inalterado) y `dvc push` completo (`7 files pushed`).

---

## MT-5 — XGBoost en GPU local

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Entrenar XGB con `nvidia-smi` disponible | El log emite `xgb_device_resolved device=cuda`; `nvidia-smi` muestra uso de GPU durante el fit | [x] verificado (RTX 4070) |
| Entrenar XGB sin GPU (CI) | `resolve_xgb_device` degrada a `cpu` sin error | [x] cubierto por tests |

---

## Pendientes para un humano

- MT-1: UI MLflow ya verificada programaticamente (HTTP 200 en `/` y `/health`);
  revision visual recomendada como sanity check, pero no bloqueante.
- MT-2, MT-3: inspeccion visual del notebook y los runs en la UI MLflow.
- MT-4: `dvc pull` en un clone limpio del repo (no reproducible desde el repo
  actual; lo cubre el clonado de otro miembro del equipo).
- Importar el bucket `agrosat-tfstate` al state de Terraform y resolver
  placeholders del modulo Azure — **rastreado en
  [`docs/product-backlog/us-019-1-terraform-tfstate-import-azure.md`](../product-backlog/us-019-1-terraform-tfstate-import-azure.md)**
  (2026-05-22). No bloquea entregables academicos; se desbloquea cuando se
  configure el subscription Azure del equipo.
