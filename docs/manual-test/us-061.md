# Manual Test — US-061: Analisis costo-beneficio (A6/A7) + CSV + LaTeX

Verificacion del entregable documental de negocio. Al ser US documental, los pasos son de verificacion
del entregable contra los AC: presencia de secciones, exactitud de cifras (reales, no sinteticas),
etiquetado de estimaciones y presencia de los `.csv`/`.tex` prometidos. El `.xlsx` de rubrica queda
diferido (B17).

> US-061 cubre Costos 20 + Beneficios 20 + Implementacion 30 de los Avances 6 y 7.

---

## MT-1 — Documento de negocio (secciones del AC)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Abrir `docs/business/costo_beneficio.md` | 8 secciones presentes (resumen, costos CRISP-ML(Q), caveat creditos, proyeccion 12m, beneficios 500 ha, ROI, intangibles, trazabilidad) | [x] verificado (14.012 B en disco) |
| Caveat del credito Vertex AI | seccion aclara que el credito GenAI App Builder ($17,178) NO cubre la SKU de Gemini API | [x] verificado |
| Proyeccion 12 meses | ~$1,380 ano 1, enlazada a `docs/operations/finops.md` (sin reescribir cifras) | [x] verificado |
| ROI break-even | mes 3 con aritmetica visible; robustez a mes 5-6 a la mitad del beneficio | [x] verificado |
| Beneficios intangibles | CAP europeo + soberania de datos (Qwen on-prem) | [x] verificado |

---

## MT-2 — Tablas fuente CSV (datos reales, etiquetados)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Abrir `docs/business/data/costos_crisp_ml.csv` | 6 fases CRISP-ML(Q) con `costo_real` + `origen_dato` | [x] verificado (datos publicos $0, GCP ~$0.30-0.49, training $0 al equipo / hist. $262-602, deploy ~$115/mes) |
| Abrir `docs/business/data/beneficios_500ha.csv` | 4 filas con rango + supuesto + fuente + `naturaleza` | [x] verificado (las 4 filas marcan `naturaleza = ESTIMACION no medida`) |
| Cifras ancladas a fuente | costos ligados a `finops.md` / factura GCP; beneficios a literatura | [x] verificado (columna `origen_dato` / `fuente_tipo_estimacion`) |

---

## MT-3 — Export LaTeX para el paper

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Abrir `docs/business/costo_beneficio.tex` | 2 tablas (`tab:costos-crisp-mlq` + `tab:beneficios-500ha`), `booktabs` + `tabularx` | [x] verificado (2.264 B; caption de beneficios aclara "no medidas de un cliente real") |
| Coherencia tex vs csv | mismas 6 fases de costos y 4 filas de beneficios | [x] verificado |

---

## MT-4 — Caveat honesto (blocker B17)

| Item | Nota | Estado |
|------|------|--------|
| Export `.xlsx` de rubrica | no generado esta sesion (sin `openpyxl`/`xlsxwriter` confirmado); respaldo `.csv` + Markdown + `.tex` | [x] documentado (B17, `docs/blockers/epic10-notas.md`) |
| Cifras sinteticas | ninguna: costos anclados, beneficios marcados como estimacion | [x] verificado |
| Una fuente de verdad por cifra | cifras operativas enlazadas a `finops.md` (sin drift) | [x] verificado |
