# Manual Test — US-033: Prototipos de fenologia reales (18 clases)

Verificacion de los prototipos fenologicos por clase (descripcion ES via Gemini Flash -> MiniLM 384).
Lo automatizable esta cubierto por `tests/ml/features/{test_phenology_class_prototypes,test_phenology_text_variant}.py`
(13 tests). El artefacto YA existia (corrida previa con Gemini real); esta US lo VALIDA y endurece los gates
(NO regenera — GEMINI_API_KEY vacia, autorizada solo como cache).

> US-033 es insumo del fix `torch.randn` de FarSLIP (US-034) y de la rama semantica TSViT.

---

## MT-1 — Suite de tests + determinismo del cache

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python -m pytest tests/ml/features/test_phenology_class_prototypes.py tests/ml/features/test_phenology_text_variant.py -q` | 13 tests pasan | [x] verificado (13 passed in 28.77s) |
| `poetry run ruff check ml/features/phenology_class_prototypes.py` | "All checks passed!" | [x] verificado |
| Determinismo cache SHA256 | `_hash_curve('class_1', curve, 'gemini-3.5-flash')` -> `78479c037b72e22b` en 2 corridas (identico); input distinto -> hash distinto | [x] verificado |
| Cache poblado offline | 1968 JSONs en `data/cache/phenology_descriptions/` | [x] verificado |
| Sin re-llamar Gemini | GEMINI_API_KEY/GOOGLE_API_KEY vacias; 13 tests con `set_llm_client(mock)`, cero red | [x] verificado |

---

## MT-2 — Parquet por-clase (artefacto real)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Leer `data/features/phenology_class_prototypes_pastis.parquet` | shape (18, 388): class_id, class_name, ndvi_curve, description, emb_000..383 | [x] verificado |
| class_id 1..18 | 18 clases PASTIS-R reales (Meadow..Sorghum) | [x] verificado |
| Descripciones ES reales (>50 chars) | lens 584-870 (min 584); markers ES: cultivo 18/18, parcela 16/18, crecimiento 17/18 | [x] verificado |
| Embeddings MiniLM 384 no-triviales | abs-sum/fila 15.224-16.006, L2-norm=1.0, 0 filas cero, todos finitos | [x] verificado |

---

## MT-3 — Variante por-parcela

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Leer `data/features/phenology_text_pastis.parquet` | shape (1080, 386): parcel_id Utf8, pheno_text_000..383 | [x] verificado |
| 1 fila por parcel_id | 1080 parcel_id unicos | [x] verificado |

---

## MT-4 — Regenerabilidad (NO ejecutar sin key)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Doc de regeneracion en el modulo | docstring lineas 29-40: comando CLI + precondicion GEMINI_API_KEY | [x] verificado |
| Regenerar el parquet con Gemini real | Requiere GEMINI_API_KEY (vacia hoy); regenerar romperia DVC/PhenoSemanticBranch | [ ] NO ejecutado por diseno (key vacia, autorizada solo como cache) |
