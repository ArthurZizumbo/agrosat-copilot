# Manual Test — US-047: Agente factory + stream_response (loop manual function-calling)

Verificacion del factory del agente (`create_agent`) y del loop manual de function-calling
(`stream_response`) con SDK `google-genai` y AUTOMATIC function calling DESHABILITADO, mas el
backend LLM dual (Gemini cloud / Qwen vLLM on-prem). Lo automatizable esta cubierto por
`tests/ml/agent/`; el streaming real con Gemini/Qwen vivos sigue `[ ]` legitimo (no CI).
Fuente de QA: handoff embebido ([docs/us-handoff/us-047.md](../us-handoff/us-047.md)).

> US-047 cubre el criterio Orquestacion del agente (EPIC 7) — Plan-and-React manual sobre las
> 9 tools de US-045, perceiver de US-046, backend abstracto inyectable.

---

## MT-1 — Suite de tests (agent + backends)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `pytest tests/ml/agent/test_agent.py test_backends.py -q` | agent + backends verdes | [x] verificado (test_agent 17 + test_backends 16 = 33 verdes hoy; cierre original 22: agent 13 + backends 9, la suite crecio con US-048/053) |
| `ruff check` sobre agent.py/backends.py/prompts.py/events.py | "All checks passed!" | [x] verificado (handoff) |
| Cobertura (>=70%) | `agent.py` 97%, `events.py` 100%, `backends.py` 67% (paths LLM en vivo, ver caveats) | [x] verificado (handoff) |

---

## MT-2 — Factory + loop manual de function-calling

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `create_agent(model, tools, instruction, settings)` | agente con tools; default = 5 sync (`get_sync_tools()`) | [x] verificado (test_agent; [ml/agent/agent.py](../../ml/agent/agent.py)) |
| `stream_response(messages, session_id, ctx)` | emite AgentEvent: tool_call, tool_result, text_delta, done, error | [x] verificado (test_agent) |
| AUTOMATIC function calling DESHABILITADO | loop manual (tools async + ToolContext/RLS) | [x] verificado (handoff) |
| Inyeccion de `session_id` por tool (multi-tenant) | el LLM nunca controla el limite de tenant | [x] verificado (test_agent: dispatch) |
| Validacion Pydantic de args pre-ejecucion | args malformados -> error tipado, no crash | [x] verificado (test_agent) |
| Guard `MAX_TURNS=8` | corta loops runaway | [x] verificado (test_agent) |
| `ANALYST_SYSTEM_PROMPT` (Be My Eyes) | rol analista, citas obligatorias, no alucina, no clasifica pixeles | [x] verificado ([ml/agent/prompts.py](../../ml/agent/prompts.py)) |
| Eventos tipados con `type` discriminante | union AgentEvent | [x] verificado (events.py 100%) |

---

## MT-3 — Backend LLM dual inyectable

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `make_backend('gemini*')` -> `GeminiBackend` | google-genai (Vertex AI o API publica) | [x] verificado (test_backends) |
| `make_backend('qwen*')` -> `VLLMOpenAIBackend` | OpenAI-compat, default `:8002/v1` | [x] verificado (test_backends) |
| `make_backend('gemma*')` -> `OllamaBackend` | multimodal (data URI base64) + reasoning fallback | [x] verificado (test_backends: images) |
| Mapeo `genai.Content` <-> OpenAI messages | tools y mensajes intercambiables sin tocar el loop | [x] verificado (test_backends) |
| Variante inyectable (Gemini default; switch a Qwen/Gemma) | factory no cambia | [x] verificado |
| `LLMBackend` ABC nunca hardcodea backend | abstraccion respetada | [x] verificado (code review handoff) |

---

## MT-4 — Cableado al backend HTTP

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `ChatService` reenvia los AgentEvent del agente por SSE | perceiver_observation + tool_call/tool_result/text_delta/done/error | [x] verificado (`backend/app/services/chat_service.py` cablea el agente real) |

---

## MT-5 — Pendientes manuales + caveats honestos

Necesitan Gemini real (API/Vertex) y/o Qwen/Gemma on-prem vivos; demo localhost. No son
codigo inacabado: no son automatizables en CI.

| Item | Nota | Estado |
|------|------|--------|
| Streaming real Gemini | `POST /chat` con query agronomica -> text_delta en vivo + tool_call/tool_result trazables | [ ] manual / browser |
| Multi-turno 3-5 tool calls | list_parcels -> timeseries -> explain -> respuesta con citas a cada tool call | [ ] manual |
| Switch A/B `/llm/switch` Gemini<->Qwen | responde por Qwen sin reescribir el loop | [ ] manual / GPU (US-048) |
| Reasoner razona sobre texto del perceiver | el LLM cita el TEXTO del perceiver, no logits | [ ] manual |
| `backends.py` 67% cobertura | paths de streaming real no unit-testeables sin LLM en vivo; logica de mapeo si testeada | [x] caveat |
| `prompts.py` 0% en coverage de tools | constante string consumida en el loop (cubierto por agent.py) | [x] caveat |
| Excepciones pre-loop | el guard de error es ultimo recurso; excepciones antes del loop no se capturan en `stream_response` | [x] caveat |
