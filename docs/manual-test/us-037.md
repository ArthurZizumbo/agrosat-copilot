# Manual Test — US-037: FarSLIP fiel vs AlphaEarth (separabilidad)

Verificacion de la eval apples-to-apples del espacio de embeddings FarSLIP fiel (US-036-a v2) contra
AlphaEarth sobre PASTIS-R real. Lo automatizable esta en `tests/ml/eval/test_farslip_eval_phenology.py`.
El run real (extraccion CLS768 + silhouette + F1-macro) se ejecuto en la H100 (2026-06-09).

> Entregable: MEDIR honesto si FarSLIP fiel supera 0.163 (previo) / alcanza AlphaEarth. NO garantizar gain.

---

## MT-1 — Suite de tests

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `pytest tests/ml/eval/test_farslip_eval_phenology.py` | silhouette por clase deterministico, apples-to-apples, deltas, rechazo checkpoint italiano, schema px_id | [x] verificado (24 passed) |
| `ruff check scripts/farslip_eval_phenology.py` | "All checks passed!" | [x] verificado |

---

## MT-2 — Extraccion embeddings FarSLIP fiel

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Cargar `faithful_v2/best.safetensors` en FarSLIPExtractor | 398 weights cargados, sin crash | [x] verificado (FarSLIP student weights cargados) |
| CLS 768 (pooler_output, el que el InfoNCE optimizo) | embeddings (N, 768) | [x] verificado (n_dims=768) |
| PASTIS real folds 4/5 held-out | cientos de patches | [x] verificado (723 patches kept, filtro 3:1 ratio=3.0) |
| Solo PASTIS real (no italiano/oficial) | guard rechaza farslip_pairs/4band-pheno/FarSLIP2 | [x] verificado (faithful_v2 aceptado) |

---

## MT-3 — Comparativa vs AlphaEarth (apples-to-apples)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Schema AlphaEarth px_id (= ID_PATCH) | load_alphaearth_for_eval maneja px_id/tile/fold | [x] verificado (parquet 2433x70, px_id) |
| Inner join por patch (mismos patches) | n_shared > 0 | [x] verificado (567 patches compartidos) |
| silhouette + F1-macro 5-fold ambos espacios | tabla 2 filas | [x] verificado |
| **FarSLIP fiel** | silhouette / F1-macro | [x] verificado (0.012 / 0.555, 768 dims) |
| **AlphaEarth 2019** | silhouette / F1-macro | [x] verificado (0.014 / 0.645, 64 dims) |
| delta vs 0.163 y AlphaEarth-aqui | reportado | [x] verificado (−0.151 / −0.0024) |
| Veredicto honesto (sin sobre-afirmar) | "NO supera 0.163; caveat dims; F1-macro es el juez" | [x] verificado (log compare_to_alphaearth_done) |

---

## Conclusion

US-037 **cumplida**. Medicion honesta: **AlphaEarth (F1-macro 0.645) supera al FarSLIP fiel (0.555)** sobre
los mismos 567 patches — confirma el limite de datos de PASTIS. El silhouette es bajo en ambos (mezcla
multi-clase a nivel patch). Resultado valido y reportable; AlphaEarth es el espacio mas fuerte para el
ensamble. Tabla en `reports/farslip/metrics/us037_farslip_fiel_vs_alphaearth.csv`.
