# Manual Test — US-030: Harness unico de metrica de segmentacion (re-score fold-5)

Verificacion del re-score apples-to-apples de los 6 segmentadores reales sobre fold-5 held-out.
Lo automatizable esta cubierto por `tests/ml/eval/{test_class_remap,test_rescore,test_rescore_harness}.py`
(41 tests). El cierre real se ejecuto en la RTX 4070 local (2026-06-07).

> US-030 es prerequisito declarado de toda US de ensamble (US-031/040/041/042).

---

## MT-1 — Suite de tests del harness

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python -m pytest tests/ml/eval/test_class_remap.py tests/ml/eval/test_rescore.py tests/ml/eval/test_rescore_harness.py -q` | 41 tests pasan (golden values de remap, remuestreo, metricas, consolidacion) | [x] verificado (41 passed in 8.39s) |
| `poetry run ruff check` sobre los 6 archivos de produccion del diff | "All checks passed!" | [x] verificado |

---

## MT-2 — Tabla comparativa fold-5 (artefacto real)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Leer `reports/segmentation/metrics/model_comparison_fold5.csv` | 6 filas, columnas model/miou/f1_macro/pixel_accuracy/fold/n_patches/status | [x] verificado (df.height=6) |
| Verificar fold-5 held-out (NO fold-4) | `fold==[5]`, `n_patches==[496]` en las 6 filas | [x] verificado |
| Verificar 6/6 modelos status=ok | `(status=='ok').all()==True` | [x] verificado |
| Verificar metricas reales (sin nulos) | miou 0.1605-0.6139, f1 0.2240-0.7401, pixel_acc 0.6010-0.8637 | [x] verificado |
| Verificar los 6 modelos reales (NO Swin-UNETR) | anysat, deeplabv3plus, segformer, tsvit-pheno, unet, utae | [x] verificado |
| Esquema 18-clase contiguo | `HARNESS_NUM_CLASSES=18` (class_remap.py:54) + per_class_iou len=18 en parquet sidecar | [x] verificado |
| Figura comparativa | `reports/segmentation/figures/barplot_comparison_fold5.png` (59783 B) | [x] verificado |
| Sidecar parquet con per_class_iou anidado | `model_comparison_fold5.parquet` (5732 B), col per_class_iou con 18 entradas/modelo | [x] verificado |

---

## MT-3 — Coherencia con el handoff

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Comparar valores CSV vs tabla del handoff | tsvit-pheno 0.6139/0.7401/0.8637; utae 0.1605 — coincide fila a fila | [x] verificado |
| Ordenamiento temporal > CNN | tsvit-pheno domina (consistente con fold-4 0.6253) | [x] verificado |

---

## MT-4 — Pasos NO automatizables (requieren GPU/notebook)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Re-ejecutar `scripts/run_us030_rescore_fold5.py` sobre los 6 best.pt | Regenera CSV+figura fold-5 (requiere dvc pull checkpoints + GPU) | [ ] cubierto por el cierre real del owner (RTX 4070, 2026-06-07); no re-ejecutado en QA-CLI |
| Actualizar prosa fold-5 en `Avance4.Equipo17.ipynb` | Celdas "Resultados:" a valores fold-5 + re-ejecucion papermill | [ ] pendiente (notebook, validacion visual) |
