# Manual Test — US-010 (EDA univariado Sentinel-2)

**Owner**: Isaac Avila Gutierrez · **Maquina objetivo**: laptop dev (Windows + WSL2 / macOS)
**Pre-requisitos**:

- Repo en branch `feature/E2-US-010-eda-sentinel2`
- PASTIS-R descomprimido en `data/PASTIS-R/` (DATA_S2 + ANNOTATIONS + metadata.geojson)
- `poetry install --with ml,geo,paper` ejecutado sin errores
- Credenciales Google Earth Engine: `ee.Authenticate()` interactivo (primera vez) o service account en `.env.local`

Los gates automaticos (lint, tests, secrets, cobertura) ya estan cubiertos por `make check` y `pytest tests/ml/`. Este documento lista solo los flujos que requieren validacion humana y/o ejecucion local con datos reales.

---

## MT-1 · Smoke papermill modo CI (rapido)

| Paso | Resultado esperado |
|------|--------------------|
| `poetry run papermill notebooks/02a_eda_sentinel2.ipynb /tmp/02a_smoke.ipynb -p sample_size 10000 -p n_patches 5 -p use_gee False` | Exit 0 en <= 3 min · sin errores · `/tmp/02a_smoke.ipynb` contiene outputs · todas las celdas ejecutadas |
| Inspeccionar `/tmp/02a_smoke.ipynb` en Jupyter | Las 10 secciones presentes con tablas y figuras renderizadas. SCL sintetica visible en seccion 3. NDVI temporal en seccion 8. |

## MT-2 · Ejecucion full modo local (AC-1)

| Paso | Resultado esperado |
|------|--------------------|
| `make eda-sentinel2` (papermill con sample_size=100_000) | Exit 0 en <= 15 min · cubre AC-1 |
| `ls paper/figures/us-010/` | >= 6 PNG (`scl_missingness_by_roi_season.png`, `band_stats_heatmap.png`, `band_distributions.png`, `outliers_by_band.png`, `class_cardinality.png`, `stretch_2_98_grid.png`) + >= 1 HTML interactivo (`ndvi_temporal_by_class_roi.html`) + mapa folium (`roi_coverage_map.html`) — cubre AC-11 |
| Abrir `paper/figures/us-010/ndvi_temporal_by_class_roi.html` en navegador | Plotly interactivo con curvas NDVI mensuales por clase x ROI · zoom y tooltips funcionan · cubre AC-7 |
| Abrir `paper/figures/us-010/roi_coverage_map.html` en navegador | Mapa folium con 4 ROIs (3 Italia + 1 PASTIS control) coloreadas distinto · popups con `region` y `crops` · cubre AC-11 |

## MT-3 · Verificacion visual de figuras matplotlib (AC-2/3/4/5/6/8)

Abrir cada PNG en `paper/figures/us-010/` y confirmar:

| Figura | Criterio visual |
|--------|-----------------|
| `scl_missingness_by_roi_season.png` | Heatmap 3 ROIs (Pianura, Toscana, Apulia + PASTIS_FR) x 3 temporadas (spring/summer/autumn); colorbar 0-100 %; otono Pianura ~30-45 % invalido; valores anotados |
| `band_stats_heatmap.png` | 10 bandas en eje Y, 10 stats (mean, std, p5..p95) en eje X; valores legibles |
| `band_distributions.png` | Grid 2 x 5 histogramas log-y; B11 y B12 visiblemente skewed a la derecha; titulo `Distribuciones por banda Sentinel-2 (log y)` |
| `outliers_by_band.png` | Barras horizontales o agrupadas con `pct_outliers` por banda x metodo (IQR vs IsoForest); IsoForest cercano a 5 % en todas las bandas |
| `class_cardinality.png` | Barras horizontales ordenadas; clase 1 (Meadow) domina; clase 0 (background) y 19 (void) EXCLUIDAS — verificar AC-5 estricto |
| `stretch_2_98_grid.png` | Grid 3 x 3 con RGB crudo / stretch 2-98 / histogramas por parcela representativa de cada ROI; contraste visualmente correcto en columna stretch |

## MT-4 · Auth Earth Engine (opcional, GEE en linea)

| Paso | Resultado esperado |
|------|--------------------|
| Primera ejecucion sin token: `ee.Authenticate()` se dispara | Browser abre flujo OAuth Google Cloud; token persistido en `~/.config/earthengine/credentials` |
| `poetry run papermill notebooks/02a_eda_sentinel2.ipynb /tmp/02a_gee.ipynb -p use_gee True -p sample_size 10000` | Cache parquet aparece en `data/cache/gee/*_pianura_padana_*.parquet`, `*_toscana_*.parquet`, `*_apulia_*.parquet`; sample muestra columnas `roi, date, band, value, lon, lat` |
| Sin auth (offline o EE no disponible) | Notebook completa sin crash · seccion GEE imprime warning · DataFrame vacio con schema correcto (degradacion graciosa) |

## MT-5 · Conclusiones accionables (AC-9)

| Paso | Resultado esperado |
|------|--------------------|
| Leer manualmente seccion 10 del notebook ejecutado | >= 7 conclusiones numeradas (actualmente 9) · cada una decide al menos uno de: (a) que bandas mantener, (b) que transformacion aplicar, (c) que umbral SCL usar, (d) que indices priorizar en US-014. Limitaciones explicitas para US-012 / US-006 al final. |

## MT-6 · nbstripout antes del commit (AC-12)

| Paso | Resultado esperado |
|------|--------------------|
| `poetry run nbstripout notebooks/02a_eda_sentinel2.ipynb` | Exit 0; `git diff notebooks/02a_eda_sentinel2.ipynb` no muestra outputs, solo cambios de codigo/markdown |
| `git diff --check notebooks/02a_eda_sentinel2.ipynb` | Sin warnings de whitespace |

## MT-7 · Revision humana (Arthur, code review)

| Item | Criterio |
|------|----------|
| Notebook secuencial | Cada seccion construye sobre la anterior sin assumptions ocultas. Variables compartidas (`df_sample`, `seed`, `sample_size`) inicializadas en seccion 1. |
| Llamadas a helpers (no logica inline) | Notebook **invoca** `summarize_bands`, `detect_outliers_iqr`, etc.; NO redefine logica. |
| Comentarios escasos pero utiles | Cada celda con 1-2 lineas de contexto. Sin paredes de comentarios. |
| Reproducibilidad | `seed=42` aplicado en `stratified_sample`, `IsolationForest`, `np.random.default_rng`. |
| Limitaciones documentadas | Seccion 10 al final reconoce: AC-6 (correlacion) diferido a US-012; SCL sintetica vs real de GEE en US-006. |

## MT-8 · Cross-validation con planning (rubrica Avance 1)

| Pregunta rubrica | Donde se contesta en el notebook |
|------------------|----------------------------------|
| 1. Valores faltantes y patron | Seccion 3 (SCL missingness por ROI x season) |
| 2. Estadisticas resumidas | Seccion 4 (`band_stats_heatmap.png` + tabla Polars) |
| 3. Outliers y deteccion | Seccion 5 (IQR + IsoForest comparados) |
| 4. Cardinalidad categoricas | Seccion 6 (20 clases PASTIS, excluye 0 y 19) |
| 5. Distribuciones / normalidad | Seccion 7 (histogramas + Shapiro + recommend_transform) |
| 6. Variables redundantes | Seccion 10 punto 9: diferido explicitamente a US-012 |
| 7. Tendencias temporales | Seccion 8 (NDVI mensual por clase x ROI) |
| 8. Normalizacion | Seccion 9 (stretch 2-98 + nota sobre NORM_S2_patch.json fold-aware) |
| 9. Clases desbalanceadas | Seccion 6 + Seccion 10 punto 5 (Meadow domina ~25 %) |
| 10. Decisiones FE | Seccion 10 (9 conclusiones accionables) |

## Cierre

Tras completar MT-1..MT-8:

- Confirmar `make check` exit 0 con notebook ya strippeado.
- `git add` solo los archivos del snapshot del handoff (`docs/us-handoff/us-010.md`).
- Commit `feat(E2): EDA univariado Sentinel-2 + PASTIS-R (US-010)`.
- PR a `develop` con enlace al plan + handoff + screenshots de las 6 figuras.
- Al merge: `docs/us-resolved/us-010.md` con evidencia de las 13 ACs (incluidos los pendientes AC-1 y AC-11 ya ejecutados).
