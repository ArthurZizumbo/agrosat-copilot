# Manual Test — US-046: Capa perceiver-reasoner (Be My Eyes) + Spatial-RAG lite

Verificacion de la capa **perceiver** (envuelve los modelos del equipo y emite TEXTO estructurado;
el reasoner NO clasifica pixeles) y del **Spatial-RAG lite** (tool diferida `retrieve_context` tras
flag `rag_enabled`, default off). Lo automatizable esta en `tests/ml/agent/` (20 tests: perceiver 6,
rag 8, retrieve_context 6). El ingest se valido con **datos reales** (20 docs, embedding 64-dim). Los
flujos con pgvector/PostGIS vivo y el evento SSE en `/chat` se marcan como validacion manual.

> Commit: `895a516 feat(E7): US-046 capa perceiver (Be My Eyes) + Spatial-RAG lite`.
> Handoff: [docs/us-handoff/us-046.md](../us-handoff/us-046.md) · Plan v8 §US-046.

---

## MT-1 — Suite de tests + lint + cobertura (automatizado)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python -m pytest tests/ml/agent/test_perceiver.py test_rag.py test_retrieve_context.py -q` | pasan | [x] verificado (20 tests) |
| `poetry run ruff check ml/agent/perceiver.py ml/agent/rag.py ml/agent/tools/retrieve_context.py` | "All checks passed!" | [x] verificado |
| Cobertura | >= 70% | [x] verificado (perceiver 100%, retrieve_context 100%, rag 92%) |

---

## MT-2 — Patron Be My Eyes: el reasoner razona sobre TEXTO, no logits (automatizado)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `PerceiverLayer.observe(parcel_id)` -> `PerceiverObservation` | objeto **texto** (cultivo/fenologia/vigor/confianza), sin tensores | [x] verificado (test_perceiver) |
| `to_prompt_block()` emite bloque ES para inyectar al reasoner | TEXTO citable, no embeddings | [x] verificado |
| `observe_aoi(aoi, year)` para AOI fresca | reusa `classify` (posterior), sin fila en `features_parcels` | [x] verificado |
| El LLM no corre dentro del perceiver | reusa descriptor fenologico (mockeado en test) | [x] verificado (test: no se invoca red) |
| `explain_prediction` es la entrada del perceiver al loop | tool sync de US-045 | [x] verificado |

---

## MT-3 — Spatial-RAG lite tras flag rag_enabled (automatizado)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `rag_enabled` declarado en Settings, default `False` | [config.py:97](../../backend/app/core/config.py#L97) `rag_enabled: bool = False` | [x] verificado |
| `rag_enabled=false` -> `retrieve_context` devuelve vacio SIN tocar DB | degradacion elegante (AC-10) | [x] verificado (test_retrieve_context) |
| `rag_enabled=true` -> inyecta contexto recuperado citado | bloque grounding `[source:parcel_id] content` | [x] verificado |
| `retrieve_context` registrada como **deferred** (no entre las 5 sync) | 5a deferred en el registry | [x] verificado |
| Pipeline `spatial_rag`: `ST_DWithin` -> pgvector cosine(64) -> fusion ponderada | fusion `sw*prox + (1-sw)*sem` | [x] verificado (test_rag, ranking) |
| Vector = AlphaEarth 64-dim (sin e5-mistral, sin GPU) | RAG lite, no completo | [x] verificado |

---

## MT-4 — Migracion rag_documents (automatizado parcial + manual pgvector)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `db/migrations/20260615082041_create_rag_documents.sql` presente | tabla `rag_documents` (parcel_id, geom 4326, content, source, embedding VECTOR(64)) | [x] verificado (archivo existe) |
| Indice GIST sobre geom; SIN HNSW | HNSW es del RAG completo (FUTURE) | [x] verificado (migracion) |
| `dbmate up` aplica la migracion sobre Postgres+pgvector real | tabla creada, columna `vector(64)` valida | [ ] **manual** (requiere imagen Postgres con pgvector; test de migracion hace auto-skip sin ella) |

---

## MT-5 — Ingest real del corpus (validado con datos reales)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python scripts/ingest_rag_documents.py` | corpus `parcel_phenology_captions.parquet` (description) + embedding `features_fused_winning_pastis.parquet` (dim_00..63) + geom por-patch | [x] verificado (smoke: 20 docs, 0 skipped, embedding 64-dim real) |
| Idempotente | re-ingest no duplica | [x] verificado |

---

## MT-6 — Flujos que requieren validacion manual (demo localhost / pgvector)

| Flujo | Pasos | Resultado esperado | Estado |
|-------|-------|--------------------|--------|
| Evento SSE `perceiver_observation` | levantar backend, `POST /chat`, observar el stream | emite `perceiver_observation` ANTES de `done` | [ ] **manual / browser** |
| RAG lite end-to-end | `dbmate up` (pgvector) + ingest + `rag_enabled=true` + query | reasoner aterriza en parcelas vecinas reales | [ ] **manual** |
| A/B hallucination +/-RAG | medirlo en el harness de eval (US-049), no aqui | tasa de alucinacion con/sin grounding | [ ] **manual / GPU** (parte de US-049) |

---

## Criterios de aceptacion (plan v8 §US-046)

| Criterio | Estado |
|----------|--------|
| `perceiver.py` envuelve modelos y emite descripcion estructurada textual | [x] cumplido |
| El reasoner consume el TEXTO, no la imagen | [x] cumplido (Be My Eyes) |
| Evento SSE `perceiver_observation` en `/chat` | [x] cumplido (router MVP; demo = MT-6) |
| `explain_prediction` es la entrada del perceiver | [x] cumplido |
| Spatial-RAG lite tras flag `rag_enabled` (default off) | [x] cumplido |
| Corpus fenologico + AlphaEarth 64-dim, sin e5-mistral/GPU | [x] cumplido |
| `retrieve_context` deferred; A/B medido en la eval | [x] cumplido |
| RAG completo (e5-mistral + HNSW + reranking) sigue FUTURE | [x] cumplido (fuera de alcance, no bloqueado) |

---

## Caveats honestos

| Item | Nota | Estado |
|------|------|--------|
| Tests de migracion pgvector | auto-skip sin imagen Postgres+pgvector (mismo patron US-015) | [x] documentado |
| Query embedding en lite | = embedding del doc mas cercano (no re-sampling GEE/texto); deliberado en la capa lite | [x] documentado |
| pgvector flat scan | mitigado por el pre-filtro `ST_DWithin` (cap 256 candidatos); HNSW = FUTURE | [x] documentado |
| `rag.py` con `parcel_id:int` | ints no son Iterable -> devuelve None correctamente (bug corregido) | [x] documentado |
