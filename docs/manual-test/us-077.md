# Manual Test — US-077 (Demo Mexico aguacate/guayaba, zero-shot CUALITATIVO)

**Fecha QA**: 2026-06-20 · **Rama**: `feature/E12-transfer-multiregion`
**Veredicto**: **ready-to-close** (verde, sin bugs)

QA ejecutado por el orquestador (no se confio en el reporte de handoff: todo re-ejecutado).

---

## 1. Lint / typing

| Check | Comando | Resultado |
|-------|---------|-----------|
| ruff | `poetry run ruff check transfer/mexico_demo.py ../tests/ml/transfer/test_mexico_demo.py ../notebooks/eda/populate_nb_02h.py` | **All checks passed!** |
| mypy | `poetry run mypy transfer/mexico_demo.py` | **Success: no issues found in 1 source file** |

## 2. Tests

`poetry run pytest tests/ml/transfer/test_mexico_demo.py -v` -> **11 passed** (12.98s).

Incluye el meta-test `test_no_classifier_no_f1` (AST + token scan) que garantiza que el
modulo no importa ninguna metrica de clasificacion ni define un estimador.

## 3. Datos REALES (regla Arthur) — VERIFICADO EN VIVO

`data/transfer/mexico_demo_ndvi.parquet`: **164 filas** reales (cols `aoi,date,doy,ndvi`):

| AOI | n imagenes S2 | NDVI medio | min | max |
|-----|---------------|-----------|-----|-----|
| `aguacate_uruapan` | **104** | 0.1882 | -0.0155 | 0.2505 |
| `guayaba_calvillo` | **60** | 0.2131 | 0.1264 | 0.4195 |

`data/transfer/mexico_demo_alphaearth.parquet`: **2 filas** x 66 cols (aoi, cultivo, A00..A63 = 64-dim).
A00 aguacate = 0.155285 · A00 guayaba = 0.070011.

**Re-extraccion en vivo de GEE (no cache)**: con el service account real (`agrosat-copilot`,
`GOOGLE/SATELLITE_EMBEDDING/V1/ANNUAL`, mosaic + reduceRegion(mean) sobre Point(-102.05,19.41).buffer(1500),
scale=10) se obtuvo `A00 = 0.1552849410451416`, que **coincide bit a bit** con el parquet cacheado
(0.1552849410451416). Confirmado: el embedding y la curva salen de GEE REAL, no inventados.

Descripcion fenologica = Gemini real (cache hit con `temperature=0.0`, en outputs del notebook,
parrafos detallados en espaniol por AOI).

## 4. Honestidad critica — SIN claim de clasificacion sobre Mexico

- Modulo `ml/transfer/mexico_demo.py`: 0 funciones de metrica. Las unicas apariciones de "F1"
  son comentarios de la regla de honestidad (lineas 18, 416).
- Notebook `02h_mexico_demo.ipynb`: 0 ocurrencias de `f1_score` / `accuracy_score` /
  `classification_report`. Las 6 menciones de "F1"/"accuracy" estan **todas** en celdas markdown
  de caveat (negaciones "NO reportamos F1 ni accuracy" / trabajo futuro "F1>=0.80"), **ninguna** es
  un valor computado.
- **Caveat prominente**: primera celda markdown (cell 0) en blockquote en mayusculas
  "METODOLOGIA ZERO-SHOT CUALITATIVA -- SIN CLAIM DE EXACTITUD" + "NO reportamos F1 ni accuracy
  para Mexico". Reforzado en cells 13 y 17.
- HCAT `PERMANENT_WOODY|orchard/vineyard` se usa como **analogia cualitativa** explicita, no como
  prediccion (cell 14, `hcat_perennial_framing()`).

Conclusion: **no hay violacion del AC ni de la regla Arthur**.

## 5. Notebook — outputs poblados

`02h_mexico_demo.ipynb` (encoding utf-8): **18 celdas** (10 codigo, 9 con output),
**2 figuras PNG** (embedding 64-dim + curva NDVI), **0 errores** de ejecucion.
Tablas Polars reales (AlphaEarth A00=0.155285; NDVI) y descripciones Gemini reales en outputs.

## 6. Reuso (DRY)

`describe_phenology()` importa y **delega** en
`ml.features.phenology_description.generate_phenology_description` (linea 46 + 399); no reimplementa
el prompt ni la llamada a Gemini. `ml/features/phenology_description.py` no fue modificado
(ultimo commit ajeno a US-077). Reuso confirmado.

## 7. Higiene git

Deliverables (parquets 20KB/3.6KB) un-ignorados explicitamente en `.gitignore` (lineas 377-378);
cache crudo GEE excluido via `data/cache/**`. Archivos US-077 aun no commiteados (estado pre-commit, esperado).

---

## Tabla de criterios de aceptacion

| Criterio | Estado |
|----------|--------|
| Pipeline europeo (AlphaEarth zonal + NDVI fenologico + descripcion Gemini) replicado a 2 AOIs MX reales | PASS |
| Curva NDVI y embedding salen de GEE REAL (164 fechas, 64-dim) — verificado en vivo | PASS |
| Descripcion fenologica = Gemini Flash real, temp=0.0 | PASS |
| NINGUN F1/accuracy/clasificador sobre Mexico (codigo + notebook) | PASS |
| Caveat metodologico PROMINENTE (1a celda markdown) | PASS |
| Degradacion grafica sin curva inventada si GEE falla | PASS |
| Notebook con outputs poblados (2 figuras, 0 errores) | PASS |
| Reusa phenology_description.py (no duplica) | PASS |
| ruff + mypy limpios | PASS |
| 11/11 tests pasan (incl. meta-test anti-violacion) | PASS |
