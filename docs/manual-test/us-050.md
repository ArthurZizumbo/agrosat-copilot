# Manual Test — US-050: Gemma 4 26B LoRA documentado como FUTURE (ADR-011)

US **documental** (sin entrenamiento, sin codigo ejecutable). El entregable es la decision
formal de por que Gemma 4 26B LoRA queda **fuera del alcance** de la presentacion del 27-jun,
con la correccion tecnica del skill `agrosat-llm-finetuning`. No hay tests automatizados:
la verificacion es **lectura humana** del ADR contra los hechos verificados en HuggingFace +
docs de PEFT, y la consistencia de los espejos `CLAUDE.md` / `AGENTS.md`.

> Commit: `26c36b6 docs(E7): US-050 Gemma 4 LoRA FUTURE (ADR-011 + correcciones skill/CLAUDE)`.
> Handoff: [docs/us-handoff/us-050.md](../us-handoff/us-050.md).

---

## MT-1 — Existencia y contenido del ADR (lectura humana)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Abrir `docs/decisions/ADR-011-gemma4-lora-future.md` | ADR presente, status FUTURE post-27jun | [x] verificado (archivo existe) |
| Verificar el id real del modelo | `google/gemma-4-26B-A4B-it` (el `gemma-4-26b-it` NO existe) | [ ] **revision humana**: contrastar contra HF |
| Verificar el bloqueo tecnico de QLoRA | expertos MoE = tensores 3D fused `nn.Parameter` -> bitsandbytes no cuantiza el layout 3D | [ ] **revision humana** |
| Verificar la via real de fine-tune | `target_parameters=["mlp.experts.gate_up_proj","mlp.experts.down_proj"]` (PEFT >= 0.17, bleeding-edge) | [ ] **revision humana** |
| Verificar que `target_modules=[gate/up/down_proj]` NO matchea | ~0.91% params entrenables (no aprende) | [ ] **revision humana** |

> Por que `[ ]`: son afirmaciones tecnicas verificables solo por un humano contra fuentes
> externas (HF model card, docs PEFT). El orquestador NO puede palomearlas con un test.

---

## MT-2 — Leakage / AgroMind eval-only (criterio NON-NEGOTIABLE)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| ADR documenta AgroMind como eval-only | ~28,482 QA sin train split -> "fine-tune sobre AgroMind" = leakage | [x] documentado en ADR-011 |
| AgroMind-IT/ES propio (500 pares) tambien es eval | no se usa para entrenar | [x] documentado |
| Coherencia con US-049 | el harness de eval es eval-only, NUNCA fine-tune | [x] coherente (ver [us-049.md](us-049.md)) |

---

## MT-3 — Ratificacion del stack (consistencia con decisiones irrevocables)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| ADR ratifica reasoner = Gemini (cloud) | sin cambio de decision | [x] verificado |
| ADR ratifica on-prem = Qwen3-30B-A3B-Int4 vLLM/llama.cpp | sustituye al inexistente `Qwen3.5-35B` | [x] verificado (coherente con US-048) |
| Gemma LoRA = FUTURE (expert-LoRA + SFT sintetico propio) | reactivable post-presentacion | [x] verificado |
| ADR-011 enlaza ADR-009 (D-1 prioridad H100) | trazabilidad de la decision | [x] verificado |

---

## MT-4 — Espejos CLAUDE.md / AGENTS.md y skill (consistencia de repo)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `CLAUDE.md` y `AGENTS.md` marcan Gemma 4 LoRA = FUTURE con id real | tabla "Decisiones Irrevocables" actualizada | [x] verificado (ambos citan ADR-011) |
| Los dos archivos siguen siendo espejos identicos | `diff CLAUDE.md AGENTS.md` minimo (solo cabecera de pointer) | [ ] **revision humana**: `diff` manual recomendado antes de PR |
| Skill `agrosat-llm-finetuning` corregido | banner FUTURE, id real, QLoRA bloqueado, `target_parameters`, AgroMind eval-only | [x] verificado (skill actualizado) |

---

## Criterios de aceptacion (plan v8 §US-050)

| Criterio | Estado |
|----------|--------|
| ADR corrige el skill (id real, QLoRA bloqueado, `target_parameters`, ~0.91%) | [x] cumplido (ADR-011) |
| ADR documenta AgroMind eval-only -> fine-tune = leakage | [x] cumplido |
| ADR ratifica Gemini + Qwen on-prem; Gemma LoRA FUTURE | [x] cumplido |
| No se entrena Gemma 4 en este horizonte | [x] cumplido (esa ES la decision) |

---

## Conclusion

US-050 **cumplida** como US documental. La unica accion pendiente de **revision humana** (no
automatizable) es contrastar las afirmaciones tecnicas del ADR contra HF/PEFT y correr un `diff`
de los espejos `CLAUDE.md`/`AGENTS.md` antes del PR. No hay codigo ni entrenamiento que probar.
