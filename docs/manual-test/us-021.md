# Manual Test — US-021: Curvas de aprendizaje, validacion y sub/sobreajuste

Flujos que requieren inspeccion visual. Lo automatizable esta cubierto
por `tests/ml/eval/test_learning_curves.py` (34 tests) y la ejecucion del
notebook con papermill.

---

## MT-1 — Modulo de curvas

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `make learning-curves-test` | 34 tests pasan; cobertura `learning_curves.py` >= 85% | [x] verificado (98%) |
| Verificar que no hay circular import | `from ml.eval import learning_curves, metrics, interpretability` carga sin error | [x] verificado |

---

## MT-2 — Notebook 04_baseline.ipynb seccion 5b

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `make baseline-notebook` | El notebook se reconstruye y ejecuta end-to-end con papermill, 23 code cells sin excepcion | [x] verificado |
| Abrir el notebook, seccion 5b | Curva de aprendizaje RF + XGB, 2 curvas de validacion, diagnostico textual `FitDiagnosis` por modelo | [ ] revision visual |
| Verificar el diagnostico | Cada modelo reporta `verdict` (overfit/underfit/good_fit) + `gap` numerico + explicacion | [ ] revision visual |

---

## MT-3 — Artefactos de curvas

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `ls reports/baseline/*curve*` tras el notebook | 4 PNG: `learning_curve_{rf,xgb}.png`, `validation_curve_rf_max_depth.png`, `validation_curve_xgb_n_estimators.png` | [x] verificado |
| Inspeccion visual de `learning_curve_rf.png` | Curvas train/val con banda ±std sombreada, dpi 200 | [ ] revision visual |

---

## MT-4 — Documentacion del spatial CV

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| Abrir `docs/spatial_cv_baseline.md` | Explica por que spatial CV (no random), H3+KMeans+buffer 1km, cita Lyons 2018 / Roberts 2017 | [ ] revision de contenido |

---

## Pendientes para un humano

- MT-2, MT-3: inspeccion visual del notebook seccion 5b y los PNG de curvas.
- MT-4: revisar el contenido de `docs/spatial_cv_baseline.md`.
