# Manual Test — US-032: Filtro 3:1 Meadow per-patch (dominance_ratio en PastisFilter)

Verificacion del modo `dominance_ratio` extendido sobre el `PastisFilter` de Isaac.
Lo automatizable esta cubierto por `tests/ml/data/test_pastis_filter.py` (23 tests, incluye 2
sobre PASTIS-R real). Validacion CLI ejecutada sobre PASTIS-R real (2433 patches, 5 folds).

> US-032 es insumo del incremental FarSLIP 4->18 clases (US-036). Directiva del sponsor.

---

## MT-1 — Suite de tests del filtro

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `poetry run python -m pytest tests/ml/data/test_pastis_filter.py -q` | 23 tests pasan (incluye 2 sobre PASTIS real) | [x] verificado (23 passed in 8.39s) |
| `poetry run ruff check ml/data/pastis_filter.py` | "All checks passed!" | [x] verificado |

---

## MT-2 — Filtro 3:1 sobre PASTIS-R real

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `PastisFilter(mode=dominance_ratio, ratio=3, meadow_class=1, target_classes={1,2,3,8})` sobre los 5 folds | Mantiene 1589/2433 patches (65.3%) Stage-1 | [x] verificado (kept=1589 total=2433 65.3% match exacto) |
| Mismo filtro con target_classes={1..18} | Mantiene 1823/2433 (74.9%) | [x] verificado (match handoff) |
| Golden TARGET_10102 (Meadow=6950, Corn=2264) | ratio 3.070 > 3 -> DROP | [x] verificado (keep=False ratio=3.070) |
| Golden TARGET_10004 (Meadow=0) | KEEP (no dominado) | [x] verificado (keep=True ratio=0.000) |

---

## MT-3 — Modo coverage legacy intacto (no destructivo)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| `PastisFilter(mode=coverage, min_coverage=0.5)` (default) | Corre independiente del modo nuevo (675/2433, 27.7% con ALL_CROP_CLASSES) | [x] verificado |
| `data/pastis_farslip_filtered_splits.json` | NO modificado por el modo nuevo (cambio aditivo) | [x] verificado |
| Firma `tuple[bool,float]` preservada | `keep,ratio = filter._passes(mask)` desempaqueta OK; filter_patch_ids/filter_folds/coverage_stats sin cambio de aridad | [x] verificado |

---

## MT-4 — Edge cases (politica documentada)

| Paso | Resultado esperado | Estado |
|------|--------------------|--------|
| meadow_px==0 | KEEP (no hay dominancia) | [x] verificado (test + TARGET_10004) |
| second_px==0 con meadow_px>0 | DROP (Meadow sin competidor) | [x] verificado (test) |
| 3:1 exacto | KEEP (`<=` inclusivo) | [x] verificado (test) |
