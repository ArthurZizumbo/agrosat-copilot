# Manual Test — US-074 Crosswalk taxonomico PASTIS-18 -> HCAT v3

**Fecha QA**: 2026-06-20 · **Rama**: `feature/E12-transfer-multiregion` · **Verdict**: ready-to-close

QA ejecutado de forma independiente (no se confio en el reporte de handoff). Cada
comando fue corrido por el revisor y su salida verificada.

## 1. Lint (ruff)

```
cd ml && poetry run ruff check .
-> All checks passed!
```

## 2. Type-check (mypy)

```
cd ml && poetry run mypy data/hcat_crosswalk.py eval/class_remap.py
-> Success: no issues found in 2 source files
```

## 3. Tests

El filtro `-k "crosswalk or label_space or hcat"` desde rootdir no colecta el archivo
(exit 5, no tests ran). Ejecutado directo el archivo de la US:

```
cd ml && poetry run pytest ../tests/ml/eval/test_hcat_crosswalk.py -v
-> 13 passed in 5.17s
```

| Test | Estado |
|------|--------|
| test_crosswalk_has_18_rows | PASSED |
| test_all_leaf_codes_exist_in_eurocrops | PASSED |
| test_semantic18_id_is_pastis_minus_one | PASSED |
| test_macro_count_in_range | PASSED |
| test_meadow_isolated_in_grassland | PASSED |
| test_three_approx_matches | PASSED |
| test_macro_vocabulary_matches_constant | PASSED |
| test_void_convention_is_crop_for_all_rows | PASSED |
| test_parquet_roundtrip | PASSED |
| test_committed_parquet_matches_rebuild | PASSED |
| test_hcat_macro_registered | PASSED |
| test_france9_intact | PASSED |
| test_classify_not_imported_change | PASSED |

## 4. Datos reales (regla cero-sinteticos)

Se abrio `data/reference/hcat_crosswalk.parquet` (18 filas, 11 columnas, `hcat_leaf_code`
como `String` para preservar ceros a la izquierda) y se cruzaron sus 18 `hcat_leaf_code`
contra la columna `HCAT3_code` de `data/reference/eurocrops_hcat3.csv` (384 nodos reales):

```
LEAF CODES IN CW: 3302000000, 3301010101, 3301010600, 3301010401, 3301060401,
3301010402, 3301060500, 3303060000, 3301290700, 3301010801, 3301010201, 3301070000,
3301030000, 3301090300, 3301160000, 3303010000, 3301010000, 3301010900
MISSING FROM EURO (BUG si no vacio): []
```

Los 18 codigos hoja existen en el CSV real de EuroCrops. **Cero codigos inventados.**
Coincide con los valores nombrados en el handoff (`pasture_meadow=3302000000`,
`winter_common_soft_wheat=3301010101`, `grain_maize=3301010600`, `vineyards=3303060000`,
`sugar_beet=3301290700`). 3 filas marcadas `match_quality=approx` (Mixed cereal, Fruits/veg,
Leguminous fodder) usan codigo de grupo, documentado y aceptado.

## 5. Notebook con outputs poblados

```
notebooks/eda/02f_crosswalk_hcat.ipynb
TOTAL CELLS: 18 · CODE CELLS: 9
CODE CELLS WITH OUTPUT: 9 · WITHOUT OUTPUT: 0
PNG images inline: 3 · HTML tables inline: 4 · ERROR outputs: 0
```

Notebook ejecutado end-to-end, sin celdas vacias, sin errores, con datos reales del
crosswalk. 3 figuras PNG inline + 4 tablas HTML, exactamente como declara el handoff.

## 6. classify.py SIN cambios (AC-6)

```
git diff --stat ml/agent/tools/classify.py
-> (vacio: 0 lineas)
```

`classify.py` consume el registry por nombre: `label_space = get_label_space(inp.label_space)`
(linea 762). No referencia HCAT ni hcat-macro directamente.

## 7. Registry extensible

El label-space `hcat-macro` se registra integramente en `ml/eval/class_remap.py`
(lineas 614-615: `HCAT_MACRO = _build_hcat_macro()` + `register_label_space(HCAT_MACRO)`),
sin tocar `classify.py`. Recuperacion en vivo:

```
get_label_space("france-9")   -> OK (intacto, 9 kept ids; test_france9_intact PASSED)
get_label_space("hcat-macro") -> name=hcat-macro, class_names=18 (test_hcat_macro_registered PASSED)
```

La firma de `restrict_posterior` no cambio; US-053 no se rompe.

## Tabla de Criterios de Aceptacion

| Criterio | Estado |
|----------|--------|
| AC-1: Crosswalk 18 clases PASTIS -> HCAT v3 hoja (codigos reales) | PASS |
| AC-2: Colapso a macro-clases en rango [10,15] | PASS (10 grupos cultivo + void) |
| AC-3: void/background -> ignore_index 255 (void_convention) | PASS |
| AC-4: label-space `hcat-macro` registrado, 18 kept ids | PASS |
| AC-5: parquet committed == re-derivacion desde CSV real | PASS |
| AC-6: classify.py SIN cambios (registry extensible) | PASS |
| AC-7: notebook EDA ejecutado con outputs (tablas + PNG) | PASS |
| AC-8: tests verdes + ruff + mypy limpios | PASS (13 passed) |

## Observaciones (no bloqueantes)

- Los artefactos `data/reference/hcat_crosswalk.parquet` y `ml/data/hcat_crosswalk.py`
  estan untracked (`??`) y `ml/eval/class_remap.py` modificado sin commitear en la rama.
  Falta `git add` + commit antes del PR; no afecta la correctitud del QA.
- `make check` frontend (ESLint v9) roto pre-existente, ajeno a US-074; ml validado aparte.

**Verdict: ready-to-close.**
