# Handoff US-016 — Fusión multisensor a nivel parcela

**Estado**: planning
**Epic**: E3 (Feature Engineering · CRISP-ML(Q) Data Preparation)
**Avance**: A2 (Feature Engineering · dom 17-may-2026, 100 pts) — cierre operativo dentro del slack si entra en S4; rueda a A3 (20-may) como precondición de baselines en su defecto.
**Sprint**: S4 (11-may a 17-may) — encadenado tras cierre US-014/US-015
**Rama prevista**: `feature/E3-US-016-multisensor-fusion-parcels`
**Última fase**: 1 — planning (2026-05-17)
**SP**: 3
**Owner técnico**: Isaac Ávila (ML/Data Scientist) — coordinación con Arthur (Platform) para Dagster + DVC + dep `h3`

---

## Dominios tocados

- [ ] backend
- [ ] frontend
- [x] **ml** (`ml/features/fusion.py`, `ml/features/spatial_split.py`, `ml/features/scaler.py`, extensión de `ml/ingest/gee_sampler.py`)
- [ ] agent (`ml/agent/`)
- [ ] infra
- [x] **db** (lectura solo — `parcels`, `features_parcels` de US-015; sin migración nueva)
- [x] **dagster** (3 assets nuevos `parcel_features_fused`, `parcel_splits_spatial_kfold`, `parcel_features_scaler`)

---

## Archivos a crear (planning — confirmar en Fase 3 coding)

- `ml/features/fusion.py` — `build_fused_features(parcels, year, blocks, *, include_farslip, stats, lazy) -> pl.DataFrame`. Helpers `_build_ae_block`, `_build_indices_stats_block`, `_build_s1_block`, `_build_srtm_block`, `_build_era5_block`, `_build_geom_block`, `_build_farslip_block`. Constantes `FUSION_STATS`, `BLOCK_NAMES`, `EXPECTED_COL_COUNT_NO_FARSLIP=189`.
- `ml/features/spatial_split.py` — `build_spatial_kfold(parcels, k=5, h3_res=5, buffer_km=1.0) -> list[FoldAssignment]` con tessellation H3 + KMeans + buffer exclusion.
- `ml/features/scaler.py` — `fit_scaler_on_train(df, train_ids, feature_cols, *, scaler_path) -> StandardScaler` + `load_scaler(path)`. Joblib persistence.
- `ml/ingest/gee_sampler.py` (extensión) — `sample_s1_roi_for_parcels`, `sample_srtm_terrain`, `sample_era5_monthly_climate`.
- `scripts/build_parcel_features.py` — CLI Typer con args `--year --regions --out --scaler-out --splits-out --include-farslip`.
- `dagster_project/assets/features.py` — 3 assets con lineage declarada.
- `tests/ml/features/test_fusion.py` (~22 casos), `tests/ml/features/test_spatial_split.py` (~10), `tests/ml/features/test_scaler.py` (~6), `tests/ml/ingest/test_s1_srtm_era5_samplers.py` (~9 + 3 `requires_gee`), `tests/dagster/test_features_assets.py` (~4).
- `data/test_fixtures/parcels_demo_3regions.parquet` — 9 parcelas sintéticas (<50 KB).
- `docs/manual-test/us-016.md` — 5 flujos manuales documentados.

## Archivos a modificar (planning)

- `ml/features/__init__.py` — re-export public API US-016.
- `ml/ingest/__init__.py` — re-export samplers nuevos.
- `dagster_project/definitions.py` — registrar 3 assets.
- `Makefile` — targets `features-fuse-demo`, `features-fuse-italy`, `features-split-spatial`, `features-scaler-fit`, `dagster-materialize-features`.
- `pyproject.toml` — `h3 ^4.1.2` en grupo `geo` (**decisión no-autónoma**, requiere aprobación equipo).
- `.dvcignore` — excepciones para outputs versionados.
- `docs/spectral_indices.md` — sección "Multisensor fusion vector layout".
- `docs/licenses/DATA_LICENSE.md` — atribuciones Sentinel-1, SRTM, ERA5-Land, h3.

## Archivos existentes reutilizables (NO duplicar)

- `ml/ingest/gee_sampler.py::sample_alphaearth_at_coords` — bloque AlphaEarth.
- `ml/ingest/gee_sampler.py::era5_annual_precip` — referencia de patrón para `sample_era5_monthly_climate`.
- `ml/features/spectral_indices.py::compute_index_timeseries` — bloque índices stats.
- `ml/features/temporal_features.py` (US-015) — referencia conceptual; los stats temporales fusionados usan subset `{mean, std, p25, p50, p95}` distinto a los 9 stats completos de US-015.
- `db/migrations/20260516210000_create_parcels.sql` + `20260516210100_create_features_parcels.sql` (US-015) — schema source; US-016 solo lee.
- `.claude/skills/agrosat-ml-features/SKILL.md` — convenciones Polars LazyFrame para fusion multisensor.

## Decisiones técnicas clave (planning)

1. **Script CLI y asset Dagster comparten `ml/features/fusion.py`** como único punto de entrada (Opción A §2.1 del plan). DRY enforced.
2. **Sentinel-1 ingest mínimo viable** en esta US (preset GRD IW VV+VH asc+desc despeckle Lee 7×7 sigma0 dB). Refactor profundo a una US-007.x futura.
3. **Stats temporales subset** `{mean, std, p25, p50, p95}` para fusión (5), distinto a los 9 stats de US-015. Justifica los 85 cols del CA (5×17). NDVI mean/std del JSONB de US-015 se reusan donde estén; resto recomputado con cache Redis de US-014.
4. **h3 res 5 + KMeans para spatial K-fold** con buffer 1 km. Fallback documentado a grid rectangular si `h3` no se aprueba.
5. **Scaler global de Fold-0** persistido con joblib en `artifacts/scaler_v1.pkl`. Trade-off vs 5 scalers por fold §2.5; opcional persistencia múltiple en US-018.
6. **FarSLIP bloque opcional (LEFT join)** — US-016 cierra sin el bloque 512-dim si US-016b no entrega en S4. Ablation declarada en notebook.
7. **DVC versioning** para `features_fused_v1.parquet`, `spatial_kfold_v1/`, `scaler_v1.pkl`. Tag `fused-features-italy-v1`.
8. **ERA5 monthly via groupby server-side GEE** sobre `ECMWF/ERA5_LAND/DAILY_AGGR` (tmean + prec_acc por mes × 12 = 24 cols). Cache parquet local.

## Zonas sensibles

- `ml/ingest/gee_sampler.py` — extensión de módulo existente que usan US-014/015/Avance 1. NO modificar funciones existentes (`sample_s2_roi`, `sample_alphaearth_*`, `era5_annual_precip`); solo añadir nuevas.
- `dagster_project/definitions.py` — registro de assets debe preservar el asset existente `health` y respetar el grouping `feature_engineering`.
- Schema layout (191 cols con FarSLIP off, 703 con FarSLIP on) — downstream US-019/020/021/025 dependen del orden exacto. Cualquier cambio post-merge requiere rollforward versionado (`features_fused_v2`).
- `artifacts/scaler_v1.pkl` — formato joblib. NO migrar a pickle desnudo (security).

## Modelos / Datos

- MLflow run id: n/a (sin training; StandardScaler no es modelo en sentido MLflow).
- DVC versions esperadas:
  - `data/features/features_fused_v1.parquet` → tag `fused-features-italy-v1`
  - `data/splits/spatial_kfold_v1/` → tag `spatial-kfold-italy-v1`
  - `artifacts/scaler_v1.pkl` → tag `scaler-v1`
- VRAM H100 consumida: 0 GB (US-016 es CPU + I/O + GEE server-side). 0 horas del presupuesto 80h.
- Quota GEE estimada: ~2M elementos · margen 5× sobre cuota diaria 10M.

## engram-memory

- Observaciones a guardar al cierre (planning → coding → qa): decisión orquestación dual (script + asset compartiendo `fusion.py`), contrato 191 cols sin FarSLIP / 703 con FarSLIP, decisión h3 res 5 vs grid rectangular fallback, scaler global Fold-0 vs 5 por fold, FarSLIP bloque opcional con LEFT join + warning, S1 ingest preset operativo (Lee 7×7 dB).
- Keywords: `us-016`, `multisensor_fusion`, `features_fused_v1`, `spatial_kfold`, `h3_tessellation`, `standard_scaler`, `farslip_optional`, `sentinel1_grd_preset`, `era5_monthly`, `srtm_terrain`.

## Próximas fases

1. **Fase 2 — Research adicional (opcional, <2h)**: pre-aprobar `h3 ^4.1.2` con Arthur. Confirmar que `data/farslip/embeddings_italy.parquet` y el formato (`parcel_id` + `farslip_{000..511}` float32) son los que Isaac entregará en US-016b. Validar provisionamiento de `gs://agrosat-dvc-remote`.
2. **Fase 3 — Coding (1.5 días)**: ejecutar plan §3.3. Dos subagentes en paralelo: `ml-engineer` para `fusion.py` + `spatial_split.py` + `scaler.py` + samplers GEE; `mlops-engineer` para 3 assets Dagster + CLI Typer + DVC versioning + tests `materialize_to_memory`.
3. **Fase 4 — QA (0.5 días)**: coverage ≥80% sobre módulos nuevos, `make check` limpio, asset Dagster materializable local, scaler/splits versionados en DVC, smoke test real GEE para 1 parcela de cada región (3 calls). 5 flujos manual-test en `docs/manual-test/us-016.md`.
4. **Fase 5 — Cierre**: PR a `develop`, `docs/us-resolved/us-016.md`, handoff a estado `ready-to-close`.

## Notas de coordinación

- **Sync con Arthur (Platform/MLOps)**: aprobar `h3` como dep, provisionar bucket DVC, revisar contratos de assets upstream (`alphaearth_embeddings_italy`, `features_parcels`) y deps declaradas en Dagster.
- **Sync con Isaac (US-016b FarSLIP, paralelo)**: contrato `farslip_{000..511}` float32 con key `parcel_id`. Si US-016b no cierra en S4, US-016 entrega sin el bloque (AC-8 opcional, LEFT join con warning).
- **Sin frontend ni i18n**: US-016 es 100% backend ML + DB lectura + Dagster. No toca `frontend/i18n/locales/`.
- **Si timebox se aprieta**: priorizar AC-1, AC-2, AC-3, AC-9, AC-10, AC-11, AC-12. FarSLIP (AC-8) y asset Dagster (AC-13) se mueven a US-016.1 si el sprint S4 acumula deuda con US-014/015 sin cerrar.

## Riesgos abiertos (cross-link plan §9)

- **R1** `h3` aprobación pendiente (mitigación: fallback grid rectangular).
- **R2** S1 ingest mínimo (mitigación: preset documentado + smoke real `requires_gee`).
- **R3** FarSLIP no entregado al cierre (mitigación: bloque opcional).
- **R9** DVC bucket no provisionado (mitigación: diferir `dvc push` a US-016.1 si bloquea).
