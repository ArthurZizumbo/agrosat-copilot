# US-061 — Handoff (COMPLETO: doc costo-beneficio A6/A7 + tablas CSV + export LaTeX)

**Status**: documento de negocio + tablas fuente `.csv` + export `.tex` COMPLETOS (entregable `.xlsx`
diferido con bloqueo documentado, no bloquea)
**QA (2026-06-20)**: **ready-to-close** — US documental (no toca código de aplicación); cifras de costo
ancladas a fuente verificable, beneficios marcados como estimación de literatura, caveat del crédito
Vertex AI explícito, ROI break-even mes 3 con aritmética visible, cero cifras sintéticas. Único parcial:
el `.xlsx` de rúbrica no se generó esta sesión (sin `openpyxl`/`xlsxwriter` confirmado en el entorno),
anotado en `docs/blockers/epic10-notas.md` B17; se entregan `.csv` fuente + tablas Markdown + `.tex`
como respaldo verificable.
**Epic**: E10 (Observabilidad y Documentación) · **Rama**: `feature/E10-observabilidad-docs`
**Plan**: `docs/us-planning/us-061.md` · **v8**: §US-061 (línea 2013) · **ADR-009**

## Que entrega US-061

Documento de negocio `docs/business/costo_beneficio.md` para los Avances 6 y 7 (rúbrica Costos 20 +
Beneficios 20 + Implementación 30). Cruza el costo del proveedor con el **valor para el cliente** —
complementa, no duplica, `docs/operations/finops.md` (US-067, operativo) ni la comparativa multi-cloud
(US-063). Incluye: tabla de costos por fase **CRISP-ML(Q)** con cifras reales del proyecto, proyección
a **12 meses operativos** (~$1,380 año 1), caveat del crédito Vertex AI, tabla de **beneficios
cuantificables para cliente tipo de 500 ha** (estimaciones de literatura, marcadas como tales),
beneficios intangibles (CAP, soberanía Qwen on-prem) y **ROI break-even mes 3** con aritmética
explícita. Tablas fuente en `.csv` al Git y export `.tex` para el paper (Track opcional).

## Estado real (DONE)

DONE (20-jun-2026, documental, commit `1d93a3b`):

- **`docs/business/costo_beneficio.md`** (8 secciones): resumen ejecutivo; costos por fase
  CRISP-ML(Q) + detalle de costos verificados anclados; caveat de créditos Vertex; proyección 12 meses;
  beneficios cuantificables 500 ha + traducción monetaria ilustrativa; ROI/break-even; beneficios
  intangibles; fuentes y trazabilidad.
- **Cifras v8 reales ancladas** (no sintéticas): operativo **~$115/mes** (Cloud Run scale-to-zero
  `min_instances=0`); GCP acumulado **~$0.30-0.49**; training histórico **$262 spot — $602 on-demand**,
  hoy **$0 al equipo** (H100 NVL 96GB del sponsor 24/7); Qwen3.5-35B-A3B vLLM on-prem **≈$0
  incremental**; Gemini API en **centavos (~$0.0001/descripción FarSLIP)**; adquisición de datos **$0**
  (fuentes públicas CC-BY-4.0 / CC-BY-SA-4.0). Cifras operativas **enlazadas** a `finops.md`, no
  reescritas (sin drift). Nomenclatura corregida: AlphaEarth V1/ANNUAL data v1.1, 64-dim (no "v2.1").
- **Caveat de créditos Vertex AI**: sección explícita aclarando que el "Trial credit for GenAI App
  Builder" (~$17,178) es de Vertex AI Search/Agent Builder y **NO** cubre la SKU de generación de texto
  de la Gemini API — y no se necesita, porque esa inferencia cuesta centavos.
- **Beneficios 500 ha con supuestos explícitos**: horas de agrónomo (30-60 h/mes), ahorro de agua
  (10-25 %), ahorro de insumos (10-20 %), reducción de detección de plagas (de días a 24-72 h). Cada
  fila con rango + supuesto + fuente, **etiquetada "estimación de literatura, no medida"** (no hay
  cliente real). Traducción monetaria ilustrativa con supuestos de precio recalibrables.
- **Beneficios intangibles**: trazabilidad para cumplimiento **CAP europeo**, reducción de riesgo
  regulatorio, imagen de sostenibilidad y **soberanía de datos** (variante on-prem Qwen3.5 vía US-048).
- **ROI break-even mes 3**: aritmética visible (beneficio mensual estimado conservador ~$1,400 vs costo
  operativo ~$115/mes + onboarding supuesto ~$2,800); robustez señalada (a la mitad del beneficio el
  break-even se mueve solo a ~mes 5-6). Presentado como proyección, no como dato histórico.

PARCIAL (documentado, no bloquea):

- **Export `.xlsx`** de rúbrica no generado esta sesión (sin `openpyxl`/`xlsxwriter` confirmado en el
  entorno; el plan permite respaldo). Bloqueo en `docs/blockers/epic10-notas.md` B17. Respaldo entregado:
  `.csv` fuente al Git + tablas Markdown + export `.tex`. Decisión: generar el `.xlsx` desde los `.csv`
  al confirmar la dependencia. **Ninguna cifra sintética.**

## Archivos

- `docs/business/costo_beneficio.md` — documento principal de negocio (español neutro, tablas Markdown).
- `docs/business/costo_beneficio.tex` — export LaTeX de las 2 tablas (costos CRISP-ML(Q) + beneficios
  500 ha) para el paper; requiere `booktabs` + `tabularx`.
- `docs/business/data/costos_crisp_ml.csv` — fuente tabular de costos por fase (6 fases, al Git).
- `docs/business/data/beneficios_500ha.csv` — fuente tabular de beneficios cliente tipo (4 filas, al Git).
- `docs/us-planning/us-061.md` — planning de la US.
- `docs/blockers/epic10-notas.md` — B17, bloqueo del `.xlsx` (respaldo `.csv`/`.tex`/Markdown).

## Lineage / QA

- US documental: **no toca código de aplicación** (`backend/`, `ml/`, `frontend/`, `db/`,
  `infrastructure/`), ni schema, ni training, ni MLflow/DVC propios de esta US.
- Verificado por el orquestador (re-corrido a mano sobre el estado de la rama, evidencia transversal E10):
  - `ruff check` sobre `backend/ ml/ dagster_project/`: **All checks passed!**
  - `mypy` package-mode (`backend.app.middleware/api/utils/services`): **Success: no issues found in 26
    source files**.
  - Suite chat+metrics: **52 passed** (`test_metrics_middleware` + `test_chat_metrics` + `test_chat_sse`
    + `integration/test_chat_endpoint` + `integration/test_chat_uses_session_model`).
  - Suite drift: **11 passed** (`tests/ml/monitoring` + `tests/dagster/test_drift_asset.py`), warnings
    benignos de scipy.
  - `dagster_project.definitions` importa limpio: schedules `['drift_check_weekly_schedule']`, jobs
    `['farslip_full_pipeline_job','drift_check_job']`.
- FIX del orquestador para US-059: `DEFAULT_REGISTRY` dedicado + `get_default_metrics()` singleton — al
  re-instanciar la app ya NO se lanza "Duplicated timeseries" (rompía 13 tests de integración de chat).
- FIX de harness para US-065: `_FakeConn`/`_FakePool` en `integration/test_chat_endpoint.py` (antes
  `object()` rompía `_resolve_variant.pool.acquire()`; fallo PRE-EXISTENTE de US-054, no regresión E10).
- Commit `1d93a3b` `docs(E10): US-061 analisis costo-beneficio para Avances 6 y 7`, autor Arthur Zizumbo,
  **sin** trailer `Co-Authored-By`. Cada US de E10 commiteada por separado. Tree limpio.
- Datasets reales del contexto E12 versionados: `data/sen4agrinet.dvc`, `data/transfer/eurocropsml.dvc`.

## Notas / blockers

- **B17 (`docs/blockers/epic10-notas.md`)**: el `.xlsx` de rúbrica queda diferido; respaldo `.csv` +
  Markdown + `.tex` entregado, sin cifras inventadas. Cerrar generando el `.xlsx` desde los `.csv` cuando
  se confirme `openpyxl`/`xlsxwriter`.
- **Una sola fuente de verdad por cifra**: las cifras operativas se enlazan a `docs/operations/finops.md`;
  si esas cifras cambian allí, sincronizar el resumen ejecutivo y la proyección 12m de este documento.
- **Separación medido vs estimado**: cualquier futura cifra de beneficio debe conservar la columna de
  supuesto/fuente y la etiqueta de estimación; jamás presentarla como medida de un cliente real (no existe).
- La rama `feature/E10-observabilidad-docs` forma parte del flujo de `feature/E12-transfer-multiregion`.
