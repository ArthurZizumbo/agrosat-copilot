# Handoff US-050 — Gemma 4 LoRA FUTURE (ADR + correcciones)

**Estado:** done (solo documentacion)
**Rama:** `feature/E7-US-050-gemma-future-adr`

## Decision clave

El plan US-050 decia "redactar ADR-009" pero **ADR-009 ya existe** (es la
reactivacion H100 + pivote FarSLIP, 6-jun). Para no sobrescribir esa decision, se
creo **ADR-011** (Gemma 4 LoRA FUTURE). La intencion del plan se cumple: documentar
formalmente por que Gemma 4 LoRA queda fuera de la presentacion, con la correccion
tecnica del skill.

## Archivos tocados

- `docs/decisions/ADR-011-gemma4-lora-future.md` — ADR nuevo. Hechos verificados:
  - id real `google/gemma-4-26B-A4B-it` (el `gemma-4-26b-it` NO existe; MoE 8 activos/128 total +1 shared, ~3.8B/25.2B).
  - Expertos = tensores 3D fused `nn.Parameter` -> `bitsandbytes` no cuantiza -> **QLoRA bloqueado**.
  - `target_modules=[gate/up/down_proj]` no matchea (~0.91% params); via real `target_parameters=["mlp.experts.gate_up_proj","mlp.experts.down_proj"]` (PEFT >= 0.17, bleeding-edge: overhead por-experto, no multi-adapter).
  - AgroMind eval-only (28482 QA sin train split) -> fine-tune = leakage; AgroMind-IT/ES (500) tambien eval.
  - Ratifica: reasoner = Gemini 2.5-pro; on-prem = Qwen vLLM; Gemma LoRA = FUTURE (SFT sintetico propio).
- `.claude/skills/agrosat-llm-finetuning/SKILL.md` — corregido: banner FUTURE, id real, QLoRA bloqueado, `target_parameters`, AgroMind eval-only, training script y config YAML actualizados, bloque de serving alineado a US-048 (Qwen3-30B-A3B-Int4, script real).
- `CLAUDE.md` + `AGENTS.md` (espejos) — tabla de Decisiones Irrevocables: Gemma 4 = id real + FUTURE (ADR-011); on-prem = Qwen3-30B-A3B-Int4; header Stack actualizado. Verificado que siguen siendo espejos identicos.

## Verificacion

- Hechos verificados en HF (gemma-4-26B-A4B-it existe; gemma-4-26b-it no) y docs de PEFT (target_parameters para MoE nn.Parameter 3D).
- ADR-011 enlaza ADR-009 (D-1 prioridad H100).

## QA verificacion (2026-06-15)

- **Manual-test:** [docs/manual-test/us-050.md](../manual-test/us-050.md).
- **Tests:** N/A (US documental, sin codigo ejecutable).
- **Verificacion automatizable:** ADR-011 presente; skill `agrosat-llm-finetuning` con banner FUTURE; CLAUDE.md/AGENTS.md citan ADR-011 + id real.
- **Pendiente revision humana (no automatizable):** contrastar id `google/gemma-4-26B-A4B-it` y la via `target_parameters` contra HF/PEFT; `diff CLAUDE.md AGENTS.md` para confirmar que siguen siendo espejos antes del PR.

## Bugs / bloqueos

- Ninguno. US documental. No se entrena Gemma 4 en este horizonte (esa es la decision).
