# US-036-a — Handoff

**Status**: `planning` (plan canonico escrito; codigo NO escrito; run H100 NO ejecutado)
**Titulo**: Fine-tune COMPLETO de FarSLIP, INCREMENTAL POR CARDINALIDAD, sobre PASTIS-R frances real

> **LA US MAS IMPORTANTE DEL LOTE (directiva sponsor)**. FarSLIP es el camino PRINCIPAL. US-036-a es el run
> PRODUCTIVO que cierra el gap 0.163 vs AlphaEarth 0.233 (medido en US-037). **MAXIMA PRIORIDAD en la cola H100.**

> **PLANNING (2026-06-07)**. Plan detallado en [docs/us-planning/us-036-a.md](../us-planning/us-036-a.md).
> Codigo NO escrito aun. La mecanica pesada YA EXISTE y se REUSA: trainer `FarSLIPDistillationTrainer`
> (`ml/farslip/distill.py`: `set_text_prototypes` reproyecta 384->768, `adapt_patch_embed_to_n_channels` 3->4,
> `save_student` safetensors), prototipos US-033 (`load_class_prototype_embeddings` (18,384), Gemini 3.5 Flash
> real), filtro 3:1 (`PastisFilter` `mode="dominance_ratio"` `ratio=3.0`), loaders PASTIS-R reales
> (`pastis_seg_dataset.py`, `pastis_loader.py`). US-036-a aporta SOLO el orquestador del curriculum + la
> logica pura de escalones + tests, y ESCALA la infraestructura de US-036.

> **CONCEPTO (aclarado por el usuario, lo que MAS importa)**: el fine-tune "completo" NO es 18 clases de
> golpe. Es INCREMENTAL por cardinalidad descendente: empieza por las 4 clases mas representativas
> (Meadow 1, Corn 3, Soft winter wheat 2, Grapevine 8) y sube de clases, MIDIENDO EN CADA ESCALON cuantas
> clases se resuelven bien y con buen % de aciertos (F1/IoU por clase). Sube hasta donde converja y describa
> bien; objetivo llegar a 18 si converge bien.

**Epic**: E5 (Modelos Alternativos / Comparativa) · **Avance**: A5 (ensamble) / S7
**Sprint**: S7 · **Owner**: Arthur Zizumbo (ML/MLOps) · **Prioridad**: **P0 — MAXIMA del lote** (FarSLIP camino principal; PRIMERA en cola H100)
**Rama**: `feature/E5-US-036-a-farslip-full-incremental-pastis`
**Plan**: [docs/us-planning/us-036-a.md](../us-planning/us-036-a.md)
**Depende de**: US-036 (infraestructura EN PARALELO: `scripts/train_incremental.py` + `create_incremental_dataset(n_classes)` + dataset builder PASTIS composite NDVI); US-033 (`phenology_class_prototypes_pastis.parquet`, 18 prototipos Gemini real, `implemented`).
**Insumo de**: US-037 (eval downstream FarSLIP vs AlphaEarth 0.163/0.233).

## Resumen ejecutivo

Fine-tune completo de FarSLIP sobre **PASTIS-R frances real** (par = composite pico-NDVI 4-banda
B02/B03/B04/B08 por patch + clase PASTIS dominante, filtro 3:1), **incremental por cardinalidad descendente**:
escalon inicial de las 4 clases dominantes (Meadow, Corn, Soft winter wheat, Grapevine) y `+2 clases` por
escalon hasta 18, cada escalon inicializado desde el `best` del anterior (`load_state_dict strict=False`),
M epochs hasta convergencia (~20-40, NO 2), prototipos PASTIS DIRECTOS (`n_regions=1`, subset del parquet
US-033 por class_id), eval F1/IoU por clase por escalon, criterio de parada cuando agregar clases degrada las
previas o las nuevas son inaceptables. Un run MLflow `:5010` por escalon; best del escalon ganador -> DVC,
insumo de US-037. REUTILIZA la infraestructura de US-036 (no la reescribe) y el trainer/prototipos existentes.

## ALCANCE OBLIGATORIO (ordenado por el usuario)

1. **SOLO PASTIS-R FRANCES REAL. PROHIBIDO italiano. PROHIBIDO sintetico. PROHIBIDO placeholders.** El
   FarSLIP previo (US-034/035, crops italianos sinteticos, vocabulario CAP, n_regions=3) es ERROR a descartar.
2. **Par = composite pico-NDVI por patch** -> imagen real 4-banda; clase = clase PASTIS dominante; filtro 3:1.
3. **Clases PASTIS directas 1..18** (NO `cap_class`, NO `expand_to_cap`); prototipos US-033 directos; `n_regions=1`.
4. **Descripciones via Gemini 3.5 Flash REAL** (`gemini-3.5-flash`; `gemini-3.1-pro` da 404), key en
   `.env.local`. **Reutilizar/FILTRAR el parquet US-033** (ya Gemini real, 18 clases), NO regenerar.
5. Run PRODUCTIVO (NO POC): lanzar y esperar convergencia real; best -> DVC.

## Dominios tocados

- [x] **ml** (`ml/farslip/incremental_curriculum.py` NUEVO, `scripts/run_us036a_farslip_full_incremental.py` NUEVO, `tests/ml/farslip/test_us036a_incremental_orchestrator.py` NUEVO)
- [x] **H100** (run productivo, MAXIMA prioridad de cola)
- [ ] backend · [ ] frontend · [ ] agent · [ ] db · [ ] dagster · [ ] infra
- [x] docs (solo estos dos `.md` de planning/handoff)

Skills: `agrosat-ml-segmentation` (FarSLIP/distill), `agrosat-llm-finetuning`, `agrosat-ml-evaluation`, `agrosat-dvc-mlflow`, `agrosat-azure-h100`, `agrosat-testing`.

## Write-set DISJUNTO (separacion vs US-036)

| US | Write-set | Tarea |
|----|-----------|-------|
| **US-036** (infra, paralela) | `scripts/train_incremental.py`, dataset builder PASTIS (par img-texto composite NDVI), `create_incremental_dataset(n_classes)` | Construye la infraestructura: par desde patch PASTIS real, filtro 3:1, entrenamiento de 1 escalon. |
| **US-036-a** (este, productivo) | `scripts/run_us036a_farslip_full_incremental.py`, `ml/farslip/incremental_curriculum.py`, `tests/ml/farslip/test_us036a_incremental_orchestrator.py`, `docs/us-planning/us-036-a.md`, `docs/us-handoff/us-036-a.md` | ESCALA la infra: ranking de cardinalidad, N escalones x M epochs, init encadenado strict=False, prototipos PASTIS directos, eval por clase, criterio de parada, run MLflow por escalon, best -> DVC. **Importa** la infra de US-036. |

> **OBLIGATORIO**: US-036-a NO escribe `scripts/train_incremental.py` ni el dataset builder (propiedad de
> US-036). Los IMPORTA/INVOCA. A la fecha de planning esos artefactos NO existen (`scripts/*us036*` = 0 hits)
> -> ver R-DEP (bloqueo si US-036 no cierra antes).

## Escalones del curriculum (propuesta + justificacion)

**Ranking de cardinalidad (EDA "Cardinalidad de cultivos PASTIS", n_pixels mayor->menor; IDs PASTIS 20-clase)**:

```
1 Meadow(1) ~20.2k | 2 Corn(3) ~8k | 3 Soft winter wheat(2) ~6k | 4 Grapevine(8) ~5.3k   <- ESCALON 0 (4 dominantes)
5 Winter barley(4) | 6 Leguminous fodder(14) | 7 Winter rapeseed(5) | 8 Orchard(16)
9 Winter triticale(10) | 10 Spring barley(6) | 11 Soybeans(15) | 12 Sunflower(7)
13 Winter durum wheat(11) | 14 Beet(9) | 15 Fruits/veg/flowers(12) | 16 Mixed cereal(17)
17 Potatoes(13) | 18 Sorghum(18)                                                          <- cola (<200px)
```

`cardinality_ranking()` golden = `[1,3,2,8,4,14,5,16,10,6,15,7,11,9,12,17,13,18]`.

**Esquema recomendado: `+2` por escalon a partir de 4** -> `4 -> 6 -> 8 -> 10 -> 12 -> 14 -> 16 -> 18`
(8 escalones, objetivo 18). **Justificacion**: (a) paso fino localiza con precision el escalon donde la
convergencia se rompe (criterio de parada), que un salto +4 ocultaria; (b) anadir solo 2 clases minimiza el
shock de distribucion para el init encadenado (currículum learning suave); (c) las clases cola (<200 px:
Sorghum 18, Potatoes 13, Mixed cereal 17) entran aisladas al final, donde es mas probable que disparen la
parada sin contaminar el diagnostico de las clases medias.

**Plan B (presupuesto H100 ajustado): `4 -> 8 -> 12 -> 18`** (4 escalones, +4). El esquema es PARAMETRO
(`step_size`), no codigo: cambiar de +2 a +4 es config.

**Criterio de parada** (`stop_criterion`): (1) 18 clases convergidas (`max_classes_reached`, exito); (2)
macro-F1 de clases previas cae > 0.05 absoluto (`prev_classes_degraded`); (3) ninguna clase NUEVA con F1>=0.30
(`new_classes_unacceptable`); (4) presupuesto agotado (`budget_exhausted`). Se reporta el ultimo escalon sano.

## Archivos a crear (write-set US-036-a)

**Crear**:
- `ml/farslip/incremental_curriculum.py`: logica pura (sin GPU) — `cardinality_ranking()`,
  `class_ids_for_step(step_idx, step_size=2, base=4)`, `select_step_prototypes(proto_18, ids_all, ids_step)`
  (filtra parquet US-033 por class_id, `n_regions=1`, devuelve `(N,384)` en orden de `ids_step`),
  `stop_criterion(metrics_curr, metrics_prev, thresholds) -> (bool, reason)`.
- `scripts/run_us036a_farslip_full_incremental.py`: orquestador productivo (Typer CLI),
  `run_incremental_curriculum(...)`. Por escalon: build dataset (US-036 `create_incremental_dataset`,
  IMPORTADO) -> select prototipos -> `FarSLIPDistillationTrainer` (cfg `n_regions=1`, `n_categories=N`,
  `n_in_channels=4`, `n_epochs=M`) -> si k>0 `load_state_dict(best_{k-1}, strict=False)` ->
  `set_text_prototypes` -> `train()` -> eval por clase -> run MLflow -> `stop_criterion`.
- `tests/ml/farslip/test_us036a_incremental_orchestrator.py`: ranking, escalones (0==[1,3,2,8], superset
  monotono), prototipos (shape/orden, sin Gemini), init encadenado (strict=False, escalon 0 = teacher),
  criterio de parada (cada reason), rechazo de `dataset_root` italiano, `n_regions=1`. Trainer/builder/eval
  mockeados (cero GPU/red/dataset real/Gemini).

**Consumir (NO escribir)**: `scripts/train_incremental.py` + `create_incremental_dataset(n_classes)` +
dataset builder PASTIS (US-036).

**Reutilizar (sin modificar)**: `ml/farslip/distill.py` (trainer + `set_text_prototypes` + `save_student` +
`adapt_patch_embed_to_n_channels`), `ml/features/phenology_class_prototypes.py` (`load_class_prototype_embeddings`),
`ml/data/pastis_filter.py` (`PastisFilter dominance_ratio ratio=3.0`, `PASTIS_CLASS_NAMES`),
`ml/data/pastis_seg_dataset.py`, `ml/ingest/pastis_loader.py`.

**NO tocar**: `data/features/phenology_class_prototypes_pastis.parquet` + `.dvc` (US-033, solo LEER/FILTRAR);
`expand_to_cap`/`load_cap_to_pastis`/`cap_vocabulary.yaml` (camino CAP italiano — descartado);
`data/farslip_pairs/` (crops italianos sinteticos); `RegionCategoryAlignmentLoss.forward`; `weights_uri` de
`farslip_extractor`; pesos frozen (teacher CLIP, MiniLM). NO regenerar el parquet, NO re-llamar Gemini.

## Logica del curriculum (resumen)

```
ranking = cardinality_ranking()                     # [1,3,2,8,4,14,5,16,10,6,15,7,11,9,12,17,13,18]
proto_18, ids_all = load_class_prototype_embeddings()   # (18,384), Gemini real US-033 (DVC, solo LEER)
best_prev = None
for k, n in enumerate([4,6,8,10,12,14,16,18]):      # +2 por escalon (o [4,8,12,18] plan B)
    ids   = class_ids_for_step(k, step_size=2, base=4)        # primeras n del ranking
    ds    = create_incremental_dataset(n_classes=n)          # US-036 (IMPORTADO), PASTIS real, filtro 3:1
    proto = select_step_prototypes(proto_18, ids_all, ids)    # (n,384), n_regions=1, PASTIS directo
    cfg   = FarSLIPTrainerConfig(n_regions=1, n_categories=n, n_in_channels=4, n_epochs=M,
                                 mlflow_run_name=f"farslip-full-incr-{n:02d}cls", ...)
    tr    = FarSLIPDistillationTrainer(cfg, dataset=ds)
    if best_prev: tr.student.load_state_dict(load(best_prev), strict=False)   # init encadenado
    tr.set_text_prototypes(proto)                            # reproyecta (n,384)->(n,768)
    tr.train()                                               # M epochs hasta convergencia
    best_k  = save -> checkpoints/farslip/incremental/{n:02d}cls/best.safetensors
    metrics = eval_per_class(student_k, ds_val, ids)         # F1/IoU por clase
    log_run(MLflow :5010, n_classes=n, metrics, loss curve)
    stop, reason = stop_criterion(metrics, metrics_prev)
    if stop: break
    best_prev = best_k
best_winner -> dvc add + dvc push (desde la VM, ADC OK)      # insumo de US-037
```

## H100 / MLOps

- **MAXIMA PRIORIDAD de cola** (FarSLIP camino principal). `nvidia-smi` ANTES (1 GPU 96 GB compartida).
  Parar el daemon idle-shutdown antes del run largo.
- **VRAM**: ViT-B/16 batch=64 **FP32 ~12 GB** (recon US-034); cabe holgadisimo en 96 GB. Escalones
  SECUENCIALES (cada uno init del anterior) -> un solo training activo (~12 GB pico). Sin paralelismo de escalones.
- **Horas estimadas**: PASTIS ~2433 patches -> 1 par/patch (MUCHO menor que los 30,173 crops italianos del
  lote previo de 27 min) -> ~0.5-0.6 min/epoch (cota superior, escalon 18); M=30 -> ~18 min/escalon (cota);
  curriculum +2 (8 escalones) ~**1.5-2.5 h total** (M=40 ~2-3.5 h); plan B +4 ~1-1.5 h. `time_cap_hours=8.0`/escalon.
- **Checkpoints**: `checkpoints/farslip/incremental/<NN>cls/best.safetensors`. Best ganador -> `dvc add` +
  `dvc push` DIRECTO desde la VM (ADC OK, memoria `session-creds-h100-gcp-available`).
- **MLflow `:5010`** experiment `farslip`: un run por escalon (`farslip-full-incr-<NN>cls`) con
  `data_version`+`code_version`+`n_classes`+metricas por clase+curva de loss. Verificar runs CERRADOS (gotcha
  subprocess `RUNNING`). Si `:5010` cae en la VM: ingestar desde log (patron US-034/035) o MLflow nativo VM.

## Riesgos principales

1. **R-DEP (ALTA)**: US-036 (infra) no materializada al implementar (`scripts/train_incremental.py` y el
   builder NO existen a la fecha). US-036-a CONSUME, no crea. Bloqueo explicito si no cierra antes; avanzar
   `incremental_curriculum.py` + tests con mocks mientras tanto.
2. **R-COLLAPSE (ALTA)**: clases cola (<200 px: Sorghum 18, Potatoes 13, Mixed cereal 17) colapsan y degradan
   previas. Mitigacion: entran al final aisladas; criterio de parada AC-8 detiene antes; reportar ultimo
   escalon sano (objetivo honesto: "se resuelven bien hasta N clases").
3. **R-ITALIAN (ALTA)**: contaminacion italiana/sintetica (usar `data/farslip_pairs`, `expand_to_cap`, CAP).
   Mitigacion: guarda que RECHAZA dataset italiano (ValueError + test); `proto_source=pastis_direct`,
   `n_regions=1`; sin imports CAP.
4. **R-DOMINANCE (MEDIA)**: clase dominante de patch mixto ambigua. Mitigacion: filtro 3:1 + composite
   pico-NDVI; caveat documentado (etiqueta de patch, no por-pixel).
5. **R-NDVI-CLOUD (MEDIA)**: composite pico-NDVI nublado. Mitigacion: pico sobre la serie T; PASTIS ya
   relativamente filtrado; caveat.
6. **R-PROTO-ALIGN (MEDIA)**: con `n_regions=1` el target es `category_id` directo; orden de prototipos debe
   seguir `class_ids_step`. Mitigacion: `select_step_prototypes` respeta el orden + test.
7. **R-CONVERGE (MEDIA)**: escalon no converge en M epochs. Mitigacion: paciencia + M maximo; +2 minimiza
   shock; aplicar parada (no forzar).
8. **R-DVC-REGEN (MEDIA)**: regenerar parquet US-033 a ciegas. Mitigacion: solo LEER/FILTRAR; NO Gemini.

## Sub-tareas / proximo paso

1 solo agente ml (secuencial respecto a US-036): (1) implementar `incremental_curriculum.py` (logica pura) +
sus tests (no dependen del builder de US-036, avanzables ya); (2) cuando US-036 cierre, implementar
`run_us036a_farslip_full_incremental.py` consumiendo `create_incremental_dataset`; (3) tests del orquestador
con trainer/builder mockeados; (4) `make check` limpio + tests verdes; (5) `nvidia-smi`, lanzar el curriculum
en H100 (esperar convergencia real), un run MLflow por escalon; (6) recopilar F1/IoU por clase por escalon,
determinar escalon ganador + `stop_reason`, `dvc add`+push del best, cerrar runs MLflow, handoff a `implemented`.

## Criterio de aceptacion (resumen)

Orquestador `run_us036a_farslip_full_incremental.py` que ejecuta el curriculum incremental por cardinalidad
sobre **PASTIS-R frances real** (par composite pico-NDVI 4-banda + clase PASTIS dominante, filtro 3:1,
`n_regions=1`, prototipos PASTIS directos del parquet US-033 filtrado por class_id), escalon 0 = las 4
dominantes (`[1,3,2,8]`), `+2`/escalon hasta 18 (o `+4` plan B), cada escalon init `load_state_dict(strict=False)`
del best anterior, M epochs hasta convergencia (~20-40), eval F1/IoU por clase por escalon, criterio de parada
explicito; un run MLflow `:5010` por escalon con `data_version`+`code_version`+`n_classes`+metricas por clase;
best del escalon ganador -> DVC (insumo de US-037); write-set DISJUNTO de US-036 (importa su infra, no la
reescribe); NO italiano/sintetico/CAP; NO regenerado el parquet US-033; `make check` limpio + tests ml verdes.
