# Handoff US-012 — Análisis bivariado, multivariado y temporal

**Estado**: implementado · 2026-05-13 (lista para revisión humana + ejecución con PASTIS-R local)
**Epic**: E2 (Análisis Exploratorio de Datos · CRISP-ML(Q) Data Understanding)
**Avance**: A1 — EDA (entregable 2026-05-03 original · entrega real 2026-05-12, recovery sprint)
**Sprint**: S2-recovery
**Rama**: `us-012` (encadenada desde `us-011`)
**SP**: 3
**Owner**: Isaac Ávila Gutiérrez (ML Engineer / Data Scientist)

---

## Snapshot de cierre (2026-05-13)

### Archivos creados

```
ml/analysis/correlations.py                ~700 LOC · 7 funciones + SPECTRAL_INDICES_CORE · cov 71 %
notebooks/02c_eda_bivariado_temporal.ipynb 28 celdas (17 code + 11 markdown) · stripped
tests/ml/test_correlations.py              31 tests (28 passed + 3 skipped por tslearn local)
paper/figures/us-012/.gitkeep              placeholder
docs/manual-test/us-012.md                 6 MT + tabla cobertura ACs
```

### Archivos modificados

```
ml/ingest/gee_sampler.py                   +era5_annual_precip, +ERA5_COLLECTION (cov 85 %)
ml/analysis/visualization.py               +pairplot_by_class, +vif_barplot,
                                           +dual_axis_precip_ndvi, +acf_grid_by_class,
                                           +dtw_centroids_plot
ml/analysis/__init__.py                    re-export 8 funciones nuevas + SPECTRAL_INDICES_CORE
docs/licenses/DATA_LICENSE.md              +bloque ERA5 Copernicus C3S
Makefile                                   +target eda-bivariado, .PHONY actualizado
```

### Resultado QA local

| Gate | Estado |
|------|--------|
| `poetry run ruff check ml tests/ml` | All checks passed |
| `poetry run ruff format --check` (archivos US-012) | already formatted |
| `poetry run pytest tests/ml/` | 125 passed + 3 skipped (tslearn no instalado local) en 8.9 s |
| Cobertura `ml/analysis/correlations.py` | 71 % (objetivo >= 70 %) |
| `nbstripout notebooks/02c_eda_bivariado_temporal.ipynb` | clean |
| Sin emojis / TODOs / prints / SQL injection / secrets | verificado |

### ACs (13) — cobertura

| AC | Test automatico | Pendiente humano |
|----|-----------------|------------------|
| AC-2, AC-3, AC-4, AC-5, AC-6, AC-8, AC-9, AC-10, AC-11, AC-12, AC-13 | OK con mocks/fixtures sinteticos | MT-3 con PASTIS-R local |
| AC-1 papermill | n/a | MT-2 sin PASTIS + MT-3 con PASTIS local |
| AC-7 DTW | tests con skipif tslearn (3 skipped local; pasan en CI con `poetry install --with ml`) | MT-3 + MT-4 |
| AC-8 ERA5 | mock `ee` OK | MT-5 requiere auth GEE (opcional-bonus) |

### Decisiones tecnicas no obvias

- **`correlation_pair`**: calcula `np.corrcoef` una sola vez sobre union `cols_a ∪ cols_b` y luego indexa por pares; mas eficiente que loops nested.
- **`vif_table`**: pre-filtra columnas con `|corr| >= 0.99` antes de invertir la matriz para evitar singular. Las descartadas quedan etiquetadas `dropped_near_perfect_corr` para trazabilidad.
- **ACF/PACF + DTW**: usan `pandas.resample("MS").mean().interpolate("linear")` como adapter borde porque `pl.DataFrame.upsample` no expone interpolacion lineal directa. Documentado en docstring.
- **`era5_annual_precip`**: `ImageCollection.sum()` temporal seguido de `reduceRegion(mean)` espacial; reporta mm (escala * 1000).
- **`parcel_id` en notebook**: composito `patch_id + y + x` para identificar cada pixel-parcela. Pivot long → wide via `pl.DataFrame.pivot` antes de `compute_indices_subset`.
- **`pairplot_by_class`**: cap top-10 clases, `corner=True`, marker size 6 para evitar cuelgues seaborn con 18 clases.
- **`max_lag=6`**: solo ~14 meses cubre PASTIS, max_lag=12 dejaria 2 pairs por lag → sin significancia.

### Bloqueos pendientes para cierre US (cierre vía PR + merge a `main`)

- [ ] `poetry lock --no-update` + `poetry install --with ml` (Isaac local) para tener tslearn/umap-learn/statsmodels en su venv.
- [ ] Ejecutar `make eda-bivariado` end-to-end con PASTIS-R local → poblar las 8+ figuras en `paper/figures/us-012/`.
- [ ] Validacion visual de las figuras temporales (MT-3): pico NDVI trigo en mayo-junio, maiz en agosto.
- [ ] AC-7 sanity DTW vs `cereals_winter_vs_spring` grouping (MT-4) → reportar si valida o registrar hallazgo.
- [ ] AC-8 ERA5 con auth GEE (opcional-bonus, MT-5).
- [ ] Redactar `docs/us-resolved/us-012.md` al merge a main.

---

## Plan canónico

Ver [`docs/us-planning/us-012.md`](../us-planning/us-012.md).

## Resumen de scope

Análisis **bivariado** (correlación bandas × índices, VIF, pairplot por clase), **multivariado** (multicolinealidad) y **temporal** (fenología pico NDVI, ACF/PACF, DTW clustering mono-vs-doble ciclo, anomalías ERA5) sobre PASTIS-R completo (multitemporal con T frames Sentinel-2 + 18 clases canónicas). Cubre preguntas 6, 7 y 10 de la rúbrica EDA — las "duras" que US-010 y US-011 dejaron parcialmente abiertas.

Entregable: `notebooks/02c_eda_bivariado_temporal.ipynb` + módulo nuevo `ml/analysis/correlations.py` (7 funciones) + extensiones a `gee_sampler.py` (ERA5) y `visualization.py` (5 plotters).

## Owner

Isaac Ávila Gutiérrez.

## Dependencias upstream

- US-001 (monorepo template) — ✅ resuelta (commit `aeee416`)
- US-010 (`ml/ingest/pastis_loader.py::pastis_to_polars`, `ml/utils/sampling.py`, `ml/analysis/visualization.py` base) — **en planning**. Opciones: esperar mergeado o crear helpers aquí.
- US-011 Bloque 0 (`poetry add umap-learn statsmodels tslearn geemap folium`) — **prerequisito obligatorio**. Si no se ejecutó, US-012 lo hace primero.
- PASTIS-R completo descomprimido (con `metadata.geojson` que incluye campo `dates-S2` por patch) — ✅ 12-may-2026
- `.gitignore` con `data/PASTIS-R/`, `data/cache/` — ✅ 12-may-2026

## Dependencias downstream

- US-014 (biblioteca 17 índices espectrales): consume la decisión sobre qué índices del subset core (NDVI, NDWI, NDMI, EVI, SAVI, NDRE) son no redundantes y completa los 11 restantes.
- US-017 (baseline XGBoost): consume la lista final de features no redundantes (VIF + correlación) y las features de fenología (peak month, peak value, doble ciclo flag).

## Bloqueos pendientes

- [ ] Confirmar que el campo `dates-S2` está presente en `metadata.geojson` (R-8 del plan). **Crítico para AC-5, AC-6, AC-7, AC-8**. Validación temprana antes de Bloque 1.
- [ ] Credenciales Google Earth Engine en máquina de Isaac (solo para AC-8 ERA5; si no hay, AC-8 queda opcional-bonus).
- [ ] `umap-learn`, `statsmodels`, `tslearn`, `geemap`, `folium` ya en `pyproject.toml` (heredado de US-011 Bloque 0). Si US-011 no se ejecutó aún, US-012 hace el `poetry add`.
- [ ] Coordinación con US-010 sobre `pastis_loader.py::pastis_to_polars` (se crea allí o aquí en paralelo).

## Snapshot de archivos a tocar

### Nuevos

```
ml/analysis/correlations.py
notebooks/02c_eda_bivariado_temporal.ipynb
tests/ml/test_correlations.py
paper/figures/us-012/.gitkeep
```

### Modificados

```
ml/ingest/gee_sampler.py          # +era5_annual_precip
ml/analysis/visualization.py      # +pairplot_by_class, +vif_barplot, +dual_axis_precip_ndvi, +acf_grid_by_class, +dtw_centroids_plot
docs/licenses/DATA_LICENSE.md     # +ERA5 Copernicus C3S
Makefile                          # +target eda-bivariado
```

## Métricas a verificar al cierre

| Métrica | Objetivo |
|---------|----------|
| `papermill notebooks/02c_eda_bivariado_temporal.ipynb` | exit 0 en ≤25 min con `n_parcels=200` |
| `pytest tests/ml/test_correlations.py --cov=ml/analysis/correlations` | ≥70 % |
| Figuras en `paper/figures/us-012/` | ≥8 PNG ≥200 DPI (2 heatmaps corr, 1 VIF, 1 pairplot, 1 box peak NDVI, 1 violin month peak, 1 ACF grid, 1 dual axis ERA5+NDVI) |
| Conclusiones en notebook | features redundantes a colapsar · timing pico NDVI por clase · features fenológicas a derivar · normalización temporal recomendada |
| ACs verificados | 13/13 (AC-8 puede degradar a opcional-bonus si GEE bloqueado) |

## Skills a auto-invocar

- `agrosat-ml-features` (Polars + features espectrales y temporales)
- `agrosat-ml-evaluation` (VIF, correlación, métricas)
- `agrosat-testing` (pytest helpers con fixtures sintéticos)

## Próximas fases

1. **Research crítico (~30 min)**: validar shape de `S2_*.npy`, presencia de campo `dates-S2` en `metadata.geojson`, lookup table 18 clases PASTIS canónicas. Sin esto, AC-5/6/7/8 son ciegos.
2. **Implementation**: 7 bloques del §3.3 del plan (~7-8 h).
3. **QA pass**: ejecutar checklist §11 + revisión humana de los plots temporales (sanity check de fenología: ¿el pico de NDVI del trigo está en junio? ¿el del maíz en agosto? si no → bug).
4. **PR review + merge** a `develop`.
5. **Cierre**: redactar `docs/us-resolved/us-012.md` con evidencia de las 13 ACs.

## Notas de coordinación

- **PASTIS-R como dataset primario**: a diferencia de US-011 (Patrón A con Italia + Francia paralelos), US-012 trabaja solo sobre PASTIS-R porque es **el único dataset disponible que es multitemporal con labels reales**. Replicar sobre Italia requiere descargar series Sentinel-2 desde GEE (no AlphaEarth, que es anual agregado) — eso son ~5 h extra y queda fuera de scope. Opcional como complemento si tiempo permite.
- **AC-8 ERA5 es opcional-bonus**: si Isaac no tiene auth GEE configurada, se documenta como limitación y se entrega sin AC-8. La rúbrica no lo exige literal — "detección de anomalías temporales" puede demostrarse sin ERA5 mostrando dispersión del peak NDVI por año dentro de cada clase.
- **Subset 6 índices vs 17**: el notebook usa el subset core (NDVI, NDWI, NDMI, EVI, SAVI, NDRE). La biblioteca completa se entrega en US-014 (Avance 2). Disclaimer explícito en el notebook. Si Dr. Camacho exige los 17 ya en A1, +1.5 h con `spyndex`.
- **US-013 NO bloqueada**: el dashboard Streamlit puede esqueletarse en paralelo con tabs placeholder.
