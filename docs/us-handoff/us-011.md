# Handoff US-011 — Notebook EDA sobre embeddings AlphaEarth

**Estado**: planning · 2026-05-12 (refactor Patrón A 12-may)
**Epic**: E2 (Análisis Exploratorio de Datos · CRISP-ML(Q) Data Understanding)
**Avance**: A1 — EDA (entregable 2026-05-03 original · entrega real 2026-05-12, recovery sprint)
**Sprint**: S2-recovery
**Rama**: `feature/E2-US-011-eda-alphaearth` (no creada aún)
**SP**: 4 + 1 (Patrón A: validación cruzada PASTIS-R)
**Owner**: Isaac Ávila Gutiérrez (ML Engineer / Data Scientist)

---

## Plan canónico

Ver [`docs/us-planning/us-011.md`](../us-planning/us-011.md).

## Resumen de scope (Patrón A · 2 secciones paralelas)

EDA de las **64 dimensiones del embedding AlphaEarth Foundations v2.1** (GEE, gratuito) en **dos análisis paralelos no concatenados**:

- **Sección 1**: AlphaEarth × **Dynamic World** (9 clases LULC) sobre las **3 ROIs italianas** — cubre la rúbrica literal del plan v6.
- **Sección 2**: AlphaEarth × **PASTIS-R** (18 cultivos canónicos, labels reales) sobre **Francia** (1,830 patches ya descargados) — validación cruzada.
- **Sección 3**: Tabla comparativa de top-10 dims Italia vs Francia + scatter `cross_region_consistency` → responde "¿el embedding generaliza?" antes de US-017.

Sin descarga masiva (ingesta productiva US-006 diferida al sprint S3). GSAA Italia queda deuda explícita a cerrar en US-008. Entregable: `notebooks/02b_eda_alphaearth.ipynb` + módulo nuevo `ml/analysis/embeddings.py` (8 funciones, incluye `cross_region_consistency`) + extensiones a `ml/ingest/gee_sampler.py`, `ml/ingest/pastis_loader.py` y `ml/analysis/visualization.py` (compartidos con US-010).

## Owner

Isaac Ávila Gutiérrez.

## Dependencias upstream

- US-001 (monorepo template) — ✅ resuelta (commit `aeee416`)
- US-010 (helpers `ml/ingest/gee_sampler.py`, `ml/ingest/pastis_loader.py`, `ml/analysis/visualization.py`, `ml/utils/sampling.py`) — **en planning**. Opciones:
  - **Esperar mergeada** (orden secuencial): bloquea ~1 día.
  - **Paralelo**: crear los helpers aquí y reconciliar en PR. Recomendado por velocidad.
- PASTIS-R completo descomprimido — ✅ 12-may-2026 (**ahora obligatorio**: Sección 2 lo usa directamente)
- `.gitignore` con `data/PASTIS-R/`, `data/dynamic_world/`, `data/cache/` — ✅ 12-may-2026

## Dependencias downstream

- US-014 (biblioteca 17 índices espectrales): se beneficia de la decisión sobre top-10 dims AlphaEarth para el ensamble FE.
- US-017 (baseline XGBoost): consume directamente las top-10 dims y el subset de dims redundantes a colapsar.
- US-012 (bivariado/multivariado): puede correlar bandas+índices+embeddings juntos si US-011 deja los samples cacheados.

## Bloqueos pendientes

- [ ] Credenciales Google Earth Engine en máquina de Isaac (`earthengine authenticate` interactivo OK para esta US; service account documentado para US-006).
- [ ] **Bloque 0 obligatorio**: `poetry add --group ml umap-learn statsmodels tslearn && poetry add --group geo geemap folium`. Commit `chore(deps): add EDA libs for US-010/011/012`. Ningún módulo nuevo importa hasta que esto se ejecute.
- [ ] Coordinación con US-010 sobre quién crea primero `ml/ingest/gee_sampler.py`, `ml/ingest/pastis_loader.py` y `ml/analysis/visualization.py` (decisión: paralelo, reconciliar en PR).

## Snapshot de archivos a tocar

### Nuevos

```
ml/analysis/embeddings.py
notebooks/02b_eda_alphaearth.ipynb
tests/ml/test_embeddings.py
paper/figures/us-011/.gitkeep
```

### Modificados

```
ml/ingest/gee_sampler.py             # +sample_alphaearth_roi, +sample_alphaearth_at_coords, +sample_dynamic_world_at, +fetch_s2_ndvi_rgb_for_parcel
ml/ingest/pastis_loader.py           # +pastis_patch_coords (2154→4326), +pastis_pixel_labels
ml/analysis/visualization.py         # +tsne_scatter, +umap_scatter, +correlation_heatmap, +qq_grid, +cross_region_scatter
docs/licenses/DATA_LICENSE.md        # +AlphaEarth (GEE ToS) +Dynamic World (CC-BY-4.0)
pyproject.toml                       # +umap-learn, statsmodels, tslearn (grupo ml) +geemap, folium (grupo geo)
Makefile                             # +target eda-alphaearth
```

## Métricas a verificar al cierre

| Métrica | Objetivo |
|---------|----------|
| `papermill notebooks/02b_eda_alphaearth.ipynb` | exit 0 en ≤25 min con `sample_size=100000` (más por las 2 secciones) |
| `pytest tests/ml/test_embeddings.py --cov=ml/analysis/embeddings` | ≥70 % |
| Figuras en `paper/figures/us-011/` | ≥10 PNG ≥200 DPI (sec1: 7 plots · sec2: 4 plots · sec3: 1 scatter cross-region) |
| Conclusiones en notebook | top-10 dims Italia · top-10 dims Francia · **tabla consistencia cross-region** · pares redundantes · decisión normalización · ventana temporal · **decisión sobre regionalización del baseline US-017** |
| ACs verificados | 15/15 |

## Skills a auto-invocar

- `agrosat-gee-alphaearth` (acceso GEE + colección AlphaEarth)
- `agrosat-ml-features` (Polars-first analysis)
- `agrosat-testing` (pytest helpers con fixtures sintéticos)

## Próximas fases

1. **Research** (opcional, ~20 min): confirmar nombres exactos de bandas en `GOOGLE/SATELLITE_EMBEDDING/V1/ANNUAL` (¿`A00`-`A63`? ¿`embedding_0`-`embedding_63`?) usando `ee.ImageCollection.first().bandNames()`. Documentar en helper.
2. **Implementation**: 5 bloques del §3.3 del plan (~7-8 h).
3. **QA pass**: ejecutar checklist §11 del plan + revisión humana de t-SNE/UMAP por Arthur (sanity check visual de la separabilidad).
4. **PR review + merge** a `develop`.
5. **Cierre**: redactar `docs/us-resolved/us-011.md` con evidencia de las 13 ACs.

## Notas de coordinación

- US-010, US-011, US-012 comparten `ml/analysis/visualization.py`, `ml/utils/sampling.py`, `ml/ingest/gee_sampler.py` y `ml/ingest/pastis_loader.py`. Cada US añade funciones específicas (US-010: stats bandas, loader patches PASTIS; US-011: t-SNE/UMAP/RF/cross-region, loader coords PASTIS reproyectadas; US-012: VIF/DTW). Reconciliar conflictos en PR es trivial si las funciones tienen nombres únicos.
- US-013 (dashboard Streamlit) depende de que los 3 notebooks estén ejecutables — agendarla después de US-012.
- **Patrón A asume que la limitación de Dynamic World (clase única `crops`) está compensada por PASTIS-R (18 cultivos reales)**. Si Dr. Camacho exige granularidad de cultivos también sobre Italia en A1, se acelera US-008 (GSAA Italia) como dependencia bloqueante — pero el Patrón A ya entrega evidencia de granularidad cultivar-específica sobre Francia, lo cual cubre el espíritu de la rúbrica.
- **No mezclar samples Italia + Francia**: regla dura del Patrón A. Cada análisis multivariado (t-SNE, UMAP, RF) corre sobre un dataset a la vez. Solo la **comparación de resultados** (no de samples) es válida — vía `cross_region_consistency()`.
