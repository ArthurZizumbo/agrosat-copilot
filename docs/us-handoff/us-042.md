# Handoff US-042 — Ensamble E-b: E-a + AlphaEarth (XGBoost stacking)

**Estado**: coding (codigo + tests verdes; run final pendiente del OOF de E-a / US-041)
**Epic**: E6 (Modelo Final — Ensambles) [CRISP-ML(Q): Evaluation]
**Avance**: A6
**Sprint**: S8
**Rama**: feature/E6-US-042-eb-alphaearth-stacking (encadenada desde feature/E6-US-041)
**Ultima fase**: 3 — 2026-06-10
**Prioridad (Backlog v8)**: P12
**Depende de**: US-041 (E-a, sidecar oof_ea_fusion_fold5), US-040 (StackingEnsemble + materialize_xgb_parcel_oof)

## Dominios y sub-tareas tocados
- [ ] backend  - [ ] frontend  - [x] ml  - [ ] agent  - [ ] infra  - [ ] db  - [ ] dagster
- ml-only. Sub-tareas: (A) AlphaEarth avg + features XGB; (B) orquestador E-b stacking.
  Dependencia SECUENCIAL de US-041 (necesita el OOF denso de E-a).

## Archivos tocados
- CREAR `scripts/run_us042_eb.py` — orquestador E-b (avg features -> XGB OOF -> reduce
  E-a a parcela -> StackingEnsemble 2-3 miembros -> eval fold-5 honesto -> MLflow).
- MODIFICAR `ml/features/alphaearth_multiyear.py` — anadido build_avg_features_for_xgb.
- MODIFICAR `tests/ml/features/test_alphaearth_multiyear.py` — +3 tests del builder XGB.
- CREAR `docs/us-planning/us-042.md`, `docs/us-handoff/us-042.md`.
- Datos generados (DVC): data/features/alphaearth_pastis_2018_2019_mean.parquet,
  data/features/features_xgb_alphaearth_avg_2018_2019.parquet.

## Archivos existentes reutilizados (DRY — NO duplicar)
- `ml/ensemble/stacking.py::StackingEnsemble` — SIN cambios (es member-generico).
- `scripts/run_us040_ensembles.py` — materialize_xgb_parcel_oof, build_parcel_ground_truth,
  build_parcel_geometries, _fold5_patch_ids.
- `ml/utils/parcel_reconcile.py` — pixel_to_parcel_probs (reduccion denso->parcela).
- `ml/eval/oof/parquet_io.py::read_softmax_parquet`.

## Decisiones tecnicas clave (CAVEATS verificados en recon)
- R-STACK-GENERIC: StackingEnsemble ya itera base_members; DEFAULT_BASE_MEMBERS ya
  incluye xgb-alphaearth. US-042 = datos + orquestador, NO cambio de clase.
- R-AE-AVG: AlphaEarth 2018+2019 ambos en disco (85951, 100% overlap), promedio por dim.
  Prefijo es dim_ (dim_00..dim_63, 2 digitos) — coincide con _ALPHAEARTH_PREFIX, NO hay
  mismatch real (el workflow lo flageo como riesgo, verificado falso).
- R-EA-MISSING: oof_ea_fusion_fold5 (US-041) es dependencia; _reduce_ea_sidecar_to_parcel
  falla claro si no existe -> correr US-041 primero.
- R-LABEL-SPACE: reporte en 18 clases (semantic18); HCAT-6 0.6535 es OTRO eje, etiquetado.
- R-PARCEL-KEY: dos namespaces parcel_id (instance-id vs ParcelIDs) puenteados por
  tabular_parcel_keys; join del stacking por canonical_parcel_id.
- R-HONEST-GAIN: E-b puede NO superar a E-a (tabular saturado en 18 clases); resultado valido.

## Zonas sensibles
- No tocar StackingEnsemble (member-generica). No sobreescribir el OOF L4 historico.
- EnsembleModel.HELD_OUT_FOLD=5 (reportar solo fold-5).

## Modelos / Datos / H100
- MLflow run id: [pendiente run final] (server :5010), run name ensemble-Eb-plus-alphaearth
- XGBoost es ligero (CPU/GPU laptop); no requiere H100.
- Verificado end-to-end: avg features -> materialize_xgb_parcel_oof -> 16640 parcelas fold-5.

## engram-memory
- Observaciones guardadas: si (plan nocturno; FarSLIP rompe techo).

## Plan completo
- docs/us-planning/us-042.md (11 secciones, mirror us-041.md).
- PENDIENTE run final: python -m scripts.run_us042_eb run --meta logreg (tras US-041 real).
