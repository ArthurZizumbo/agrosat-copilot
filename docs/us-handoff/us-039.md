# US-039 — Handoff

**Status**: `implemented (codigo; train pendiente GPU, tras US-038)`
**Titulo**: TSViT-pheno full-config retrain H100 (rama contrastiva lambda=0.3) — ablacion honesta vs TSViT base

> **IMPLEMENTACION (2026-06-08)**. El codigo del orquestador, el registry y la tabla delta honesta esta
> COMPLETO, sin stubs ni TODOs, y testeado en CPU (ruff limpio, 14 tests nuevos verdes + 0 skip de lo
> nuevo; sin regresiones en tsvit/pheno/registry/rescore). **NO se entreno** (GPU ocupada con US-036-a,
> y la US depende de la ventana H100 tras US-038): el `best.pt`, el run MLflow `alt-tsvit-pheno-fullm-v1`
> y el re-score fold-5 quedan pendientes de la ventana H100. Ver "Estado de implementacion".

**Epic**: E5 (Modelos Alternativos / Comparativa) · **Avance**: A5 (ensamble) / S7
**Sprint**: S7 · **Owner**: Arthur Zizumbo (ML/MLOps) · **Prioridad**: P-H100 (cola: **tras US-038**, antes de US-041, por debajo de US-036-a P0)
**Rama**: `feature/E5-US-039-tsvit-pheno-fullm-h100`
**Plan**: [docs/us-planning/us-039.md](../us-planning/us-039.md)
**Depende de**: **US-038** (config full TSViT, MISMO pipeline de train — ver R-DEP) · **US-033** (`phenology_class_prototypes_pastis.parquet`, 18 prototipos Gemini real, `implemented`) · **US-030** (harness fold-5 held-out, `implemented`).
**Insumo de**: **US-041** (ensamble E-a: el best checkpoint pasa como base learner).

> **PLANNING (2026-06-07)**. Plan detallado en [docs/us-planning/us-039.md](../us-planning/us-039.md).
> Codigo NO escrito aun. **El pipeline pesado YA EXISTE y se REUSA** (US-025): el loop de train
> (`ml/train/train_segmentation.py`:`build_and_train`/`train_segmentation`), la rama contrastiva
> (`phenology_contrastive_loss`, `_run_epoch` con `use_phenology`/`lambda_contrast`), el modelo
> (`build_tsvit` con `return_visual_proj=True`), la rama semantica (`PhenoSemanticBranch`) y los
> prototipos US-033 (`load_class_prototype_embeddings`, Gemini real, 18 clases). US-039 aporta SOLO
> el **orquestador del run full + tests + la tabla delta honesta + estos dos `.md`**.

## Resumen ejecutivo

Re-entrenar TSViT con la **MISMA config full de US-038** + la **rama contrastiva fenologica**
(`ml/models/pheno_semantic_branch.py`, Wen et al. 2025) activada con `lambda_contrast=0.3`, sobre
**PASTIS-R frances REAL** (SOLO real, sin sintetico). Run MLflow `alt-tsvit-pheno-fullm-v1` (`:5010`,
tags `data_version`+`code_version`). Re-score del **fold-5 held-out** del best con el harness US-030
(apples-to-apples, 18-clase 128 NEAREST). El best -> **ensamble E-a (US-041)**. El entregable
cientifico NO es ganar mIoU: es la **ablacion honesta** con-fenologia vs sin-fenologia (base US-038).

## HONESTIDAD CRITICA (directiva v8 — riesgo #1 de la US)

- **Delta ESPERADO `~0.3%`, NO 5%.** El `+0.004` mIoU actual (fold-4: tsvit-pheno 0.6253 vs tsvit base
  0.6215) es **RUIDO**, no una mejora.
- En **supervisado** el modelo ya aprende las firmas temporales de cultivo desde las etiquetas densas;
  la rama fenologica aporta **poco** marginal. El resultado MAS PROBABLE es **saturacion supervisada**
  (Delta ~0), y eso es un hallazgo VALIDO y reportable.
- El **valor real de la fenologia contrastiva** esta en el **self-supervised / zero-shot de FarSLIP
  (US-036-a)**, NO en este setup supervisado full-label.
- Se REPORTA el Delta tal cual salga (positivo, ~0 o negativo). Un `Delta >= +0.03` se trata con
  **SOSPECHA** (fuga/seed/config), no se celebra. PROHIBIDO sobre-afirmar el aporte fenologico.

## REUSO, NO DUPLICAR (clave de la US)

US-039 **NO escribe logica de modelo ni de entrenamiento nueva**. Invoca el pipeline existente:

```python
build_and_train(
    "tsvit-pheno",                  # activa use_phenology=True + carga prototipos
    lambda_contrast=0.3,            # criterio v8
    mlflow_run_name="alt-tsvit-pheno-fullm-v1",
    **CFG_FULL_TSVIT,               # config full de US-038 (un solo lugar de verdad)
)
```

`build_and_train("tsvit-pheno", ...)` ya: construye `build_tsvit(num_classes=18, semantic_dim=384)`,
instancia `PhenoSemanticBranch(384).get_class_prototypes()` -> `(18,384)`, y llama `train_segmentation`
con `use_phenology=True`, que en `_run_epoch` suma `0.3 * phenology_contrastive_loss(visual_proj, y,
prototypes)` a la Dice+CE. Todo probado en US-025.

## Dominios tocados

- [x] **ml** (`scripts/run_us039_tsvit_pheno_fullm.py` NUEVO, `tests/ml/train/test_us039_tsvit_pheno_fullm.py` NUEVO, `ml/eval/checkpoint_registry.py` 1 edit)
- [x] **H100** (run productivo, cola tras US-038)
- [ ] backend · [ ] frontend · [ ] agent · [ ] db · [ ] dagster · [ ] infra
- [x] docs (solo estos dos `.md` de planning/handoff)

Skills: `agrosat-ml-segmentation`, `agrosat-ml-evaluation`, `agrosat-dvc-mlflow`, `agrosat-azure-h100`, `agrosat-testing`.

## Write-set (DISJUNTO de US-038)

| Archivo | Tipo | Tarea |
|---------|------|-------|
| `scripts/run_us039_tsvit_pheno_fullm.py` | **nuevo** | Orquestador productivo Typer: `run_tsvit_pheno_full(*, cfg_full, lambda_contrast=0.3, run_name="alt-tsvit-pheno-fullm-v1", rescore_fold5=True)`; invoca `build_and_train`, construye la tabla `pheno_vs_base`. NO reimplementa el loop. |
| `tests/ml/train/test_us039_tsvit_pheno_fullm.py` | **nuevo** | Contrato del orquestador con trainer/proto/harness MOCKEADOS (cero GPU/PASTIS/Gemini): lambda=0.3, config full == US-038, prototipos no-regenerados, folds (1,2,3)/(4), tabla delta, rechazo sintetico, fold-5. |
| `reports/segmentation/metrics/tsvit_pheno_vs_base_fold5.csv` | generado (cierre) | 2 filas (base US-038, pheno US-039) + `delta_miou`/`delta_f1_macro` + nota honesta. Git (no DVC). |
| `ml/eval/checkpoint_registry.py` | **edit** | Apuntar/alias `tsvit-pheno-fullm` -> `checkpoints/segmentation/tsvit-pheno-fullm-v1/best.pt` para el re-score (sin pisar el historico de US-030 sin decision). |
| `docs/us-planning/us-039.md`, `docs/us-handoff/us-039.md` | **nuevo** | Esta planificacion. |

**Reusar SIN modificar**: `ml/train/train_segmentation.py` (`build_and_train`, `train_segmentation`,
`phenology_contrastive_loss`, `_run_epoch`), `ml/models/tsvit_wrapper.py` (`build_tsvit`),
`ml/models/pheno_semantic_branch.py` (`PhenoSemanticBranch`), `ml/features/phenology_class_prototypes.py`
(`load_class_prototype_embeddings`), `ml/data/pastis_seg_dataset.py`, harness US-030
(`run_us030_rescore_fold5` / `rescore_all_checkpoints`).

**NO tocar**: `data/features/phenology_class_prototypes_pastis.parquet` + `.dvc` (solo LEER, NO
regenerar, NO Gemini); la logica de `train_segmentation.py` / `tsvit_wrapper.py` /
`pheno_semantic_branch.py` (INVOCAR, no reescribir); `data/farslip_pairs/`, `expand_to_cap`,
`cap_vocabulary.yaml` (camino sintetico/italiano PROHIBIDO); el best historico de US-030 si se usa alias.

## Dependencia de US-038 (R-DEP, ALTA)

US-039 = **US-038 (config full TSViT) + rama contrastiva lambda=0.3**. La unica diferencia controlada
es `use_phenology=True` + `lambda_contrast=0.3`; TODO lo demas IDENTICO a US-038 (apples-to-apples).

- `CFG_FULL_TSVIT` es el **UNICO punto de acoplamiento**: si US-038 expone su config como constante,
  US-039 la **IMPORTA** (cero duplicacion); si no, la **replica** con comentario `# origen: US-038
  <archivo>:<linea>` y la marca como deuda a unificar.
- **F1 (orquestador + tests con mocks) es avanzable YA** con `CFG_FULL_TSVIT` provisional (el pipeline
  US-025 ya existe). El **run real en H100 BLOQUEA hasta que US-038 cierre** (define la config y libera
  la GPU). No lanzar H100 sin la config real de US-038.
- Cola H100: **tras US-038**, antes de US-041, por debajo de US-036-a (P0).

## Metricas y umbrales (referencia)

| Metrica | Referencia | Criterio |
|---------|-----------|----------|
| mIoU fold-5 pheno full | base tsvit-pheno fold-5 = **0.6139** (US-030); base full = la de US-038 | No-regresion: `>= base US-038 - 0.01`. |
| **Delta mIoU (pheno - base US-038)** | fold-4 historico +0.0038 | **Esperado `[-0.005, +0.01]` (~0.3%)**. `>= +0.03` -> SOSPECHA. `< 0` valido. |
| F1-macro fold-5 | 0.7401 (US-030) | Reportado honesto, no-regresion. |
| Saturacion | — | Si pheno == base `+/-0.005` -> "saturacion supervisada confirmada" (hallazgo, no fallo). |

> Metricas reales fold-5 NO se conocen hasta ejecutar el run (tras US-038); se reportan al cierre.

## H100 / MLOps

- **`nvidia-smi` ANTES**; parar daemon idle-shutdown. Tunel Cloudflare (`User1@127.0.0.1`, reiniciar `cloudflared` si zombi).
- **VRAM**: TSViT full + rama contrastiva. La rama anade memoria ACOTADA (`max_pixels=4096` -> matriz `(4096,18)`, despreciable). Estimacion `< 40 GB` en 96 GB salvo batch muy agresivo de US-038; validar `nvidia-smi` epoch 1. OOM -> bajar batch (NO tocar `max_pixels`).
- **Horas**: mismo orden que el run base US-038 (rama contrastiva `< 5%` extra/step). Del orden de **1-3 h** segun epochs de US-038. `time_cap` de seguridad.
- **AMP/estabilidad**: ya cableados (AMP + GradScaler + clipping `max_norm=1.0` + warmup lineal->cosine). Smoke 50 patches antes del full.
- **Checkpoints**: `checkpoints/segmentation/tsvit-pheno-fullm-v1/{last.pt,best.pt}` (resume si la ventana se corta). Best -> `dvc add` + push desde la VM (ADC OK; si 401 gcsfs, push local o scp).
- **MLflow `:5010`** experiment `agrosat-segmentation`, run `alt-tsvit-pheno-fullm-v1`: tags `data_version`+`code_version`, params (lambda_contrast=0.3, config full), curva loss/mIoU, artefactos (best.pt, confusion, per-class — ya los genera `_log_final_artifacts`). Verificar run CERRADO (gotcha doble almacen, MEMORY.md). Si `:5010` cae en la VM: MLflow nativo SQLite + ingestar (patron US-034/035).

## Riesgos principales

1. **R1 (CRITICA) — Deshonestidad / sobre-afirmar el aporte fenologico** (riesgo #1). Delta SIEMPRE pheno full vs base full US-038, MISMO fold-5, MISMA config salvo `use_phenology`. Nota honesta en tabla + handoff. `< 0` se reporta; `>= +0.03` -> sospecha, no celebracion.
2. **R2 (ALTA, esperada) — Saturacion supervisada** (Delta ~0). Es el resultado mas probable y VALIDO; se declara hallazgo. NO forzar positivo subiendo lambda ni cambiando metrica.
3. **R3 (ALTA) — R-DEP US-038 no cerrada**. F1 (orquestador+tests mocks) avanzable ya; run real bloquea hasta US-038.
4. **R4 (ALTA) — Divergencia de config vs US-038**. `CFG_FULL_TSVIT` unico lugar de verdad (importar de US-038); test lo verifica.
5. **R5 (MEDIA) — Regenerar parquet US-033**. Solo LEER; NO Gemini; test lo verifica.
6. **R6 (MEDIA) — Contaminacion sintetica/italiana**. SOLO PASTIS real; guarda que rechaza root sintetico.
7. **R7 (ALTA) — Fuga fold-5**. Re-score EXCLUSIVO con harness US-030 sobre `fold=5`; norm solo train (1,2,3).
8. **R8 (MEDIA) — NaN/divergencia a config full**. Warmup+clipping+AMP ya cableados; smoke antes del full.
9. **R10 (MEDIA) — Sobrescribir best historico US-030**. Ruta NUEVA `tsvit-pheno-fullm-v1/` + alias en registry; no pisar sin decision.
10. **R11/R12 (BAJA) — Run `RUNNING` / ventana cortada**. Verificar FINISHED; `resume=True` + checkpoints por epoch.

## Sub-tareas / proximo paso

**1 solo agente ml, secuencial respecto a US-038** (write-set pequeno; camino critico = config US-038 + cola H100):

1. (avanzable ya) `scripts/run_us039_tsvit_pheno_fullm.py` + tests con mocks (trainer/proto/harness), `CFG_FULL_TSVIT` provisional, reusando el pipeline US-025.
2. (al cerrar US-038) Fijar `CFG_FULL_TSVIT` = config real de US-038 (importar o replicar con origen).
3. (cola H100, tras US-038) `nvidia-smi`, smoke 50 patches, lanzar run `alt-tsvit-pheno-fullm-v1`, esperar convergencia, best -> `dvc add`+push.
4. (cierre) Re-score fold-5 (harness US-030), tabla `pheno_vs_base_fold5.csv`, registry alias, cerrar runs MLflow, handoff a `implemented` con el **Delta honesto** + nota de saturacion/FarSLIP.

## Estado de implementacion (2026-06-08)

**Codigo COMPLETO y testeado en CPU. Run H100 PENDIENTE (GPU ocupada con US-036-a; cola tras US-038).**

### Decision clave (R10): coexistencia, NO sobrescritura

Se anadio una entrada **NUEVA** `tsvit-pheno-fullm` al registry del harness (ruta
`checkpoints/segmentation/tsvit-pheno-fullm-v1/best.pt`), que **COEXISTE** con la historica `tsvit-pheno`
(L4, `tsvit-pheno-v1/best.pt`, US-030/US-025) en vez de pisarla. Asi la tabla fold-5 publicada de US-030
sigue comparable y el re-score corre las dos filas (L4 historica + Full-M nueva) apples-to-apples. El
registry pasa de 7 a **8** entradas.

### Contrato de capacidad (R4, R-HARNESS): un solo lugar de verdad

`CFG_FULL_TSVIT` (en el orquestador) **deriva** de `TSVIT_FULLM_CONFIG` (US-038, `ml/models/tsvit_wrapper.py`),
NO la redefine. La capacidad Full-M (`n_timesteps=64, dim=192, depth 6+6, heads=6, dim_head=64`) que se
ENTRENA es byte-identica a la base US-038 `tsvit`. La entrada `tsvit-pheno-fullm` del registry lleva
`model_kwargs=dict(TSVIT_FULLM_CONFIG)`, asi `build_model_for_kind` reconstruye la MISMA topologia antes de
`load_state_dict` (el `best.pt` NO incrusta la capacidad; un rebuild L4 daria shape mismatch). La rama
contrastiva es solo una **loss de entrenamiento** (NO parte del forward de segmentacion), por lo que el
`state_dict` guardado es identico al de la topologia base -> re-score 1:1 contra `tsvit`.

### Cambios aplicados (write-set)

- `scripts/run_us039_tsvit_pheno_fullm.py` (**NUEVO**): orquestador Typer. `run_tsvit_pheno_full(*, cfg_full,
  lambda_contrast=0.3, run_name="alt-tsvit-pheno-fullm-v1", rescore_fold5=True, ...)` invoca
  `build_and_train("tsvit-pheno", lambda_contrast=0.3, **CFG_FULL_TSVIT)` (NO reimplementa el loop ni la loss
  ni la carga de prototipos), valida el root (rechaza `farslip_pairs`), y tras el run re-score el fold-5 con
  `rescore_all_checkpoints(fold=5)` y construye `build_pheno_vs_base_table(...)`. Seams `train_fn` / `rescore_fn`
  para testear sin GPU. CLI `run` con `--device/--num-workers/--ckpt-dir/--data-root/--mlflow-uri/--rescore`.
- `ml/eval/checkpoint_registry.py` (**edit minimo**): `ModelKind` gana `"tsvit-pheno-fullm"`; entrada nueva
  `tsvit-pheno-fullm` (kind homonimo, path `tsvit-pheno-fullm-v1/best.pt`, 18 clases, ignore 255, 10 bandas,
  `model_kwargs=dict(TSVIT_FULLM_CONFIG)`). La historica `tsvit-pheno` (L4) queda INTACTA.
- `ml/eval/segmentation_inference.py` (**edit minimo**): `build_model_for_kind` y `_forward_logits` enrutan
  `tsvit-pheno-fullm` por la misma rama TSViT que `tsvit-pheno` (aplica `model_kwargs` Full-M). `rescore_all_checkpoints`/`_rescore_one` NO se tocaron.
- `ml/eval/dense_metrics.py` (**edit minimo**): el set `is_temporal` incluye `tsvit-pheno-fullm` (consume la
  serie temporal completa, no el composite mediano).
- `tests/ml/train/test_us039_tsvit_pheno_fullm.py` (**NUEVO**, 13 tests): contrato del orquestador con
  trainer/harness MOCKEADOS (cero GPU/PASTIS/Gemini): `tsvit-pheno`+lambda=0.3, config == `TSVIT_FULLM_CONFIG`,
  `CFG_FULL_TSVIT` deriva del single-source, folds (1,2,3)/(4) y fold-5 fuera, run name, ckpt slug fullm,
  prototipos NO regenerados (sin Gemini), rechazo de root sintetico, rechazo de `cfg_full` colando
  `lambda_contrast`, tabla delta (schema + `delta = pheno - base`, nota honesta, delta negativo valido),
  re-score con `fold=5`.
- `tests/ml/eval/test_registry_tsvit.py` (**+2 tests**): entrada `tsvit-pheno-fullm` con metadatos + capacidad
  Full-M; `build_model_for_kind` la reconstruye Full-M (dim 192, depth 6+6, n_timesteps 64).

### Cambio colateral necesario (requerido por la nueva entrada)

- `tests/ml/eval/test_rescore.py`: el contrato US-030/US-038 asumia 7 modelos; la entrada legitima
  `tsvit-pheno-fullm` lo lleva a 8. Se actualizaron: `_EXPECTED_MODELS` (+`tsvit-pheno-fullm`), `len==8`,
  `test_registry_paths_match_contract` (+ruta `tsvit-pheno-fullm-v1/best.pt`), `test_registry_native_class_conventions`
  (`tsvit-pheno-fullm` en el grupo 18-clase ignore 255). `_FORBIDDEN_FRAGMENTS` ya permitia `-fullm-`;
  `rescore_all_checkpoints`/`_rescore_one`/loss/FarSLIP siguen INTACTOS.

### Verificacion (sin GPU)

- `ruff check` sobre los 7 archivos del write-set: **All checks passed**.
- `pytest tests/ml/train/test_us039_tsvit_pheno_fullm.py tests/ml/eval/test_registry_tsvit.py
  tests/ml/eval/test_rescore.py -q`: **40 passed, 1 skipped** (best.pt Full-M real ausente, DVC).
- No-regresion `pytest tests/ml/ -k "tsvit or pheno or registry or rescore" -q`: **210 passed, 1 skipped**.
  Las 3 fallas restantes (`test_phenology_description.py`, `test_phenology_text.py`: regex ES "Gemini no esta
  configurado" vs mensaje EN) son **PREEXISTENTES** (confirmado con `git stash`), ajenas a esta US y fuera del
  write-set.
- Cobertura del orquestador: `coverage run --include="scripts/run_us039_tsvit_pheno_fullm.py"` -> **88%**
  (>= 70%; las lineas sin cubrir son los fallbacks de import real `from ml...` y el CLI, solo activos en el run H100).

### Comandos para la ventana H100 (cuando la GPU se libere, tras US-038)

**1) nvidia-smi ANTES** (confirmar que US-036-a y US-038 liberaron VRAM; parar daemon idle-shutdown).

**2) Verificar datos en la VM**: `data/PASTIS-R` en F: con `metadata.geojson` + `NORM_S2_patch.json`; parquet
US-033 presente (solo LEER).

**3) Run de entrenamiento (orquestador, 40 ep, batch 20, T completo, Full-M, lambda=0.3, folds train 1,2,3 / val 4)**:

```bash
poetry run python scripts/run_us039_tsvit_pheno_fullm.py run \
  --device cuda --num-workers 10 \
  --ckpt-dir F:/checkpoints/segmentation/tsvit-pheno-fullm-v1 \
  --mlflow-uri http://localhost:5010
```

> Equivale a `build_and_train("tsvit-pheno", lambda_contrast=0.3, n_timesteps=64, dim=192, depth_temporal=6,
> depth_spatial=6, heads=6, dim_head=64, epochs=40, batch_size=20, lr=1e-3, train_folds=(1,2,3), val_folds=(4),
> mlflow_run_name="alt-tsvit-pheno-fullm-v1")`. Auto-resume: relanzar el MISMO comando reanuda desde `last.pt`.
> Si OOM, bajar `batch_size` (NO tocar `max_pixels=4096` de la rama contrastiva, ya acotado). `--rescore` (default)
> dispara el re-score fold-5 + la tabla delta al cierre; `--no-rescore` para entrenar sin re-score.

Alternativa CLI directa (mismo resultado, sin el re-score/tabla automaticos):

```bash
poetry run python -m ml.train.train_segmentation \
  --model tsvit-pheno --epochs 40 --batch-size 20 --n-timesteps 64 \
  --dim 192 --depth-temporal 6 --depth-spatial 6 --heads 6 --dim-head 64 \
  --lambda-contrast 0.3 --device cuda --num-workers 10 \
  --run-name alt-tsvit-pheno-fullm-v1 \
  --ckpt-dir F:/checkpoints/segmentation/tsvit-pheno-fullm-v1 \
  --train-folds 1,2,3 --val-folds 4
```

**4) Re-score fold-5 (lo dispara el orquestador con `--rescore`; o manual con el harness US-030)**:

```bash
poetry run python -c "from ml.eval.dense_metrics import rescore_all_checkpoints; \
df = rescore_all_checkpoints(fold=5, device='auto'); \
print(df.filter(df['model'].is_in(['tsvit','tsvit-pheno-fullm'])))"
```

> El harness reconstruye `tsvit-pheno-fullm` con `model_kwargs` Full-M (R-HARNESS) -> carga `best.pt` sin shape
> mismatch -> re-score 18-clase 128 NEAREST apples-to-apples contra la base `tsvit`. La tabla
> `reports/segmentation/metrics/tsvit_pheno_vs_base_fold5.csv` (2 filas + `delta_miou`/`delta_f1_macro` + nota
> honesta) se genera automaticamente con `--rescore`.

**5) DVC del checkpoint**: `dvc add checkpoints/segmentation/tsvit-pheno-fullm-v1/best.pt` + commit `.dvc` + push GCS.

**6) MLflow**: verificar que el run `alt-tsvit-pheno-fullm-v1` (`:5010`, experiment `agrosat-segmentation`) quedo
CERRADO (no RUNNING).

### Reporte HONESTO del Delta (al cierre, con metricas reales)

Pendiente del run H100. El handoff de cierre debe reportar el Delta `pheno - base` sobre el MISMO fold-5 tal cual
salga (positivo, ~0 o negativo), enmarcado como **ablacion controlada**: "Delta esperado ~0.3%, NO 5%; en
supervisado la fenologia aporta poco (saturacion supervisada confirmada si Delta ~0); el valor real de la
fenologia esta en el contrastivo self-sup de FarSLIP US-036-a, NO aqui". Un `Delta >= +0.03` se trata con
SOSPECHA (fuga/seed/config), no se celebra. Esa nota ya viaja embebida en cada fila de la tabla CSV (`note`).

## Criterio de aceptacion (resumen)

Orquestador `run_us039_tsvit_pheno_fullm.py` que re-entrena TSViT con la **MISMA config full de US-038**
+ rama contrastiva `lambda_contrast=0.3` sobre **PASTIS-R real** (folds 1,2,3 / val 4; fold-5 held-out),
prototipos US-033 directos (parquet DVC, NO regenerado, sin Gemini), REUSANDO `build_and_train("tsvit-pheno")`
(NO duplica el loop); run MLflow `alt-tsvit-pheno-fullm-v1` (`:5010`, tags `data_version`+`code_version`);
re-score fold-5 del best con el harness US-030 (apples-to-apples 18-clase 128 NEAREST); tabla
`pheno_vs_base_fold5.csv` con `delta_miou` y **reporte HONESTO** (Delta esperado ~0.3%, NO 5%; saturacion
supervisada confirmada si Delta ~0; el valor real de la fenologia esta en FarSLIP self-sup US-036-a, NO
aqui); best -> DVC, insumo de ENSAMBLE E-a (US-041); write-set DISJUNTO de US-038; `make check` limpio +
tests ml verdes.
