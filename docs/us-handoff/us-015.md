# Handoff US-015 — Features temporales agregados por parcela

**Estado**: planning
**Epic**: E3 (Feature Engineering · CRISP-ML(Q) Data Preparation)
**Avance**: A2 (Feature Engineering · dom 17-may-2026, 100 pts)
**Sprint**: S4 (11-may a 17-may)
**Rama prevista**: `feature/E3-US-015-temporal-features-parcels`
**Última fase**: 1 — planning (2026-05-16)
**SP**: 3
**Owner técnico**: Isaac Ávila (ML/Data Scientist) — coordinación de schema con Arthur Zizumbo

---

## Dominios tocados

- [ ] backend
- [ ] frontend
- [x] **ml** (`ml/features/temporal_features.py`, `ml/features/persist_features.py`)
- [ ] agent (`ml/agent/`)
- [ ] infra
- [x] **db** (migraciones `parcels` + `features_parcels`)
- [ ] dagster

---

## Archivos a crear (planning — confirmar en Fase 3 coding)

- `ml/features/temporal_features.py` — función pública `extract_temporal_features(parcel_timeseries: xr.DataArray) -> pl.DataFrame` con stats (153 cols), FFT 3 armónicos (24 cols), fenología NDVI (8 cols). Polars LazyFrame con `streaming=True`.
- `ml/features/persist_features.py` — `load_features_parcels(df, engine, on_conflict="update")` con UPSERT `ON CONFLICT (parcel_id, year) DO UPDATE`.
- `db/migrations/{ts}_create_parcels.sql` — tabla `parcels` mínima (precondición de `features_parcels`).
- `db/migrations/{ts}_create_features_parcels.sql` — UNIQUE `(parcel_id, year)`, FK CASCADE a `parcels`.
- `tests/ml/features/test_temporal_features.py` — 14 casos: sintéticos sin/gaussiana + edge cases + fixture demo.
- `tests/ml/features/test_persist_features.py` — 6 casos UPSERT/skip/raise.
- `tests/db/test_migrations_us015.py` — round-trip up/down/up + constraints + índices.
- `data/test_fixtures/parcel_demo_ts.zarr` — fixture demo (<100 KB).
- `scripts/generate_demo_parcel_ts.py` — operativo permanente para regenerar fixture (no viola anti-patrón `scripts/_*.py`).

## Archivos a modificar (planning)

- `ml/features/__init__.py` — re-export `extract_temporal_features`.
- `Makefile` — targets `features-extract-demo`, `features-persist`, `db-test-us015`.
- `pyproject.toml` — confirmar `polars`, `numpy`, `xarray`, `psycopg[binary]`; añadir `pytest-postgresql` o `testcontainers[postgres]` al grupo dev si falta.
- `docs/spectral_indices.md` — sección "Temporal aggregation" con refs académicas.

## Archivos existentes reutilizables

- `ml/ingest/gee_sampler.py` — provee xarray output de Sentinel-2 (US-007). NO duplicar — entrada de US-015.
- `db/migrations/20260511213942_initial_schema.sql` — extiende su patrón (PostGIS GIST en geom, BTREE en FK, TIMESTAMPTZ).
- `db/CLAUDE.md` — describe schema objetivo de `parcels` y `features_parcels`. US-015 lo implementa por primera vez en migraciones.
- `.claude/skills/agrosat-ml-features/SKILL.md` — provee snippets de referencia para `extract_temporal_features`, `fft_harmonics`, `detect_phenology`. US-015 los expande con tests, edge cases, persistencia.

## Decisiones técnicas clave (planning)

1. **Opción A: US-015 crea tanto `parcels` como `features_parcels`**. La tabla `parcels` no existe en migraciones aún, sólo descrita en `db/CLAUDE.md`. Para no diferir un FK + violar regla "FK con ON DELETE explícito", US-015 crea `parcels` mínima (id, session_id, aoi_id, geom POLYGON 4326, crop_class, area_ha, year) + `features_parcels` con FK CASCADE. Trade-off rechazado: Opción B (sólo `features_parcels` con `parcel_id` sin FK).
2. **Polars LazyFrame con `streaming=True`** para escalar a ~30k parcelas Italia (~5 GB denso → ~800 MB streaming). Cumple skill `agrosat-ml-features`.
3. **xarray como entrada** mantiene homogeneidad con upstream `ml/ingest/gee_sampler.py` (que ya devuelve xarray).
4. **numpy FFT** (no scipy) — sin dependencia adicional, `np.fft.rfft` suficiente.
5. **JSONB para `ndvi_stats` y `phenology`** + columnas escalares sólo para fenología (queries frecuentes). Evita 153 columnas escalares manteniendo flexibilidad de queries `ndvi_stats->'mean'`.
6. **Sin Redis cache en US-015** — la agregación es one-shot por (parcel_id, year). Recálculo barato vs I/O del Zarr. Cache se difiere a US-016 si bulk extractor lo justifica.
7. **Pre-interpolación lineal a grid diario antes de FFT** — Sentinel-2 tiene revisita irregular con huecos por nubes. Documentado en docstring + test `test_fft_irregular_resamples_first`. Alternativa Lomb-Scargle diferida a backlog.
8. **Fenología UNIQUE asume un ciclo vegetativo anual por parcela**. Cultivos doble-ciclo (raros en Italia) se modelarán en US futura con `season SMALLINT` vía rollforward. No bloquea US-015.

## Bugs resueltos

| Bug | Causa | Solución | Estado |
|-----|-------|----------|--------|
| n/a (planning) | — | — | — |

## Zonas sensibles

- `db/migrations/` — JAMÁS modificar la migración ya aplicada (`20260511213942_initial_schema.sql`). US-015 crea dos nuevas vía `dbmate new`. Round-trip up/down/up debe ser idempotente.
- `ml/features/temporal_features.py:detect_phenology` — debe retornar NaN/None graceful cuando NDVI nunca cruza 0.3 o peak está en extremo del ciclo. NO lanzar excepción.
- Schema de `parcels` — superset compatible con la versión "rich" futura. NO introducir CHECK constraints que limiten extensión (`crop_class TEXT` sin enum; `confidence REAL` sin rango).

## Modelos / Datos

- MLflow run id: n/a (sin training)
- DVC version esperada: `features-parcels-italy-v1` (opcional — puede diferirse a US-016 si timebox aprieta)
- VRAM H100 consumida: 0 GB (US-015 es CPU + I/O puro). 0 horas del presupuesto 80h.

## engram-memory

- Observaciones a guardar al cierre: decisión Opción A (US-015 crea ambas tablas), contrato FFT con pre-interpolación, fenología graceful con NaN, UNIQUE(parcel_id, year) asume ciclo anual.
- Keywords: `us-015`, `features_parcels`, `temporal_features`, `phenology`, `fft_harmonics`, `polars_lazy_streaming`, `parcels_table_creation`.

## Próximas fases

1. **Fase 2 — Research adicional (opcional, <2h)**: confirmar con Isaac que el formato xarray que devuelve `gee_sampler.py:560` coincide con el contrato de entrada (dim `(time, band)`, attrs `parcel_id`, `year`). Si no, ajustar el conversor `_xr_to_lazy` o exponer un adaptador.
2. **Fase 3 — Coding (1.5 días)**: ejecutar plan §3.3. Dos subagentes en paralelo: `ml-engineer` para Polars+FFT+fenología, `backend-engineer` para migraciones+persistencia.
3. **Fase 4 — QA (0.5 días)**: coverage ≥80 % sobre módulos nuevos, `make check` limpio, round-trip dbmate, security grep (no secrets, no `pickle`, no `eval`).
4. **Fase 5 — Cierre**: PR a `develop`, `docs/us-resolved/us-015.md`, handoff a estado `ready-to-close`.

## Notas de coordinación

- **Sync con Isaac (US-014, mismo sprint S4)**: contrato `compute_index → xr.DataArray dim=band` confirmado antes de empezar. Test integración cross-US `tests/ml/features/test_us014_us015_pipeline.py` validará composición.
- **Sync con Arthur (Platform)**: revisar schema `parcels` antes de `dbmate up` para confirmar que es superset de la versión "rich" futura.
- **Sin frontend ni i18n**: US-015 es 100 % backend ML+DB. No toca `frontend/i18n/locales/`.
- **Si timebox aprieta**: priorizar AC-1 (stats Polars) + AC-3 (fenología NDVI) + AC-5 (migraciones). FFT (AC-2) y derivativos (AC-4) son "nice-to-have" si US-014 acumula deuda.
