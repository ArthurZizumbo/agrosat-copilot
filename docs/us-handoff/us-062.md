# US-062 — Handoff (DONE: analisis de riesgos categorizados + matriz prob x impacto)

**Status**: documento `docs/risks/riesgos.md` COMPLETO — 4 categorias de rubrica (datos/ataques/confianza/cumplimiento) + 6 riesgos de ejecucion v8 + matriz 3x3
**QA (2026-06-20)**: **DONE** — ruff limpio (backend/ ml/ dagster_project/: "All checks passed!"), mypy package-mode 26 files "Success: no issues found", suite chat+metrics 52 passed, suite drift 11 passed, `dagster_project.definitions` importa limpio. US documental: la verificacion es el documento contra los AC, no tests de aplicacion.
**Epic**: E10 (Observabilidad y Documentacion) · **Rama**: `feature/E10-observabilidad-docs` (parte de `feature/E12-transfer-multiregion`)
**Plan**: `docs/us-planning/us-062.md` · **v8**: `context/RefinamientoPlaneacionAgroSatCopilot_v8.md` §US-062 (linea 2036), §4 "Gestion de Riesgos" (linea 2530), §5 (linea 2544) · **ADR-009**

## Que entrega

Documento unico `docs/risks/riesgos.md` que cierra el criterio "Riesgos" (20 pts) de la rubrica
del Avance 7. Categoriza los riesgos del proyecto en las **cuatro categorias de la rubrica**
(datos, ataques, confianza, cumplimiento) y agrega una categoria transversal de **ejecucion
(realidad v8)** con los **6 riesgos del §4** del plan v8. Cada riesgo tiene id estable, Prob
(Alta/Media/Baja), Impacto (Alto/Medio/Bajo) y una mitigacion concreta y accionable; se cierra
con una **matriz probabilidad x impacto 3x3** que ubica todos los ids y destaca la esquina
critica. Ancla cada afirmacion a evidencia REAL del repo (resultados E12, migraciones aplicadas,
ADR-009, FinOps), respetando dos claims defendibles: transferibilidad espacial limitada de
AlphaEarth (arXiv:2601.00857) y **NO sobre-afirmar F1>=0.80 en Mexico** sin ground-truth.

## Estado real (DONE)

DONE (documento, 20-jun-2026):
- **Cuatro categorias de rubrica completas**, cubriendo todos los items nombrados en el AC:
  - **DATOS** (`R-DAT-01..05`): disponibilidad CDSE, calidad de labels (partial-label/null-class
    crosswalk HCAT US-074), cobertura de nubes, **transferibilidad espacial AlphaEarth** y
    descarga de subsets multi-region. R-DAT-04 cita la evidencia REAL E12: FR -> Catalonia
    zero-shot mIoU 0.0000 vs few-shot 0.2468 (Delta +0.2468,
    `reports/segmentation/sen4agrinet_transfer_result.json`).
  - **ATAQUES** (`R-ATK-01..04`): adversarial, DDoS/abuso API, prompt-injection/tool-abuse ADK,
    exfiltracion multi-tenant. R-ATK-04 referencia RLS por `session_id` YA aplicado (US-051,
    `20260620000418_rls_multi_tenant.sql`, rol `agrosat_app` NOBYPASSRLS, fail-closed).
  - **CONFIANZA** (`R-CNF-01..04`): hallucinations, sesgos regionales, falsas alarmas y
    **R-CNF-04 "no sobre-afirmar F1>=0.80 en Mexico"** (US-077 zero-shot cualitativa, sin claim
    de F1; ADR-009 + §4/§11.2 v8).
  - **CUMPLIMIENTO** (`R-CMP-01..04`): GDPR, licencias multi-region (Sen4AgriNet/EuroCropsML
    CC-BY-SA-4.0, AlphaEarth V1/ANNUAL v1.1 CC-BY-4.0, ODbL), politicas Copernicus y caveat del
    credito cloud Trial GenAI App Builder (FinOps US-061).
- **Ejecucion realidad v8** (`R-EJE-01..06`): los 6 riesgos del §4 (H100 una sola GPU; RLS
  migracion falla; OOF loop 10-20h; FarSLIP band mismatch 4 vs 10; Gemini rate-limit; incremental
  Stage-1 no converge), cada uno con Prob del v8, impacto y estado real del repo. Incluye el
  gotcha de trazabilidad MLflow (server Docker `:5010`, no `./mlruns`).
- **Matriz 3x3** (filas Impacto x columnas Prob) con todos los ids ubicados; esquina critica
  destacada: `R-EJE-01` (GPU unica) y `R-DAT-04` (transferibilidad AlphaEarth).
- **Resumen ejecutivo** top-5 por severidad derivado de la matriz.
- **Correcciones de fidelidad documentadas en el propio doc**:
  - AC cita "§11 del v8"; la seccion vigente de riesgos de ejecucion es el **§4** (linea 2530).
  - `docs/STATUS.md` **no existe** en el repo; se sustituye por las **migraciones aplicadas**
    (`db/migrations/`, verificables con `dbmate status`) como fuente de verdad del estado RLS.
    El faltante queda anotado en `docs/blockers/epic10-notas.md`.

## Archivos

- `docs/risks/riesgos.md` — documento principal (entregable de la US, 9 secciones: metodologia,
  resumen ejecutivo top-5, 4 categorias de rubrica, ejecucion v8, matriz 3x3, plan de monitoreo,
  referencias).

> US 100% documental: no toca `backend/`, `ml/`, `frontend/`, `db/`, `infrastructure/` ni
> `dagster_project/`. No requiere `dbmate up`, MLflow ni `dvc add`.

## Lineage / QA

- **Sin codigo de aplicacion**: la verificacion es la revision del documento contra los AC
  (mapeo AC-1..AC-5 del planning) y la rubrica del Avance 7 (criterio "Riesgos", 20 pts).
- Verificacion transversal del estado del repo en esta sesion (re-corrida a mano por el
  orquestador, no solo reportada):
  - `ruff check` sobre `backend/ ml/ dagster_project/`: **"All checks passed!"**.
  - `mypy` package-mode (`backend.app.middleware/api/utils/services`): **"Success: no issues
    found in 26 source files"**.
  - Suite chat+metrics: **52 passed** (`test_metrics_middleware` + `test_chat_metrics` +
    `test_chat_sse` + `integration/test_chat_endpoint` + `integration/test_chat_uses_session_model`).
  - Suite drift: **11 passed** (`tests/ml/monitoring` + `tests/dagster/test_drift_asset.py`),
    warnings benignos de scipy.
  - `dagster_project.definitions` importa limpio: schedules=`['drift_check_weekly_schedule']`,
    jobs=`['farslip_full_pipeline_job', 'drift_check_job']`.
- Datasets reales versionados referenciados por el doc: `data/sen4agrinet.dvc`,
  `data/transfer/eurocropsml.dvc`.
- Commit propio de la US, autor Arthur Zizumbo, **sin trailer `Co-Authored-By`**.

## Notas / blockers

- **Referencia "§11 del v8"**: corregida a §4 (linea 2530) dentro del documento; no es un blocker,
  es nota de fidelidad.
- **`docs/STATUS.md` ausente**: el planning lo referenciaba como fuente del estado RLS; no existe
  en el repo. Sustituido por las migraciones aplicadas (`db/migrations/`); faltante anotado en
  `docs/blockers/epic10-notas.md`. No bloquea: el estado RLS queda verificado contra
  `20260620000418_rls_multi_tenant.sql`.
- **Deuda RLS residual** (mencionada en R-ATK-04/R-EJE-02, no es blocker de US-062): backfill de
  `parcels.session_id` a NOT NULL y la app DEBE conectar como `agrosat_app` (no superusuario, que
  bypassa RLS).
- **AlphaEarth**: citado siempre como `SATELLITE_EMBEDDING/V1/ANNUAL` data v1.1 64-dim CC-BY-4.0
  (la denominacion "v2.1" no existe; el doc corrige menciones muertas).
- **FIX del orquestador para US-059** (contexto de la rama, no de esta US): `DEFAULT_REGISTRY`
  dedicado + `get_default_metrics()` singleton -> re-instanciar la app ya NO lanza "Duplicated
  timeseries" (rompia 13 tests de integracion de chat). **FIX de harness US-065**:
  `_FakeConn`/`_FakePool` en `integration/test_chat_endpoint.py` (el `object()` previo rompia
  `_resolve_variant.pool.acquire()`; fallo PRE-EXISTENTE de US-054, no regresion de E10).
