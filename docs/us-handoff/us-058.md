# Handoff US-058 — MapView: useMap.ts + link parcela->chat

**Estado**: READY-TO-CLOSE (QA verde, 2026-06-20). Rama `feature/E9-US-058-mapview` (desde US-057). Dominio: solo frontend.

Plan detallado: [`docs/us-planning/us-058.md`](../us-planning/us-058.md).

## Resumen ejecutivo

Extraer la logica inline de `MapCanvas.vue` a `composables/useMap.ts` (init/destroy MapLibre, sources/layers, handlers) y anadir el **link parcela->chat**: click en una parcela publica su `parcel_id` REAL al chat store, que `useChat` adjunta como `ChatRequest.parcel_id`. Remover `deck.gl` (0 usos, deuda). i18n `map.*` ampliado. Tests del store + cross-store link + useMap mock.

## Subtareas (SECUENCIAL — comparten MapCanvas.vue, NO paralelizar)

1. **fe/A** (PRIMERO): crear `composables/useMap.ts` extrayendo de MapCanvas; MapCanvas consume; remover deck.gl. Criterio: paridad funcional, no romper el mapa.
2. **fe/B** (SEGUNDO, sobre useMap): `stores/map.ts` (+selectedParcel/visibleBbox), `stores/chat.ts` (+activeParcelId), `useChat` (+parcel_id en body), callback `onParcelSelect` en useMap, i18n.

`deck.gl` (remover) va en fe/A; i18n va en fe/B.

## Archivos

- **fe/A**: `composables/useMap.ts` (NUEVO), `components/map/MapCanvas.vue`, `package.json`, `nuxt.config.ts`, `frontend/CLAUDE.md`.
- **fe/B**: `stores/map.ts`, `stores/chat.ts`, `composables/useChat.ts`, `composables/useMap.ts`, `components/map/MapCanvas.vue`, `i18n/locales/{it,es,en}.json`.
- **tests**: `tests/unit/map-store.test.ts` (ampliar), `tests/unit/map-chat-link.test.ts` (NUEVO), `tests/unit/use-map.test.ts` (NUEVO).

## Decisiones clave

- **useMap firma**: `useMap(opts) -> { initMap(container), destroyMap(), flyToDemoAoi, locateParcel, isReady, drawRect }`. SSR-safe (import dinamico de maplibre dentro de initMap, sin singletons de modulo). MapCanvas conserva template/chips/draw-rect-UI/defineExpose.
- **Link**: callback `onParcelSelect(parcel_id)` que MapCanvas pasa -> `mapStore.setSelectedParcel` + `chatStore.setActiveParcelId`. Cross-store explicito en el componente (useMap queda agnostico/testeable).
- **deck.gl: REMOVER** (`pnpm remove deck.gl`) — 0 usos reales (solo un comentario en nuxt.config.ts), AC no lo pide, eliminar deuda.
- **Datos REALES**: `parcel_id` de la feature renderizada (id real backend/demo); geometria densa = FUTURE (tool_result no la trae); demoFindings = datos de demo, no inferencias falsas.

## Estado de ejecucion

- **fe/A** (DONE): `composables/useMap.ts` creado; MapCanvas lo consume; `deck.gl` removido.
- **fe/B** (DONE): link parcela->chat implementado. Detalle abajo.

### fe/B — link parcela->chat (implementado)

- `stores/map.ts`: `+SelectedParcel`/`VisibleBbox` types; state `selectedParcel`/`visibleBbox`; getter `hasSelectedParcel`; actions `setSelectedParcel`/`clearSelectedParcel`/`setVisibleBbox`. No guarda geometria (FUTURE).
- `stores/chat.ts`: state `activeParcelId: number | null`; action `setActiveParcelId(id|null)`; `reset()` lo limpia a null.
- `composables/useChat.ts`: el body del POST /chat incluye `parcel_id: store.activeParcelId ?? null` (el contrato `ChatRequest.parcel_id` ya existia).
- `composables/useMap.ts`: handler `map.on('moveend', ...)` -> `mapStore.setVisibleBbox([minLng,minLat,maxLng,maxLat])`. El click handler ya invocaba `opts.onParcelSelect(parcelId, props)` con el `parcel_id` REAL de `e.features[0].properties` (sin cambios, viene de fe/A).
- `components/map/MapCanvas.vue`: cross-store EXPLICITO en el componente — `useMap({ onParcelSelect: (id, props) => { mapStore.setSelectedParcel({parcel_id:id, crop_class}); chatStore.setActiveParcelId(id); } })`. useMap queda agnostico del chat store. Chip "parcela seleccionada" con boton de limpiar (`clearParcelSelection` limpia ambos stores en lockstep).
- `i18n/locales/{it,es,en}.json`: `map.selected_parcel`/`map.parcel_in_chat`/`map.clear_selection` en sync (3 locales).

**Cross-store**: map-store guarda el highlight/chip; chat-store guarda el id que viaja al backend. Se setean juntos en el click. parcel_id REAL de la feature renderizada (backend o demo); geometria densa = FUTURE (no inventada).

### Tests (DONE)

- **`tests/unit/use-map.test.ts` (NUEVO)**: ejercita el composable REAL `useMap` con `maplibre-gl` MOCKEADO (jsdom no carga WebGL). Cobertura:
  - `initMap` construye el mapa y, en `load`, registra sources (`findings`, `active-aoi`) + layers (`findings-fill/line/highlight`, `active-aoi-fill/line`) y handlers (`click:findings-fill`, `moveend`) — assert sobre `addSource`/`addLayer`/`on` del mock.
  - `initMap` idempotente (segunda llamada no crea segundo mapa).
  - `destroyMap` llama `map.remove()` y resetea `isReady`/`drawRect`.
  - `moveend` -> `mapStore.visibleBbox` flat `[w,s,e,n]` desde `getBounds()`.
  - **click handler extrae el `parcel_id` REAL** de `e.features[0].properties` (feature demo parcel 101) y llama `onParcelSelect(id, props)`; no dispara si la feature no trae `parcel_id`.
  - **Cross-store (datos reales)**: el fan-out estilo MapCanvas guarda la parcela en map-store Y el `parcel_id` en chat-store; clear limpia ambos en lockstep. parcel_id = id real de `demoFindings()` (101/102/103), no inventado.
- **`tests/unit/map-store.test.ts` (AMPLIADO)**: `selectedParcel`/`hasSelectedParcel`/`clearSelectedParcel`, `visibleBbox`, y link cross-store a nivel store (map.selectedParcel <-> chat.activeParcelId con id real; `chat.reset()` limpia `activeParcelId`).

Mock MapLibre: `Map` (constructor que registra handlers por `event`/`event:layer` y permite `fire()`), `Popup`, `NavigationControl`, `ScaleControl`, `LngLatBounds`, `GeoJSONSource.setData`. Auto-imports Nuxt (`ref`/`watch`/`useI18n`/`useAoi`) via `vi.stubGlobal` (`useAoi` es el composable REAL).

### Gates verificados (REAL, este cierre)

- `pnpm typecheck`: limpio (EXITCODE 0).
- `pnpm vitest run`: **7 files, 53 tests PASS** (use-map 8 + map-store 14 nuevos/ampliados incluidos).
- `node ../scripts/i18n_check.mjs`: `i18n parity OK across it, es, en` (claves `map.selected_parcel`/`map.parcel_in_chat`/`map.clear_selection` ya en sync en los 3 locales; sin cambios necesarios).

### Pendiente

- Nota MEMORY: ESLint v9 puede romper `make check` en frontend (pre-existente) — validar backend/ml aparte si aplica.

### QA independiente (2026-06-20) — READY-TO-CLOSE

Gates re-ejecutados por el QA (no se confio en el reporte previo):

- `cd frontend && pnpm typecheck` -> exit 0, sin `error TS`.
- `cd frontend && pnpm vitest run` -> **7 files, 53 tests PASS** (suite COMPLETA; use-map 8 + map-store ampliado; US-057 NO rota: sse-parser/chat-store/chat-persist/use-chat-retry/markdown verdes).
- `node scripts/i18n_check.mjs` (raiz, no `frontend/scripts/`) -> `i18n parity OK across it, es, en`.

AC verificados por lectura de codigo:

- `composables/useMap.ts` existe; `MapCanvas.vue` lo CONSUME (logica imperativa movida, no duplicada).
- Link parcela->chat usa `parcel_id` REAL de `e.features[0].properties` (contrato `ChatRequest.parcel_id` en `types/agent.ts:143`); geometria densa = FUTURE (no inventada).
- Cross-store EXPLICITO en MapCanvas: `onParcelSelect` -> `mapStore.setSelectedParcel` + `chatStore.setActiveParcelId`; `useChat` envia `parcel_id: store.activeParcelId ?? null` en el body.
- `deck.gl` REMOVIDO de `package.json` (0 matches); solo menciones en docs/skill-table (`CLAUDE.md`/`AGENTS.md` espejos).
- Basemap Esri World Imagery keyless intacto; sin Mapbox (solo el comentario "nada de Mapbox" en `nuxt.config.ts:3`).
- Cleanup en `onBeforeUnmount` -> `destroyMap()` (map.remove + removeEventListener keydown + stop watchers + reset refs + `mapApiRef.value=null`).
- SSR-safe: `import("maplibre-gl")` dentro de `initMap`, llamado bajo `import.meta.client`; sin singletons de modulo.
- i18n `map.selected_parcel`/`map.parcel_in_chat`/`map.clear_selection` en sync en los 3 locales (lineas 102-104).

Manual-test: [`docs/manual-test/us-058.md`](../manual-test/us-058.md).

> `make check` completo NO corrido (ESLint v9 pre-existente, ajeno a esta US).
