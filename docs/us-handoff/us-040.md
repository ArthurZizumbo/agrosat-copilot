# US-040 — Handoff

**Status**: `planning`
**Titulo**: Cuatro ensambles base de rubrica (Voting / Bagging / Stacking / Blending)
**Epic**: E6 (Ensambles) · **Avance**: A5 (ensamble)
**Sprint**: S7 · **Owner**: Arthur Zizumbo (ML/MLOps) · **Prioridad**: P8
**Rama**: `feature/E6-US-040-ensembles-base`
**Plan**: [docs/us-planning/us-040.md](../us-planning/us-040.md)
**Depende de**: US-031 (`implemented`) — consume los 12 parquet OOF fold-5 de `ml/eval/oof/` y reusa
`ml/utils/parcel_reconcile.py` (`pixel_to_parcel_probs`, `load_pastis_parcel_ids`).

## Resumen ejecutivo

Poblar el modulo vacio `ml/ensemble/` (hoy solo `__init__.py`) con los **4 ensambles obligatorios por
rubrica**, todos sobre PASTIS-R real y consumiendo los OOF fold-5 de US-031 (sin re-inferir densos):

1. **E1 Voting homogeneo (pixel)** — soft-vote = promedio de PROBABILIDADES post-softmax de TSViT-pheno +
   U-TAE + U-Net; argmax sobre el promedio. NUNCA promedia logits.
2. **E2 Bagging tabular (parcela)** — XGBoost-AlphaEarth, N bootstraps + Optuna; promedio de proba parcela.
3. **E3 Stacking heterogeneo (parcela)** — base learners {TSViT-pheno->parcela, U-TAE->parcela,
   XGBoost-AlphaEarth} -> meta-learner (XGB/logreg) entrenado SOLO sobre OOF con CV ESPACIAL.
4. **E4 Blending Optuna (parcela)** — mismos 3 base learners, pesos simplex Optuna sobre holdout
   espacialmente disjunto (minimiza gap F1 train-val).

Lo mas critico de la rubrica es el **anti-fuga (NON-NEGOTIABLE)**: probabilidades no logits; meta-learner
solo OOF con CV espacial; blending sobre holdout disjunto; **reportar en fold-5 held-out, NO en fold-4**
(que fue seleccion). Entregables: tabla comparativa (mejor individual 0.6253 vs 4 ensambles + tiempo
inferencia + modelo elegido) y >=4 figuras interpretadas.

## Correcciones / caveats a la US (verificados en recon)

- **Un solo fold held-out**: US-031 solo dumpeo fold-5 (`held_out=True`); folds 1-4 NO existen como OOF.
  El "CV espacial sobre OOF" del Stacking se hace con **sub-folds geograficos del fold-5** via
  `build_spatial_kfold`, no re-prediciendo otros folds (R-OOF-DEPTH).
- **`ml/eval/plots.py` NO existe**: figuras en NUEVO `ml/eval/ensemble_figures.py` reusando
  `ml/eval/avance4_figures.py` (`confusion_from_cm`, `per_class_iou_figure`, `optuna_convergence_figure`).
- **"TSViT base" no esta dumpeado**: tercer votante denso = **U-TAE / U-Net** (no TSViT base). Voting por
  defecto = TSViT-pheno + U-TAE + U-Net (R-VOTE).
- **`pixel_to_parcel_probs` YA EXISTE** (US-031, `ml/utils/parcel_reconcile.py`): REUSAR, no recrear.
  Firma real: `pixel_to_parcel_probs(probs_18, parcel_ids, *, patch_id, method="mean", ignore_index=255,
  ignore_parcel_id=0) -> pl.DataFrame` con `canonical_parcel_id`, `prob_000..prob_017`, `pred_class`, `n_pixels`.

## Dominios tocados

- [x] **ml** (`ml/ensemble/`, `ml/eval/`, `tests/ml/ensemble/`, `scripts/`)
- [ ] backend · [ ] frontend · [ ] agent · [ ] db · [ ] dagster · [ ] infra
- [ ] docs (solo estos dos `.md` de planning/handoff)

Skills: `agrosat-ml-ensemble`, `agrosat-ml-evaluation`, `agrosat-dvc-mlflow`, `agrosat-ml-baseline`, `agrosat-testing`.

## Sub-tareas paralelas (decision)

**1 base bloqueante (Fase 1) -> 4 agentes ml en paralelo (Fase 3, write-sets disjuntos) -> 1 cierre (Fase 4).**

### Fase 1 — bloqueante (secuencial, 1 agente)

| Agente | Write-set | Tarea |
|--------|-----------|-------|
| **ml/base** | `ml/ensemble/base.py`, `ml/ensemble/__init__.py`, `tests/ml/ensemble/{__init__,test_base}.py`, `tests/ml/ensemble/fixtures/synthetic_oof.py` | `EnsembleModel` abstracta (carga OOF, `evaluate(fold=5)` con anti-fuga, MLflow). Congela el contrato de firmas (Seccion 4 del plan). |

### Fase 3 — 4 SUBTAREAS PARALELAS (write-sets disjuntos)

| Agente | Write-set (disjunto) | Tarea |
|--------|----------------------|-------|
| **ml/A — Voting** | `ml/ensemble/voting.py`, `tests/ml/ensemble/test_voting.py` | Soft-vote pixel: mean(probs post-softmax)->argmax de TSViT-pheno+U-TAE+U-Net. |
| **ml/B — Bagging** | `ml/ensemble/bagging.py`, `tests/ml/ensemble/test_bagging.py` | XGB-AlphaEarth, N bootstraps + Optuna; promedio proba parcela. |
| **ml/C — Stacking** | `ml/ensemble/stacking.py`, `tests/ml/ensemble/test_stacking.py` | Base learners parcela -> meta-learner SOLO OOF con CV espacial (sub-folds fold-5). |
| **ml/D — Blending** | `ml/ensemble/blending.py`, `tests/ml/ensemble/test_blending.py` | Pesos simplex Optuna sobre holdout disjunto; minimiza gap F1. |

**Write-sets verificados disjuntos**: `voting.py`/`bagging.py`/`stacking.py`/`blending.py` y sus tests no
se solapan. Las 4 subtareas LEEN (no escriben) `base.py`, `parcel_reconcile.py`, `spatial_split.py`,
`baseline.py`, `oof/parquet_io.py` y los OOF parquet.

### Fase 4 — cierre (secuencial, 1 agente)

| Agente | Write-set | Tarea |
|--------|-----------|-------|
| **ml/cierre** | `ml/eval/ensemble_figures.py`, `tests/ml/ensemble/test_anti_leakage.py`, `scripts/run_us040_ensembles.py`, `Makefile` (atajo `ensembles-test`) | `dvc pull` OOF -> 4 ensambles -> tabla comparativa -> >=4 figuras -> 4 runs MLflow `:5010` con tag `chosen_model`. Punto serializado unico. |

**Contrato**: las firmas de la Seccion 4 del plan son el contrato. Fase 3 no arranca hasta que
`EnsembleModel` exista (Fase 1). ml/A-D heredan de `base.py`; entre si no se importan.

## Archivos a crear / modificar

**Crear**: `ml/ensemble/{base,voting,bagging,stacking,blending}.py`, `ml/eval/ensemble_figures.py`,
`tests/ml/ensemble/{__init__,test_base,test_voting,test_bagging,test_stacking,test_blending,test_anti_leakage}.py`,
`tests/ml/ensemble/fixtures/synthetic_oof.py`, `scripts/run_us040_ensembles.py`.

**Modificar**: `ml/ensemble/__init__.py` (exportes), `Makefile` (atajo `ensembles-test`, opcional).

**Reusar sin tocar**: `ml/utils/parcel_reconcile.py`, `ml/eval/oof/parquet_io.py`, `ml/eval/oof/*.parquet`
(DVC), `ml/features/spatial_split.py`, `ml/train/baseline.py`, `ml/utils/mlflow_utils.py`,
`ml/eval/{dense_metrics,metrics,avance4_figures}.py`.

## Reglas anti-fuga (NON-NEGOTIABLE)

1. **Promediar probabilidades, NO logits** — la entrada de US-031 ya es post-softmax (sum-to-1); el
   ensamble nunca consume ni expone logits.
2. **Meta-learner solo OOF con CV espacial** — features del meta en sub-fold k provienen solo de base
   learners predichos en folds != k; particion via `build_spatial_kfold`.
3. **Blending sobre holdout espacialmente disjunto** — pesos optimizados en un sub-fold geografico
   separado, objetivo minimiza gap F1 train-val.
4. **Reportar en fold-5 (test held-out), NO fold-4** — `EnsembleModel.HELD_OUT_FOLD=5`; `evaluate(fold!=5)`
   -> `ValueError`. `test_anti_leakage.py` consolida los 4 cortes.

## Metricas esperadas y umbrales

| Modelo | F1-macro fold-5 (umbral / esperado) |
|--------|--------------------------------------|
| Mejor individual (TSViT-pheno) | 0.6253 (referencia) |
| E1 Voting | `>= 0.6253` / 0.63-0.65 |
| E2 Bagging | `>= 0.55` / 0.55-0.60 |
| E3 Stacking | `>= 0.6253` / 0.63-0.66 (tipicamente el mejor) |
| E4 Blending | `>= 0.62` / 0.62-0.65 |

**Umbral de exito rubrica**: al menos un ensamble iguala o supera 0.6253 en fold-5.

## Estado MLOps / H100

- **NO requiere H100** (no toca la ventana de 80 h): los 4 ensambles reusan los OOF de US-031, no
  re-infieren densos. Voting = numpy CPU; Bagging/Stacking/Blending = XGB tabular + Optuna CPU-bound
  (GPU local opcional `<= 1 GB` VRAM por bag via `resolve_xgb_device()`).
- **MLflow** (`:5010`, experimento `ensemble`): 1 run por ensamble via `track_experiment("ensemble", ...)`
  con `data_version` + `code_version` + tag `chosen_model`. Cerrar runs RUNNING manualmente.
- **DVC**: OOF ya versionados (US-031). `dvc pull` si faltan; NO regenerar salvo corruptos.

## Riesgos principales

1. **R-LEAK (riesgo #1)**: meta viendo no-OOF / reporte en fold-4 / promediar logits. Mitigacion:
   `HELD_OUT_FOLD=5` fijo + meta solo-OOF con CV espacial + entrada post-softmax; `test_anti_leakage.py`.
2. **R-OOF-DEPTH**: un solo fold held-out limita el CV OOF a sub-folds del fold-5 (documentado).
3. **R-MARGIN**: pixeles mixtos de margen (pureza ~98%); reportar `n_pixels`, auditar parcelas pequenas.
4. **R-VOTE**: tercer votante = U-TAE/U-Net (TSViT base no dumpeado).
5. **R-TAB-FEATURES**: features AlphaEarth por parcela PASTIS pueden faltar; degradar a 2 base learners
   densos con caveat si XGB-AlphaEarth no es viable.

## Rubrica A5

Ensambles 60 (4 obligatorios anti-fuga) + Seleccion 20 (tabla comparativa + modelo elegido justificado) +
Graficos 20 (>=4 figuras interpretadas). Anti-fuga es pre-requisito transversal de los 3 componentes.

## Proximo paso

Fase 1: crear `EnsembleModel` (`ml/ensemble/base.py`) + fixtures + tests base, congelando el contrato de
firmas. Luego fan-out de los 4 agentes ml (A/B/C/D) sobre write-sets disjuntos. Cierre secuencial
(Fase 4): `dvc pull` OOF -> 4 ensambles -> tabla + >=4 figuras -> 4 runs MLflow `:5010` con `chosen_model`
-> handoff a `implemented` con metricas reales y modelo elegido.

## Criterio de aceptacion (resumen)

Modulo `ml/ensemble/` poblado con los 4 ensambles (Voting pixel / Bagging / Stacking / Blending parcela),
anti-fuga garantizada (probs no logits, meta solo OOF con CV espacial, blending holdout disjunto, reporte
fold-5 no fold-4), reconciliacion pixel<->parcela documentada reusando `pixel_to_parcel_probs`, tabla
comparativa (mejor individual 0.6253 vs 4 ensambles + tiempo inferencia + modelo elegido), >=4 figuras
interpretadas, 4 runs MLflow `:5010` con `data_version` + `code_version` + `chosen_model`, tests
`tests/ml/ensemble/` verdes (incl. anti-fuga) y `make check` limpio.
