# Handoff US-041 — Ensamble E-a: TSViT-pheno + FarSLIP (fusion dual-head)

**Estado**: coding (codigo + tests verdes; run final pendiente del FarSLIP-pheno definitivo)
**Epic**: E6 (Modelo Final — Ensambles) [CRISP-ML(Q): Evaluation]
**Avance**: A5 (ensamble)
**Sprint**: S7
**Rama**: feature/E6-US-041-ea-tsvit-pheno-farslip-fusion
**Ultima fase**: 3 — 2026-06-10
**Prioridad (Backlog v8)**: P10

## Snapshot Fase 3 (codigo)
- Commits: 34f79b8 (plan+handoff), 06be14b (code+tests).
- ml/ensemble/dual_head_fusion.py (DualHeadFusionHead + build_class_prototypes +
  farslip_cosine_map), scripts/run_us041_ea.py (orquestador), tests 14 verdes.
- Suite ensembles completa: 161 passed. ruff limpio.
- PENDIENTE: ejecutar scripts/run_us041_ea.py --redump-oof en la VM cuando exista el
  FarSLIP-pheno final (barrido 69k). Eso produce alpha + metricas fold-5 + MLflow run
  + oof_parcel_ea-fusion_fold5.parquet (input de US-042).

## Dominios y sub-tareas tocados
- [ ] backend  - [ ] frontend  - [x] ml  - [ ] agent  - [ ] infra  - [ ] db  - [ ] dagster
- Sub-tareas (mismo archivo dual_head_fusion.py -> SECUENCIAL, no paralelo real):
  - Fase 1 (bloqueante): re-dump OOF tsvit-pheno-fullm + esqueleto modulo
  - Fase 3 A: build_class_prototypes (visuales CLS-768) + broadcast cosine per-pixel
  - Fase 3 B: DualHeadFusionHead (alpha aprendible) + OOF spatial-CV (depende de A)

## Archivos a crear/modificar (del plan sec. 3)
- CREAR `ml/ensemble/dual_head_fusion.py` — DualHeadFusionHead(EnsembleModel) + build_class_prototypes
- CREAR `tests/ml/ensemble/test_dual_head_fusion.py` — band-indexing 4vs10, shapes, prototipos, anti-fuga
- MODIFICAR `ml/eval/oof/dump_oof.py` o invocar — re-dump OOF tsvit-pheno-fullm fold-5
- CREAR script orquestador run de E-a (MLflow ensemble-Ea-tsvit-pheno-farslip)

## Archivos existentes reutilizados (DRY — NO duplicar)
- `ml/ensemble/base.py::EnsembleModel` — validate_probs, spatial_subfolds, assert_oof_only,
  compute_metrics, reduce_pixel_to_parcel, log_to_mlflow, load_oof_members
- `ml/farslip/extract_embeddings.py::_load_student`, `adapt_patch_embed_to_n_channels`
- `ml/farslip/distill.py::_proto_to_clip_proj` (384->768, frozen ortogonal)
- `ml/farslip/parcel_crop_dataset.py::ParcelCropDataset`
- `ml/eval/.../softmax_patch_for_kind` (TSViT-pheno dense softmax)
- `ml/eval/oof/dump_oof.py::dump_oof`, `ml/features/spatial_split.py::build_spatial_kfold`
- `ml/utils/mlflow_utils::track_experiment`

## Decisiones tecnicas clave (CAVEATS verificados en recon)
- R-DIM-768: embedding FarSLIP es 768 (CLS), NO 512. cosine en 768. reproyeccion 384->768.
- R-PROTO-VISUAL: build_class_prototypes promedia CLS-768 de parcelas TRAIN por clase (visuales),
  distinto del banco de TEXTO MiniLM del checkpoint.
- R-OOF-FULLM: re-dumpear OOF tsvit a Full-M (manifest solo tiene L4 tsvit-pheno-v1).
- R-OOF-DEPTH: alpha entrenado via spatial_subfolds de fold-5 (solo fold-5 dumpeado).
- R-HONEST-GAIN: ganancia ~0.3% (US-037 0.555<0.645; US-039 delta -0.0033 ruido); alpha ~0.85-0.95.
- ml/CLAUDE.md ESTA STALE: ensembles SI implementados; subclasear EnsembleModel.

## Zonas sensibles
- `ml/farslip/distill.py:594` _proto_to_clip_proj — no romper firma (hidden_size>=D_in).
- `ml/eval/checkpoint_registry.py` tsvit-pheno-fullm (n_timesteps=37) — no editar.
- EnsembleModel.HELD_OUT_FOLD=5 — reportar SOLO fold-5 (fold-4 fue seleccion).

## Modelos / Datos / H100
- MLflow run id: [pendiente Fase 3] (lineage server :5010, NO ./mlruns)
- run name: ensemble-Ea-tsvit-pheno-farslip
- VRAM: FarSLIP student (~ViT-B/16) + TSViT-pheno inference; validar nvidia-smi antes
- Checkpoints/OOF: ruta relativa (cae en F:)

## engram-memory
- Observaciones guardadas: si (plan nocturno autonomo, keywords US-041 E-a dual-head)

## Plan completo
- docs/us-planning/us-041.md (505 lineas, 11 secciones)
