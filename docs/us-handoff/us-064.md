# US-064 — Handoff (COMPLETO: seguridad + 4 Model Cards + glosario)

**Status**: documentacion de cierre del MVP COMPLETA — `docs/security.md` (OWASP Top 10 a
controles reales), `docs/model_cards/` (indice + 4 cards, SIN Gemma 4) y `docs/glosario.md`
(IT/ES/EN) creados; toda metrica trazada a artefacto real del repo.
**QA (2026-06-20)**: ruff limpio sobre `backend/ ml/ dagster_project/` ("All checks passed!"),
mypy package-mode "Success: no issues found in 26 source files", suite chat+metrics 52 passed,
suite drift 11 passed, `dagster_project.definitions` importa limpio. US documental: no toca
codigo de app, pero los fixes de E10 que la rodean estan verificados (ver Lineage/QA).
**Epic**: E10 (Observabilidad y documentacion) · **Rama**: `feature/E10-observabilidad-docs`
(parte de `feature/E12-transfer-multiregion`)
**Plan**: `docs/us-planning/us-064.md` · **v8**: §US-064 (linea 2075), §EPIC 10 (subtotal 13 SP)

## Que entrega

Cierre documental del MVP en su vertiente de seguridad y trazabilidad de modelos, de cara a la
Presentacion Final del 27-jun. Tres entregables que no existian en el repo, mas el registro de
claims no verificables de la EPIC:

1. **`docs/security.md`** — revision OWASP Top 10 (2021) mapeada a los controles **reales** del
   codigo, con estado por fila (Implementado / Full-post-presentacion / Deuda anotada) y evidencia
   (ruta de archivo o US).
2. **`docs/model_cards/`** — indice + 4 Model Cards estilo HuggingFace de los modelos realmente
   entrenados o servidos. **Sin card de Gemma 4 fine-tuned** (Gemma LoRA OUT, ADR-009/ADR-011
   future); en su lugar la card de serving de Qwen.
3. **`docs/glosario.md`** — glosario tecnico trilingue IT/ES/EN agrupado por dominio.
4. **`docs/blockers/epic10-notas.md`** — registro de claims no verificables de la EPIC 10
   (pen-test no ejecutado contra endpoint live, `docs/STATUS.md` ausente, tokens Qwen sin
   `include_usage`, etc.).

## Estado real (DONE)

DONE (documentacion, 20-jun, esta sesion):

- **OWASP Top 10 a controles reales** en `docs/security.md`: una fila por categoria A01..A10 con
  estado y ruta. Anclas reales documentadas:
  - A01: aislamiento multi-tenant por `session_id`; RLS PostgreSQL = **Deuda anotada** (pendiente
    US-051, `aois`/`parcels`/`features_parcels`/`chat_sessions` sin policy).
  - A03: SQLModel/SQLAlchemy parametrizado + saneado XSS del markdown del LLM con
    `isomorphic-dompurify` (US-057).
  - A05: CORS endurecido (`allow_headers` whitelist, no `*`) + `Settings.extra="forbid"` +
    handlers TiTiler tipados, en `backend/app/main.py` y `core/config.py`.
  - A07: `user_id` demo hardcoded para el 27-jun; Clerk OAuth2 + JWT rotacion/refresh + HTTPS via
    Cloud Load Balancer = **Full / post-presentacion** (tabla MVP vs Full).
  - A08: DVC + tags MLflow `data_version`/`code_version` + migraciones rollforward dbmate.
  - A09: structlog (US-065) + `/metrics` Prometheus (US-059); scrape/alertas reales = Deuda anotada.
  - A10 SSRF: `validate_cog_url()` con allowlist (gs:// del bucket, hosts de
    `settings.tile_url_allowed_hosts`) antes del fetch de GDAL — `backend/app/services/cog_tiler.py`
    (US-055).
- **Correccion de la referencia muerta del plan v6**: el secrets-scan vive en `make secrets-scan`
  (gitleaks) + CI; el proyecto **NO usa `.pre-commit-config.yaml` ni un hook `detect-secrets`**
  (regla irrevocable). Documentado en §3 de `docs/security.md`.
- **Procedimiento de pen-test manual** (nmap / nikto + verificacion de aislamiento por sesion)
  documentado como check humano pre-deploy; no ejecutado contra endpoint live esta sesion (sin
  staging activo) — anotado en el blocker doc.
- **4 Model Cards** (plantilla HuggingFace de 8 secciones, metricas solo desde artefactos reales):
  - `ensemble-final-e6.md` — Stacking/Blending sobre miembros base; campeon E3 Stacking parcela
    f1_macro 0.7470 (`comparison_us040.csv`), Stacking-5 +FarSLIP 0.6477 f1_macro
    (`us043_farslip_stacking_blending.csv`). "Stacking +Gemma 4" documentado como **diseno**, no run.
  - `tsvit-pheno.md` — mejor segmentador del banco fold-5 (mIoU 0.6139 / F1 0.7401,
    `model_comparison_fold5.csv`); delta fenologico supervisado ~0 / leve negativo (-0.0033),
    lectura honesta.
  - `farslip-pheno.md` — espacio fiel vs AlphaEarth (no supera, se reporta tal cual), barrido de
    cardinalidad, y generalizacion transfer FR->Catalonia (Delta mIoU **+0.2468** few-shot,
    `sen4agrinet_transfer_result.json`); atribucion Sen4AgriNet CC-BY-SA-4.0.
  - `qwen35-vllm-serving.md` — **reemplaza la card de Gemma 4**: serving on-prem
    `Qwen/Qwen3-30B-A3B-Instruct-2507-GPTQ-Int4` (Apache 2.0), llama.cpp OPERATIVA / vLLM BLOQUEADA
    en la VM Windows del sponsor; honestidad de tokens (sin `include_usage` -> `None`, no se
    inventa).
- **Glosario IT/ES/EN** por dominio (EO/datasets, etiquetas, arquitecturas, MLOps, agente/LLM,
  agronomia). Usa **AlphaEarth V1/ANNUAL v1.1, CC-BY-4.0** (nunca el termino muerto "v2.1");
  documenta el gotcha MLflow dos almacenes y la convencion codigo-EN / prosa-ES.

Fuera de alcance de este doc (coordinado, no duplicado): Cloud Load Balancer + JWT refresh + Clerk
+ HTTPS managed = **Full post-presentacion**; RLS por tenant = US-051; Data Sheets + correccion
"AlphaEarth v2.1" en `DATA_LICENSE.md` = US-066; README reproducible = entregable separado.

## Archivos

Creados (documentacion pura, sin codigo de app, sin migraciones, sin deps):

- `docs/security.md` — OWASP Top 10, gestion de secretos, auth MVP vs Full, pen-test manual.
- `docs/model_cards/README.md` — indice de las 4 cards + plantilla + gotcha MLflow.
- `docs/model_cards/ensemble-final-e6.md`
- `docs/model_cards/tsvit-pheno.md`
- `docs/model_cards/farslip-pheno.md`
- `docs/model_cards/qwen35-vllm-serving.md`
- `docs/glosario.md` — glosario tecnico IT/ES/EN.
- `docs/blockers/epic10-notas.md` — registro de claims no verificables de la EPIC 10.

Artefactos reales referenciados (verificados en disco, no modificados):
`reports/ensemble/us043_farslip_summary.json`,
`reports/ensemble/metrics/{us043_farslip_stacking_blending,us043_farslip_grid,comparison_us040}.csv`,
`reports/farslip/metrics/{us037_farslip_fiel_vs_alphaearth,parcel_sweep,faithful_v2_summary}.csv`,
`reports/segmentation/metrics/{model_comparison_fold5,tsvit_pheno_vs_base_fold5}.csv`,
`reports/segmentation/sen4agrinet_transfer_result.json`.

## Lineage / QA

Hechos verificados por el orquestador (re-corridos a mano, no solo reportados):

- **ruff** sobre `backend/ ml/ dagster_project/` (completo): "All checks passed!".
- **mypy** package-mode (`backend.app.middleware`/`api`/`utils`/`services`): "Success: no issues
  found in 26 source files".
- **Suite chat+metrics: 52 passed** (`test_metrics_middleware` + `test_chat_metrics` +
  `test_chat_sse` + `integration/test_chat_endpoint` + `integration/test_chat_uses_session_model`).
- **Suite drift: 11 passed** (`tests/ml/monitoring` + `tests/dagster/test_drift_asset.py`),
  warnings benignos de scipy.
- **`dagster_project.definitions`** importa limpio: `schedules=['drift_check_weekly_schedule']`,
  `jobs=['farslip_full_pipeline_job','drift_check_job']`.
- **Fix orquestador US-059**: `DEFAULT_REGISTRY` dedicado + `get_default_metrics()` singleton ->
  re-instanciar la app ya NO lanza "Duplicated timeseries" (rompia 13 tests de integracion de chat).
- **Fix de harness US-065**: `_FakeConn`/`_FakePool` en `integration/test_chat_endpoint.py` (antes
  `object()` rompia `_resolve_variant.pool.acquire()`; fallo PRE-EXISTENTE de US-054, no regresion
  de E10).
- Toda metrica de las Model Cards proviene de un artefacto real del repo (rutas exactas en cada
  card); claims no verificables anotados en `docs/blockers/epic10-notas.md`, nunca placeholders.
- Datasets reales versionados: `data/sen4agrinet.dvc`, `data/transfer/eurocropsml.dvc`.
- Cada US se commiteo por separado, autor Arthur Zizumbo, SIN trailer `Co-Authored-By`. Tree limpio.

## Notas / blockers

- **Gemma 4 fine-tuned NO tiene Model Card** — es decision, no omision: Gemma LoRA esta OUT
  (ADR-009 alcance v8, ADR-011 future). La card de Qwen serving ocupa su lugar y enlaza ADR-011.
- **JWT refresh + Clerk OAuth2 + HTTPS via Cloud Load Balancer = Full post-presentacion**: el
  middleware de auth aun no existe (`backend/app/middleware/` solo `__init__.py` + `metrics.py`);
  documentado en `docs/security.md` §4, no se reclama como implementado.
- **RLS por tenant = Deuda anotada (US-051)**: a la fecha sin policy en las tablas clave; el
  aislamiento vivo es por `session_id` en cada query.
- **Pen-test no ejecutado contra endpoint live** (sin staging activo esta sesion): el procedimiento
  nmap/nikto queda listo como check manual pre-deploy; anotado en el blocker doc.
- **`docs/STATUS.md`** referenciado por el plan v8 no existe en `docs/` (solo
  `docs/audit/STATUS_us-025.md`); discrepancia anotada en el blocker doc, no bloquea.
- **Tokens de Qwen** no disponibles en streaming sin `stream_options={"include_usage": True}` ->
  `chat_turn_metrics.tokens_*` queda `None` (no se inventa); Gemini si los entrega. Blocker B6.
