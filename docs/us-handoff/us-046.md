# Handoff US-046 — Perceiver-reasoner + Spatial-RAG lite

**Estado:** done (24 tests verdes + 8 skip pgvector/Docker; ingest validado con datos reales)
**Rama:** `feature/E7-US-046-perceiver-rag-lite`
**Plan:** [docs/us-planning/us-046.md](../us-planning/us-046.md)

## Decisiones tecnicas (fijadas)

- Perceiver = wrapper que reusa el clasificador xgb-alphaearth (US-045 `classify`) + descriptor fenologico (`phenology_description`) -> `PerceiverObservation` (texto). Patron Be My Eyes: el reasoner razona sobre TEXTO, no logits.
- RAG-lite: corpus texto REAL `data/farslip/parcel_phenology_captions.parquet` (col `description`); vector AlphaEarth 64-dim REAL `data/features/features_fused_winning_pastis.parquet` (cols `dim_00..dim_63`). Sin e5-mistral, sin GPU.
- Tabla nueva `rag_documents` (geom + `embedding VECTOR(64)` + content TEXT + parcel_id TEXT). GIST(geom), sin HNSW (HNSW es del RAG completo FUTURE).
- Pipeline `ml/agent/rag.py`: `ST_DWithin` -> pgvector cosine(64) -> fusion ponderada.
- Tool `retrieve_context` DEFERRED tras flag `rag_enabled` (default off en Settings).
- Evento SSE `perceiver_observation` en `/chat` (router minimo).
- Contrato US-045 reutilizado: `ToolContext`, `session_scoped_conn`, registro de tools.

## Archivos (write-sets disjuntos)

| Sub-tarea | Archivos |
|-----------|----------|
| db | `db/migrations/<ts>_create_rag_documents.sql` |
| ml/A perceiver | `ml/agent/perceiver.py` |
| ml/B rag+tool+ingest | `ml/agent/rag.py`, `ml/agent/tools/retrieve_context.py`, `scripts/ingest_rag_documents.py`, `ml/agent/tools/__init__.py` (anade entry retrieve_context) |
| backend | `backend/app/services/chat_service.py`, `backend/app/api/chat.py`, `backend/app/core/config.py` (flag rag_enabled) |
| TESTS | `tests/ml/agent/test_perceiver.py`, `test_rag.py`, `test_retrieve_context.py`, `tests/db/test_migrations_rag.py`, backend chat sse test |

## Snapshot de archivos tocados

- `db/migrations/20260615082041_create_rag_documents.sql` — tabla `rag_documents` (parcel_id TEXT, geom GEOMETRY(GEOMETRY,4326), content, source, embedding VECTOR(64)), GIST(geom), sin HNSW (FUTURE).
- `ml/agent/perceiver.py` — `PerceiverObservation` (+ `to_prompt_block()`) y `PerceiverLayer.observe/observe_aoi`. Reusa classify (`_load_classifier`, `_fetch_parcel_embedding`, `run`) + explain (`run`). Be My Eyes: emite TEXTO, el LLM no corre.
- `ml/agent/rag.py` — `spatial_rag` (ST_DWithin -> pgvector cosine 64-dim -> fusion ponderada), `ingest_rag_documents`, `RAGDocument`. Validado contra PostGIS real.
- `ml/agent/tools/retrieve_context.py` — tool DEFERRED tras flag `rag_enabled` (default off, degradacion elegante sin tocar DB). Registrada como 5a deferred.
- `scripts/ingest_rag_documents.py` — ingest REAL: corpus `parcel_phenology_captions.parquet` (description) + embedding `features_fused_winning_pastis.parquet` (dim_00..63) + geom por-patch `data/PASTIS-R/metadata.geojson` (centroide). Smoke OK: 20 docs, 0 skipped, embedding 64-dim real.
- `backend/app/services/chat_service.py` + `backend/app/api/chat.py` + `main.py` (include_router) — `/chat` SSE MVP que emite `perceiver_observation` antes de `done`. Reasoner Gemini real es US-047 (enganche documentado).
- `backend/app/core/config.py` — flag `rag_enabled: bool = False`.

## Contrato publico (para US-047)

- `from ml.agent.perceiver import PerceiverLayer, PerceiverObservation` (`observe(parcel_id)`, `observe_aoi(aoi, year)`, `to_prompt_block()`).
- `from ml.agent.rag import spatial_rag, ingest_rag_documents, RAGDocument`.
- `retrieve_context` en TOOL_REGISTRY como deferred; respeta `ctx.settings.rag_enabled`.
- `ChatService.stream(messages, session_id)` async gen emite eventos: `perceiver_observation`, `text_delta`, `done`, `error`.

## Bugs / bloqueos

- Tests de migracion `rag_documents` hacen auto-skip sin imagen pgvector (mismo patron que US-015); la logica espeja el test probado. Para correrlos de verdad: imagen Postgres con pgvector.
- `rag.py` corrigio el caso `parcel_id:int` (ints no son Iterable) -> devuelve None correctamente.
