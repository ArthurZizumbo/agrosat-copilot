# Handoff US-073 -- Seccion de paper: transferencia multi-region

**Status**: ready-to-close (2026-06-20 -- seccion redactada, tablas/figuras generadas
desde artefactos REALES E12, tests + lint verdes; bloqueos no-autonomos registrados)
**Epic**: E11 (Paper Track) -- POST-PRESENTACION, OPCIONAL
**Avance**: N/A (no compromete ningun Avance del curso)
**Sprint**: buffer 28-jun -> 3-jul (diferible)
**Rama**: `feature/E11-paper-track-docs`
**SP**: 3 (la US mas ambiciosa de E11)
**Owner**: Arthur Zizumbo (tech lead / paper-writer)
**Naturaleza**: REDACCION + figuras reproducibles. NO se re-entreno nada: toda la
evidencia (Delta mIoU FR->Catalonia, curva k-shot EuroCropsML, crosswalk HCAT, demo
Mexico) ya fue producida por E12 (US-074..077) con datos reales y versionada en DVC.

---

## Dominios tocados

- [ ] backend
- [ ] frontend
- [x] **ml** (`scripts/build_us073_transfer_figures.py` reusa `ml.transfer.eurocropsml_fewshot`)
- [ ] agent
- [ ] infra
- [ ] db
- [ ] dagster
- [x] **paper** (`paper/sections/`, `paper/tables/us-073-transfer/`, `paper/figures/us-073-transfer/`)

---

## Archivos creados

- `paper/sections/experiments_multiregion.tex` -- seccion `\input`-eable (prosa EN, 6
  subsecciones + caveat). Recipe train-FR -> extend-elsewhere: (1) framing/motivacion,
  (2) label-space HCAT v3 (11 macro-clases), (3) camino denso Sen4AgriNet Catalonia
  (tabla domain-gap, texto honesto del zero-shot=0.0000), (4) few-shot EuroCropsML (tabla
  + figura k-shot, correccion LV[+PT]->EE), (5) demo Mexico cualitativa (sin F1),
  (6) FUTURE WorldCereal/HGC, (7) caveat arXiv:2601.00857. SIN `\documentclass`.
- `scripts/build_us073_transfer_figures.py` -- builder reproducible (Typer CLI +
  `build_all` importable). Lee los 3 artefactos reales -> 2 tablas `.tex` (`booktabs`) +
  2 figuras (`.png`+`.svg`). Determinista (seed fijo, Agg), idempotente, `structlog`,
  type hints, sin emojis. Reusa `summarize_curve` (DRY).
- `paper/tables/us-073-transfer/sen4agrinet_domain_gap.tex` -- GENERADA. zero 0.0000 ->
  few 0.2468 / F1 0.3005 / pixel-acc 0.9179 / Delta +0.2468 (exacto del JSON).
- `paper/tables/us-073-transfer/eurocropsml_kshot.tex` -- GENERADA. scenario x k ->
  F1-macro mean +/- std (3 seeds, 8 clases), del parquet.
- `paper/figures/us-073-transfer/kshot_curve.{png,svg}` -- GENERADA. curva k vs F1-macro,
  3 escenarios, error bars 3 seeds, eje x log.
- `paper/figures/us-073-transfer/mexico_phenology.{png,svg}` -- GENERADA. NDVI aguacate
  (Uruapan) + guayaba (Calvillo), cualitativo, banner "NO se reporta F1/accuracy".
- `tests/scripts/test_build_us073_transfer_figures.py` -- 6 tests (skip si faltan
  artefactos; nunca corren sobre datos fabricados).
- `docs/us-handoff/us-073.md` -- este handoff.

## Archivos modificados

- `Makefile` -- target `us073-transfer-figures` + entrada en `.PHONY`.
- `docs/blockers/epic11-notas.md` -- seccion US-073 (B-073-1..6) anexada.

## Archivos verificados (sin cambios necesarios)

- `docs/licenses/DATA_LICENSE.md` -- ya tenia Sen4AgriNet CC-BY-SA-4.0, EuroCropsML
  CC-BY-SA-4.0, AlphaEarth V1 CC-BY-4.0 + tabla por-region (US-066). NO se duplico.

---

## Como reproducir

```bash
make us073-transfer-figures
# o: poetry run python -m scripts.build_us073_transfer_figures
poetry run pytest tests/scripts/test_build_us073_transfer_figures.py -q   # 6 verdes
poetry run ruff check scripts/build_us073_transfer_figures.py            # clean
poetry run mypy scripts/build_us073_transfer_figures.py                  # clean
```

---

## Correcciones factuales aplicadas (NON-NEGOTIABLE)

- AlphaEarth = `SATELLITE_EMBEDDING/V1/ANNUAL` v1.1, 64-dim, CC-BY-4.0. NUNCA "v2.1"
  (test `test_no_alphaearth_v21_string`).
- EuroCropsML: protocolo real LV+PT->EE / LV->EE (Reuter et al. 2025 Tabla II), NO
  "Francia->Estonia"; Francia NO esta en EuroCropsML; el vector es la serie S2 propia, no
  AlphaEarth (test `test_eurocropsml_scenario_is_real`).
- Gemini 2.5-pro GA (1M ctx, NO 2M) = reasoner FROZEN (Be My Eyes, arXiv:2511.19417);
  Qwen3.5-35B-A3B vLLM GPTQ-Int4 single-GPU = variante on-prem (soberania).
- Sen4AgriNet CC-BY-SA-4.0, EuroCropsML CC-BY-SA-4.0; nota share-alike.

## Claims defendibles (sin sobre-afirmar)

- El Delta mIoU (zero 0.0000 -> few 0.2468) ES el entregable, no un accuracy alto.
- NO "zero-shot funciona fuera de Francia" (el zero-shot dio 0.0000, lo prueba).
- NO "F1>=0.80 Mexico" (demo metodologico cualitativo zero-shot, sin ground-truth).
- NO "VLM fine-tuned supera a Gemini" (la capa LLM = comunicacion + soberania).
- Caveat citado: arXiv:2601.00857 "Harvesting AlphaEarth" (transferibilidad espacial
  limitada -> presupuestar few-shot, no prometer zero-shot magico).

---

## Definicion de hecho (checklist)

- [x] `experiments_multiregion.tex` redactada (EN), recipe completo, 6 subsecciones + caveat.
- [x] `build_us073_transfer_figures.py` genera 2 tablas + 2 figuras desde artefactos reales.
- [x] Target `make us073-transfer-figures` funcional.
- [x] Tabla Sen4AgriNet domain-gap con Delta mIoU +0.2468 exacto del JSON.
- [x] Tabla + figura curva k-shot EuroCropsML (LV+PT->EE, LV->EE, sin-pretrain; mean+/-std).
- [x] Crosswalk PASTIS-18->HCAT v3 (11 macro-clases) referenciado correctamente.
- [x] Demo Mexico cualitativa sin F1; WorldCereal/HGC marcado FUTURE.
- [x] DATA_LICENSE.md con las 3 atribuciones (verificado; ya existian via US-066).
- [x] Correcciones factuales aplicadas (sin "v2.1"; LV/PT->EE no Francia->Estonia).
- [x] 6 tests verdes (solo archivos de esta US); `ruff` + `mypy` limpios.
- [x] Items no-autonomos (UMAP ES, NDVI desfasado FR/ES, revision EN, integracion main.tex)
      en `docs/blockers/epic11-notas.md` (B-073-1..6).
- [x] `structlog`, type hints, sin secrets, sin emojis; commit sin trailer `Co-Authored-By`.

## Pendiente (no-autonomo, ver epic11-notas.md B-073-*)

- B-073-1: panel UMAP ES del domain-gap (requiere embedding AlphaEarth de los parches ES,
  GEE auth). La tabla densa ya da la evidencia cuantitativa; el UMAP es ilustrativo.
- B-073-2: curvas NDVI desfasadas FR vs ES (requiere series S2 ES materializadas).
- B-073-3: re-run finetune denso (NO requerido; el JSON es la fuente de verdad).
- B-073-4: revision humana del texto EN (US-071, Grammarly + Dr. Camacho).
- B-073-5: WorldCereal/HGC tropical (explicitamente FUTURE).

## Integracion en el manuscrito (RESUELTA, B-073-6)

US-071 ya hizo `\input{sections/experiments_multiregion}` en `paper/main.tex`. Verificado:
`make paper-cite-check` verde (mis 9 keys resuelven) y `make paper-pdf` compila
`paper/main.pdf` (15 pag, 0 refs/citas indefinidas); mi seccion = Section 7 (7.1-7.6),
Tablas 8 y 9, Figura 1, todas renderizadas. Las citas en prosa se convirtieron a `\cite{}`
contra `paper/bib/refs.bib`. No queda nada pendiente de integracion.
