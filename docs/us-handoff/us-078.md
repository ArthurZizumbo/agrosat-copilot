# Handoff US-078 — Generar el dataset homologo a PASTIS de Italia 2018

**Estado**: coding
**Epic**: E12 (Transfer learning / homologo mediterraneo) [CRISP-ML(Q): Data Preparation]
**Avance**: A7 (27-jun-2026)
**Sprint**: S7
**Rama**: feature/E12-US-078-italia-pastis-homologo
**SHA base**: 5b610ae
**Ultima fase**: 3 — 2026-06-25 (programacion / fan-out)
**Prioridad (Backlog v8)**: P1 (precede US-079)

## Dominios y sub-tareas tocados
- [ ] backend  - [ ] frontend  - [x] ml  - [ ] agent (ml/agent/)  - [ ] infra  - [ ] db  - [ ] dagster
- Sub-tareas paralelas: NO. Una sola sub-tarea ML (builder + runner + EDA), write-set
  disjunto del de US-079. El builder es upstream de todo lo demas.

## Decision de scope (ratificada por Arthur, 25-jun-2026)
- **Piloto de 20 patches REALES ahora** (Sentinel Hub, ~600 peticiones, ~6% de la
  cuota ~10,800). NO smoke; descarga genuina + EDA con outputs.
- **GATE EXPLICITO**: al terminar los 20 patches, REPORTAR a Arthur (cobertura media,
  clases presentes, % nubes residual, cuota consumida) ANTES de escalar al dataset
  completo. NO correr el dataset full sin su confirmacion.
- Entorno: laptop local (RTX 4070 8GB). La descarga es CPU/red, NO usa GPU. La H100
  remota es solo para US-079.

## Archivos tocados (write-set del agente ML — disjunto) — REALES
- `ml/data/eurocrops_pastis_builder.py` — NUEVO. Orquesta pasos 1-5 del plan
  (load_labeled_polygons, select_dense_patches, download_patch_series,
  rasterize_patch_mask, save_pastis_format + metadata/folds/resume helpers).
- `scripts/build_italia_pastis.py` — NUEVO. Runner CLI (--n-patches, --season,
  --min-support, --n-frames, --max-cloud, --out). Resume por patch, MLflow.
- `notebooks/transfer/us078_italia_pastis_eda.ipynb` — NUEVO. EDA del homologo
  (paso 6), ejecutado con papermill: 4 figuras PNG + 13 tablas/prints, 0 errores.
- `scripts/build_us078_eda_notebook.py` — NUEVO. Builder idempotente del notebook.
- `pyproject.toml` — MODIFICADO (1 linea aditiva): per-file-ignore E501 para
  `scripts/build_us078_eda_notebook.py`, igual que los otros 12 builders de
  notebook (f-strings de celda largas). Cambio no destructivo de config compartida.
- (datos generados) `data/pastis_italia_2018/` — NUEVO, versionado con DVC.
  `.dvc` md5 `195795287a61a3c6f1a7b2d1cefabd94.dir` (64 archivos, 167 MB).
  Layout PASTIS: `DATA_S2/S2_<id>.npy (T,10,128,128) int16` +
  `ANNOTATIONS/TARGET_<id>.npy (128,128) int32` + `ANNOTATIONS/dates_<id>.npy (T,) int32` +
  `metadata.parquet` + `class_table.parquet` + `class_mapping.json` + `pilot_summary.json`.

## Archivos existentes reutilizados (NO duplicar, NO reescribir)
- `ml/ingest/sh_client.py` — `SentinelHubClient.crop()`, `PASTIS_BANDS`, retry 429,
  `post_process`. Reusar tal cual.
- `ml/ingest/sh_path.py` — `download_parcels_by_tile`, `_download_tile`
  (retorna `(stack (T,10,H,W), rasterio_ds)` -> usar el `ds.transform` para rasterizar
  la mascara con el MISMO transform/grid). `_cluster_centroids` para zonas densas.
- `ml/ingest/cdse_client.py` — `CDSEClient.search_s2` para fechas <20% nubes.
- `ml/data/hcat_crosswalk.py` — `load_crosswalk`/`build_crosswalk` (codigo->HCAT->id).
  Reusar; extender solo si falta el filtro `nuts` de Italia.
- `data/reference/eurocrops_v2/iti1_2018.parquet` (643k parcelas), `eurocrops_mapping.csv`,
  `hcat4.csv` — entradas. Ya descargados + .dvc.
- `data/PASTIS-R/` — referencia de formato: `S2_<id>.npy (T,10,128,128)` +
  `ParcelIDs_<id>.npy (128,128) int32` + `metadata.geojson`. El homologo es ESPEJO.

## Decisiones tecnicas clave
- Layout de salida ESPEJO de PASTIS: `S2_<id>.npy (T,10,128,128)` +
  `TARGET_<id>.npy (128,128) int32` + `dates_<id>.npy (DOY)` + `metadata.parquet`.
- 10 m/px nativo S2 (= PASTIS); pedir mas fino solo upsamplea (verificado, std identico).
- Mascara: rasterizar con `rasterio.features.rasterize` usando el transform del tile SH
  (mismo grid que la imagen) — NO un transform from_bounds independiente, para garantizar
  alineacion pixel-perfect imagen<->mascara. Geometrias son MultiPolygon (usar .geoms).
- id 0 = fondo/no-cultivo. Clases <MIN_SUPPORT parcelas -> "other" (no inflar label-space).
- Fold espacial en metadata (para CV sin fuga en US-079).

## Bugs resueltos
| Bug | Causa | Solucion | Estado |
|-----|-------|----------|--------|
| Celda densa espuria con bbox `(inf,inf,inf,inf)` | EuroCrops Italia trae 908 geometrias vacias; su centroide es NaN y todas caen en una misma celda fantasma que rankeaba #1 | Dropear geometrias null/empty en `load_labeled_polygons` antes del grid (lossless: no tienen area rasterizable) | resuelto |
| Cobertura media 0.55 < 0.70 (AC3) con seleccion por conteo | Rankear celdas por numero de centroides premia celdas con muchas parcelas diminutas (13% de area cubierta); el conteo no es la cobertura | Rankear por area de parcela (`area_ha` sumada por celda) con piso de `MIN_PARCELS_PER_PATCH=30` para conservar mosaicos multi-clase; cobertura subio a 0.812, 17/20 patches >0.70 | resuelto |

## Zonas sensibles
- `ml/ingest/sh_client.py`, `ml/ingest/sh_path.py` — reutilizados tal cual
  (no se tocaron). `download_patch_series` llama `_download_tile` y recorta el
  centro 128px del tile (sobre-dimensionado con margen) usando el transform hijo
  `ds.transform * Affine.translation(c0, r0)` -> mascara pixel-perfect.
- `ml/data/hcat_crosswalk.py` — NO se toco. El filtro `nuts it` se resuelve en
  el builder (`_load_italy_code_to_hcat` lee `eurocrops_mapping.csv` directo),
  no via `hcat_crosswalk.py` (ese modulo es del label-space PASTIS-18, no IT).
- Cuota SH (AC5): el `sh_path.py` ACTUAL no tiene cache de tile en disco (el
  `_sh_patch_cache` del plan no esta en el codigo vigente). La proteccion de
  cuota es el RESUME por patch del runner (`patch_artifacts_exist` salta patches
  ya escritos) -> re-correr cuesta 0 peticiones nuevas. La cuota consumida se
  reporta (20 peticiones, 1/patch).
- `get_settings()` lee `.env.local` relativo al cwd: el runner DEBE correr desde
  la raiz del repo (`python -m scripts.build_italia_pastis`), no desde `ml/`.

## Modelos / Datos / H100
- MLflow run id: `aac7cc72d61f42d5bc877a4f4e352ad0` (experiment
  `us078_italia_pastis_homologo`, status FINISHED). Tags `code_version`
  `45ef707f9137c582ccb8e718058b33814a8c61e6` + `data_version`
  `dvc:195795287a61a3c6f1a7b2d1cefabd94.dir`.
  NOTA: el server MLflow Docker `:5010` estaba caido (Docker Desktop no
  arranco en la laptop), asi que `track_experiment` degrado honestamente a
  `file:./mlruns` (con los tags obligatorios). Al reactivar `:5010` se puede
  re-loguear el run; el lineage actual vive en `file:./mlruns`.
- DVC version: `.dvc` md5 `195795287a61a3c6f1a7b2d1cefabd94.dir`
  (`data/pastis_italia_2018.dvc`, 64 archivos, 167 MB). Pendiente `dvc push`
  (requiere ADC GCS; el `.dvc` queda listo para commit).
- VRAM validada: N/A (US-078 no usa GPU; descarga CPU/red en laptop local).
- nvidia-smi revisado antes de training: N/A

## Llave de provenance (cierre)
- `US-078 @ 45ef707 + mlflow:aac7cc72d61f42d5bc877a4f4e352ad0 + dvc:195795287a61a3c6f1a7b2d1cefabd94.dir`

## engram-memory
- Observaciones guardadas: seleccion de patches por AREA (no conteo) con piso
  de 30 parcelas; empty-geom drop EuroCrops; SH client necesita cwd=raiz para
  leer `.env.local`; MLflow degrada a file store con Docker caido.

## Criterios de aceptacion (del plan) — estado
1. [OK] 20 patches piloto en formato PASTIS exacto, versionados en DVC.
2. [OK] EDA ejecutado (papermill, 4 figuras + tablas): distribucion clases +
   visuales RGB+mascara + std NDVI 0.164 vs PASTIS 0.181 (misma textura).
3. [OK] Cobertura media 0.812 > 0.70 (17/20 patches individuales >0.70; los 3
   bajos son olivar/vinedo de copa rala, reportado honestamente).
4. [OK] Mapeo original_code->HCAT 100% (0 sin mapear); 138 clases de cola larga
   agrupadas en "other" explicito (umbral min_support=500); documentado en
   `class_mapping.json` + `class_table.parquet`.
5. [OK] 20 peticiones SH consumidas (1 por patch); resume por patch evita
   re-descarga; ~41 peticiones totales en la sesion (incl. 1 pre-flight) = ~0.4%
   de la cuota ~10,800.
6. [OK] ruff limpio en los 3 .py; type hints + docstrings Google + structlog.

## REPORTE GATE 20 PATCHES (cifras reales — decision de escalado de Arthur)

Run MLflow `aac7cc72d61f42d5bc877a4f4e352ad0` | dataset DVC
`195795287a61a3c6f1a7b2d1cefabd94.dir` | `data/pastis_italia_2018/pilot_summary.json`.

| Metrica | Valor | Referencia / objetivo |
|---------|-------|----------------------|
| Patches generados | 20 / 20 | >= 20 (AC1) |
| Cobertura media pixeles-con-clase | **0.812** (min 0.546, max 0.984) | > 0.70 (AC3) — CUMPLE |
| Patches con cobertura > 0.70 | **17 / 20** | — (3 bajos = olivar/vinedo) |
| n clases presentes (todo el piloto) | **23** | — |
| Clases compartidas con PASTIS presentes | olive*, vineyards/grapes, common_soft_wheat, barley, maize, sunflower, oats, sorghum | (*olive = clase NUEVA mediterranea) |
| Clases/patch (media) | 4.7 (min 2, max 10) | mosaico multi-clase |
| % nube residual media (SCL) | **8.4%** | bajo = serie coherente |
| Fechas por patch (media) | **26.4** (min 9, max 39) | objetivo 30-45 (denso) |
| std NDVI homologo (media) | **0.164** | PASTIS 0.181; pixel-punto ~0.05 — TEXTURA REAL |
| Peticiones SH consumidas (run) | **20** (1/patch) | — |
| Peticiones SH sesion total | ~41 (incl. pre-flight) | ~0.4% de ~10,800 |
| Cuota SH restante estimada | ~10,759 peticiones | suficiente para el full |

Soporte por clase (pixeles, todo el piloto): dominan `tree_wood_forest` (160.6k,
paisaje toscano), `permanent_grassland` (18.8k), `arable_crops` (16.2k),
`common_soft_wheat` (11.1k), `maize` (9.6k), `alfalfa` (8.6k), `clover` (7.6k),
`barley` (7.9k). Cola: chickpeas (472), spelt/sweet_chestnuts/sorghum/oats (138-553).

Escalado al dataset completo: a ~1 peticion/patch, 200 patches = ~200 peticiones
(~2% de la cuota). El piloto valida el formato, la textura y la cobertura; queda
a decision de Arthur escalar (NO se corrio el full).
