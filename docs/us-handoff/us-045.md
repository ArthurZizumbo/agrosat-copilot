# Handoff US-045 — Nueve FunctionTools geoespaciales

**Estado:** ready-to-close (53 tests verdes, cobertura ml.agent 87%; cierre documental 2026-06-24 -> [docs/us-resolved/us-045.md](../us-resolved/us-045.md))
**Rama:** `feature/E7-US-045-functtools-geoespaciales`
**Plan:** [docs/us-planning/us-045.md](../us-planning/us-045.md)

## Decisiones tecnicas (fijadas en planeacion)

- DB ya existe (`parcels`, `aois`, `features_parcels` con `alphaearth_embedding VECTOR(64)`, `ndvi_stats`/`phenology` JSONB). Acceso via **asyncpg** directo (no hay SQLModel ORM).
- RLS policy es EPIC 11: las tools dejan `SET LOCAL app.current_session` + filtro `WHERE session_id` (defensa en profundidad) via `session_scoped_conn`.
- `classify_new_parcel` sobre una parcela NUEVA usa la ruta **xgb-alphaearth** (embedding 64-dim -> XGBoost): es la unica inferencia directa por-parcela; los segmentadores densos (tsvit/utae) necesitan raster temporal y van por OOF/Pub/Sub. `compare_models` compara las predicciones ya dumpeadas (OOF parquets) de los miembros.
- `explain_prediction` emite TEXTO estructurado (cultivo, fenologia, vigor, confianza) reutilizando `generate_phenology_description` -> entrada del patron Be My Eyes (US-046).
- 5 tools sync (demo) + 4 deferred (`search_stac`, `get_tiles`, `add_aoi`, `compare_models`).
- SDK `google-genai` para `FunctionDeclaration` (NO google-adk).

## Archivos (write-sets disjuntos)

| Sub-tarea | Archivos |
|-----------|----------|
| ml/A nucleo | `ml/agent/schemas.py`, `ml/agent/db.py`, `ml/agent/context.py`, `ml/agent/tools.py` |
| ml/B DB tools | `ml/agent/tools/parcels.py`, `ml/agent/tools/timeseries.py`, `ml/agent/tools/aoi_stats.py` |
| ml/C ML tools | `ml/agent/tools/classify.py`, `ml/agent/tools/explain.py`, `ml/agent/tools/compare.py` |
| ml/D deferred | `ml/agent/tools/stac.py`, `ml/agent/tools/tiles.py`, `ml/agent/tools/add_aoi.py`, `scripts/gen_tools_doc.py` |
| TESTS | `tests/ml/agent/*` |

## Snapshot de archivos tocados

**Nucleo:** `ml/agent/schemas.py` (9 pares ToolInput/ToolOutput + tipos compartidos), `ml/agent/db.py` (`get_pool`, `session_scoped_conn` con `set_config('app.current_session',$1,true)`, `close_pool`, `to_asyncpg_dsn`), `ml/agent/context.py` (`ToolContext`: pool/settings/session_id/defer), `ml/agent/tools/__init__.py` (registro lazy `TOOL_REGISTRY`, `ToolSpec`, `build_function_declarations`, `get_sync_tools`/`get_deferred_tools`, conversor Pydantic->genai Schema).

**Tools (9):** parcels, timeseries, aoi_stats (DB sync); classify, explain, compare (ML); stac, tiles, add_aoi (deferred). Cada una `async def run(inp, ctx) -> Output`.

**Auto-doc:** `scripts/gen_tools_doc.py` -> `docs/agent/tools.md` (9 tools, schemas reales).

**Tests:** `tests/ml/agent/` (8 archivos, 53 tests, cobertura 87%).

## Contrato publico (para US-046/047)

- `from ml.agent.tools import TOOL_REGISTRY, build_function_declarations, get_sync_tools, get_deferred_tools, ToolSpec`
- `from ml.agent.db import session_scoped_conn, get_pool, close_pool`
- `from ml.agent.context import ToolContext`  (pool, settings, session_id, defer)
- Cada tool: `async def run(inp: <X>Input, ctx: ToolContext) -> <X>Output`.
- 5 sync: list_parcels, get_parcel_timeseries, get_aoi_stats, classify_new_parcel, explain_prediction. 4 deferred: search_stac, get_tiles, add_aoi, compare_models.

## QA verificacion (2026-06-15)

Evidencia embebida (antes en manual-test, ahora unica fuente de verdad). Lo automatizable
esta cubierto por `tests/ml/agent/`; los flujos que requieren Postgres+PostGIS con datos
reales de sesion se listan como pendientes manuales y siguen `[ ]` de forma legitima
(no son codigo incompleto, son no-automatizables en CI).

### Verificado (palomeado)

- **Suite de tests:** 53 verdes (schemas 15, db_tools 8, ml_tools 9, function_declarations 10, rls_isolation 5, deferred_tools 6). Re-corridos hoy dentro de los 98 de `tests/ml/agent/`.
- **Lint:** `ruff check ml/agent/` -> "All checks passed!".
- **Cobertura `ml.agent` ~86%:** parcels/aoi_stats/add_aoi/compare/tiles/retrieve_context 100%, schemas 99%, tools/__init__ 94%, stac 94%, timeseries 92%, explain 83%. `classify.py` 39% (path XGBoost+DB no unit-testeable sin modelo/DB reales; ver caveat).
- **Contratos Pydantic:** 9 pares `ToolInput`/`ToolOutput` v2 strict en `ml/agent/schemas.py`. `build_function_declarations()` deriva 10 declaraciones genai (9 US-045 + `retrieve_context` US-046). `BBox`/`GeoJSONGeometry` rechazan entrada malformada antes del service.
- **Split tools:** 5 sync (list_parcels, get_parcel_timeseries, get_aoi_stats, classify_new_parcel, explain_prediction) + 4 deferred (search_stac, get_tiles, add_aoi, compare_models; +retrieve_context US-046).
- **Seguridad (agrosat-security-audit):** SQL 100% parametrizado (`$1..$n`); `set_config('app.current_session',$1,true)` con bind param ([db.py:96-99](../../ml/agent/db.py#L96-L99)), no interpolacion; `WHERE session_id=$1` en cada tool de DB (parcels, timeseries, aoi_stats, classify, explain, add_aoi); `search_stac` via `pgstac.search($1::jsonb)`; 0 SQL por f-string/`.format`/`%`; 0 `print()`; 0 secretos hardcodeados. Aislamiento cross-session verificado (test_rls_isolation, 5).
- **Invariantes por tool (mocks):** `list_parcels` sesion vacia -> lista vacia, cap 1000; `get_parcel_timeseries` reconstruye de percentiles JSONB sin valores fabricados; `get_aoi_stats` area (ha) + cultivo dominante via ST_Intersects; `explain_prediction` TEXTO estructurado sin cifras inventadas (descriptor mockeado); `compare_models` OOF parquets reales con camino vacio honesto.
- **Auto-doc:** `scripts/gen_tools_doc.py` regenera `docs/agent/tools.md` desde los schemas reales (9 tools).
- **Code review (agrosat-code-review):** ruff limpio, structlog, type hints, SoC (logica en tools no en routers). Sin hallazgos.

### Pendiente (verificacion manual)

No automatizable en CI: requiere Postgres+PostGIS con parcelas de una sesion real y (para
classify) el modelo XGBoost cacheado. Validar en demo localhost.

- [ ] `list_parcels` real (manual): levantar Postgres+PostGIS con parcelas de `session_id=X`; la tool devuelve solo parcelas de X.
- [ ] RLS cross-tenant real (manual): invocar `list_parcels` con `session_id=Y` sobre datos de X -> lista vacia (no fuga).
- [ ] `classify_new_parcel` real (manual / GPU-opcional): parcela con `alphaearth_embedding` 64-dim -> XGBoost -> posterior 18-clase coherente.
- [ ] `classify` sin embedding (manual): parcela sin embedding persistido -> sentinel `needs_gee_sampling` (no excepcion).
- [ ] `explain_prediction` real (manual): parcela con fenologia -> descriptor Wen 2025 -> TEXTO Be My Eyes con cifras trazables a tool calls.

### Caveats honestos

- `classify.py` 39% cobertura: el path de carga XGBoost + fetch DB no es unit-testeable sin modelo/DB reales; la logica de borde si esta testeada.
- Policy RLS Postgres: la **policy** es EPIC 11; hoy las tools garantizan aislamiento por `WHERE session_id` + `SET LOCAL` (defensa en profundidad).
- `explain` descriptor: el generador fenologico (red) esta mockeado en tests; ejecucion real en pendientes manuales.
- `compare_models` con `parcel_id` plano: no matchea ids compuestos OOF (`patch_local`); camino vacio honesto cubierto.

## Bugs / bloqueos

- **Fix aplicado:** `GeoJSONGeometry._ALLOWED_TYPES` debia ser `ClassVar[frozenset[str]]` (Pydantic v2 lo trataba como `ModelPrivateAttr` -> `TypeError` al construir cualquier geometria). Corregido en `ml/agent/schemas.py`.
- **Nota para US-046/047:** `get_settings()` real NO se puede llamar en tests (el `.env.local` dev tiene llaves que `Settings(extra="forbid")` rechaza). Los tests usan `FakeSettings`. Relevante para el loop del agente.
- **compare_models:** un `parcel_id:int` plano no matchea los OOF (ids compuestos `patch_local`, ej. `10003_1103071`). Camino vacio honesto cubierto.

## Cierre (2026-06-24)

- **Estado -> ready-to-close.** US-resolved: [docs/us-resolved/us-045.md](../us-resolved/us-045.md); manual-test: [docs/manual-test/us-045.md](../manual-test/us-045.md).
- Validacion documental: suite `tests/ml/agent/` = 129 passed, 1 failed. El unico fallo
  (`test_perceiver::test_observe_aoi_uses_classifier_posterior`) es AJENO a las 9 tools de US-045:
  surge del default `restrict_to_resolved_classes=france-9` (US-053) en el AOI path del perceiver,
  no de las tools. Documentado en [docs/blockers/epic7-notas.md](../blockers/epic7-notas.md) B-E7-2.
