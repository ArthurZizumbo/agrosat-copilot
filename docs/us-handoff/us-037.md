# US-037 — Handoff

**Status**: `implemented` (codigo completo + tests verdes; **run real pendiente del checkpoint US-036-a**)
**Titulo**: Evaluacion FarSLIP-pheno (US-036-a, PASTIS-R real) vs AlphaEarth — cierre honesto del gap 0.163 vs 0.233

> **Estado de implementacion (2026-06-08)**: el helper `scripts/farslip_eval_phenology.py` y su test
> `tests/ml/eval/test_farslip_eval_phenology.py` estan COMPLETOS y verdes (21 passed, ruff limpio, cobertura 78%
> de la logica testeable). El run real (extraccion en H100 + runs MLflow) esta BLOQUEADO por R-DEP: el
> `best.safetensors` del escalon ganador de US-036-a aun se esta entrenando. El codigo maneja gracefully el caso
> "checkpoint no existe aun" (`FileNotFoundError` claro, no crash silencioso). Falta: ejecutar el run cuando el
> checkpoint exista y agregar la seccion al notebook `04_farslip_eval_pastis.ipynb` re-ejecutada con outputs.

## Comando exacto para el run (cuando US-036-a cierre)

```bash
# 1) nvidia-smi ANTES (1 GPU compartida 96 GB; parar el daemon idle-shutdown si aplica)
# 2) ajustar <NN> a la cardinalidad del escalon ganador (log de US-036-a: "winner step ... n_classes=<NN>")
poetry run python -m scripts.farslip_eval_phenology eval \
    --checkpoint-path checkpoints/farslip/incremental/<NN>cls/best.safetensors \
    --n-classes <NN> --embedding-space cls768 --eval-folds 4,5 \
    --pastis-root data/PASTIS-R \
    --alphaearth-path data/cache/gee/alphaearth_parcels_pastis_parcels_2019_85951.parquet \
    --mlflow-uri http://localhost:5010
# Espacio secundario (R-EXTR-512, proyeccion teacher no afinada): --embedding-space proj512
```

**Epic**: E5 (Modelos Alternativos / Comparativa) · **Avance**: A5 (ensamble) / A6 (evaluacion) / S7
**Sprint**: S7 · **Owner**: Arthur Zizumbo (ML/MLOps) · **Prioridad**: P1 (insumo directo del ensamble E-a de US-041)
**Rama sugerida**: `feature/E5-US-037-farslip-eval-vs-alphaearth`
**Plan**: [docs/us-planning/us-037.md](../us-planning/us-037.md)

**Depende de** (BLOQUEANTE):
- **US-036-a** (`planning` -> requiere `implemented` con run productivo cerrado): aporta el `best.safetensors` del
  **escalon ganador** del fine-tune COMPLETO incremental sobre PASTIS-R real
  (`checkpoints/farslip/incremental/<NN>cls/best.safetensors`, DVC). **Es el checkpoint a evaluar.** Ver R-DEP.
- **US-036** (`implemented`): `ml/farslip/pastis_pair_dataset.py` (`create_incremental_dataset`,
  `peak_ndvi_composite`, `dominant_class`, `active_classes`) — la fuente del par PASTIS real (composite pico-NDVI).

**Insumo de**: **US-041** (ensamble E-a). Decide, con evidencia medida, si FarSLIP-pheno entra al ensamble.

## Resumen ejecutivo

Medir, apples-to-apples, si el espacio de embeddings del **student FarSLIP-pheno de US-036-a** (fine-tune COMPLETO
incremental sobre **PASTIS-R frances real**) supera el `0.163` (FarSLIP previo) y se acerca al `0.233` (AlphaEarth)
en **separabilidad/silhouette por clase** (NO mIoU denso). Los embeddings FarSLIP-pheno se EXTRAEN de PASTIS-R real
con el **mismo dataset builder de US-036** (composite pico-NDVI 4-banda por patch) pasando los patches por el student
de US-036-a, y se comparan contra AlphaEarth 64-dim sobre los **MISMOS patches** (mismos `parcel_id`, mismas
etiquetas), en **fold-4/5 held-out**. El reporte es **honesto**: documenta las asimetrias (768/512 vs 64 dim,
granularidad patch vs parcela) y NO sobre-afirma. Se REUSA `ml/eval/embedding_separability.py` (`eval_space`,
`align_spaces_on_parcels`, `build_balanced_eval_set`) — el motor de eval que ya usa el notebook oficial.

## ALCANCE CRITICO (usuario, 2026-06-07)

- **SOLO PASTIS-R FRANCES REAL.** PROHIBIDO italiano, sintetico, placeholder.
- **Checkpoint = US-036-a** (incremental sobre PASTIS real), **NO** el FarSLIP italiano de US-034/035 (descartado),
  **NO** el oficial publicado (`FarSLIP2_ViT-B-16.pt`).
- **Embeddings FarSLIP-pheno** = extraidos de PASTIS-R real via el builder de US-036
  (`ml/farslip/pastis_pair_dataset.py`, composite pico-NDVI por patch) + student US-036-a.
- **Honestidad (v8)**: objetivo = superar el 0.163 propio y acercarse a 0.233, **NO** prometer "+5pp open-vocabulary"
  sin medicion apples-to-apples. fold-4/5 held-out.

## Contexto tecnico verificado (recon de repo)

- **0.163 / 0.233 = metricas de separabilidad/silhouette por clase** del espacio de embeddings (NO mIoU denso).
  Se REUSA `ml/eval/embedding_separability.py` (firma leida): `eval_space(matrix, labels, *, label, n_splits=5,
  random_state=42, max_iter=2000, silhouette_sample_size=10000) -> SeparabilityResult` (silhouette global +
  LogReg F1-macro 5-fold); `align_spaces_on_parcels(labels_df, spaces)` (inner join apples-to-apples por
  `parcel_id`); `build_balanced_eval_set(...)`; `load_alphaearth_embeddings(path, *, year_suffix="")`.
- **Extractor**: `ml/extractors/farslip_extractor.py` `FarSLIPExtractor` carga un student de `distill.py` en
  `model.vision_model` (`load_state_dict(strict=False)`), `extract_embeddings -> (B,512)` (proyeccion teacher,
  NO afinada), `extract_patch_features -> (B,196,768)`, `_prep_crops` (uint16/10000, 4 bandas, resize 224). Para
  US-037 se instancia con `weights_uri=<best US-036-a>`. **Espacio recomendado = CLS 768** (el que el InfoNCE de
  US-036-a optimizo, via `pooler_output`); el 512 es secundario (recon US-034: el loss contrasta el CLS 768, no
  la proyeccion 512).
- **US-036-a produce** `checkpoints/farslip/incremental/<NN>cls/best.safetensors` (confirmado en
  `scripts/run_us036a_farslip_full_incremental.py`: `output_root=checkpoints/farslip/incremental`,
  `best_ckpt=step_dir/"best.safetensors"`). El `<NN>` es la cardinalidad del escalon ganador.
- **AlphaEarth** = Satellite Embedding V1 Annual v1.1, 64-dim. Parquet por parcela en
  `data/cache/gee/alphaearth_parcels_pastis_parcels_2019_85951.parquet` (el notebook existente ya lo usa);
  `data/features/features_fused_pastis.parquet` (DVC, md5 `9ddc0df4...`) tiene la fusion.
- **Notebook eval YA existe**: `notebooks/baseline/04_farslip_eval_pastis.ipynb` implementa el protocolo
  apples-to-apples (FarSLIP-s1/s2 oficial vs AlphaEarth-2019/multi, silhouette + F1-macro 5-fold, balanceado +
  completo, UMAP por clase, runs MLflow, fallback `file:./mlruns`). US-037 AGREGA una seccion (FarSLIP-pheno
  US-036-a como espacio mas), NO reinventa.
- **PASTIS-R real presente** en `data/PASTIS-R/` (DATA_S2, ANNOTATIONS, INSTANCE_ANNOTATIONS, metadata.geojson).

## Dominios tocados

- [x] **ml** (`scripts/farslip_eval_phenology.py` NUEVO, `tests/ml/eval/test_farslip_eval_phenology.py` NUEVO,
  seccion en `notebooks/baseline/04_farslip_eval_pastis.ipynb`)
- [x] **H100** (extraccion breve de embeddings con GPU; fallback CPU)
- [x] **docs** (solo estos dos `.md` de planning/handoff)
- [ ] backend · [ ] frontend · [ ] agent · [ ] db · [ ] dagster · [ ] infra

Skills: `agrosat-ml-evaluation`, `agrosat-ml-segmentation` (FarSLIP/extractor), `agrosat-dvc-mlflow`,
`agrosat-azure-h100`.

## Sub-tareas paralelas (decision)

**1 SOLO agente ml. NO paralelizable; SECUENCIAL respecto a US-036-a.** Pipeline acoplado: extraer del student
US-036-a -> alinear con AlphaEarth -> silhouette por clase -> tabla comparativa -> seccion notebook. El helper, su
test y la seccion del notebook comparten contrato (esquema del parquet de embeddings, columnas de la tabla
comparativa) y se deciden de una vez. El run de extraccion es secuencial al codigo y bloqueado por US-036-a.

| Agente | Write-set (DISJUNTO de US-036-a) | Tarea |
|--------|----------------------------------|-------|
| **ml/A (unico)** | `scripts/farslip_eval_phenology.py` (NUEVO), `tests/ml/eval/test_farslip_eval_phenology.py` (NUEVO), seccion en `notebooks/baseline/04_farslip_eval_pastis.ipynb`, `docs/us-planning/us-037.md`, `docs/us-handoff/us-037.md` | (1) `extract_pheno_embeddings` (best US-036-a -> parquet FarSLIP-pheno PASTIS real, folds 4/5, CLS 768); (2) `silhouette_per_class`; (3) `compare_to_alphaearth` (IMPORTA `eval_space`/`align_spaces_on_parcels`); (4) tests con mocks + matrices sinteticas; (5) seccion del notebook; (6) tras US-036-a cerrada: `nvidia-smi` + extraccion + runs MLflow `:5010` cerrados + conclusion honesta. |

## Archivos a crear / modificar

**Crear**:
- `scripts/farslip_eval_phenology.py` (NUEVO): helper de extraccion + silhouette por clase + comparativa
  apples-to-apples (firmas en planning Seccion 4). IMPORTA `eval_space`/`align_spaces_on_parcels`/
  `build_balanced_eval_set` de `ml/eval/embedding_separability.py` y `create_incremental_dataset` de
  `ml/farslip/pastis_pair_dataset.py`; INSTANCIA `FarSLIPExtractor`. NO reescribe ninguno.
- `tests/ml/eval/test_farslip_eval_phenology.py` (NUEVO): silhouette por clase deterministico + consistente con
  el global, apples-to-apples (mismas filas/etiquetas), deltas en la tabla comparativa, rechazo de checkpoint
  italiano. Extractor/builder mockeados; matrices sinteticas con separabilidad conocida.

**Modificar**:
- `notebooks/baseline/04_farslip_eval_pastis.ipynb`: agregar seccion "US-037: FarSLIP-pheno (US-036-a) vs
  AlphaEarth" (importa el helper, agrega el espacio FarSLIP-pheno a `align_spaces_on_parcels`, corre
  `eval_space` + `silhouette_per_class`, tabla comparativa vs golden 0.163/0.233, UMAP por clase, "Lectura
  honesta"). Re-ejecutado end-to-end con outputs poblados.

**NO tocar**: `ml/eval/embedding_separability.py` (reusar), `ml/extractors/farslip_extractor.py` (reusar,
`weights_uri` default intacto), `ml/farslip/pastis_pair_dataset.py` (US-036, reusar), el orquestador y los
checkpoints de US-036-a (write-set disjunto), los `.dvc` de AlphaEarth / US-033 / best US-036-a (solo LEER),
pesos frozen (teacher CLIP, MiniLM). NO re-entrenar. NO usar checkpoint italiano/oficial ni `data/farslip_pairs`.

## H100 / MLOps

- **H100 (extraccion breve)**: `nvidia-smi` ANTES (1 GPU compartida 96 GB). `FarSLIPExtractor` en `inference_mode`,
  ViT-B/16 4-banda, batch=64, FP32 ~**6-10 GB** (sin grafo de gradientes). Folds 4/5 = cientos de patches, 1
  forward por patch -> **<15 min** de extraccion; eval (silhouette + LogReg 5-fold) en CPU, minutos. **Total <30
  min de GPU.** Fallback CPU (`device="auto"`) si la H100 no esta disponible. Sin checkpoints nuevos.
- **MLflow `:5010`** experiment `farslip`/`evaluation`: un run por espacio/regimen (`farslip-eval-pheno`,
  `farslip-eval-alphaearth-2019`, opcional `-multi`; balanceado + completo) con tags `data_version`
  (`dvc_data_version` del best US-036-a + parquet AlphaEarth) + `code_version` (`git_sha(short=True)`); metricas
  `silhouette`/`f1_macro_mean`/`delta_vs_0163`/`delta_vs_alphaearth_here`; artefactos tabla por clase + comparativa
  + UMAP. **Runs CERRADOS** (subprocess deja RUNNING — gotcha ml/AGENTS.md). Fallback `file:./mlruns` si `:5010`
  no responde (patron del notebook existente).
- **DVC**: sin pesos nuevos. Parquet `data/farslip/embeddings_pheno_pastis.parquet` (insumo US-041) opcional
  `dvc add`. Insumos solo lectura.

## Riesgos principales (detalle en planning Seccion 9)

1. **R-DEP (ALTA)**: US-036-a sin cerrar -> sin checkpoint que evaluar. Bloqueo explicito (no sustituir por
   italiano/oficial). Avanzar `silhouette_per_class` + tests con matrices sinteticas mientras tanto.
2. **R-DIM (ALTA)**: 768/512 vs 64 dim no es invariante para silhouette. Apples-to-apples + reportar ambas dims +
   F1-macro (menos sensible a dim) + no afirmar superioridad solo por silhouette mayor.
3. **R-GRAN (MEDIA)**: granularidad patch (US-036/036-a) vs parcela (notebook). Evaluar FarSLIP-pheno a nivel
   patch (la unidad de entrenamiento); regimen por-parcela separado para continuidad. Caveat documentado.
4. **R-NOGAIN (MEDIA)**: FarSLIP-pheno podria no superar 0.163. El AC es MEDIR Y REPORTAR honestamente, no
   garantizar. Si no supera, documentar el por que (caveat US-034) — info valiosa para US-041.
5. **R-CLAIM (MEDIA)**: no sobre-afirmar ("+5pp open-vocabulary"). Conclusion cinida a lo medido.
6. **R-LABEL (MEDIA)**: desalineacion de etiquetas tras el join. `labels_df` desde la misma fuente para ambos
   espacios; test `test_aligned_spaces_same_rows_and_labels`.
7. **R-EXTR-512 (MEDIA)**: extraer 512 (proyeccion teacher no afinada). Default CLS 768 (lo que el fine-tune
   optimizo); 512 secundario etiquetado.
8. **R-MLFLOW (BAJA)** / **R-DVC-READ (BAJA)**: runs `RUNNING` / tocar `.dvc` de insumo. Cerrar runs; solo LEER.

## Criterio de aceptacion (resumen)

`scripts/farslip_eval_phenology.py` extrae los embeddings del **best de US-036-a** (PASTIS-R real, builder de
US-036, composite pico-NDVI, folds 4/5 held-out, CLS 768), calcula **silhouette por clase** (REUSANDO
`embedding_separability.eval_space` + `silhouette_per_class`) y los compara **apples-to-apples** contra AlphaEarth
64-dim sobre los MISMOS patches; la **tabla comparativa** reporta el delta vs `0.163` (FarSLIP previo) y `0.233`
(AlphaEarth) con **conclusion honesta** (supera/no el 0.163; se acerca/no a 0.233; sin sobre-afirmar); un **run
MLflow `:5010`** por espacio con `data_version`+`code_version`+silhouette+F1-macro+tabla por clase (artefacto),
runs CERRADOS; **seccion agregada** a `notebooks/baseline/04_farslip_eval_pastis.ipynb` re-ejecutada con outputs;
`tests/ml/eval/test_farslip_eval_phenology.py` verde (extractor/builder mockeados, silhouette deterministico,
rechazo de checkpoint italiano); `make check` limpio. Write-set DISJUNTO de US-036-a.

## Estado de implementacion del codigo (2026-06-08)

**HECHO** (este lote, write-set disjunto de US-036-a):

- `scripts/farslip_eval_phenology.py` (NUEVO): `extract_pheno_embeddings` (best US-036-a -> parquet FarSLIP-pheno
  PASTIS real, folds 4/5, CLS 768 default / proj512 secundario; IMPORTA `create_incremental_dataset` e INSTANCIA
  `FarSLIPExtractor`), `silhouette_per_class` (Polars `[class_id, silhouette_class, n]`, deterministico),
  `aggregate_alphaearth_to_patch` (AlphaEarth por-parcela -> nivel patch por media, R-GRAN), `compare_to_alphaearth`
  (IMPORTA `align_spaces_on_parcels`/`eval_space`/`build_balanced_eval_set`; tabla comparativa con `delta_vs_0163`
  y `delta_vs_alphaearth_here`; veredicto honesto), `run_eval` + CLI Typer `eval`, `_log_space_run` (MLflow :5010,
  un run por espacio CERRADO, `data_version`+`code_version`). Guardas de alcance: `_validate_checkpoint` (rechaza
  4band-pheno/FarSLIP2_ViT-B-16/farslip_pairs/farslip-clip-italy) + `_validate_pastis_root`. `FileNotFoundError`
  claro si el checkpoint aun no existe (R-DEP, no crash silencioso).
- `tests/ml/eval/test_farslip_eval_phenology.py` (NUEVO): 21 tests verdes (extractor + builder MOCKEADOS, matrices
  sinteticas con separabilidad conocida): silhouette por clase deterministico + consistente con el global,
  apples-to-apples (mismas filas/etiquetas + dims documentadas), deltas en la tabla con signo correcto, veredicto
  honesto (no asume gain, sin "+5pp"), agregacion AlphaEarth a patch, rechazo de checkpoint italiano/oficial/
  sintetico, `FileNotFoundError` si falta el checkpoint. `ruff` limpio; cobertura 78% de la logica testeable.

**PENDIENTE** (bloqueado por R-DEP — US-036-a entrenando):

1. **Bloqueante**: esperar a que US-036-a cierre (`best.safetensors` del escalon ganador en DVC).
2. Tras US-036-a cerrada: `nvidia-smi` -> correr el comando de arriba (`eval --checkpoint-path ...`) ->
   `extract_pheno_embeddings` (folds 4/5, CLS 768) -> runs MLflow `:5010` (cerrados) -> tabla comparativa + UMAP ->
   "Lectura honesta" -> agregar la seccion a `notebooks/baseline/04_farslip_eval_pastis.ipynb` re-ejecutada con
   outputs -> `make check` limpio -> completar este handoff con la tabla comparativa, silhouette por clase, delta
   vs 0.163/0.233 y la conclusion honesta sobre el cierre del gap.
