# Handoff US-013 — Dashboard Streamlit de EDA y reporte PDF

**Estado**: planning · 2026-05-12
**Epic**: E2 (Análisis Exploratorio de Datos · CRISP-ML(Q) Data Understanding)
**Avance**: A1 — EDA (entregable 2026-05-03 original · entrega real 2026-05-12, recovery sprint)
**Sprint**: S2-recovery
**Rama**: `feature/E2-US-013-dashboard-streamlit` (no creada aún)
**SP**: 2
**Owner**: Aaron Bocanegra (Full-Stack/Backend Lead) · contenido coordinado con Isaac

---

## Plan canónico

Ver [`docs/us-planning/us-013.md`](../us-planning/us-013.md).

## Resumen de scope

Cierra Avance 1 con dos entregables consolidados:

1. **Dashboard Streamlit `app/eda_dashboard.py`** con 6 tabs (univariado S2, AlphaEarth, bivariado, temporal, espacial folium, conclusiones CRISP-ML(Q)). File-driven: lee PNG/CSV de `paper/figures/us-{010,011,012}/`.
2. **Reporte PDF `paper/avance1_eda_report.pdf`** vía Jinja2 HTML + weasyprint. **Es el entregable oficial del A1 al curso**.

El dashboard es **valor agregado para sponsor + defensa oral**; el PDF es **obligatorio para la rúbrica**. Se trabajan en paralelo con US-010/011/012 — el esqueleto Streamlit se escribe primero con placeholders, las tabs se llenan automáticamente cuando cada notebook exporta sus figuras.

## Owner

**Aaron Bocanegra** (Full-Stack/Backend). Streamlit es UI server-side Python, cae en su scope. **Isaac aporta contenido**: conclusiones CRISP-ML(Q) y validación de figuras (no toca código UI).

## Dependencias upstream

- US-001 (monorepo) — ✅ resuelta
- US-010 (figuras EDA Sentinel-2) — en planning · provee `paper/figures/us-010/*.{png,csv}`
- US-011 (figuras EDA AlphaEarth) — en planning · provee `paper/figures/us-011/sec{1,2,3}_*.{png,csv}`
- US-012 (figuras bivariado/temporal) — en planning · provee `paper/figures/us-012/*.{png,csv}`
- PASTIS-R `metadata.geojson` (para tab 5 mapa folium) — ✅ disponible 12-may
- `config/rois.yaml` (geometrías Italia para tab 5) — ✅ disponible

**Diseño file-driven**: ninguna de las US bloquea US-013. El dashboard funciona aún con figuras parciales (muestra `st.info("Pendiente — US-XXX")` donde falten).

## Dependencias downstream

- Entrega Avance 1 al curso (deadline original 2026-05-03 · real 2026-05-12)
- Demo al sponsor Dr. Camacho post-cierre Avance 1
- Defensa oral final 2026-06-21 (el dashboard se puede archivar o reutilizar)

## Bloqueos pendientes

- [ ] **Bloque 0 obligatorio**: `poetry add --group paper streamlit streamlit-folium weasyprint jinja2`. Commit `chore(deps): add Streamlit + weasyprint for US-013`. Sin esto el código nuevo no compila.
- [ ] **weasyprint en Windows requiere GTK runtime** — documentar instalación en README o validar en WSL2 antes de Bloque 2. Fallback: `reportlab` si bloquea.
- [ ] **Cruzar con Dr. Camacho**: ¿el entregable A1 oficial es PDF, dashboard, o ambos? Default asumido: PDF obligatorio + dashboard bonus.
- [ ] Contenido de conclusiones CRISP-ML(Q) coordinado con Isaac (~30 min de borrador inicial async, revisión equipo).

## Snapshot de archivos a tocar

### Nuevos

```
app/__init__.py
app/eda_dashboard.py
ml/report/__init__.py
ml/report/export_pdf.py
ml/report/templates/avance1_eda.html.j2
ml/report/templates/styles.css
tests/app/test_dashboard.py
tests/ml/report/test_export_pdf.py
```

### Modificados

```
Makefile                                # +eda-dashboard, +eda-pdf, +eda-dashboard-test, +eda-all
pyproject.toml                           # +streamlit, streamlit-folium, weasyprint, jinja2 (grupo paper)
docs/licenses/DATA_LICENSE.md            # verificar PASTIS-R, Sentinel, AlphaEarth, DW, ERA5, HCAT3 completos
.env.example                             # +STREAMLIT_PORT=8501
```

## Métricas a verificar al cierre

| Métrica | Objetivo |
|---------|----------|
| `make eda-dashboard` | exit 0 · server arranca en ≤10 s en localhost:8501 |
| `make eda-pdf` | exit 0 · `paper/avance1_eda_report.pdf` ≤8 MB con 6 secciones CRISP-ML(Q) |
| `pytest tests/app/ tests/ml/report/ --cov` | ≥70 % sobre `app/eda_dashboard.py` + `ml/report/` |
| Tabs funcionando | 6/6 con contenido real (no placeholders) |
| ACs verificados | 13/13 (AC-10 y AC-11 marcados opcional-bonus) |
| Atribución | PASTIS-R · Sentinel-2 · AlphaEarth · Dynamic World · ERA5 · HCAT3/EuroCrops |

## Skills a auto-invocar

- `agrosat-backend-api` (Streamlit es UI server-side Python; el resto del backend Python toca convenciones similares de servicio + puerto custom + env vars)
- `agrosat-testing` (Streamlit `AppTest` + Playwright opcional para smoke test)

## Próximas fases

1. **Research opcional (~10 min)**: revisar API actual de `streamlit-folium` (cambios de signature entre 0.20 y 0.27) y `weasyprint` 65.x (cambios de paginación en versión mayor reciente). Pinear versiones.
2. **Implementation**: 7 bloques del §3.3 del plan (~5-6 h).
3. **Coordinación contenido**: Isaac escribe el borrador de conclusiones CRISP-ML(Q) (1 párrafo por fase × 6) en paralelo.
4. **QA pass**: ejecutar checklist §11 + smoke test manual (abrir cada tab + generar PDF + revisar atribuciones).
5. **PR review + merge** a `develop` con screenshot del dashboard y PDF adjunto.
6. **Cierre**: redactar `docs/us-resolved/us-013.md` con evidencia de 13 ACs + subir PDF al sistema del Tec antes de deadline.

## Notas de coordinación

- **El dashboard es opcional para la rúbrica, el PDF es obligatorio**. Si el cierre del sprint se complica, priorizar `make eda-pdf` sobre `make eda-dashboard`.
- **Streamlit `AppTest` API estable desde 1.28** — usar `from streamlit.testing.v1 import AppTest`. No usar el namespace experimental viejo.
- **No integrar el dashboard al frontend Nuxt** (ver §2.3 del plan). El frontend Nuxt es para el agente conversacional, no para EDA interno. Mezclar arruina la responsibility separation.
- **Si la entrega ya pasó deadline** (12-may vs 3-may): documentar como "entrega tardía con compensación de profundidad" en el PDF · pedir feedback al sponsor antes de Avance 2 para no acumular deuda en sprints siguientes.
- **El dashboard sobrevive al A1**: queda como herramienta interna para Avance 5 (modelo final), Avance 6 (conclusiones), y defensa oral 2026-06-21. Reusar template Jinja2 para reportes posteriores.
