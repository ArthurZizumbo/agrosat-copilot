# Handoff US-022-c — Cierre fenologico: FarSLIP en GCP + fix XGBoost + full CUDA + DVC pendientes + Gemini real

**Estado**: planning · **P1 etapas 3-7 BLOQUEADO 2026-05-24** (ver §"P1 en curso — monitor manual" al final)
**Epic**: E3/E4/E5 (transversal — CRISP-ML(Q) Data Preparation + Modeling + Evaluation)
**Avance**: post-A3 — alimenta A4 (24-may, ya entregado por US-022-b) y A5 (31-may)
**Sprint**: S7 (1-jun → 7-jun)
**Rama**: `feature/E5-US-022c-cierre-fenologico` (alias corto `us-022-c`)
**Ultima fase**: 2 — plan canonico publicado 2026-05-23
**SP**: 13 (P1=5, P2=1, P3=3, P4=3, P5=1)
**Owner**: Arthur (P1, P4, P5 infra/MLOps) + Isaac (P2, P3 ML)

> US-022-c es la convergencia operativa de 4 backlog items + bloque A de `us-019-1`. Cierra el ciclo
> del reencuadre fenologico end-to-end. **NO toca H100 (V1-V6)** — el bloque B Azure permanece en
> [`us-019-2-azure-pendiente.md`](../product-backlog/us-019-2-azure-pendiente.md).
>
> Cuando los 5 bloques cierren, US-022-b pasa a `closed` y se libera el PR a `develop`.

---

## Dominios tocados

- [ ] backend
- [ ] frontend
- [x] **ml** — `ml/farslip/extract_embeddings.py` (nuevo), `scripts/build_reencuadre_notebook.py` fix XGB (P2), notebook 05 re-ejecucion full CUDA (P3), `ml/features/phenology_description.py` (uso real P5)
- [ ] agent (ml/agent/)
- [x] **infra** — `terraform import` tfstate + `terraform apply` GCP completo (Cloud Run MLflow + Cloud SQL + buckets + artifact registry), cloudbuild imagen `ml-train`, Vertex AI custom-job L4
- [ ] db (sin migraciones)
- [x] **dagster** — re-materializacion opcional `farslip_embeddings_consolidated` asset post-P1 (ya existe en `dagster_project/assets/farslip_pipeline.py`)

---

## Archivos a crear (planning — confirmar en Fase 3)

| Ruta | Sub-bloque | Proposito |
|------|------------|-----------|
| `notebooks/features/04_farslip_eval_pastis.ipynb` | P1 (B-3) | Eval mIoU FarSLIP vs RemoteCLIP sobre PASTIS-R, gate +0.05 |
| `scripts/build_farslip_eval_notebook.py` | P1 (B-3) | Builder programatico del notebook 04 (patron `build_reencuadre_notebook.py`) |
| `ml/farslip/extract_embeddings.py` | P1 (B-4) | CLI extract_embeddings desde student MLflow @Production |
| `tests/ml/farslip/test_extract_embeddings.py` | P1 (B-4) | >= 6 tests (smoke + shape + determinism + CLI parsing + device fallback + parquet schema) |
| `docs/operations/terraform-bootstrap.md` | P4.3 (AC-P4-6) | Procedimiento documentado de re-import tfstate |
| `docs/manual-test/us-022-c.md` | cierre QA | Comandos exactos para reproducir P1-P5 |
| `docs/us-resolved/us-022-c.md` | cierre | Resumen ejecutivo + tabla final con resultados de los 5 bloques |
| `docs/us-resolved/us-016.md` | P4.1 (AC-P4-5) | Resumen US-016 con AC-14 ✅ (deferido cerrado) |

## Archivos a modificar (planning)

| Ruta | Sub-bloque | Cambio |
|------|------------|--------|
| `scripts/build_reencuadre_notebook.py` | P2 | Fix lookup XGBoost (lineas ~395) + assert defensivo |
| `notebooks/feature_engineering/05_reencuadre_fenologico.ipynb` | P2 + P3 + P5 | Re-ejecutado FULL CUDA + ablation con `pheno_text` real |
| `infrastructure/terraform/modules/gcp/main.tf` | P4.3 | Validar `google_storage_bucket.tfstate` declarado (ya esta lineas 392-414) y `versioning_enabled = true` |
| `data/.gitignore` | P4.2 | Limpiar entradas temporales que DVC gestiona |
| `Makefile` | P1 | Activar `farslip-eval-pastis` (lineas 410-411 son TODO) + agregar `farslip-extract-embeddings` |
| `docs/l4_log.md` | P1 + P5 | Entradas reales con `Job ID`, `Image SHA`, `Duracion`, `Costo`, `MLflow run` |
| `docs/us-handoff/us-022b.md` | P3 (AC-P3-6) | Reemplazar §"Resultados FULL CUDA" con datos del run post-fixes; marcar `closed` al final |
| `docs/licenses/DATA_LICENSE.md` | P1 (B-6) | Atribuciones CLIP MIT, RemoteCLIP Apache-2.0, GSAA Italia ODbL (deuda US-022-b §"Pendiente B-6") |
| `context/RefinamientoPlaneacionAgroSatCopilot_v6.md` | cierre | Referenciar US-022-c en EPIC 3/4/5 |

---

## Archivos existentes reutilizados (NO duplicar)

- `ml/farslip/dataset.py`, `dataset_audit.py`, `distill.py`, `train.py` — pipeline FarSLIP de US-017. P1 los **ejecuta**, no los recodifica.
- `ml/features/phenology_description.py` — cliente Gemini 3.5 Flash + text-encoder ya implementado en US-022-b. P5 lo usa con `skip_llm=False` sobre subset real.
- `ml/features/fusion.py` — constantes `EXPECTED_COL_COUNT_WITH_PHENO_TEXT = 573` ya definidas (lineas 94-100). P5 valida shape.
- `ml/train/baseline.py` — `_load_baseline_dataset`, `_prepare_dataframe` reusados para construir el subset estratificado P5.
- `ml/eval/feature_ablation.py` — `run_feature_ablation`, `build_default_feature_sets` ya soportan `with_pheno_text` graceful. P5 los invoca real.
- `scripts/build_reencuadre_notebook.py` — patron del builder. P2 solo modifica la celda XGB lookup.
- `infrastructure/docker/ml-train.Dockerfile` — imagen CUDA 13.0 ya construible. P1 etapa 2 la pushea via cloudbuild.
- `ml/configs/l4_spot.yaml` — Vertex AI custom-job config completa para L4 spot. P1 lo invoca via `make train-l4`.
- `infrastructure/terraform/modules/gcp/main.tf` — modulo GCP completo (Cloud Run MLflow scale-to-zero, SAs least-privilege, buckets versionados). P1 etapa 1 = `terraform apply` sin cambios al HCL.
- `dagster_project/assets/farslip_pipeline.py` — asset `farslip_embeddings_consolidated` para re-materializacion opcional post-P1.
- `scripts/cost_audit.sh` — gate L4 RUNNING/PENDING. Reusado pre y post de cada job P1.

---

## Decisiones tecnicas clave (planning)

- **D-1**: P1 ejecuta el pipeline FarSLIP en **L4 GCP dedicada** (no GPU local del equipo) por decision del usuario (2026-05-23) — busca mayor exactitud y reproducibilidad.
- **D-2**: P3 corre en **GPU local CUDA** (no L4 spot) — 4060/4080 es suficiente para 200 epochs x 85951 parcelas + early stopping (~45-60 min). L4 spot solo para P1 (FarSLIP training pesado).
- **D-3**: P2 (fix XGBoost) **DEBE preceder a P3** — si arrancamos full CUDA con el bug, perdemos 45-60 min de wall clock y la grafica final queda con 1 barra missing.
- **D-4**: P4 (DVC + tfstate import) NO bloquea el PR a `develop` — puede ejecutarse paralelo o despues. Es housekeeping de reproducibilidad cross-machine.
- **D-5**: P5 (Gemini real) corre sobre subset estratificado (~216 parcelas, 12 por clase x 18 efectivas) para acotar costo a ~$0.20 USD. Si en un futuro EPIC 6 quiere escalar a las 85951 parcelas, sera nueva US con presupuesto Gemini dedicado.
- **D-6**: Si el gate B-3 (mIoU FarSLIP - mIoU RemoteCLIP >= +0.05) falla, **la US cierra igual** (R2). Resultado negativo es honesto y reportable; FarSLIP queda como base learner opcional del stacking ensemble EPIC 6.
- **D-7**: Si P3 produce F1-macro temporal < 0.20 despues de los 4 fixes ML, se documenta hallazgo honesto + hipotesis (FFT reconstruccion vs serie diaria real) y se considera nuevo backlog `us-022-d-temporal-daily-series.md` para EPIC 5.
- **D-8**: Bloque B Azure (`us-019-2-azure-pendiente.md`) fuera de scope. Cero horas H100 consumidas. Presupuesto 80 h H100 intacto para V1-V6.

---

## Bugs resueltos

| Bug | Causa | Solucion | Estado |
|-----|-------|----------|--------|
| XGBoost NaN en `model_comparison.parquet` | `scripts/build_reencuadre_notebook.py:~395` lookup `model_kind == 'xgb'` vs canonico `'xgboost'`; `next()` retorna `None` | Replace `next(...)` por busqueda con fallback a `max(xgb_results, key=lambda r: r.f1_macro)` + assert defensivo en notebook | **pendiente coding (P2)** |

---

## Zonas sensibles

- `scripts/build_reencuadre_notebook.py` — builder programatico complejo (~450 lineas). El fix P2 debe preservar el orden de celdas y el patron de timestamps `HH:MM:SS +Ns [tag]` consistente con `build_baseline_notebook.py`.
- `ml/features/fusion.py` — orden estable de columnas; downstream (notebook 05, feature_ablation) depende. El bloque `pheno_text_*` debe seguir el LEFT JOIN sin romper `EXPECTED_COL_COUNT_WITH_PHENO_TEXT = 573`.
- `infrastructure/terraform/modules/gcp/main.tf` — el modulo es **largo (~700 lineas)** y declara Cloud Run MLflow, Cloud SQL, buckets, SAs, Pub/Sub. **No editar para P4.3 — solo importar** el bucket tfstate al state. Lifecycle rules del HCL (lineas 405-411, 30 versiones) deben coincidir con el bucket real o el `terraform plan` post-import reportara drift.
- `ml/configs/l4_spot.yaml` — referencia `MLFLOW_TRACKING_URI` desde env del caller (Makefile o terraform output). Si Cloud Run MLflow no esta running, el job fallara con `unbound variable`. Verificar `terraform output -raw mlflow_tracking_uri` antes de cada `train-l4`.
- `ml/features/phenology_description.py` — costo Gemini sensible. Sin cache + temperature alta + 85951 parcelas = $$$. P5 fuerza `temperature=0.0` + cache `data/cache/phenology_descriptions/` + subset 216.
- `data/farslip_pairs/` — output de US-017 etapa 4. Si `farslip-dataset-build` se re-ejecuta sobre el dataset existente, **NO debe sobreescribir** (idempotente). P1 etapa 4 valida `farslip-dataset-check` antes de re-construir.

---

## Modelos / Datos

- **MLflow runs esperados (3 nuevos)**:
  - `farslip-clip-italy-v1` (P1) — stage `Production` post-B-5
  - `phenology-tempcnn-v2` (P3) — stage `Staging`
  - `phenology-inception-v2` (P3) — stage `Staging`
- **DVC tags esperados (6 nuevos)**:
  - `farslip-embeddings-italy-v1` (P1, ~170 MB)
  - `fused-features-italy-v1` (P4.1, ~5-15 MB)
  - `spatial-kfold-italy-v1` (P4.1, <1 MB)
  - `scaler-v1` (P4.1, <10 KB)
  - `eurocrops-fr-2018-v1` (P4.2, ~11 GB)
  - `feature-selection-parcels-v1` (P4.2, ~76 MB)
- **VRAM / GPU**: **L4 24 GB GCP spot** (P1: ~18-20 GB usado / 24 GB cap), **GPU local CUDA 4060/4080** (P3: ~6-10 GB), **CPU** (P5: API calls). **0 h H100, 0 h del presupuesto 80 h H100.**
- **Costo cloud presupuestado**:
  - P1 FarSLIP L4 spot: ~$2 USD (6.5h)
  - P1 smoke: ~$0.20 USD (10 min)
  - P3: $0 (GPU local)
  - P4: $0 (bucket ya existe; tfstate import no genera costo)
  - P5 Gemini: ~$0.20 USD (216 calls)
  - **Total: ~$2.40 USD** (cap hard $5)

---

## engram-memory

- Observaciones a guardar al cierre (via `mem_save`):
  - decision/architecture: FarSLIP materializado en L4 dedicado (no GPU local) por exactitud
  - decision: P2 antes que P3 para no perder wall clock del full CUDA
  - pattern: terraform import del bucket tfstate (paradoja bootstrap) documentado en `docs/operations/terraform-bootstrap.md`
  - bugfix: XGBoost lookup `model_kind` `xgb` vs `xgboost` con fallback
  - architecture: cierre del ciclo fenologico end-to-end → habilita stacking EPIC 6
- Keywords: `us-022c`, `us-022b_closed`, `farslip_embeddings_materialized`, `farslip_l4_dedicated`, `xgboost_comparison_lookup_fix`, `full_cuda_post_fixes`, `dvc_eurocrops_fr`, `dvc_us016_outputs`, `terraform_tfstate_import`, `gemini_phenology_real_subset`.

---

## Proximas fases

1. **Fase 1 — Visto bueno** (DONE 2026-05-23): backlog `us-022-c-cierre-fenologico-farslip-dvc.md` aprobado por usuario; bloque B Azure formalmente diferido a `us-019-2-azure-pendiente.md`.
2. **Fase 2 — Plan canonico** (DONE 2026-05-23): este documento + `docs/us-planning/us-022-c.md`.
3. **Fase 3 — Coding** (PENDIENTE, ~1.5 dias dev):
   - P2 (30 min) → P3 (~2h GPU local) → cierre handoff US-022-b
   - En paralelo: P1 (1.5 dias humano + 6h L4) → P5 (0.5 dia) → P4 (4-6h)
4. **Fase 4 — QA** (~0.5 dia):
   - Re-correr `tests/ml/` + tests nuevos `tests/ml/farslip/test_extract_embeddings.py`
   - Cobertura ML >= 75 % del modulo nuevo + 53 tests pre-existentes verdes
   - `make check`, `make notebooks-check`, `make cost-audit`
5. **Fase 5 — Cierre**:
   - PR `feature/E5-US-022c-cierre-fenologico` → `develop` con Conventional Commit
   - `docs/us-resolved/us-022-c.md`, `docs/us-resolved/us-022b.md` (resumen ejecutivo)
   - US-022-b handoff → `closed`
   - US-016 handoff → `closed` + `docs/us-resolved/us-016.md`
   - Refinamiento v6 actualizado (EPIC 3/4/5)
   - `mem_save` con keywords §"engram-memory"

---

## Sub-agentes a lanzar en Fase 3

| Sub-agente | Bloques | Skill primaria | Orden |
|------------|---------|----------------|-------|
| `ml-engineer` | P2 fix builder · P3 FULL CUDA · P5 Gemini real | `agrosat-ml-baseline`, `agrosat-ml-segmentation`, `agrosat-ml-features` | 1 (P2 critical path) |
| `mlops-engineer` | P1 etapas 1-7 · P4.3 terraform import | `agrosat-terraform`, `agrosat-gcp-services`, `agrosat-dvc-mlflow` | 2 (paralelo con ml) |
| `geo-data-engineer` | P1 etapa 6 (extract embeddings) · P4.1 + P4.2 DVC | `agrosat-gee-alphaearth`, `agrosat-ml-features` | 3 (post-P1 etapa 5) |
| `finops-auditor` | gate B-6 (costo <= $5) + revision post-P1 | `agrosat-finops` | 4 (post-P1 cierre) |

---

## Referencias

- Plan canonico (este handoff): [`docs/us-planning/us-022-c.md`](../us-planning/us-022-c.md)
- Backlog origen: [`docs/product-backlog/us-022-c-cierre-fenologico-farslip-dvc.md`](../product-backlog/us-022-c-cierre-fenologico-farslip-dvc.md)
- Handoff padre: [`docs/us-handoff/us-022b.md`](us-022b.md)
- Plan padre: [`docs/us-planning/us-022b.md`](../us-planning/us-022b.md)
- ADR-006 Aceptada: [`docs/decisions/ADR-006-reencuadre-baseline-fenologico.md`](../decisions/ADR-006-reencuadre-baseline-fenologico.md)
- Paper-faro: Wen et al. (2025) — `docs/general/papers/paper.pdf`
- Azure fuera de scope: [`docs/product-backlog/us-019-2-azure-pendiente.md`](../product-backlog/us-019-2-azure-pendiente.md)
- Manual-test US-016 (comandos DVC): [`docs/manual-test/us-016.md`](../manual-test/us-016.md)
- Skill auto-invoke: [`docs/orchestration/auto-invoke.md`](../orchestration/auto-invoke.md)
- Rubrica Avance 5: [`docs/general/Rubricas Integrador.html`](../general/Rubricas%20Integrador.html)

---

## P1 — L4 job EN CURSO (monitor manual Arthur) — 2026-05-24 10:25 UTC

### Estado por gate (post-quota L4 aprobada 09:36 UTC)

| Gate | Estado | Comentario |
|------|--------|------------|
| B-1 dataset >=30k pares + balance >=0.20 | **VERDE** | 30173 pares, balance 0.987. Subido a `gs://agrosat-artifacts-dev/datasets/farslip_pairs_v1/` (15 GB utiles). |
| B-2 MLflow run `farslip-clip-italy-v1` | **PENDIENTE** | Smoke v2 L4 on-demand en curso (job `57653699656286208`); Etapa 5 lista para lanzar tras SUCCEEDED. |
| B-3 mIoU FarSLIP - mIoU RemoteCLIP >= +0.05 | **PENDIENTE** | depende B-2 |
| B-4 parquet (85951, 514) + tag git | **PENDIENTE** | depende B-2 |
| B-5 DVC push + MLflow Registry @Production | **PENDIENTE** | depende B-2 |
| B-6 costo L4 <= $5 USD | **VERDE** | Acumulado ~$0.35 USD. Restante $4.65 USD (suficiente para Etapa 5 spot ~$1.70). |

### Smoke L4 on-demand en curso

- **Job ID smoke v2**: `57653699656286208`
- **Display name**: `train-E5-US-022b-smoke`
- **Machine**: `g2-standard-8 + NVIDIA_L4 x1`, `STANDARD` (on-demand)
- **Motivo on-demand**: smoke v1 SPOT `7359114595530702848` quedo 22 min PENDING sin asignacion (sin capacidad L4 spot us-central1 ventana 09:52-10:14 UTC). Cancelado y relanzado on-demand para validar la cadena rapido. `ml/configs/l4_smoke.yaml: strategy: STANDARD` documentado en archivo.
- **Timeout**: 1200s (20 min hard cap)
- **Console**: https://console.cloud.google.com/vertex-ai/training/custom-jobs/locations/us-central1/training-pipelines/57653699656286208?project=agrosat-copilot
- **MLflow run esperado**: `smoke-gpu-tempcnn` en experimento `us-022b-a-smoke`

Comandos:

```bash
# Monitor estado (RUNNING/SUCCEEDED/FAILED)
gcloud ai custom-jobs describe 57653699656286208 --region=us-central1 --project=agrosat-copilot --format='value(state,error.message,startTime,endTime)'

# Logs en tiempo real
gcloud ai custom-jobs stream-logs 57653699656286208 --region=us-central1 --project=agrosat-copilot

# Cancelar si necesario
gcloud ai custom-jobs cancel 57653699656286208 --region=us-central1 --project=agrosat-copilot
```

### Dataset hidratado en GCS para Etapa 5

- Local: `data/farslip_pairs/` 15 GB, 30176 archivos (pianura 10112 + puglia + toscana).
- Remoto: `gs://agrosat-artifacts-dev/datasets/farslip_pairs_v1/` 21.4 GB.
- **Nota**: subdirectorio espurio `gs://.../farslip_pairs_v1/farslip_pairs/` (13.4 GB) producto de un rsync con doble prefix. **No afecta** al training job. Limpieza opcional:
  ```bash
  gcloud storage rm -r gs://agrosat-artifacts-dev/datasets/farslip_pairs_v1/farslip_pairs/
  ```

### Cambios YAML aplicados

- **`ml/configs/l4_smoke.yaml`**: revertido T4 -> L4 (`g2-standard-8 + NVIDIA_L4`), fix shape TempCNN (`X=(B,T,C)` directo sin `.transpose(1,2)`), `strategy: STANDARD` (excepcion documentada).
- **`ml/configs/l4_spot.yaml`**:
  - `scheduling.timeout: 21600s` -> `25200s` (7h, incluye descarga dataset).
  - Container args ahora hidrata dataset `gs://agrosat-artifacts-dev/datasets/farslip_pairs_v1/` -> `/app/data/farslip_pairs/` via SDK `google.cloud.storage` (ThreadPoolExecutor 32 workers).
  - CLI corregido: invoca `python -m ml.farslip.train` con flags reales del Typer (`--rois italy --epochs 4 --batch-size 64 --lr 1e-5 --seed 42 --output-dir /app/artifacts/farslip --gcs-output-uri gs://agrosat-artifacts-dev/vertex-jobs/farslip/farslip-clip-italy-v1/ --dataset-root /app/data/farslip_pairs --teacher-model-id openai/clip-vit-base-patch16 --time-cap-hours 6.5`). Los flags `--epic --us --data-version --mlflow-uri` del YAML previo NO existen en el CLI — los tags MLflow los pone `ml/farslip/distill.py:558-572` internamente.

### Etapa 5 — Lanzar FarSLIP cuando smoke v2 termine en SUCCEEDED

**Pre-flight**:

```bash
export MLFLOW_TRACKING_URI="https://agrosat-mlflow-dev-gox5zzl7wa-uc.a.run.app"
# Verificar smoke v2 OK
gcloud ai custom-jobs describe 57653699656286208 --region=us-central1 --format='value(state)' | grep SUCCEEDED
# Verificar no hay jobs activos
make cost-audit
# Verificar token vigente
gcloud auth application-default print-access-token | head -c 20
```

**Lanzar Etapa 5**:

```bash
export MLFLOW_TRACKING_URI="https://agrosat-mlflow-dev-gox5zzl7wa-uc.a.run.app"
make train-l4 epic=E3 us=US-017 script=farslip/train_student.py
# El flag `script=` se conserva por compatibilidad de la API del target,
# pero `l4_spot.yaml` ahora invoca `python -m ml.farslip.train` directo
# con flags hardcodeados. ETA fin = startTime + 6h.
```

**Monitor a 15 min**:

```bash
JOB_ID=<id-impreso-por-make>
gcloud ai custom-jobs describe $JOB_ID --region=us-central1 --format='value(state,error.message,startTime)'
# JOB_STATE_RUNNING o PENDING < 30 min -> exito de lanzamiento, dejar correr 6h.
# JOB_STATE_PENDING > 30 min -> sin capacidad spot. Cambiar a STANDARD:
#   editar ml/configs/l4_spot.yaml: `strategy: SPOT` -> `strategy: STANDARD`.
#   Costo on-demand 6h ~$4.30 USD (vs $1.70 spot).
# JOB_STATE_FAILED -> revisar logs:
#   gcloud ai custom-jobs stream-logs $JOB_ID --region=us-central1
```

### Etapa 6 — POST-Etapa 5 SUCCEEDED (~6h despues)

```bash
# 1. Promover modelo a Production en MLflow Registry
poetry run python - <<'PY'
import os, mlflow
mlflow.set_tracking_uri(os.environ["MLFLOW_TRACKING_URI"])
client = mlflow.MlflowClient()
exp = client.get_experiment_by_name("farslip")
runs = client.search_runs([exp.experiment_id], order_by=["start_time DESC"], max_results=1)
run = runs[0]
model_uri = f"runs:/{run.info.run_id}/model"
mv = mlflow.register_model(model_uri, "farslip-clip-italy-v1")
client.transition_model_version_stage("farslip-clip-italy-v1", mv.version, "Production")
print(f"Registered version {mv.version} as Production")
PY

# 2. Extract embeddings
make farslip-extract-embeddings
poetry run python -c "import polars as pl; df = pl.read_parquet('data/farslip/embeddings_italy.parquet'); assert df.shape == (85951, 514); print('OK')"

# 3. DVC + tag
dvc add data/farslip/embeddings_italy.parquet
dvc push
git add data/farslip/embeddings_italy.parquet.dvc
git commit -m "feat(E3): materializa farslip_embeddings_italy_v1 (85951 x 514)"
git tag farslip-embeddings-italy-v1 -m "FarSLIP student italia v1"
```

### Etapa 7 — Eval mIoU PASTIS-R (gate B-3)

```bash
make farslip-eval-pastis
# Verificar mIoU_FarSLIP - mIoU_RemoteCLIP >= +0.05 en notebooks/features/04_farslip_eval_pastis.ipynb
```

### Riesgo conocido — capacidad spot L4 us-central1

Smoke v1 SPOT estuvo 22 min PENDING sin asignacion (2026-05-24 09:52-10:14 UTC). Si Etapa 5 spot tampoco arranca en 30 min, Arthur debe cambiar `ml/configs/l4_spot.yaml: strategy: STANDARD` (impacto: $1.70 -> $4.30 USD, dentro del cap $5).

### Acciones bloqueadas por classifier auto-mode

- `gcloud storage rm -r gs://agrosat-artifacts-dev/datasets/farslip_pairs_v1/farslip_pairs/`: el classifier marco la delete recursiva de prefix compartido como out-of-scope. Limpieza manual opcional por Arthur (~13 GB de duplicado, no afecta funcionamiento).

---

## P1 status (2026-05-24 08:50 UTC, HISTORICO) — Cadena infra/auth VERDE, GPU bloqueada por quota Vertex L4

### Estado por gate

| Gate | Estado | Comentario |
|------|--------|------------|
| B-1 dataset >=30k pares + balance >=0.20 | **VERDE** | `make farslip-dataset-build` con `n_per_roi=25000` → `data/farslip_pairs/` total **30173** (10112 pianura + 10083 toscana + 9978 puglia), balance 0.987. `make farslip-dataset-check` exit 0. |
| B-2 MLflow run `farslip-clip-italy-v1` | **BLOQUEADO** | Cadena MLflow OK (run smoke `fd1f4db41ebf4466b9ccfc23bde3e22d` creado), pero training student necesita L4 24GB. Sin quota L4 disponible en Vertex AI. |
| B-3 mIoU FarSLIP - mIoU RemoteCLIP >= +0.05 | **PENDIENTE** | depende de B-2 |
| B-4 parquet (85951, 514) + tag git | **PENDIENTE** | depende de B-2 |
| B-5 DVC push + MLflow Registry @Production | **PENDIENTE** | depende de B-2 |
| B-6 costo L4 <= $5 USD | **VERDE** | ~$0.10 USD gastado en 5 smoke jobs (4 CPU + 1 T4 cortos). Presupuesto $4.90 USD restante. |

### Logrado en esta sesion (auto-mode mlops-engineer)

1. **Etapa 3b smoke — cadena validada parcialmente**:
   - 5 Vertex AI custom-jobs lanzados y limpiados (`make cost-audit` exit 0).
   - **Identificados y corregidos en `ml/configs/{l4_smoke,l4_spot}.yaml`** 3 issues bloqueantes de la API Vertex que el prompt previo NO consideraba:
     - `scheduling.timeoutSeconds` → `scheduling.timeout: "1200s"` (Duration string).
     - `diskSpec.bootDiskType: pd-balanced` → `pd-ssd` (Vertex solo acepta pd-ssd, pd-standard, hyperdisk-balanced).
     - `diskSpec.bootDiskSizeGb: 50` → mininum `100` (smoke).
   - **Identificado y corregido IAM faltante**: `ml-train-runner-sa@agrosat-copilot.iam.gserviceaccount.com` NO tenia `roles/run.invoker` sobre `agrosat-mlflow-dev` (el prompt afirmaba que si lo tenia; `gcloud run services get-iam-policy agrosat-mlflow-dev` mostraba etag vacio). Aplicado via `gcloud run services add-iam-policy-binding`.
   - **Cadena CB → AR → Vertex CPU → metadata ID token → Cloud Run MLflow auth → POST run**: VERDE (run `fd1f4db41ebf4466b9ccfc23bde3e22d` registrado en `us-022b-a-smoke`).
   - Documentacion exhaustiva de los 5 jobs en `docs/l4_log.md` con root-cause de cada uno.

2. **Etapa 4 dataset build**:
   - `data/farslip_pairs/` rebuild con `n_per_roi=25000` → 30173 pares.
   - Gate B-1 verde.

3. **Nuevo archivo creado**: `ml/configs/cpu_smoke.yaml` (fallback CPU para validar la cadena sin GPU cuando hay bloqueante de quota).

### Bloqueante critico para Etapas 5, 6, 7 — Quota Vertex AI L4 = 0

`gcloud alpha services quota list --service=aiplatform.googleapis.com --consumer=projects/agrosat-copilot --filter="metric:custom_model_training_*nvidia_l4_gpus"` confirma:

- **0 / 0 en TODAS las regiones US/EU** (preemptible Y on-demand).
- T4 SPOT esta disponible (1 / 1 us-central1) pero (a) driver host CUDA 12.2 incompatible con imagen `ml-train:latest` CUDA 13, (b) 16 GB VRAM insuficiente para FarSLIP student (necesita 18-20 GB).

### Continuacion manual (Arthur, fuera de scope auto-mode sub-agente)

**Paso 1 — Solicitar quota L4 en GCP Console** (5 min UI + 1-2 dias aprobacion):

1. Abrir https://console.cloud.google.com/iam-admin/quotas?project=agrosat-copilot
2. Filtrar `Service: Vertex AI API` + nombre que contenga `Nvidia L4 GPUs`.
3. Solicitar `+1` en `us-central1` para:
   - `Preemptible custom model training Nvidia L4 GPUs per region` (preferida, ~70 % cheaper)
   - `Custom model training Nvidia L4 GPUs per region` (fallback on-demand)
4. Justificar con `dev/test ML training; project agrosat-copilot; estimated burst 6h spot/week`.

**Paso 2 — Una vez aprobada la quota, reanudar P1 etapa 5**:

```bash
export MLFLOW_TRACKING_URI="https://agrosat-mlflow-dev-gox5zzl7wa-uc.a.run.app"

# Revertir downgrade T4 → L4 en ml/configs/l4_smoke.yaml:
#   machineType: g2-standard-8
#   acceleratorType: NVIDIA_L4
#   acceleratorCount: 1
# Ademas FIX shape TempCNN (ver §"Smoke bug residual" en docs/l4_log.md): pasar X=(64, 24, 10) directo sin transpose, y model(X) sin .transpose(1,2).

# Smoke real L4 (~10 min, $0.20)
make train-l4-smoke
# Esperar JOB_STATE_SUCCEEDED + verificar run `smoke-gpu-tempcnn` en MLflow UI.

# Etapa 5 FarSLIP student (~6h L4 spot, $1.7)
make cost-audit   # gate pre-flight
make train-l4 epic=E3 us=US-017 script=farslip/train_student.py
# Anotar Job ID + MLflow run id en docs/l4_log.md.

# Etapa 6 (POST-job, una vez termine)
make farslip-extract-embeddings
poetry run python -c "import polars as pl; df = pl.read_parquet('data/farslip/embeddings_italy.parquet'); assert df.shape == (85951, 514); print('OK')"
dvc add data/farslip/embeddings_italy.parquet
dvc push
git add data/farslip/embeddings_italy.parquet.dvc
git commit -m "feat(E3): materializa farslip_embeddings_italy_v1 (85951 x 514)"
git tag farslip-embeddings-italy-v1 -m "FarSLIP student italia v1"

# Etapa 7
make farslip-eval-pastis
```

---

## P1 BLOQUEADO smoke FarSLIP — dataset `farslip_pairs` no esta en GCS (2026-05-24, sub-agente mlops-engineer)

**Resumen**: el smoke FarSLIP REAL solicitado por Arthur (pre-flight gate antes del training full 6h) NO se ejecuto. Hallazgo upstream que tambien bloquea el training full y obliga a un fix de infra antes de cualquier `make train-l4` que invoque `ml.farslip.train`.

**Diagnostico (evidencia)**:

1. `data/farslip_pairs/` existe SOLO en el filesystem local del agente integrator paralelo (Windows). 15 GB / 30173 crops .tif + 3 manifests Polars. Confirmado por `du -sh` y `ls */manifest.parquet`.
2. `gcloud storage ls --recursive gs://agrosat-artifacts-dev/ gs://agrosat-data/ gs://agrosat-artifacts/` retorna **vacio** para `farslip_pairs`. El bucket DVC remote (`gs://agrosat-dvc-remote/`) solo tiene 5 blobs MD5 pequenos (los `.dvc` ya tracked: `breizhcrops`, `eurocrops`, etc.) — ningun blob de farslip_pairs.
3. `data/farslip_pairs.dvc` **NO EXISTE**. El dataset NUNCA fue `dvc add`-eado. `data/.gitignore` no contiene entrada `/farslip_pairs`, lo que confirma que el build de US-017 quedo solo en la maquina del agente.
4. `infrastructure/docker/ml-train.Dockerfile` lineas 119-122: `COPY ml/`, `COPY scripts/` — **NO copia `data/`**. La imagen `ml-train:latest` no tiene ningun crop ni manifest dentro.
5. `ml/configs/l4_spot.yaml` linea 64: `--dataset-root data/farslip_pairs` es **ruta relativa al WORKDIR=/app del container**. Sin `data/` montado ni copiado ni descargado, `FarSLIPDataset.__init__` lanzara `FileNotFoundError: manifest no existe: data/farslip_pairs/.../manifest.parquet` ANTES de la primera iteracion.
6. `FarSLIPDataset._load_crop` (ml/farslip/dataset.py:385) usa `rasterio.open(path)` con ruta del filesystem; NO soporta `gs://` URIs nativo. Tampoco hay codigo de download al inicio del job.

**Implicacion**:

- El **training full 6h ($1.70)** del plan original tambien fallaria en los primeros segundos. No es solo el smoke FarSLIP.
- El gate B-1 marcado "VERDE" en este handoff (linea 198) es enganoso: el dataset es VERDE solo en local; NO esta accesible para Vertex AI L4 jobs.
- El smoke TempCNN PENDING (Job 7359114595530702848) no toca el dataset (usa tensor sintetico `torch.randn(64, 24, 10)`), por eso si validaria la cadena infra/auth/MLflow/IAM/ID-token. Pero no valida el path real de `ml.farslip.train`.

**Decision tomada**:

- **NO** se crea `ml/configs/l4_farslip_smoke.yaml` ni el target `train-l4-farslip-smoke` en este momento. Ejecutarlos garantizaria un `JOB_STATE_FAILED` en <30s y gastaria startup time inutil ($~0.01) sin proveer la validacion solicitada del pipeline FarSLIP.
- **NO** se modifica `ml/farslip/dataset.py` ni `ml/farslip/train.py` para soportar `gs://` directo — politica del usuario "NO edites codigo ML autonomo durante la noche".
- **NO** se cancela el smoke TempCNN PENDING (es legitimo, valida la cadena no-dataset y costo es <$0.20).

**Bloqueante para Arthur al despertar (orden recomendado)**:

1. **Subir dataset a GCS** (~15 GB, ~30-60 min depende de upload, ~$0 storage Standard us-central1 < $5/mes):
   ```bash
   gcloud storage cp -r data/farslip_pairs gs://agrosat-artifacts-dev/datasets/farslip_pairs/ \
     --project=agrosat-copilot
   # Verificar
   gcloud storage du gs://agrosat-artifacts-dev/datasets/farslip_pairs/ --summarize
   ```
   En paralelo (recomendado para reproducibilidad cross-machine, P4.1 deuda):
   ```bash
   dvc add data/farslip_pairs
   dvc push
   git add data/farslip_pairs.dvc data/.gitignore
   git commit -m "feat(E5): versiona dataset farslip_pairs (30173 pares italia v1)"
   git tag farslip-pairs-italy-v1 -m "Dataset US-017 italia v1"
   ```

2. **Modificar `l4_spot.yaml`** (y futuro `l4_farslip_smoke.yaml`) para descargar el dataset al inicio del job — opcion mas simple, paralelizable con `gsutil -m cp`:
   ```yaml
   # En containerSpec.args, ANTES del `poetry run python -m ml.farslip.train`:
   - |
     set -euo pipefail
     mkdir -p data/farslip_pairs
     gsutil -m cp -r gs://agrosat-artifacts-dev/datasets/farslip_pairs/* data/farslip_pairs/
     # ... resto del script igual
   ```
   Costo descarga intra-region (GCS us-central1 -> Vertex us-central1): **gratis**. Tiempo: ~3-5 min para 15 GB sobre red interna GCP. Disk requerido en `l4_spot.yaml` (200 GB pd-ssd) cubre los 15 GB del dataset + imagen + checkpoints.
   Verificar que `ml-train-runner-sa` ya tiene `roles/storage.objectViewer` sobre `gs://agrosat-artifacts-dev/` (segun comentario lineas 92-93 del yaml).

3. **DESPUES** de (1) y (2), reanudar plan del prompt original:
   - Crear `ml/configs/l4_farslip_smoke.yaml` clonando `l4_spot.yaml` con `--epochs 1` (NO existe flag `--max-pairs`/`--limit` en CLI `ml.farslip.train` — confirmado leyendo `ml/farslip/train.py` lineas 36-93; agregar flag seria edit de codigo ML, prohibido nocturno).
   - Con dataset completo + `--epochs 1`: smoke FarSLIP tomaria ~1.5h en L4 (30173 pares / batch 64 = 472 steps; ~10s/step CLIP-B forward+backward bf16 = ~80 min). Excede el `timeoutSeconds: 3600` (1h) sugerido en el prompt. Hay que decidir: (a) reducir batch + epochs y subir timeout a 5400s (1.5h), o (b) agregar flag `--max-pairs` al CLI (edit codigo ML, requiere autorizacion Arthur).
   - Costo smoke con dataset completo + 1 epoch L4 spot: ~$0.50-0.60 USD (excede el cap $0.50 del prompt). Si Arthur reduce a `--batch-size 32 --epochs 1` + manifest filter externo (truncar manifest.parquet copia en el job), baja a ~$0.25.

4. **Recomendacion final**: el camino mas limpio y honesto es **agregar flag `--max-pairs N`** al CLI `ml.farslip.train` (en realidad: `--max-samples-per-roi` para mantener balance), de la mano de modificar `FarSLIPDataset.__init__` para truncar el manifest. Es ~15 min de codigo + tests, debe hacerlo Arthur despierto (politica anti-edit-nocturno + paper-faithful: el flag debe estar documentado en `ml/farslip/train.py` docstring y `docs/us-resolved/us-017.md`).

**Job ID del smoke FarSLIP esperado**: NO LANZADO. No hay Job ID que reportar.

**Smoke TempCNN sigue su curso** (Job 7359114595530702848 PENDING; valida la cadena no-dataset). El integrator main puede esperarlo segun plan original. SI termina SUCCEEDED, NO PROCEDER a Etapa 5 (training full FarSLIP) sin antes resolver el bloqueante dataset GCS arriba descrito — fallaria igual que el smoke FarSLIP propuesto.

**Costo extra gastado esta sesion**: $0 (no se lanzo ningun job nuevo; solo lecturas gcloud).

---

**Estado previo** (mantengo historico para diff):

### 2026-05-24 (sesion anterior) — BLOQUEADO en Etapa 3a por mismatch de password Cloud SQL ↔ Secret Manager.

**Lo ya completado por el sub-agente `mlops-engineer` esta sesion**:

- Terraform state inspection: `module.gcp.google_sql_user.agrosat` + `google_secret_manager_secret_version.db_password_v` + `random_password.db_password` confirmados en state.
- Intento de deploy Cloud Run MLflow: `gcloud run services update agrosat-mlflow-dev --image=...mlflow:latest` ejecutado. Resultado: revision `agrosat-mlflow-dev-00002-wt5` creada pero con `HEALTH_CHECK_CONTAINER_ERROR`.
- Logs Cloud Run capturados: `psycopg2.OperationalError: FATAL: password authentication failed for user "agrosat"` (8 retries con exponential backoff hasta `mlflow.cli: Error initializing backend store`).
- `terraform plan -refresh-only`: no detecta drift (limitacion provider `google_sql_user`, password es write-only).
- Etapa 4 verificada idempotencia: `data/farslip_pairs/` NO existe localmente, build necesario (no skip).
- Documentado en `docs/l4_log.md` entrada 2026-05-24 con root cause + Opciones A/B de remediacion + comandos exactos de continuacion.

**Acciones que el SUB-AGENTE NO PUDO ejecutar** (politica auto-mode bloqueante porque tocan credenciales/infra compartida sin autorizacion explicita en el prompt):

1. `gcloud sql users set-password agrosat --password=<secret>` — bloqueado: modificar credenciales compartidas.
2. `terraform apply -target='module.gcp.google_sql_user.agrosat'` — bloqueado: targeted apply sin plan visible fuera del scope estricto del prompt.
3. Imprimir partial del secret a transcripcion — bloqueado: credential leakage.

**Continuacion manual (Arthur, ~5 min)**:

Ejecutar **Opcion A** del `docs/l4_log.md` (mas rapida, no toca state):

```bash
DBPASS=$(gcloud secrets versions access latest --secret=agrosat-db-password --project=agrosat-copilot)
gcloud sql users set-password agrosat \
  --instance=agrosat-pg-dev \
  --project=agrosat-copilot \
  --password="$DBPASS"

gcloud run services update agrosat-mlflow-dev \
  --image=us-central1-docker.pkg.dev/agrosat-copilot/agrosat/mlflow:latest \
  --region=us-central1 \
  --project=agrosat-copilot

# Verificar
MLFLOW_URL=$(gcloud run services describe agrosat-mlflow-dev --region=us-central1 --project=agrosat-copilot --format='value(status.url)')
curl -fsSL "$MLFLOW_URL/health" && echo OK
export MLFLOW_TRACKING_URI="$MLFLOW_URL"
```

Una vez OK, **re-invocar al sub-agente `mlops-engineer`** con un prompt que incluya:

> "MLflow Cloud Run OK en URL <X>. MLFLOW_TRACKING_URI exportado. Continuar US-022-c P1 etapas 3b (smoke), 4 (dataset farslip build ~30min CPU), 5 (training student L4 ~6h), 6 (extract embeddings), 7 (eval PASTIS). Comandos exactos en docs/l4_log.md entrada 2026-05-24."

**Decisiones tomadas autonomamente esta sesion** (auto-mode, redirigir si no procede):

- Skip Etapa 4 dataset build local por inutilidad si Etapa 5 (training) sigue bloqueada. Si Arthur prefiere paralelizar el build mientras resuelve MLflow, ejecutar `make farslip-dataset-build` (~30 min CPU local) en otro shell.
- No marco P1 como `failed` en planning; sigue `in-progress` con bloqueante operativo de credenciales (no de codigo ni de infra HCL).
- No edito Terraform HCL — el modulo es correcto, solo hay desincronizacion runtime entre el password real del SQL user y el almacenado en Secret Manager.


