# Handoff US-049 — Evaluacion del copiloto (AgroMind + GeoAnalystBench)

**Estado:** done (46 tests verdes; loaders validados con datos reales; ruff limpio)
**Rama:** `feature/E7-US-049-agent-bench`
**Plan:** [docs/us-planning/us-049.md](../us-planning/us-049.md)

## Decisiones tecnicas

- EVAL-ONLY, NUNCA fine-tune (AgroMind sin train split -> leakage; ADR-009).
- Datos REALES descargados: `data/agromind/agromind_subset_500.json` (500 estratificado), `data/geoanalystbench/GeoAnalystBench.csv` (50 tareas). Ambos en .gitignore.
- 3 variantes: gemini (multimodal), gemma-base (multimodal, referencia), qwen (SOLO texto -> subset textual de AgroMind, documentado; GeoAnalystBench completo).
- Reusa `ml.agent.backends.make_backend`, `ml.utils.mlflow_utils.track_experiment`, `deepeval` 4.0 (LLM-as-judge).
- Ejecucion real de Qwen depende de US-048 (bloqueado); harness validado con mocks. Gemini/Gemma via API.

## Archivos (write-sets disjuntos)

| Sub-tarea | Archivos |
|-----------|----------|
| ml/A metricas+report | `ml/eval/agent_metrics.py`, `ml/eval/agent_report.py` |
| ml/B orquestador | `ml/eval/agent_bench.py`, `scripts/download_agromind_images.py` |
| TESTS | `tests/ml/eval/test_agent_bench.py`, `tests/ml/eval/test_agent_metrics.py` |

## Snapshot de archivos tocados

- `ml/eval/agent_metrics.py` — exact_match, f1_squad, bertscore_f1 (proxy semantico sentence-transformers, documentado), tool_call_accuracy, hallucination_rate (DeepEval judge inyectable -> NaN sin judge), codebleu_score (BLEU n-gram + keyword overlap, aproximacion documentada de CodeBLEU sin AST data-flow), workflow_semantic_similarity.
- `ml/eval/agent_report.py` — `build_report_html`: tabla comparativa por variante x benchmark + error bars (matplotlib PNG base64), targets de rubrica con check/cruz, prosa ES. Crea reports/agent_bench/.
- `ml/eval/agent_bench.py` — AgroMindItem/GeoTask/ReasonerVariant, load_agromind_subset, load_geoanalystbench, eval_agromind (Qwen text-only salta multimodales), eval_geoanalyst (semantic sim + codebleu, pass por umbral), run_benchmark (3 seeds, mean+std, MLflow track_experiment inyectable, genera HTML), CLI main.
- `scripts/download_agromind_images.py` — baja SOLO las imagenes del subset 500 (mapea path->zip HF, extrae las necesarias). Idempotente.
- `tests/ml/eval/test_agent_metrics.py` (29) + `test_agent_bench.py` (17) — mock LLM/judge/encoder, sin red.

## Datos reales verificados

- AgroMind subset: **500 items = 494 multimodal + 6 textual**. Qwen (text-only) evalua solo los 6 textuales -> n_skipped=494. **LIMITACION documentada**: el subset estratificado por level1_id quedo casi todo multimodal; para una eval real de Qwen conviene un subset textual mayor (ampliar `make_subset` con filtro is_multimodal=False). Gemini/Gemma evaluan los 500.
- GeoAnalystBench: 50 tasks (100% texto) -> las 3 variantes lo corren completo.

## Bugs / bloqueos

- Ejecucion real de Qwen depende de US-048 (bloqueado nested-virt). Gemini/Gemma via API (con budget). Harness validado con mocks.
- Imagenes crudas AgroMind no descargadas (solo el subset bajo demanda via download_agromind_images.py).
- TODO opcional: ampliar subset textual de AgroMind para que Qwen evalue >6 items.
