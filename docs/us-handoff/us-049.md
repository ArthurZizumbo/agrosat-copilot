# Handoff US-049 — Evaluacion del copiloto (AgroMind + GeoAnalystBench)

**Estado:** done (46 tests verdes; loaders validados con datos reales; ruff limpio)
**Rama:** `feature/E7-US-049-agent-bench`
**Plan:** [docs/us-planning/us-049.md](../us-planning/us-049.md)

## Decisiones tecnicas

- EVAL-ONLY, NUNCA fine-tune (AgroMind sin train split -> leakage; ADR-009).
- Datos REALES descargados: `data/agromind/agromind_subset_500.json` (500 estratificado), `data/geoanalystbench/GeoAnalystBench.csv` (50 tareas). Ambos en .gitignore.
- 3 variantes: gemini (multimodal), gemma-base (multimodal, referencia), qwen (SOLO texto -> subset textual de AgroMind, documentado; GeoAnalystBench completo).
- Reusa `ml.agent.backends.make_backend`, `ml.utils.mlflow_utils.track_experiment`, `deepeval` 4.0 (LLM-as-judge).
- Ejecucion real de Qwen depende de US-048 (bloqueado); harness validado con mocks. Gemini/Gemma via API.

## Archivos (write-sets disjuntos)

| Sub-tarea | Archivos |
|-----------|----------|
| ml/A metricas+report | `ml/eval/agent_metrics.py`, `ml/eval/agent_report.py` |
| ml/B orquestador | `ml/eval/agent_bench.py`, `scripts/download_agromind_images.py` |
| TESTS | `tests/ml/eval/test_agent_bench.py`, `tests/ml/eval/test_agent_metrics.py` |

## Snapshot de archivos tocados

- `ml/eval/agent_metrics.py` — exact_match, f1_squad, bertscore_f1 (proxy semantico sentence-transformers, documentado), tool_call_accuracy, hallucination_rate (DeepEval judge inyectable -> NaN sin judge), codebleu_score (BLEU n-gram + keyword overlap, aproximacion documentada de CodeBLEU sin AST data-flow), workflow_semantic_similarity.
- `ml/eval/agent_report.py` — `build_report_html`: tabla comparativa por variante x benchmark + error bars (matplotlib PNG base64), targets de rubrica con check/cruz, prosa ES. Crea reports/agent_bench/.
- `ml/eval/agent_bench.py` — AgroMindItem/GeoTask/ReasonerVariant, load_agromind_subset, load_geoanalystbench, eval_agromind (Qwen text-only salta multimodales), eval_geoanalyst (semantic sim + codebleu, pass por umbral), run_benchmark (3 seeds, mean+std, MLflow track_experiment inyectable, genera HTML), CLI main.
- `scripts/download_agromind_images.py` — baja SOLO las imagenes del subset 500 (mapea path->zip HF, extrae las necesarias). Idempotente.
- `tests/ml/eval/test_agent_metrics.py` (29) + `test_agent_bench.py` (17) — mock LLM/judge/encoder, sin red.

## Datos reales verificados

- AgroMind subset: **500 items = 494 multimodal + 6 textual**. Qwen (text-only) evalua solo los 6 textuales -> n_skipped=494. **LIMITACION documentada**: el subset estratificado por level1_id quedo casi todo multimodal; para una eval real de Qwen conviene un subset textual mayor (ampliar `make_subset` con filtro is_multimodal=False). Gemini/Gemma evaluan los 500.
- GeoAnalystBench: 50 tasks (100% texto) -> las 3 variantes lo corren completo.

## QA verificacion (2026-06-16)

### Verificado (automatizado / datos reales)

| Check | Evidencia |
|-------|-----------|
| Suite de tests `tests/ml/eval/` | 46 verdes (agent_bench 10, agent_metrics 36) |
| Cobertura | agent_metrics 97%, agent_report 97%, agent_bench 77% (>= 70%) |
| `ruff check` (agent_bench/agent_metrics/agent_report/run_us049_eval) | "All checks passed!" |
| `load_agromind_subset` sobre `data/agromind/agromind_subset_500.json` | 500 items (494 multimodal + 6 textual), estratificado |
| Imagenes AgroMind via `download_agromind_images.py` | 657 extraidas (subset bajo demanda, idempotente) |
| `load_geoanalystbench` sobre `data/geoanalystbench/GeoAnalystBench.csv` | 50 tasks (100% texto) |
| Datos en .gitignore (no al repo) | verificado |
| `eval_agromind` (exact_match, f1_squad, bertscore_f1, tool_call_accuracy, hallucination_rate) | LLM-as-judge DeepEval inyectable -> NaN sin judge (test_agent_metrics, 36) |
| `eval_geoanalyst` (workflow_semantic_similarity + codebleu, pass por umbral) | plan-and-react (test_agent_bench, 10) |
| Qwen (text-only) salta items multimodales | `n_skipped` reportado honestamente |
| `run_benchmark` (mean+std multi-seed, MLflow inyectable, genera HTML) | reporte comparativo |

- **Desbloqueo Qwen:** US-048 resuelto via llama.cpp -> Qwen evaluable on-prem REAL (ya NO solo mocks). Gemma resuelto via Ollama MoE 26b-a4b (118 tok/s).
- **Cableado en vivo anadido:** `scripts/run_us049_eval.py` (setea endpoints), `OllamaBackend` multimodal en `backends.py`, variante `gemini-3.5-flash` + `gemma4:26b-a4b-it-q4_K_M` en `agent_bench.py`. ruff limpio, tests verdes.
- **Seguridad:** eval-only (sin fine-tune = sin leakage); datos en .gitignore; sin secretos.

### Criterios de aceptacion (plan v8 §US-049)

| Criterio | Estado |
|----------|--------|
| AgroMind con cada variante: exact/f1/bertscore/hallucination | [x] cumplido (corrida v1 subset 167, seed 0) |
| GeoAnalystBench en plan-and-react | [x] cumplido (50 tasks x variantes) |
| Modelos como benchmark, NUNCA fine-tune | [x] cumplido (eval-only) |
| Tabla comparativa + analisis latencia/costo | [x] tabla (HTML); costo Gemini ~$0.3-0.6, on-prem $0 |
| MLflow con data_version + code_version | [x] cumplido (run `c7643c8` FINISHED, code_version=4429e75) |

### Resultados de rubrica — corrida v1 (1 seed) PENDIENTE re-run v2

> Los siguientes numeros son de la **corrida v1 (3 variantes, 1 seed, 2026-06-16)**: subset
> acotado de 167 AgroMind (165 multimodal + 2 textual) + 50 GeoAnalyst, MLflow run `c7643c8`
> (`us049_agent_bench`, FINISHED, code_version=4429e75), reporte HTML 58 KB.
> Son un datapoint real, NO finales: **pendiente re-run v2 con qwen36-vl multimodal +
> prompting CoT (few-shot) + CodeBLEU real**.

| Metrica | Target plan v8 | Resultado v1 (seed 0, subset) | Estado |
|---------|----------------|-------------------------------|--------|
| AgroMind (Gemini 3.5-flash) | >= 0.75 | exact **0.539** (n=167) | corrida v1; target era 2.5-pro (flash < pro) |
| AgroMind (Gemma-base, ref) | referencia | exact **0.335** (n=167) | corrida v1; Gemini > Gemma en multimodal |
| AgroMind (Qwen text-only) | >= 0.70 (subset textual) | **0.0** (n=2) | corrida v1; N/A, no comparable (ver hallazgo abajo) |
| GeoAnalystBench pass rate | >= 0.65 | Gemini **0.18** / Gemma **0.36** / Qwen **0.54** | corrida v1; hallazgo: on-prem (Qwen) > flash en code-gen |

**Hallazgo clave (v1):** AgroMind es ~100% visual (0 items textuales reales en 28482) -> el
subset textual queda en ruido (n=2), por lo que **Qwen-text es N/A en AgroMind, NO 0.0**. La
comparacion multimodal on-prem correcta es **qwen36-vl** (4ta variante de la corrida v2 en curso).

### Pendiente (verificacion manual)

| Item | Razon | Estado |
|------|-------|--------|
| Corrida REAL de las variantes con endpoints on-prem vivos (Gemini 3.5-flash API + Qwen `:8002` + Gemma MoE `:11435`) | GPU / manual | [ ] |
| AgroMind (Gemini) >= 0.75 NO alcanzado (target era 2.5-pro; flash < pro) | revision humana | [ ] |
| AgroMind (Qwen) N/A: subset textual insuficiente (B-7) -> ruido, no comparable | revision humana | [ ] |
| GeoAnalystBench pass rate >= 0.65 NO alcanzado (hallazgo: on-prem Qwen > flash en code-gen) | revision humana | [ ] |
| Re-run v2: 4 variantes incl. qwen36-vl multimodal, few-shot+CoT, CodeBLEU real | GPU / manual | [ ] |

### Caveats honestos

| Item | Nota |
|------|------|
| Subset AgroMind casi todo multimodal | 494/500 multimodal -> Qwen (text-only) solo evalua 6; para eval real de Qwen ampliar `make_subset` con filtro `is_multimodal=False` |
| LLM-as-judge = costo extra | la hallucination_rate usa un juez (Gemini/vLLM) -> dobla las llamadas LLM por item |
| codebleu aproximado | BLEU n-gram + keyword overlap (sin AST data-flow); v2 usara CodeBLEU real |
| bertscore_f1 = proxy | sentence-transformers, documentado como proxy semantico |
| Gemini 3.5-flash sustituye a 2.5-pro | decision del usuario (menor costo); el harness es agnostico al id |

## Bugs / bloqueos

- Ejecucion real de Qwen depende de US-048 (bloqueado nested-virt). Gemini/Gemma via API (con budget). Harness validado con mocks.
- Imagenes crudas AgroMind no descargadas (solo el subset bajo demanda via download_agromind_images.py).
- TODO opcional: ampliar subset textual de AgroMind para que Qwen evalue >6 items.
