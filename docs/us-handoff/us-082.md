# Handoff US-082 — Re-extracción del dataset Italia COMPLETO + re-evaluación honesta del TL

**Estado**: planning
**Epic**: E12 (final-model) — transfer learning (`ml/transfer/`) + eval (`ml/eval/`) · fase CRISP-ML(Q): Data Understanding + Data Preparation + Evaluation
**Avance**: A7 (cierre TL con dato real al 100 %)
**Sprint**: post-presentación / asíncrono
**Rama**: `feature/E12-US-082-italia-dataset-completo`
**SHA base**: `48dd6cf` (rama `feature/E12-US-082-italia-dataset-completo` APILADA sobre `us-081`@`48dd6cf`; incluye todo el trabajo de US-081. El diff de US-082 = `git diff --name-only 48dd6cf`)
**Última fase**: 1 (planning) — 2026-06-29
**Prioridad (Backlog v8)**: P1 (desbloquea el verdadero techo del TL Italia)

## Hallazgo central (lo que motiva la US)
El F1-macro 0.13 del TL Italia (US-079) **NO es dataset pobre ni bug de código**: el modelo entrenó/evaluó con el **PILOTO del 1 %** (20 patches / 884 parcelas) en vez del dataset completo (**1,438 patches / 107,493 parcelas**, ya en disco, 11 GB de rasters S2 + TARGET masks). La re-extracción AlphaEarth sobre el dataset completo nunca se corrió (estaba pendiente del fin de descarga, que sí terminó). Ver `docs/us-planning/us-082.md` §0.

## Dominios y sub-tareas tocados (PLANEADOS — aún sin código de Fase 3)
- [ ] ml (`ml/transfer/`, `ml/eval/`)  - [x] data (re-extracción GEE)  - [ ] backend  - [ ] frontend  - [ ] agent  - [ ] infra  - [ ] db  - [ ] dagster
- Sub-tareas: **Fase 1** re-extracción AlphaEarth 1,438 patches (GEE/ADC) · **Fase 2** re-entreno + re-eval TL con soporte real · **Fase 3** EDA + separabilidad post-extracción + veredicto N clases objetivo.

## Archivos tocados (hasta ahora: planeación + 1 herramienta)
- `docs/us-planning/us-082.md` — plan completo (hallazgo, diagnóstico cuantitativo, 3 fases, KPIs, DoD). **Creado.**
- `docs/us-handoff/us-082.md` — este handoff. **Creado.**
- `ml/transfer/separability_italia.py` — diagnóstico JM/Bhattacharyya por clase (entra en este PR; ya validado: con el piloto da JM=2.0 espurio por covarianza degenerada → confirma que el piloto no alcanza para estimar separabilidad). **Creado (29-jun).**

### Fase 3 — CÓDIGO programado (29-jun nocturno, rama `feature/E12-US-082-italia-dataset-completo`)
- `ml/transfer/eda_italia.py` — **Creado**. EDA formal Polars del dataset completo: volumen (patches/parcelas), soporte por clase (presencia en patches + estimación de parcelas), techo temporal (distribución de `n_fechas`, cohorte < 16 fechas), co-ocurrencia inter-clase, distribución por fold. Lee del `metadata.parquet` real. `compute_italia_eda()` → `ItaliaEdaReport` (JSON-serializable).
- `ml/transfer/eval_three_ways.py` — **Creado**. Orquestador de LAS 3 VÍAS de Arthur: **A** (39 hojas HCAT nativas = FINE), **B** (mapeo al crosswalk PASTIS = COARSE vía `coarse_of`), **C** (procedimiento completo end-to-end, puntuado en granularidad nativa). `compare_three_ways()` → `ThreeWayComparison` con tabla comparativa (macro-F1, nº clases ≥0.6/≥0.8 por vía). Reutiliza `transfer_italia_eval.py` (no duplica).
- `scripts/build_us082_eval_notebook.py` + `notebooks/transfer/us082_dataset_completo_eval.ipynb` — **Creados**. Notebook 8 secciones estilo Avance5 (hallazgo piloto-1%, EDA, separabilidad, 3 vías, A/B piloto vs completo, F1 estratificado por fechas, veredicto KPI-2, conclusiones). Estructura LISTA, celdas con estado "PENDIENTE" honesto hasta que la corrida real en VM produzca los artefactos (papermill los pobla). REGLA: solo valores reales.
- `tests/ml/transfer/test_eda_italia.py` + `test_eval_three_ways.py` — **Creados**. 12 tests verdes (fixtures deterministas pequeños, marcados como mecánica NO métricas titulares). Validan agregación de volumen, soporte por clase, cohorte temporal, co-ocurrencia, colapso de vía B, tabla de las 3 vías.
- `pyproject.toml` — **Modificado**. `build_us082_eval_notebook.py` añadido a `per-file-ignores` (E501/RUF001-3, patrón establecido de los builders de notebook).

### Pendiente de EJECUCIÓN REAL en la VM H100 (orquestador, no agente de código)
1. Sync VM: push rama 082 a origin → fetch/checkout en `F:\projects\agrosat-copilot` (repo VM sucio → vía `git stash` o worktree dedicado). Dataset real en `F:\worktrees\us078\data\pastis_italia_2018` (1438 patches verificados, leído por path absoluto).
2. Extracción AlphaEarth: subset 50-100 patches (medir costo/tiempo GEE) → full 1438 → `data/features/alphaearth_italia_2018_full1438.parquet`. ADC OK en VM (verificado), GEE init OK, GPU ~94GB libre.
3. Re-entreno TL + OOF fold-5 + Voting-3 sobre el dataset completo; las 3 vías evaluadas; A/B piloto vs completo; separabilidad post-extracción; poblar notebook con papermill. MLflow server :5010 (data_version + code_version).
4. Traer resultados a local (DVC push desde VM o scp + dvc add) — repo local = fuente de verdad.

## Archivos existentes reutilizados (NO duplicar)
- `scripts/build_alphaearth_italia.py` — runner de extracción AlphaEarth + train xgb + dump OOF. Parametrizable; para el full NO pasar `--patches-metadata` (ver gotcha abajo).
- `ml/transfer/alphaearth_italia.py` — `build_alphaearth_italia_features`, `load_patch_bboxes`, `parcels_in_patches`. Lee TODOS los patches del metadata (sin cap). El comentario `:300` ("20 pilot, ~1226 full") es la prueba documental del piloto.
- `ml/transfer/italia_label_space.py` — label-space Italia (39 hojas, crosswalk conservado a PASTIS-18). El mapeo class_id↔nombre está CORRECTO (verificado).
- `ml/eval/transfer_italia_eval.py` — eval del TL Italia; apuntar a las features completas.
- `data/reference/eurocrops_v2/iti1_2018.parquet` — polígonos EuroCrops Italia 2018 (228 MB, presente). Fuente de las parcelas.

## Decisiones técnicas clave (planeadas)
- **Salida a path NUEVO** `data/features/alphaearth_italia_2018_full1438.parquet` — NO sobrescribir el piloto (respaldado en `data/features/alphaearth_italia_2018_PILOT20.parquet.bak`, 444 KB).
- **No reagrupar clases** (mantener 39 hojas HCAT) — restricción de Arthur.
- **Remuestreo temporal**: Italia tiene media 24.3 fechas (vs 43 PASTIS), min 9 max 40. El campeón usa n_timesteps=32 → padding/remuestreo con máscara temporal; F1 estratificado por nº de fechas.
- **Target honesto por-clase**: 10-15 clases ≥0.6-0.8 (NO el F1-macro-39 global, cuyo techo lo fijan las ~23 clases de cola minoritarias reales).

## Bugs / gotchas detectados (verificados 29-jun)
| Tema | Detalle | Acción |
|------|---------|--------|
| Piloto del 1 % | features = 20 patches / 884 parcelas vs 1,438 / 107,493 reales | Fase 1: re-extraer completo |
| Gotcha runner `--patches-metadata` | `metadata_dir = patches_metadata.parent` → pasar `X/foo.parquet` busca `X/metadata.parquet`, NO el archivo dado | Full: NO pasar el flag (default = `dataset_dir/metadata.parquet` = 1,438). Fix opcional: usar el archivo dado directamente |
| Separabilidad piloto espuria | JM=2.0 universal por covarianza degenerada (clases de 1-5 parcelas en 64 dims) | Re-correr `separability_italia.py` tras la re-extracción (cientos de parcelas/clase) |

## Zonas sensibles
- `data/features/alphaearth_italia_2018.parquet` — el PILOTO; NO sobrescribir hasta validar el completo. Respaldo `.bak` creado.
- `scripts/build_alphaearth_italia.py` — la corrida full consume cuota GEE (~107k parcelas, ~122× el piloto). Medir con subset 50-100 antes del full.
- Remuestreo temporal — un patch de 9 fechas mal padeado contamina; usar máscara.

## Modelos / Datos / H100 / GEE
- **GEE**: ADC logueado (`artzizumbo@gmail.com`), proyecto `agrosat-copilot`. Token ADC válido (verificado 29-jun). Extracción = `reduceRegions` por lotes (`batch_size`, default 100).
- **Datos en disco**: `data/pastis_italia_2018/` — 1,438 rasters `DATA_S2/S2_*.npy` (11 GB), 1,438 `ANNOTATIONS/TARGET_*.npy` + `dates`, `metadata.parquet` (1,438 patches, bboxes EPSG:4326, fold_espacial), `class_mapping.json` (39 clases), `pilot_summary.json`.
- **DVC**: la nueva `alphaearth_italia_2018_full1438.parquet` se versiona con su `.dvc` + push GCS. El piloto y `iti1_2018.parquet` ya están en DVC.
- **Cantidad vs PASTIS**: Italia = 58 % patches / ~87 % parcelas / ~74 % píxeles. Series temporales = 56 % (24 vs 43 fechas) — el límite estructural.
- **MLflow**: re-entreno loguea a server Docker `:5010` con `data_version` + `code_version`. (Gotcha: subprocess deja runs RUNNING; cerrar manual.)
- **H100/VRAM**: el re-entreno TSViT/U-TAE puede ir a H100; xgb y eval son CPU.

## Llave de provenance (cierre)
> `US-082 @ <git_sha7> + mlflow:<run_id|-> + dvc:<rev|->` — rellenar en Fase 7.

## engram-memory
- Observaciones a guardar: hallazgo del piloto del 1 % (causa raíz real US-079), EDA dataset completo, comparación cantidad/fechas vs PASTIS, gotcha runner. Keywords: `italia-piloto-1pct`, `dataset-completo-1438`, `series-temporales-24-vs-43`, `re-extraccion-gee`, `separabilidad-italia`.

## Siguiente paso inmediato (Fase 3 coding, cuando se apruebe)
1. Medir costo/tiempo GEE con subset 50-100 patches (sin `--patches-metadata`, con `metadata.parquet` recortado en un dir propio para respetar el gotcha).
2. Lanzar extracción full → `_full1438.parquet`.
3. Re-entrenar + A/B piloto vs completo + EDA/separabilidad + notebook poblado.
