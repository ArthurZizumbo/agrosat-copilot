# US-066 — Handoff (COMPLETO: atribuciones licencia multi-region)

**Status**: ready-to-close (validacion E10) — entrada AlphaEarth corregida + Sen4AgriNet + EuroCropsML + WorldCereal/HGC (FUTURE) +
tabla de atribucion por region COMPLETOS
**QA (2026-06-20)**: **DONE** — US solo de documentacion (prosa `.md`, sin codigo). El gate de la
suite E10 con la que comparte rama esta limpio: ruff `All checks passed!` sobre `backend/ ml/
dagster_project/`, mypy `Success: no issues found in 26 source files` (package-mode middleware/api/
utils/services), 52 passed chat+metrics, 11 passed drift, `dagster_project.definitions` importa
limpio. Esta US no toca archivos con tests asociados, por lo que no agrega suite propia.
**Epic**: E10 (Observabilidad y Documentacion) · **Rama**: `feature/E10-observabilidad-docs`
(parte de `feature/E12-transfer-multiregion`)
**Plan**: `docs/us-planning/us-066.md` · **v8**: `context/RefinamientoPlaneacionAgroSatCopilot_v8.md`
§US-066 (linea 2130)

## Que entrega US-066

Registro de las atribuciones de licencia de los datasets introducidos por la historia de
transferencia multi-region (EPIC 12, US-074..077) en `docs/licenses/DATA_LICENSE.md`, para cumplir
la regla de atribuciones y el criterio de cumplimiento legal de la rubrica. En concreto: anade
**Sen4AgriNet** y **EuroCropsML**, **corrige** la entrada erronea de AlphaEarth ("AlphaEarth
Foundations v2.1" -> `SATELLITE_EMBEDDING/V1/ANNUAL` data v1.1, CC-BY-4.0), pre-registra la
atribucion **FUTURE** de WorldCereal RDM / Harmonized Global Crops con su nota de compatibilidad
share-alike para el espacio unificado HCAT v3, y centraliza la **tabla de atribucion por region**.
Es una US **solo de documentacion**: no toca codigo de aplicacion, modelos, migraciones ni
pipelines; la unica edicion es prosa en espanol neutro dentro de un `.md`.

## Estado real (DONE)

DONE (verificado contra el archivo en disco):

- **Correccion AlphaEarth** (lineas 7-28): titulo ya factualmente correcto
  `### AlphaEarth Satellite Embedding V1/ANNUAL (data v1.1) — Google DeepMind`; `License:
  CC-BY-4.0` declarada explicitamente (antes solo citaba GEE ToS); conservadas la nota de cuota EE
  y el use scope US-011. Nota de correccion explicita: la cadena "AlphaEarth Foundations v2.1"
  (version inexistente) ya no es el titulo, queda solo referida en la nota como correccion de
  realidad del plan v8.
- **Sen4AgriNet (nueva, lineas 134-153)**: `### Sen4AgriNet — Sykas et al. (NOA / paren8esis)`.
  HF `paren8esis/S4A`; subset Catalonia tile 31TCG versionado en DVC/GCS (`data/sen4agrinet.dvc`,
  md5 `e292ab8cfbfb05ecc9ab5310e01dd79e.dir`, 88 files, ~943 MB, 40 patches). **License:
  CC-BY-SA-4.0**. Cita Sykas, Sdraka, Zografakis, Papoutsis (2022) J-STARS, DOI
  10.1109/JSTARS.2022.3198140. Attribution required + use scope US-075 (Delta mIoU +0.2468 REAL,
  `reports/segmentation/sen4agrinet_transfer_result.json`) + nota share-alike.
- **EuroCropsML (nueva, lineas 155-178)**: `### EuroCropsML — Reuter et al. (TUM)`. Paquete
  `eurocropsml` (PyPI/Conda, descarga desde Zenodo); subset versionado en
  `data/transfer/eurocropsml.dvc`. **License: CC-BY-SA-4.0**. DOI Zenodo **10.5281/zenodo.15095445**
  (verbatim del plan v8) + DOI concept canonico 10.5281/zenodo.10629609 (ambos documentados; la
  version efectiva se fija en el `.dvc`/lineage MLflow). Cita Reuter et al. (2025), Nature Sci.
  Data 12:664 / arXiv:2407.17458. Contents (S2 L1C 13 bandas, `EC_hcat_c` 176 clases, EE/LV/PT) +
  attribution required + use scope US-076 + **referencia cruzada** explicita a la entrada
  EuroCrops/HCAT3 (datasets distintos, misma taxonomia HCAT) + nota share-alike.
- **WorldCereal RDM / Harmonized Global Crops (FUTURE, lineas 180-196)**: estado FUTURE/paper,
  pre-registrado por el criterio de aceptacion del plan v8. RDM **CC-BY por coleccion** (verificar
  por coleccion al ingerir); HGC HF `torchgeo/harmonized_global_crops` **CC-BY-SA-4.0**. Nota de
  compatibilidad share-alike (clave HCAT v3): SA domina al combinar derivados; CC-BY de WorldCereal
  es absorbible aguas arriba pero el resultado combinado queda gobernado por la clausula SA.
- **Tabla transversal de atribucion por region (lineas 222-237)**: subseccion `### Atribucion
  requerida por region (figuras y reportes derivados)` con tabla Region | Dataset | Licencia |
  Texto de atribucion para Francia (PASTIS-R + EuroCrops/HCAT3 + RPG 2018), Catalonia
  (Sen4AgriNet), Estonia/Letonia/Portugal (EuroCropsML), Mexico zero-shot (AlphaEarth) y Tropical
  FUTURE (WorldCereal/HGC). Cierra el criterio de aceptacion 3 de forma centralizada y verificable.

## Archivos

Modificado (unico archivo):

- `docs/licenses/DATA_LICENSE.md` — las 4 ediciones del plan §4 (fix AlphaEarth + Sen4AgriNet +
  EuroCropsML + WorldCereal/HGC FUTURE) mas la tabla transversal por region; sin reformatear el
  resto del archivo.

No tocados (verificacion explicita): codigo (`backend/ ml/ frontend/ db/ dagster_project/`), los
`.dvc`, las migraciones, y el resto de secciones de `DATA_LICENSE.md`.

Punteros DVC referenciados (no creados por esta US; ya versionados en EPIC 12):

- `data/sen4agrinet.dvc` — subset Catalonia 31TCG (US-074..077).
- `data/transfer/eurocropsml.dvc` — subset few-shot EE/LV/PT (US-076).

## Lineage / QA

- US **solo de documentacion**: sin migraciones, sin entrenamiento, sin generacion de data nueva,
  sin i18n; no aplica `dbmate up`, run MLflow ni `dvc add`. Los `.dvc` referenciados ya estaban
  versionados por EPIC 12.
- Gate de la rama `feature/E10-observabilidad-docs` (compartido, re-corrido a mano por el
  orquestador): ruff sobre `backend/ ml/ dagster_project/` -> `All checks passed!`; mypy
  package-mode (`backend.app.middleware/api/utils/services`) -> `Success: no issues found in 26
  source files`; suite chat+metrics 52 passed (`test_metrics_middleware`, `test_chat_metrics`,
  `test_chat_sse`, `integration/test_chat_endpoint`, `integration/test_chat_uses_session_model`);
  suite drift 11 passed (`tests/ml/monitoring`, `tests/dagster/test_drift_asset.py`, warnings
  benignos scipy); `dagster_project.definitions` importa limpio (schedules
  `['drift_check_weekly_schedule']`, jobs `['farslip_full_pipeline_job','drift_check_job']`).
- Toda licencia/DOI/cita del archivo o esta verificada contra fuente primaria o documentada con su
  ambiguedad declarada en el propio `.md` (regla Arthur: cero datos fabricados). El DOI ambiguo de
  EuroCropsML se documenta con ambas formas (version + concept) en lugar de fijar una sola.
- Commit propio por separado, autor Arthur Zizumbo, **sin** trailer `Co-Authored-By` de IA. Tree
  limpio.

## Notas / blockers

- **Sin blockers.** La US esta cerrada; toda la informacion de licencia requerida por los criterios
  de aceptacion del plan v8 esta presente y verificable en `docs/licenses/DATA_LICENSE.md`. No fue
  necesario abrir `docs/blockers/epic10-notas.md`.
- **EuroCrops/HCAT3 vs EuroCropsML**: son datasets distintos con el mismo linaje taxonomico HCAT;
  el archivo los mantiene como dos entradas separadas con referencia cruzada explicita para evitar
  confusion.
- **DOI EuroCropsML**: el plan v8 cita `10.5281/zenodo.15095445` (version) y el concept canonico es
  `10.5281/zenodo.10629609`; ambos quedan documentados, la version efectiva la resuelve el paquete
  `eurocropsml` y se fija en el `.dvc`/lineage MLflow.
- **WorldCereal/HGC**: entrada marcada FUTURE; el plan la pide condicional ("si se ingiere") pero la
  pre-registra. No implica descarga ni ingesta.
- **FIX del orquestador (contexto de la rama, no de esta US)**: US-059 quedo estabilizada con
  `DEFAULT_REGISTRY` dedicado + `get_default_metrics()` singleton (re-instanciar la app ya no lanza
  "Duplicated timeseries", que rompia 13 tests de integracion de chat); el harness de US-065 usa
  `_FakeConn/_FakePool` en `integration/test_chat_endpoint.py` (antes `object()` rompia
  `_resolve_variant.pool.acquire()`; fallo PRE-EXISTENTE de US-054, no regresion de E10).

## Cierre documental (2026-06-24, validacion E10)

- **Estado**: ready-to-close. Cierre verificado en `validacion/E10-observabilidad`.
- **Evidencia**: `docs/licenses/DATA_LICENSE.md` (30.862 B) con las 4 ediciones del plan §4 — fix AlphaEarth (V1/ANNUAL v1.1 CC-BY-4.0), Sen4AgriNet (CC-BY-SA-4.0), EuroCropsML (CC-BY-SA-4.0, DOI 10.5281/zenodo.15095445 + concept), WorldCereal/HGC FUTURE — mas la tabla de atribucion por region. Verificado por grep en disco. Sin blockers propios.
- **Docs de cierre generados**: [`docs/us-resolved/us-066.md`](../us-resolved/us-066.md) + [`docs/manual-test/us-066.md`](../manual-test/us-066.md).
- **Verdict**: **ok** — 4 ediciones + tabla por region en disco, licencias/DOI verificados o con ambiguedad declarada; sin blockers.
