services:
  postgres:
    image: postgis/postgis:15-3.4
    environment:
      POSTGRES_USER: agrosat
      POSTGRES_PASSWORD: agrosat
      POSTGRES_DB: agrosat
    ports: ["${POSTGRES_HOST_PORT:-55432}:5432"]
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agrosat"]
      interval: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports: ["${REDIS_HOST_PORT:-63790}:6379"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s

  api:
    build:
      context: .
      dockerfile: infrastructure/docker/backend.Dockerfile
      target: dev
    ports: ["${API_HOST_PORT:-8010}:8000"]
    env_file: .env.local
    volumes: ["./backend:/app/backend", "./ml:/app/ml"]
    depends_on:
      postgres: { condition: service_healthy }
      redis: { condition: service_healthy }
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build:
      context: ./frontend
      dockerfile: ../infrastructure/docker/frontend.Dockerfile
      target: dev
    ports: ["${FRONTEND_HOST_PORT:-3010}:3000"]
    env_file: .env.local
    volumes: ["./frontend:/app"]
    command: pnpm dev

  titiler:
    image: ghcr.io/developmentseed/titiler:latest
    ports: ["${TITILER_HOST_PORT:-8011}:8000"]
    environment:
      GDAL_DISABLE_READDIR_ON_OPEN: EMPTY_DIR
      VSI_CACHE: "TRUE"

  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.16.2
    ports: ["${MLFLOW_HOST_PORT:-5010}:5000"]
    depends_on:
      postgres: { condition: service_healthy }
    command: >
      mlflow server
      --backend-store-uri postgresql://agrosat:agrosat@postgres:5432/agrosat
      --default-artifact-root /mlruns
      --host 0.0.0.0 --port 5000
    volumes: ["mlflow_data:/mlruns"]

  dagster:
    build:
      context: .
      dockerfile: infrastructure/docker/dagster.Dockerfile
    ports: ["${DAGSTER_HOST_PORT:-3011}:3000"]
    env_file: .env.local
    depends_on:
      postgres: { condition: service_healthy }
    volumes: ["./dagster_project:/app/dagster_project"]
    command: dagster dev -m dagster_project.definitions -h 0.0.0.0

  ollama:
    image: ollama/ollama:latest
    ports: ["${OLLAMA_HOST_PORT:-11434}:11434"]
    volumes: ["ollama_data:/root/.ollama"]

volumes:
  postgres_data:
  mlflow_data:
  ollama_data:
